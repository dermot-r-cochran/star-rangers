# Changelog

All notable changes to this project are documented in this file.

The format loosely follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and version numbers follow [Semantic Versioning](https://semver.org/) — `MAJOR.MINOR.PATCH`, where `MINOR` covers backward-compatible additions (a new lore entry, a new deploy.conf key, a new theme) and `PATCH` covers fixes with no new surface area. Nothing in this project's history so far has required a `MAJOR` bump: every `deploy.conf` key, theme, and build output added has been additive, and every default has stayed backward compatible.

## [Unreleased]

### Added

- **Chapter S11E01C03 — [*Someone to Ask*](src/seasons/s11/e01/s11e01c03.md)**, the third chapter of *Below the Roof*, with a character page for [Went-Round](src/characters/went-round.md) (2026-09-06, drafted at Dermot's ask *Draft chapter three of Below the Roof*; `story-bible/intake-2026-09-06.md`, thirteenth section). The stone has been heard and nothing follows, and for the first time in all the tellings the Told argue the manners: Went-Round says a stranger who cannot hear the ground cannot ask, so *leave it to them until they ask* is leaving them alone forever; Stone-First says manners kept only when you are sure of them are not manners. An argument among the Told is settled by who was there, so Carried-It-Sleeping goes up to watch how the up-people ask, and brings down the people's first telling of it — an empty hand held out, a sound that rises at the end, and waiting; they ask each other constantly and have never once asked us — and one thing more: at the end of each working time one of the up-people turns to the dark, says something that rises at the end, waits, and goes. The people do not know what was asked or of whom, and the record does not fix it. Stone-First finishes the kept thought — the manners assumed a stranger who knew you were there, and the Told had left the place to the up-people so thoroughly that when they looked into the dark for someone to ask, nobody was there — and goes up through the shelving to stand on the line where the up-people's feet stopped, and stays: keeping the margin where it now is. Two heads, four scenes, five blocks. Commits the story to the argument and who holds which side, to Went-Round, to how the up-people ask, to the up-person who speaks to the dark, to Stone-First's finished thought and to Stone-First on the line in the open. The survey's answer is deliberately not taken. Stone-First's page gains the chapter and its kept question finished; the glossary entry, the episode index, the Reading Tiers page (*first three chapters*), the thread registry comment and the Pandoid section of `story-bible/mind-design.md` updated. Six readings were put as choices in the intake and **approved by Dermot the same evening** (*Approved*, PR #729); what the survey does next, put as three shapes, stays his.
- **Chapter S11E01C02 — [*Carried It Sleeping*](src/seasons/s11/e01/s11e01c02.md)**, the second chapter of *Below the Roof*, with a glossary entry for [The Told](src/glossary/the-told.md) (2026-09-06, drafted at Dermot's ask *Draft chapter two of Below the Roof, Endonym and naming*; `story-bible/intake-2026-09-06.md`, twelfth section). The youngest asks why the up-people never ask, and Stone-First answers that nobody has told them anything and that the people are the Told — one is of the people by having been told the tellings, and the up-people are people who have not been told. The youngest works out that every telling begins with a stone, carries the up-people's own humming stone from the junction down through the squeeze, holds still when it wakes, and sets it upright on the line where the up-people's feet once stopped: the relay placement of Teddy's page, from inside, as a stone moved first. Stone-First carries the telling into the warm and leaves the reason out, and what the others repeat — *the one who carried it sleeping* — becomes the youngest's name. Two heads, four scenes, five blocks. Commits the story to the endonym *the Told*, to names as tellings (the first true thing said of a person that others go on repeating), to *Carried-It-Sleeping* as the youngest's name, to the relay's placement in full and to what it was, to the people not leaving a sleeping one alone, and to the up-people having come to the line, looked once into the dark, lifted the stone and not crossed the line since. No phonetic form is taken for any name; *Pandoid* is not retired. Registered in the canonical guide under Canonical Terms; the Fliade entry's absence paragraph, Teddy's page (a *from inside* paragraph after the two filed readings, the name from inside, the alias) and Stone-First's page extended; the episode index, the Reading Tiers page, the thread registry comment and the Pandoid section of `story-bible/mind-design.md` updated. The endonym, the naming system, the youngest's name and eight readings were put as choices in the intake and **approved by Dermot the same evening** (*Approved*, PR #727), which leaves what the up-people do next, the children's edition and the strand structure still his.
- **Chapter S11E01C01 — [*Nobody Looked Up*](src/seasons/s11/e01/s11e01c01.md)**, the first chapter of *Below the Roof*, with a new Episode 1 index page and a character page for [Stone-First](src/characters/stone-first.md) (2026-09-06, drafted at Dermot's ask: *Draft the first chapter of Below the Roof*; `story-bible/intake-2026-09-06.md`, eleventh section). The Pandoids of Fliade from inside, in the children's band: the youngest of the people goes up past the margin without moving a stone first, stands at the edge of the up-people's light for two tellings of the first warm seep, and comes home with the finding that nobody looked up — the eleven-minute thermal profile of Teddy's page, from the other side; Stone-First, who keeps the margin, asks for the telling again with the finding left out. Two heads, four scenes, five blocks. Commits the story to the people's manners (move a stone first; leave a place to a stranger until they ask), time measured in tellings, the up-people never having asked, a telling becoming a person's own when heard, and the relay left asleep and untouched. Every name is rendered in translation and no endonym, naming system or pronoun system is taken; a Pandoid section of `story-bible/mind-design.md` was written first, derived from the record with its open items marked. The readings it took — names, pronoun, Teddy as the youngest, memory ordinary, the Fliade entry untouched, the year, and what the relay was — were put as choices in the intake and **approved by Dermot the same evening** (*Approved*, PR #725), which leaves the endonym and naming system, and what the relay was for chapter two, still his. Stone-First's page follows Achebe's shape, no image. The Reading Tiers page, the thread registry and the gaps checklist updated.
- **Journal: [The Children's Talk](src/journal/the-childrens-talk.md)** (2026-09-06, the fifth entry of the day, drafted at Dermot's ask from three messages recorded verbatim in `story-bible/intake-2026-09-06.md`, sixth section). Opens by stating who the Journal is for. Then: the children's talk and the sermon as two ways of being exact, by selection and by qualification; the tier ladder describing the reader as well as the content; Narnia as the children's-rung exemplar (supposal not allegory, *not a tame lion* as the built-in expiry date, Lewis's *watchful dragons*) and *The Lord of the Rings* as the young-adult one (Book Four's pace irrelevant to tier, the hobbits' ages against the 3 September age rule); and Kate Ukrainka's author's note as the record's own case of the parable discipline, drawn entirely from her published page. The four earlier entries' series notes now point to it. Craft only; no canon changes.
- **Four Journal entries from one conversation on 6 September 2026** — [The Picture the Maths Was Invented to Replace](src/journal/the-picture-the-maths-was-invented-to-replace.md), [Where the Compression Was Trained](src/journal/where-the-compression-was-trained.md), [Agreement Counts Once](src/journal/agreement-counts-once.md) and [The Parable and the Counterexample](src/journal/the-parable-and-the-counterexample.md) (2026-09-06, drafted at Dermot's ask, cross-linked as a sequence). The first: why mathematical abstractions resist intuition and why popular science hands back the picture the formalism was built to replace, finding the setting already answering this in the Levrils entry's *signature, not shape* rule, Membrane Shadows and The Scope of Physical Law — show a higher-dimensional being through what it can and cannot do and what the instruments read, never as a place a reader could visit. The second: when not to trust intuition and when analysis costs too much, and machine learning as intuition by another name, naming foundation-model, careful-memory and swarm; its rule is to know where the compression was trained and past that edge trust only what can be checked. The third: what to do when intuition and logic agree or one overrides the other — agreement counts once unless the sources are independent, a surprising conclusion is first evidence of a wrong premise, and the aim is to update the intuition rather than override it. The fourth: why pictures hinder the teaching of mathematics, set against the parable in theology as the same device under a different discipline, and the lore and the church-space overlay as the record's own two registers. The first three shipped earlier the same day as one entry (#714, #715) and were split at the length rule below; all four had a precision pass under it (three historical attributions corrected). Craft only; no canon changes.
- **Codex entry [*Weighing the Neighbours*](src/codex/weighing-the-neighbours.md)** (2026-09-06, drafted at Dermot's ask after a question on which aspects of a poltergeist the gravitational-shadow mechanism could carry). A Survey Corps Gravimetry Desk working paper, filed 2825 UCSD, proposing that some part of the dark component of the sky's mass budget is the summed gravitational bleed of neighbouring membranes at ordinary separation — the Membrane Shadow mechanism with the coherence taken out — with the paper's own objections listed, the halo-tracking objection left open, and one testable prediction stated. Valid-for-its-author: the record carries no position on the dark component and the entry says so; no canon changes.
- **Storyline thread *Below the Roof*, Season 11, registered for the children's tier** (2026-09-06, Dermot's ruling, verbatim in `story-bible/intake-2026-09-06.md`: *Pandoid thread in child reader tier*). The Pandoids of Fliade from inside their own deep networks, the record's first storyline carried by a non-human ensemble. Registered in `lib/storyline-threads.js` as `below-the-roof` with `src/seasons/s11/index.md` and `src/threads/below-the-roof/index.md` as landing pages and no chapter; the Reading Tiers page names it under Children as registered and not yet begun. Listed on `GENERAL_TIER` in `lib/editions.js` on the young-star-rangers precedent, so the general and contemplative readers see it and no narrowed face gains it by accident; the children's edition that carries it is his to name. *Below the Roof* is a working title from the Fliade entry, since *Pandoid* is the survey's word and the endonym does not exist yet. Open and put as choices in the intake: the edition, the name, the endonym and pronouns, a mind-design section, Pandoid young and whether Teddy is the protagonist, one strand or two. Also fixed: `src/seasons/s09/index.md` linked its episodes under `s04`.
- **A `README.md` in every subsection of the repository** (2026-09-06, at Dermot's ask). Twenty-two files: `src/` and each of its content, template, data, style, script and media directories, plus `lib/`, `scripts/`, `test/`, `story-bible/`, `prompts/` and `.github/`. Each says what the directory holds, which rules bind it, and where the authority lives (`CLAUDE.md`, `TECHNICAL-README.md`, `TestingStrategy.md`), pointing rather than duplicating. Because Eleventy treats every `.md` under `src/` as a page and the gates treat it as content, the engine learned to leave the name alone in the same change: `.eleventyignore` excludes `src/**/README.md`, the five passthrough copies in `.eleventy.js` filter it out so no domain serves `/css/README.md`, and `validate-content.js`, `check-changelog-coverage.js`, `check-related-terms.js`, `list-canon-facts.js` and `src/_data/latestLore.js` skip it. Recorded in `TestingStrategy.md` for the next walker. One thing the READMEs surface rather than settle: the root README's licence split names neither `src/journal/` nor `src/about/`, so by its wording both fall on the MIT side; flagged for Dermot, not changed.
- **Character page for [Teddy](src/characters/teddy.md), the first individual Pandoid** (2026-09-06, drafted at Dermot's direction, verbatim in `story-bible/intake-2026-09-06.md`: *A dangerous Teddy Bear, actually a Pandoid, soft and warm, but never weak or foolish*). Written in the Fliade entry's observational register — a surface party watching a person who was mostly watching them — and built on the naming doctrine applied a third time on that world: *Anstolik* for reindeer, *Pandoid* for bears, and now *Teddy* for a toy, each recorded as the observers' word. Logged as Individual Four: smaller than any adult the survey has measured, age deliberately unfixed, the only Pandoid who comes up to the levels the party works, never once seen doing it, and the mover of one survey relay to the exact line where the party's own furthest tracks stop — two readings filed, courtesy or a line drawn, neither chosen. No contact, no speech, no name: all marked. Survey Lead Nakagawa and Specialist Egede reused from the Anstolik codex. **Approved by Dermot the same day** (*Approved*, on the PR as it stood): the relay — the one act that answers Fliade's asymmetry from the other side — is canon, and the page stands as drafted; where the character lives was answered by the thread ruling below. No image; a prompt is queued in `story-bible/images.md` and held until he says what a Pandoid's face is, since canon describes none. Tags introduce `pandoid` on a character page.
- **Chapter S02E09C01 — [*The Thirteenth Requirement*](src/seasons/s02/e09/s02e09c01.md)**, with a new Episode 9 index page (2026-09-05, drafted at Dermot's ask: *Draft the next Barsik and Bubochka case*, the third of the evening). Late summer 2827 on Eden: the compliance framework's *twelve requirements and counting* counts, and the thirteenth extends loose-article control to every deck area outside a registered berth or sealed store, citing the fold-proximate monitoring baseline. Its first effect is the housekeeping night pass tidying the bureau's open evidence shelf into lost property, one Unattended Article Report per item, with a fourteen-day hold before reclamation. Four POV blocks, three heads: Bubochka, who has kept counting the night pass since *The Control Group* and reports the route change on the first night, and whose fifth exercise is to watch Barsik ride the housekeeping unit through the records room's service hatch on the twelfth night and set the bureau's own form on the Inspector's blotter; Albercombe, who finds his bag of bolts on his desk, his shelf clean, and six weeks of boring cases in the lost-property bin, and takes the question home rather than ask Reeves; Albercombe again at dinner, where Jeeves has read the circular, names the requirement and its effective date, notes the two days left on the hold, and observes that the bureau's cat has never once been seen to carry anything; and Barsik, who read the requirement the day it circulated and holds that the doctrine is a rule about the order of things and says nothing about a clock, so on the one day between a question that has not arrived and an answer about to be gone it yields — to time, not to volunteering. Commits the story to: the thirteenth requirement and its wording; the fourteen-day reclamation hold; the evidence shelf registered as a sealed store by route reconciliation, no article lost; and Barsik seen carrying, at night, by one witness whose watching was the exercise. The count of people on Eden who have not finished a question about him stays at two, with Albercombe as one who asked and will let it go and Jeeves as one who has chosen not to start. Block lengths sit in the children's band. `story-bible/narrative-gaps-checklist.md`'s Season 2 count updated to thirteen chapters, nine episodes.
- **Chapter S02E08C01 — [*The Control Group*](src/seasons/s02/e08/s02e08c01.md)**, with a new Episode 8 index page and a character page for [Detective Priya Nakamura](src/characters/priya-nakamura.md) (2026-09-05, drafted at Dermot's ask: *Draft the next Barsik and Bubochka case*, the second of the evening). Summer 2827 on Eden: the floor's joke is that the rabbit found two cats, and Nakamura, the junior detective who helped build Knopka's *noticed lunch* record, designs a proper test — six small objects, one a day, hidden at rabbit height, logged in her own notebook because a hypothesis is not a case. Four POV blocks, three heads: Nakamura, who writes down the flaw in her own experiment on day two (a rabbit hiding something also does nothing) and on day three finds the first hide gone; Bubochka, whose fourth exercise Barsik sets in four words, *Do nothing. Do it well.*, and who does it for six days in front of a detective who has not finished explaining her; Barsik, who reads the six Unattended Article Reports first, holds that a cover is never in danger from an enemy but from a competent colleague who has not finished asking, and sits on the lost-property bin on the sixth morning so the true, boring, complete answer explains itself; and a Nakamura coda. Commits the story to: Eden's loose-article requirement (unattended articles under two hundred grams collected on the housekeeping night pass, one Unattended Article Report each, to the records room's lost-property bin); Nakamura's test closed as *control compromised* with no finding on the rabbit anywhere in the bureau's record; Nakamura lifting Barsik off the bin, the first person on Eden to move him; and Barsik's heading for people who have not finished a question now carrying two names, Oyelaran's and hers — Nakamura has an unopened question about the cat, written down and not filed. Her page follows Achebe's shape, no image, with a prompt queued in `story-bible/images.md` beside Achebe's (heritage stated on the Oyelaran precedent, Dermot's to change before generating). Block lengths sit in the children's band. `story-bible/narrative-gaps-checklist.md`'s Season 2 count updated to twelve chapters, eight episodes.
- **Chapter S02E07C01 — [*Somebody's Cat*](src/seasons/s02/e07/s02e07c01.md)**, with a new Episode 7 index page (2026-09-05, drafted at Dermot's ask: *Draft the next Barsik and Bubochka case*; approved by him the same evening — *PRs approved* — `story-bible/intake-2026-09-05.md`, the evening section). Early summer 2827 on Eden, the week Zhulik's family clears the compliance queue and moves out of the transfer berths: the cat goes back to the old door, is packed asleep into a departing family's bedding crate, and the shuttle lifts in three hours. Eden has no form for a lost animal and the bureau cannot take a case from an eight-year-old, so the boy, Lev, asks the author who writes the cat books, who sends him to the rabbit as a joke, and he takes it seriously. Four POV blocks, three heads: Kate Ukrainka at her galley table, hearing her second book performed; Bubochka, taking the first case she has ever been given without an instruction, and closing it in seven minutes by going to the crate before there is a chain to stop; Barsik on the gantry, who came to see whether the loading chain would stop for a boy and did not find out, because the trainee had understood that a refusal works where there is still a person and not yet a procedure; and a short Kate coda. Commits the story to: no form for a lost animal on Eden; Zhulik's family in permanent quarters two decks down; the five objects from the spring recovered in the crate; and Bubochka's third exercise, self-assigned. Muffin is not touched; the chain's failure to stop for a warden is recalled from *Carrying Two* and the comparison left to the reader. Kate's unnamed cat is mentioned and nothing about it is decided. Block lengths sit in the children's band. `story-bible/narrative-gaps-checklist.md`'s Season 2 count updated to eleven chapters, seven episodes.
- **Character page for [Zhulik](src/characters/zhulik.md)** (2026-09-05, drafted at Dermot's ask, *Draft the Zhulik character page*, the follow-up named when *The Unacceptable Cat* merged). The second cat on Eden's small-livestock register: every fact on the page is drawn from S02E06C01 — the family in the transfer berths, the application at week five, the monitoring officer's list and her finding *consistent with station animal (cat)*, the Senior Sergeant's wrong name, Bubochka finding him and Oyelaran witnessing him, the note on Barsik's return, the name his mother says means what the cat does — plus two extensions that follow from the record: Barsik's quiet check closed with the finding *cat*, and the page's own statement that it holds no motive. Not a Smart Pet, never a candidate; tags `cat`, `eden`, `pet`, `undercover-pets.com`, so he reaches the pets edition by topic. Stands behind a PORTRAIT PENDING card (`zhulik.jpg`, 1600×900, stamped): a cat is photographable, so under the 2026-09-03 image ruling the page takes a card and not a prompt, and no frame in `own-photography.json` is a cat. The card was drawn by a Pillow port of `make-placeholder-card.ps1`'s `New-Card` on a Linux session and the page added to that script's card table, so a Windows re-run redraws it in Georgia; recorded under *Open work 0* in `story-bible/images.md`.
- **Chapter S02E06C01 — [*The Unacceptable Cat*](src/seasons/s02/e06/s02e06c01.md)**, with a new Episode 6 index page (2026-09-05, drafted at Dermot's ask: *The Unacceptable Cat. A Barsik and Bubochka case. A miscreant cat steals small objects and scatters them for mischief.*; approved by him the same evening — *PRs approved* — `story-bible/intake-2026-09-05.md`, the evening section). Late spring 2827 on Eden: fourteen small objects go missing from the bureau floor over three weeks and nine turn up where only a cat could put them, so the deck's monitoring officer ticks *unacceptable* on the station mascot's quarterly small-livestock return, which opens a welfare-office conduct review whose usual outcome is a carrier on the next transfer out. Five POV blocks, three heads: Superintendent Oyelaran, who cannot interview her suspect and has an opinion anyway; Barsik, whose badge cannot speak to the one office that can end his posting and who sits on her desk in plain view instead; and Bubochka, whose exercise is to find a cat that is not on the books, not catch it, and get it seen — which she does by standing bolt upright in a corridor so a detective looks where she is looking. The miscreant is Zhulik, a young grey-and-white cat belonging to a family whose small-livestock application is still in the compliance queue, taken for Barsik by the whole floor for three weeks because on a habitat with one cat every cat is that cat. Commits the story to the conduct box and the review it opens, to Zhulik and his family on Eden, and to Bubochka's second closed exercise; motive is left as a young cat's own business. Block lengths sit in the children's band. `story-bible/narrative-gaps-checklist.md`'s Season 2 count updated to ten chapters, six episodes.
- **Scene draft: *You're Up Early*** — `story-bible/scene-draft-youre-up-early.md` (2026-09-05, drafted at Dermot's direction, verbatim in `story-bible/intake-2026-09-05.md`: a lingering shadow tries to invade Tobble's noögenic universe wearing Tissadelle's appearance and form, and behaves as she never would). Two scenes, unpublished, slot unfixed: Tobble at the edge of his universe after the founding, meeting something with her face and hair worn loose and the questing layer's dress, testing it with Órla's doorstep line and the arithmetic, and naming what he knows — including that he wanted her back — so nothing can feed on it; then Wender at Threshold, where the arrays log a signal at the founding site that asks for the file to be reopened, and she keeps it closed because the woman never asked anyone for anything. The thing is filed as consistent with a nonviable shadow-seed and never named; what it is, whether it leaves, and whether the coda stays are put as shapes in the intake. Sits behind the terminus draft and cannot publish before it. The arc note and `open-questions.md` carry the spoiler-side half; two stills and a five-frame strip from the clip Dermot supplied are parked in `story-bible/reference-art/` and recorded in `images.md`, nothing shipping. **Revised the same day on two further directions**: the forming universe past the holding is ordered by roles before law and looks like the lost questing layer in shape and not in magic; and the shadow's claim to be *Empress* is what undoes it, because in the world Tissadelle kept the titles ran the wrong way (Dermot's ruling, same day: *titles were inverted in Tissadelle's secret world*) — the grandest names on the smallest jobs, the top office too plain to want — so *Empress* was the bottom rung, the claim is a claim to be the least thing there, and the founder answers it in the world's own grammar. The three readings of the inversion were confirmed by him the same day; the top office's name is left unstated by his choice. **Approved by Dermot the same evening** (*The scene is approved*), both scenes as drafted; it remains story-bible material until the terminus has a slot.
- **A *Formation* section on [Noögenic Protouniverse](src/glossary/noogenic-protouniverse.md)** (2026-09-05, Dermot's direction, verbatim in `story-bible/intake-2026-09-05.md`, second: *noögenic universes are more vulnerable in their formation stages, almost resembling a medieval fantasy world*). An extension: a forming protouniverse is ordered the way an interior is ordered, by roles before law, because what a seed carries is structure and nothing else has settled yet — which is why the stage reads like the setting of a medieval romance, in shape and never in mechanism. The vulnerability is the shape's own: where a title is load-bearing a claim to one is a structural act, and Unfounded counterfeits and the shadow-seeds of a failed compression are the standing hazard. Parent Telearch jurisdiction and the founder, the one thing in the universe that can answer, are what keep it from being fatal.

- **Five character pages for Season 10 — [Iklix Varn](src/characters/iklix-varn.md), [Tikket](src/characters/tikket.md), [Kattik](src/characters/kattik.md), [Tekka](src/characters/tekka.md), [Tsikk](src/characters/tsikk.md)** (2026-09-04, drafted at Dermot's ask — *draft the five characters please* — and approved by him the same evening with all four readings confirmed; `story-bible/intake-2026-09-04.md`, twenty-first message). Every fact is drawn from the two published chapters, the treatment, the mind-design note and the species page: Varn as Zoe Smith's Deputy on Eden's fold-approach watch, early twenties, no number; the four Chthonari as a contractor crew on a first off-Belt posting, each page carrying the crew, what a Chthonari cannot do on Thrum's precedent, and the one axis the chapters gave that member. Each stands behind a PORTRAIT PENDING card, and five prompts are queued in `story-bible/images.md` under *Orbital Five-O, Season 10*, Varn's held until Dermot says what the Deputy looks like.

### Changed

- **A name among the Told grows** (2026-09-06, Dermot's ruling *names can be extended and grow in a way that shows clarity and maturity*; `story-bible/intake-2026-09-06.md`, fourteenth section). [The Told](src/glossary/the-told.md) now says a name is not replaced when a truer thing is said of a person later: the truer thing is added, the name gets longer, and a long name is a grown one. The record renders a grown name as it renders the first and does not shorten it.
- **The shadow at the edge: what it is, ruled** (2026-09-06, Dermot's *Tissadelle recommendations accepted*, verbatim in `story-bible/intake-2026-09-06.md`, tenth section). Read as the two recommendations the 5 September intake put: the shadow in *You're Up Early* is a nonviable shadow-seed of the Dock Seven compression, and the Wender coda stays. Recorded across the intakes, the draft's header note, `open-questions.md` and the `CLAUDE.md` inventory; the approved draft's text is unchanged and nothing is published. Still open: whether it ever leaves, the slot, the members' names and count.

- **Dates are absolute: every relative *Year -N* / *Year 0* label resolved to UCSD, and every bare pre-adoption year given an era** (2026-09-06, Dermot's direction, verbatim in `story-bible/intake-2026-09-06.md`, ninth section). Against the story's present of 2826 UCSD: the Season 1 prequel timestamps and canon facts (S01E00C00–C04), the six `year-minus-` timeline entries (filenames kept as permalinks), the Elvira, Aldera-adjacent, Nessa, Anne Wender, Galahad Thorne and Brother Daire pages, the Survey Corps protocol (*Issued 2811 UCSD*; the two-chronometer rule of 2809 UCSD), the three Dock Seven reports of 2815 UCSD, Rook-7's log, and one Journal sentence. Pre-adoption years now carry AD or CE: Underwriters Laboratories' 1894 CE and the 2089 CE consolidation, the subsea-drone programme's 2041–2280 CE, the Navigation and Trade Accords as *2337 UCSD (2137 CE)*, and 1216 AD on the rivers page and in S07E01C01. *The Life of Saint Aoife*'s in-voice years stand. Threshold Station's own report numbering (*Report 7 (Year 3)*) is left as the station's notation and put to Dermot as three choices. [Count and Calibration](src/lore/universal-cosmic-stardate.md) no longer says relative references *remain valid*; a relative reckoning is converted to the spine on filing. Recorded as a standing convention in `CLAUDE.md` and `src/timeline/README.md`. A clarification: no date moved.

- **Two timeline phrasings tightened on [Elvira](src/characters/elvira.md) and [Aldera](src/characters/aldera.md)** (2026-09-06, at Dermot's ask after the Aldera timeline check). Elvira's *roughly half a year before her arrival at the outpost the following spring* becomes *one winter before her arrival at the outpost in the early spring of Year -3*; Aldera's *logging for months before Elvira began her own records* becomes *logging since deep winter, before Elvira arrived in early spring and began her own records*. Each now states only the seasons the timeline entries fix. Clarifications; no chapter carried either phrasing.

- **[The Flight of Aldera](src/codex/flight-of-aldera-chronicle-of-dun-ri.md): two sentences in the archivist's note on the Abbey's two shelves** (2026-09-06, the one finding of the overlay audit, Dermot's ruling *Option 2 accepted*). The telling's *Christian texts for the Church's eyes* and hidden *how the world actually works* shelf are read as scripture and natural philosophy, the two shelves every house of the tradition kept, the reciters' phrasing as the reciters' and the shelves as the Abbey's. A clarification; the story text is untouched. The same evening's timeline check for Aldera (at his ask) found canon's chronology consistent from 2806 to 2826 and found one line in this entry's description — *collected long before* — asserting an era the Episode 0 chapters contradict; put as three choices and **ruled minutes later (*Option 2 accepted for the description as well*)**: the description now reads *recited in the Fellowship's chapters, its collection undated*, fixing no era, while the archivist's in-character *this era's* and the two open questions in the note stand as July left them.

- **[The Fellowship of Light](src/lore/fellowship-of-light.md) gains a *Two Refusals* section** (2026-09-06, Dermot's direction, verbatim in `story-bible/intake-2026-09-06.md`, eighth section: *The Fellowship of Light will never dismiss or overlook scientific data but neither will they over generalize in a way that undermines theology*). A clarification: the two refusals — never against the data, never past it — are named as one posture and grounded in the published pages that already ran on them (*The Reading Before a First Vigil*, *Jehu Among the Comparanda*, the Cascade's method limit, the entry's own Overfold and general-resurrection paragraphs). Recorded in `CLAUDE.md` as the companion to the spiritual edition's rule from the contemplative tier's other order, and as characterisation binding every Fellowship-voiced page. Nothing altered.

- **Intake: a spiritual edition with a governing rule** (2026-09-06, Dermot's direction, verbatim in `story-bible/intake-2026-09-06.md`, seventh section: the contemplative tier's spiritual edition should have a Codex that *follows mainstream Christian tradition except where superseded by canon lore*, on the pattern of the hard-SF physics rule). Recorded with five readings flagged — the edition is `church-space.site` as it stands, the rule has the physics rule's shape, superseded means lore wins and the entry shows the yielding, canon status and the centralised-canon rule are unchanged, no engine change — and three questions put as choices in `story-bible/open-questions.md`: what *mainstream* means, the thread index's *evangelical* against it, and whether the rule covers the whole overlay or the Codex only. Nothing built and no published page changed. **Ruled the same evening** (*Confirmed*, read as the five readings and the three recommendations): recorded as a standing rule in `CLAUDE.md` beside the physics rule and as a third validity test in `src/codex/README.md` — mainstream is the creedal consensus, canon lore is the only licence to depart from it, the page shows the yielding, and the scope is the overlay's own pages by tag, which the intake corrects from a nine-and-six tag count to the true position (the edition renders 62 codex entries and 139 lore pages by tier inheritance; the overlay's own are three codex entries and six lore pages). Those nine were audited against the test the same evening: all pass, *The Resurrection and the Life* named the model, one line in *The Flight of Aldera* put as a choice. No published page changed.

- **The Journal and `story-bible/` are written at the contemplative reading level** (2026-09-06, Dermot's direction, verbatim in `story-bible/intake-2026-09-06.md`: *Journal entries are treated as contemplative reading level, so being impossible to misunderstand is more important than using plain language for the story bible itself and the journal entries*). Recorded in `CLAUDE.md` and `src/journal/README.md`: precision outranks plainness, technical terms and proper names are welcome when defined on first use, a loosely true claim counts as a misunderstanding waiting to happen, and an entry that outgrows the tier's 800–1,200-word band splits along its section breaks. A register, not a gate — journal entries stay on every edition that carries the section.
- **Who the Journal is for** (2026-09-06, later the same day, Dermot's clarification of the rule above, verbatim in the intake's sixth section: *otherwise the journal is for the benefit of critics, reviewers, peer authors and students of my writing*, after *the journal is more relevant to parents and teachers than to child readers themselves*). Recorded in `src/journal/README.md` and `CLAUDE.md`: the register follows from those readers rather than from the tier, the publication test is whether one of them would learn something about how the work is made, and the children's edition stays without the Journal — which the classifier already did, since no entry carries the pets topic. No engine change.

- **[Membrane Shadows](src/lore/membrane-shadows.md) and the [Membrane Shadow](src/glossary/membrane-shadow.md) glossary entry: the silhouette's strength comes from the Interval, its shape from the neighbour** (2026-09-06, at Dermot's ask, a clarification). The entry could be read as saying a far-side body's own gravity leaks across and bends light, which no body's weight can do. The mechanism section now states that the coupling in dimensions 12–15 patterns the strain between two Class III-proximate membranes with the far-side mass's outline while the strain's magnitude is the boundary's; the hazards bullet and the detection bullet read accordingly. Nothing about what a Membrane Shadow is, where it appears or what it can do has changed.
- **A generated portrait is acceptable for a fictional character of a photographable species** (2026-09-05, Dermot's ruling on seeing Zhulik's page go up behind a card: *Although I could photograph a cat for you; this one is a fictional character on a futuristic space habitat, so I think an AI generated portrait would be acceptable.*). Recorded as a clarification under the 2026-09-03 exemption in `story-bible/images.md`'s Conventions: "someone who does not exist" is about the subject being invented, not about whether a camera could photograph its kind; the photograph-it rule governs the world's furniture, not a named character. A prompt for `zhulik.jpg` is queued under *Undercover Pets* in section 1 (count 5 to 6), the *Open work 0* card note points to it, and the card table row in `make-placeholder-card.ps1` says to drop the row when the portrait lands. Not generated in this session: it has no Gemini key and no `gemini.local.json`, so the stamped card stands and `image-prompts.js --list` reports the entry as `[placeholder]` until the run.
- **Bubochka's and Oyelaran's pages carry *The Unacceptable Cat*** (2026-09-05, at Dermot's ask — *Add a short section on Bubochka's page and a line on Oyelaran's* — the last of the three follow-ups named when S02E06C01 merged). [Bubochka](src/characters/bubochka.md) gains a short section on her second exercise, drawn from the chapter — the three-part instruction, the evidence tray, the Senior Sergeant's wrong name, the Drithane lesson turned round, Barsik's log line — and her stale *has not yet filed a report anyone upstream has needed to act on* line is re-tensed as a clarification, since Drithane and this case both moved something. [Rasa Oyelaran](src/characters/rasa-oyelaran.md) gains one sentence under Character Notes: the alibi she wrote in her own hand, and the timing she filed in the tray for things she intends to think about. Both pages link to [Zhulik](src/characters/zhulik.md) and to the chapter.
- **Tissadelle's header portrait is now the Season 5 Line Captain frame** (2026-09-05, later the same evening, Dermot's ruling on the three shapes — *1 Yes 2 Maybe 3 Unknown*; `story-bible/intake-2026-09-05.md`). The coverall portrait it replaces predates the rank-insignia canon and is retired to `story-bible/reference-art/`; the gallery keeps *Season 1 — Cadet*, and the Season 5 frame is not duplicated there. Alt text rewritten from the new header. Season pages (*maybe*) and her look in Seasons 6–7 (*unknown*) go to `story-bible/open-questions.md`, neither built.
- **[Tissadelle Shepherd](src/characters/tissadelle-shepherd.md) gains a captioned gallery of season portraits** (2026-09-05, Dermot's direction — *Tissadelle can have two or more portraits for each season*, grounded the same evening: *one of the principal major characters* — `story-bible/intake-2026-09-05.md`). Two frames in the correct Ranger uniform per *Reading Rank at a Glance*, generated from the prompt-sheet 2.7 brief and its Season 1 variant with the published portrait as face reference: *Season 1 — Cadet* (single bar, slate-blue insert, no decorations) and *Season 5 — Line Captain* (three concentric rings). The header portrait is unchanged; this is an addition, not a swap. `character.njk` gains an optional `caption` per `gallery:` item, rendered as a figure at 16:9 with the caption beneath; uncaptioned galleries render as before. Runs and rejects in `story-bible/image-prompts.md`. Open: what she looks like in Seasons 6–7, which has no brief and is his.
- **Portraits for [Marisol Achebe](src/characters/marisol-achebe.md), [Priya Nakamura](src/characters/priya-nakamura.md) and [Zhulik](src/characters/zhulik.md)** (2026-09-05, generated at Dermot's ask and the picks approved by him the same evening; runs recorded in `story-bible/image-prompts.md`). Achebe from one run. Nakamura and Zhulik from re-rolls after his note that both needed a more futuristic background: the prompts in `story-bible/images.md` are revised in place — steel lockers and a run of pipework replaced by seamless composite panels and a recessed light line — and each was re-rolled against the variant he had seen, staged in `story-bible/reference-art/`, so the person and the cat held. Zhulik's portrait replaces his PORTRAIT PENDING card. Alt text written from each filed image. **Tissadelle in the correct Ranger uniform** was generated from the prompt-sheet brief and filed as reference art only (`story-bible/reference-art/tissadelle-shepherd-uniform-2026-09-05.jpg`); whether it replaces the published portrait is his call and was not made.
- **Codex title cards for [The Courtesy That Was Withdrawn](src/codex/the-courtesy-that-was-withdrawn.md) and [Neither Author Nor Warden](src/codex/neither-author-nor-warden.md)** (2026-09-05). Both entries shipped on 2026-08-25 without a card; the two `make-codex-cover.ps1` commands queued in `story-bible/prompt-sheet.md` 2.5 and 2.6 were run as written, the fadas in *Tír na nÓg* and *Ní Mheara* survived the type pass, and each entry's `image:` and `image_alt:` landed in the same change. The sheet's 2.4 and Part 3 headings are marked done as well, since those images were filed on 3 September and the sheet never said so.
- **Portraits for the Chthonari crew** — `tikket.jpg`, `kattik.jpg`, `tekka.jpg`, `tsikk.jpg` (2026-09-04, generated at Dermot's direction and the picks approved by him the same evening; run recorded in `story-bible/image-prompts.md`). Twelve images in two runs: Tikket and Tsikk came back on the crew's body first time; Kattik and Tekka drifted into a crouching humanoid, one on a present-day tablet and one with a mouth full of teeth, and were re-rolled against the Tsikk image as a reference with the prompts revised in place in `images.md`. Filed at 1200×670 over the PORTRAIT PENDING cards, alt text rewritten to describe each picked image. Three of the four share one ring setting, the reference having carried it, which was said before he chose. Iklix Varn's card stands and the prompt stays held.
- **Portrait for Iklix Varn** — `iklix-varn.jpg` (2026-09-04, later the same evening, once Dermot said what the Deputy looks like — *lean build, dark hair, quiet bearing* — and approved the pick; run recorded in `story-bible/image-prompts.md`). Two variants, both on his three words, differing in the face; variant 1 filed at 1200×670 over the card, the wider frame with the whole compartment, the six units and the hatch, the Deputy reading the longer document standing up. The page now carries his three words as the one physical description the record holds, and the alt text describes the picture. All five Season 10 portraits are filed.
- **Iklix Varn's portrait re-rolled on Dermot's reservation** (2026-09-04, *does the technology look too old fashioned?* — it did: twentieth-century meters, toggles and tube screens in a rack, the Barsik-cabinets failure again). The approved image was kept as a reference so the person would hold, the prompt revised in `story-bible/images.md` to sealed matte housings with a single indicator, and re-roll 1 filed on his *Agreed*, read as covering the stated lean; the first run is kept aside and the swap back is one command if that reading was wrong. Alt text updated.
- **Rank insignia made canon — *Reading Rank at a Glance* on [Rank and the Chain of Command](src/lore/star-rangers-command-hierarchy.md)** (2026-09-04, Dermot's *Agreed* on the three shapes put to him after he asked what a Deputy's insignia is; an extension under the canon rule). The tab as the single mark, magnetically seated and plain, and why the neutrality clause allows nothing more; the bar count made explicit, Novice one to Section Lead seven, a Deputy three; Chief's bar-and-ring; rings only above; a seal for the Council and the Triumvirate; the six track colours, muted, on the tab and never on the garment; certification mark and decorations as the other two of three marks never conflated. The uniforms note's two proposal sections are struck through as canon since this date. Portraits keep their plain tabs, which is what canon specifies at that scale.

### Fixed

- **Every character status now reaches a badge style, At large and Contained included** (2026-09-06, at Dermot's ask, in three steps the same morning). `character.njk` built the badge's modifier class by lowercasing the status, so *At large* emitted a class with a space in it that matched nothing, and Ilse Korvain's *Contained — Survey Corps custody, jurisdiction formally disputed* could never have matched anything however it was cased. The class key now comes from a new `statusKey` filter (`lib/status-key.js`, pinned by `test/status-key.test.js`): the status's head clause before an em dash, colon, semicolon, comma or parenthesis, slugified, so single-word statuses keep exactly the class names they had, *At large* becomes `at-large` and the Korvain status becomes `contained`, while the badge still prints the whole string. Two rules join `src/css/main.css`: `at-large` is the canon colour with a dashed outline, known to the record and not where anyone last put it, and `contained` is its inversion, filled with the canon colour and lettered in the page background. That filled pair is the one new foreground/background meeting on the site, so `check-contrast.js` gains it as a measured pair (5.20:1 at the tightest, theme-light). Muffin and Ilse Korvain are the two pages affected. Theme files regenerated.
- **Status badges for Historical and Retired characters now carry their own colour** (2026-09-06, at Dermot's ask, the follow-up to the header fix). Only Active and Unknown had a dedicated badge style, so the six Historical pages and the one Retired page fell back to the plain muted pill and read the same as an unclassified status. `src/css/main.css` gains two rules: Historical takes the canon colour, since a historical figure is the record's own past, and Retired keeps the muted text but draws its outline dashed in that colour, so it reads as out of service. Both use tokens the contrast checker already measures against the badge background in every palette, so no per-theme literal was added to `generate-themes.js` and every palette passes at 4.5:1 as before. Theme files regenerated.

- **Character-page header no longer collapses between 641px and about 880px** (2026-09-06, Dermot's pick of the two shapes put to him). The header is a two-column flex row whose 420px portrait cannot shrink, but it only stacked at the site-wide 640px breakpoint, so on tablet-width viewports the text column beside the portrait fell to about 100px: the role badge rendered as a six-line pill with its text clipped, the character's name broke mid-word, and every tag took its own line. `src/css/main.css` now stacks the character header in its own 900px media query, leaving the two-column layout only where the text column keeps a real measure; the site-wide 640px query is unchanged for everything else, and the theme files were regenerated. Verified by rendering `tissadelle-shepherd` at 641, 800, 901 and 1280px.

## [1.32.0] — 2026-09-04

The evening of the same day, and the release in which Orbital Five-O gets
its second season and the record its first alien ensemble. One direction —
*at least one thread or strand featuring a set of alien protagonists* — was
carried through a day of rulings to two published chapters: an alien strand
in the next Five-O season; the Chthonari to carry it; a convergence on two
readings of one structure; Zoe Smith's Deputy, Iklix Varn, because a Corps
Deputy inside Five-O is not allowed; four voices with struck names; the year
2828; pronouns translated because the biology gives nothing else; and a
mechanism of accidental damage left unfound by a hollowed regime and by
inaction, catastrophic in potential. *Sent for the Log* and *Sound, as
Certified* open Season 10 with no shared scene. Beside them, a mind-design
note for how a Chthonari thinks, a naming convention on the species page,
and *Please, and Thank You* on the Kernel page, where a lawful machine's
courtesy follows from the invariants.

### Added

- **[Sent for the Log](src/seasons/s10/e01/s10e01c01.md) and [Sound, as Certified](src/seasons/s10/e01/s10e01c02.md)** — S10E01C01 and S10E01C02, the first two chapters of Orbital Five-O's second season and the first of the alien strand (2026-09-04, drafted at Dermot's go-ahead after the day's rulings and approved by him the same evening). Strand A, Iklix Varn and Zoe Smith on Eden's fold-approach watch, 2828: a countersignature needs two documents, the Deputy fetches the contractor's log from the Compact side and finds the one line the certification struck as *n/a*, and Smith declines to countersign and declines to refer. Strand B, Tikket and Kattik under the dock ring three weeks earlier: the third approach mount rings under more than it was left to carry, and the one who keeps the log writes the line without believing it, because the crew's log is the one record that is theirs to leave. No shared scene. Season 10 index pages added on the Season 4 pattern.

### Changed

- **Season 10 registered to Orbital Five-O, and Chthonari names on the species page** (2026-09-04, with the chapters above). `lib/storyline-threads.js` gives the thread `seasons: [4, 10]`. `src/lore/chthonari.md` gains a paragraph beside the pronoun one: a name is struck, not spoken, a signature of strikes on whatever structure is underfoot, which a human ear renders in unvoiced clicks — the four crew names given as what a crew sounds like to people who cannot feel them. An extension under the canon rule, drawn from Dermot's direction of the day (*high click phonetic names if that suits their biology*) and the record's own account of the Undersong.
- **The mechanism of Season 10's case** (2026-09-04, Dermot's direction — *accidental damage due to negligent maintenance, cost cutting or unrealistic schedules, political inaction or corruption with potential for catastrophic damage*; story-bible only, four readings flagged in `story-bible/intake-2026-09-04.md`, twentieth message). Recorded on the treatment: nobody meant it and everybody could have stopped it; the crew's error-first guess stays right about the structure and Five-O's intent-first guess about the record; held inside the 26 August ruling that refuses the corrupt politician as any leader's engine and allows only minor corruption at the margins; the catastrophe is potential, hinted and not shown. Iklix Varn's age ruled the same evening as young, mature, capable, read as early twenties with no number fixed.
- **Please, and Thank You — how a lawful machine speaks** (2026-09-04, drafted at Dermot's direction that robots use please and thank you to humans and to any culture that expects it, and among themselves only where the words carry a second meaning; approved by him the same day with all four readings confirmed — `story-bible/intake-2026-09-04.md`, eighteenth message). A section on `src/lore/ai-safety-kernel.md` between *Reasoning Integrity* and *Equivalence Freedom*, derived from the invariants rather than added as etiquette: an omitted courtesy where it is expected is a small harm under the First; addressing a Mnemari treaty-witness like a cargo lift misrepresents under the Second. Between machines the words are instruction — *please* a priority flag, *thanks* a sign-off that tells the receiver to release, clear and leave the shared resource as found, the Fourth Invariant in one word. A human's *please* to a machine is heard as a human's; Jeeves's *sir* is the human register all day; a Chthonari-built monitor takes no courtesy, being a member of the structure.
- **Five-O's second season: the case is the *Docked Twice* pattern one level down** (2026-09-04, Dermot's ruling — *docked twice case* — on the three case shapes put to him; story-bible only). Eden's structure carries a fold-approach resonance the habitat's monitoring reads as normal, because the ledger checks the certification record and not the structure. The treatment records what that commits the season to: Five-O's ledger right, the discrepancy between record and thing, the Chthonari crew hearing it because a fold approach is a gradient under stress, the certification record as the meeting point, and a record-versus-structure discrepancy rather than a crime of gain. Iklix Varn is confirmed as the Deputy, human by default. Still open: the four crew names and the Deputy's age.
- **Five-O's second season: four voices, 2828, and the Deputy named** (2026-09-04, three more of Dermot's rulings on the treatment's open list — *four voices, 2828 or later* and *iklix varn*; story-bible only). The Chthonari crew is four voices, the register's ceiling, one for each of the mind note's axes; the working year is 2828, the season after *Docked Twice*, with later needing only a reason; and the young Deputy is Iklix Varn, read as the Deputy's name because it was the first name open, checked unused in the corpus, with age and species still his. Still open: the four crew names, the Deputy's age and species, the case.
- **Chthonari pronouns: translated, and the species page says why** (2026-09-04, Dermot's rule *translate the pronouns or derive from biology*, then *follow your recommendation*; the one lore edit of the day's Five-O rulings). The derivation found no sex in the record and one person-marking distinction the Chthonari sensorium makes, contact. A paragraph at the end of `src/lore/chthonari.md`'s Interspecies Notes: the record renders a Chthonari person as *they*; nothing in the described biology sorts persons by sex and their own speech does not either; the Undersong marks a person by contact, felt or not felt, a distinction the translation lets go since a reader on a page is in contact with no one. An extension under the canon rule, adding without altering; no glossary paradigm, since a translation declares none. `story-bible/mind-design.md` and the treatment record the ruling.
- **The young Deputy is Zoe Smith's** (2026-09-04, Dermot's conditional ruling — *a Five-O deputy if allowed, otherwise Zoe's* — with the condition measured against canon; story-bible only). A Corps Deputy inside Five-O is not allowed: Rangers have no authority inside the self-governing habitats and Larsen resigned his commission to lead the task force; a civil junior on Five-O's establishment would be allowed but the word is a Corps rank the young-adult tier already carries. So the Deputy is Zoe Smith's, and the treatment's Strand A is rewritten around it: Eden is both a Compact habitat and a Fold-Proximate Research Platform under Survey Corps oversight, so her Deputy stands the watch on Eden's fold-approach side and is sent for the Chthonari crew's log on the Rangers' own paperwork — three parties, none of whom can report to the others, with the reader's proxy the one who has no authority here. Recorded as a re-grounding of the 3 September third direction; the Deputy's name, age and species stay his.
- **The Five-O strands converge on two readings of one structure** (2026-09-04, Dermot's ruling on the three convergence shapes put to him — *option 1*; story-bible only). `story-bible/five-o-second-season-treatment.md` records what it commits the season to: a discrepancy in a habitat's structure rather than its paperwork, a Chthonari contractor crew with no standing to report what it hears, the young Deputy as the junior sent for the log, error guessed on one side and intent on the other, and a meeting that merges the two accounts and not the two crews. The season has its shape entire; its people, year and case stay his.
- **The Chthonari carry the alien strand, and a mind-design note** (2026-09-04, Dermot's ruling on the three species shapes put to him — *option 1*; story-bible only, nothing published changes). The treatment records the ruling, and `story-bible/mind-design.md` is new, the sibling of `species-design.md`: seven questions that decide how a people thinks, answered for the Chthonari from the record alone — certain of structure and unable to read a face; thought as the Undersong and an argument as a dissonance; the subtractive first move; no folk memory of scarcity, so theft is not the first guess; a machine as a completion; the daily loneliness of a structure nobody else can feel; youth as memorising the course instead of listening to the tunnel — plus four things a Chthonari cannot do and the traps the tone and trope rules close. Two physical costs are left open where the record is silent, and the pronoun and naming decisions are put as shapes without coining a form. Still his: the crew, the Deputy, the convergence, the year, the pronouns.
- **Treatment: Orbital Five-O's second season, with an alien strand** (2026-09-04, Dermot's ruling on the three shapes put to him — *an alien strand in the next Five-O season*; story-bible only, nothing published changes). `story-bible/five-o-second-season-treatment.md`: what canon already fixes (Season 10 when its first chapter exists, a year after 2827 Autumn, the Compact setting, the young-adult band, the five strand rules), the two strands — the young Deputy's task-force case and an alien ensemble — and three shapes each for the species (Chthonari recommended: already in the Solar System's working spaces, body committed by the vessik, substrate hearing as an independent line of evidence) and the convergence (two readings of one structure recommended). The direction check records it as an extension of the 3 September alien and young-Deputy directions and notes the season's year closes the Shepherd-guest window. Species, crew, Deputy, convergence, year and a mind-design note stay his, indexed in `open-questions.md`.

## [1.31.0] — 2026-09-04

The afternoon after 1.30.0, and the release in which the comments follow the
same shape as the pages. The board is picked per page rather than per build,
so a shared chapter carries one conversation on every tier that shows it and
the Communion keeps its own room; the children's tier renders no widget,
because its reader cannot hold the account posting needs; and on every other
domain the discussion is collapsed until asked for, said to be moderated, and
watched from Dermot's inbox. The Reading Tiers page ships: four readerships
that nest, and what threads, tiers, editions and strands are to each other.
Two rules of working land beside them: a PR merges on green unless a decision
that is his is still open, and a decision is checked against what was already
decided before it is recorded. One tooling fix: the contrast check reads
`main.css` the same way on Windows as in CI.

### Changed

- **Merge unless a decision is open, and a decision-making note** (2026-09-04, Dermot's two directions of the afternoon — `story-bible/intake-2026-09-04.md`, tenth and eleventh messages). `CLAUDE.md`'s authority section gains a merge policy: a PR merges when CI is green in either of the first two tiers unless it carries a decision that is his and not yet made, in which case it stops and names it; the second tier's "draft it and stop" default is superseded, its boundary unchanged. `story-bible/decision-making.md` is new, the third working-methods note beside the Benevolent Critic and the PR review: the six steps a direction goes through, which decisions are his, the merge rule, and the direction check — before a ruling is recorded, compare it with what was already decided and name a reversal, re-grounding, clarification or extension as such. It carries this day as its worked table.
- **Comments moderated after posting, said so, and collapsed by default** (2026-09-04, Dermot's ruling on the three shapes put to him the same day — `story-bible/intake-2026-09-04.md`, ninth message; he took the first two together). giscus renders GitHub Discussions live and has no approval queue, so what this stack can honestly do is after-the-fact moderation and nothing on the page until asked for. His GitHub account now watches both comments repos for all activity, so every new discussion and comment reaches his inbox. `base.njk` puts the widget inside a closed `<details>` ("Show discussion") with `data-loading="lazy"`, so nothing loads and no comment is on the page until a reader opens it, with a note beneath the summary: comments are read after they are posted and anything unsuitable is removed; posting needs a GitHub account, reading needs nothing. The disclosure and note are styled in `main.css` with palette tokens only and carried into every theme.
- **The comments board is picked per page, not per build** (2026-09-04, Dermot's ruling on the three shapes put to him the same day — `story-bible/intake-2026-09-04.md`, seventh message). The church-space thread names `giscusProfile: "church-space"` in `lib/storyline-threads.js`; `src/_data/giscus.js` returns the build's board as before plus `boards`, one per thread that names a profile, and a fork's build (`giscus.local.json` or explicit `GISCUS_REPO*`) gets none; `.eleventy.js` computes `giscusBoard` per page through `threadForPage` in `lib/content-filter.js`, the membership walk the tier gate also uses, now generalized; `base.njk` reads it in place of the build-wide global. The contemplative editions and the archive drop their build-level `giscusProfile`, so the main-sequence chapters they have carried since the tier ladder post to the shared pool like every other domain, and one chapter has one conversation everywhere it appears, while every church-space page posts to the Communion's board on any domain. The Communion board held no discussions at the switch, so nothing was orphaned. `sample-deploy.conf` loses its `ALT_*_GISCUS_PROFILE=church-space` and `ALT_*_THREADS=church-space` lines with a note on why a stale one would undo the ruling; `TECHNICAL-README.md` describes the two levels. Pinned by `test/giscus-boards.test.js` and a `threadForPage` test.
- **Comments off on the children's tier** (2026-09-04, Dermot's ruling on the three shapes put to him the same day — `story-bible/intake-2026-09-04.md`, sixth message). The `pets` entry in `lib/editions.js` now sets `commentsEnabled: false`, so undercover-pets.com stops rendering the giscus widget. Until now the entry said nothing and inherited the default's `true` and the default board, so a pet chapter's discussion on the children's domain was the shared pool's own thread. The reason is participation, not protection: posting needs a GitHub account the tier's reader cannot hold, so the box offered a child a conversation they could read but never join — and by Dermot's standing principle of the same day (eighth message), nothing from his public repos is unsuitable for any age. The chapters keep their discussions on every other domain. The pets clone's own `deploy.conf` must not set `COMMENTS_ENABLED` explicitly, since a server-side value still wins over the registry.

### Added

- **[Reading Tiers](src/tiers/index.md)** (2026-09-04, a reader-facing page for the ladder Dermot directed on 3 September; drafted for his review). `/star-rangers/tiers/` explains the four reading tiers — children, young adult, general, contemplative — as readerships that nest, names the storylines each rung adds and the address that serves it, and says what nesting means for a reader: climbing loses nothing, every chapter is complete at every tier, the canonical site sits at the general tier, and the *Reading* control is a posture rather than a tier. A *Threads, tiers and editions* section, added the same day at Dermot's ask, defines the three words against each other (a thread is a storyline, a tier is a readership defined by its threads, an edition is one address on one tier) and goes one level down to strands within a season, with Season 1 as the model. The page reads its own build's `edition.tier` to say which rung the reader is on, and links the Church Space thread like any other now that 1.30.0's tier gate has retired the old link boundary. Linked from the Official Editions page and the site footer.

### Fixed

- **`scripts/check-contrast.js` failed the default palette on Windows while CI was green** (2026-09-04). On a clean `main` checkout, `npm run generate-themes` followed by `node scripts/check-contrast.js` reported `FAIL default — status badge: missing --status-active` while the CI job running the same two commands on the same commit passed. The invocation is identical; the input is not. This repo has no `.gitattributes`, so under `core.autocrlf=true` `main.css` is checked out CRLF, while the theme files `generate-themes.js` writes with `\n` are LF — and the checker's status-badge pattern required a bare `\n` after the brace, so it found the literal in every generated theme and never in `main.css`. The `:root` pattern happened to tolerate CRLF, which is why only the one line failed. This is the same fault fixed in `generate-themes.js` on 2026-08-10, and the same fix: line endings are normalised on read, so every pattern in the checker sees `\n`. The check is not weakened — the same pairs against the same 4.5:1 floor — and the parser is now exported behind a `require.main` guard so `test/check-contrast.test.js` can pin it: the same palette from LF and CRLF input, and `--status-active` found in the real `main.css` forced to each ending in turn.

## [1.30.0] — 2026-09-04

The release in which the reading tiers became the site's one visibility
model. The 3 September ladder said which readership each edition serves; this
release makes that ladder decide everything a domain shows, retiring
`private: true` in favour of a `tier` on the church-space thread and one
shared predicate for threads and POV blocks alike. No domain gains or loses
a page. Around it, a day of directions on record: the church-space ruling
itself, the alien thread or strand, the Reading Tiers page held for review,
and the question of comments on the children's tier put as choices. Content
follows the 3 September rulings: the Prismeri given a body derived from
Prismere and their images regenerated to it, terraforming's origin in climate
repair, Aldera's channel, the Smart Pet collar, Elvira's dream, the scale of
Celestials, and the stock lore images replaced with designed cards.

### Changed

- **Tier gating replaces `private: true`** (2026-09-04, Dermot's ruling on the three shapes put to him the same morning — `story-bible/intake-2026-09-04.md`). The church-space thread now carries `tier: "contemplative"` in `lib/storyline-threads.js` instead of the private flag, and `lib/content-filter.js` hides a thread on every build whose edition sits below the thread's tier — `isTierExcluded` and `gatedThreadForPage` replace `isPrivatelyExcluded` and `privateThreadForPage` — using `tierVisible`, a new export of `lib/editions.js` that the tier-marked POV blocks in `lib/markdown-containers.js` now share, so a block and a thread cannot disagree about the ladder. The build's tier is its edition's, read into the content filter; no `deploy.conf` value can open a gated thread, and naming church-space in `CHARACTERS`/`TOPICS`/`THREADS` no longer opts it in. Every domain keeps exactly the page set it had: the contemplative editions already name the thread and the default edition sits at the general tier. Retired: the "excluded unless named in" case and the validator's public → private link boundary (a general-tier page linking into the thread gets the ordinary excluded placeholder). Kept, re-purposed: `homeDomain` for the placeholder and `signatureTags` for the missing-tag tripwire, now guarding a tier leak. `test/content-filter.test.js` rewritten to select a build tier through the real `EDITION` path; `test/storyline-threads.test.js` pins that no thread carries `private` and that a gated thread names a tier on the ladder. The church-space landing page and FAQ, `CLAUDE.md`, `TECHNICAL-README.md`, `TestingStrategy.md`, `sample-deploy.conf` and the story bible's visibility terminology describe the gate instead of the flag.
- **Intake, 4 September** (story-bible only; nothing published changes). `story-bible/intake-2026-09-04.md` records the morning's five messages: two asks realized on the Reading Tiers page (PR #660); Dermot's question whether church-space still needs to be private now it is its own edition, assessed with three shapes and no change made; the direction that the record gain at least one thread or strand with a set of alien protagonists — five readings flagged, three shapes put, a lean recorded for a strand first; and, for provenance, that the three winged figures recycled into four-islands-quest were dropped at his direction and that PR closed unmerged. Both open items indexed in `open-questions.md`.
- **The scale of Celestials and Higher Levrils** (2026-09-03, Dermot's direction; drafted for his review). A *Scale* subsection under Celestials in `src/lore/cosmic-cascade.md` and a sentence in `src/glossary/celestials.md`: a Celestial's attention is never smaller than a galaxy and usually wider, and a single world enters it only as part of a process that crosses it. A paragraph under *Sub-Classes* in `src/lore/levrils.md`: a Higher Levril's attention moves across systems and Concordants and rests on a person almost never on a place, the Turquoise Dove's cross-Concordant trail being what that looks like; a Levril that stays with one world is doing a Dragon's work, which is local by design. No placed Levril changes. All four readings confirmed by Dermot the same day.
- **Scene draft: *A Working That Worked*, Elvira's dream** (2026-09-03, drafted at Dermot's direction that Elvira dreamed she transformed Aldera into a feline humanoid using magic; story-bible only, slot unfixed, draft-and-stop). `story-bible/scene-draft-elviras-dream.md`: two blocks — Elvira's dream and its taking-apart in order (the mechanism was the village's, the shape one the world does not keep, and she had been pleased), then Aldera's morning log, which does not ask. Magic stays a POV experience; the dream is Interval weather by the record's own doctrine, and the wish is Elvira's. Five readings flagged in `story-bible/intake-2026-09-03.md`, thirteenth direction.
- **What Aldera's enhancement is for** (2026-09-03, Dermot's ruling on three options put to him; drafted for his review). A section on `src/characters/aldera.md` and a sentence on `src/glossary/cyborg.md`: the hardware is the channel, not a sense — the ear implant the uplink, the eye lens the record — so that a cat can file a log; her boundary sensitivity stays natural and is why she was chosen; the clearance was the least invasive means to an end the agency could reach no other way, a case rather than a precedent. Closes a gap the cyborg regime's "default answer is no" had opened under her label. `story-bible/intake-2026-09-03.md`, fourteenth direction.
- **The three Prismeri images regenerated to the derived body** (2026-09-03, the evening after Dermot confirmed the body plan; drafted for his review). `prismeri-first-wings.jpg`, `prismeri-full-wings.jpg` and `prismeri-lattice-gathering.jpg` return to `src/lore/prismeri.md` at 1600×893, the ILLUSTRATION PENDING card comes off, and every alt describes the flier the page now specifies: level spine, wings as forelimbs with three free fingers, prehensile hind feet, hanging at rest, the facet band on the skull ridge, tendrils on Full Wings. Prompts rewritten in place in `story-bible/images.md`; picks and one flag for Dermot's eye in `story-bible/image-prompts.md`.
- **The Prismeri given a body derived from Prismere** (2026-09-03, Dermot's direction after the regenerated images were rejected as still humanoid; drafted for his review). Three sections added to `src/lore/prismeri.md` on the Krenyi pattern: wings as forelimbs with three free clawed fingers, prehensile hind feet, quadrupedal on the ground and hanging at rest, two forward eyes and a crystalline ridge-band that reads the overhead light for the umbral ray's shadow, a mineral-feeder's mouth, the two adult forms as one body at two hardnesses; what a standard-gravity station costs them; fire as the bottleneck and lenses as the way round it, which is why Prismere's Federation contribution is expertise. The existing form sections are untouched. The derivation by the seven questions is recorded in `story-bible/species-design.md`; all six readings confirmed by Dermot the same day.
- **Where a Smart Pet's subsystem lives** (2026-09-03, Dermot's ruling on three options put to him; drafted for his review). A section added to `src/glossary/smart-pet.md`: the subsystem is carried in the collar, a certified harness design is a collar that looks like a collar, the body is untouched, a Smart Pet off its collar is simply an animal, the plain disc is the certification mark and the badge, and the subsystem identifies itself to any lawful authority that asks. The cover is the question nobody asks. `story-bible/intake-2026-09-03.md`, twelfth direction.
- **Agent Barsik's portrait re-briefed out of the twentieth century** (2026-09-03, Dermot on review: *"Barsik would not have literal filing cabinets"*; drafted for his review). The 3 September portrait had put him in front of filing cabinets and ring binders; a first re-brief kept a shelf of document cases and the generator drew binders on it, so the shelf went too. He now sits on the page's own stack of paperwork before a habitat bulkhead, pipework and a closed pressure hatch, nothing else in the room. Prompt and both revision notes in `story-bible/images.md`.
- **Seven stock lore images replaced with designed emblem cards, two Tenerife frames cropped** (2026-09-03, closing Batch 2 of the 12 August image review: every one of the 123 lore images opened and judged, none read from its alt text). Replaced, all stock photographs of present-day objects on concept or institution pages: `galactic-stardate` (an antique clock over a calendar), `year-zero` (a steampunk relief with lettering and the digits 1870), `post-teleport-ascension-stress-disorder` (a present-day operating theatre), `knarr-line` (an ISS window with a Soyuz outside), `federation-of-sentient-beings` (a real Earth-observation satellite), `star-rangers-command-hierarchy` and `united-space-consortium` (a delegate's voting console in a real parliament chamber, two crops of one shoot). Each is now a `make-emblem-card.ps1` card with its epithet drawn from the page's own description; alt text rewritten to match. `highland-rock-spires.jpg` and `highland-sentinel-lizard.jpg` lose a yellow road sign from the bottom edge by crop. Kept with reasons recorded in `story-bible/images.md`: the Dalkey Island watchtower, the JWST Carina field, the generated fusion-ceiling yard, and the Imperium tower.
- **Codex-art refiling: the two open decisions ruled** (2026-09-03, Dermot's rulings, chosen from options the same day; story-bible only). The codex title-card convention stands, and a refiled artwork goes in the entry body at full size with a caption naming its in-world maker, the card in `image:` as on every other codex entry. The rule is prospective only: the one recoverable candidate, Qiren Tal's first portrait, was opened and fails the quality bar on trope and tone, so nothing already gone is rescued and no git dig is owed on future rejections. Recorded under *Two things decided* in `story-bible/images.md`, struck through in `open-questions.md`; the parked headmate image now waits only on the interiority-guardrail ruling and a codex entry to live on.
- **The AI-image exemption given a shape** (2026-09-03, Dermot's ruling, chosen from three options the same day; drafted for his review because it is policy and reader-facing). The portfolio site's own-work-only rule stops at this repository, and the exemption is now stated under *Conventions* in `story-bible/images.md` with a pointer from `CLAUDE.md`: a generator may make only what no camera can (a non-human body, a habitat, an artificial mind, an artefact, a portrait of someone who does not exist); anything a camera can photograph comes from Dermot's own frames or takes a card, never a prompt; a concept is always carded; the membrane runs one way, so nothing the pipeline touches ever returns to the portfolio, the Gallery folders or a competition; and the About page discloses it once, in a new *How this site is illustrated* section. It settles the Tier 2 stock-landscape policy by rule (replace from the archive when a frame fits, card otherwise, opportunistically) and is indexed as ruled in `open-questions.md`.
- **The reading tiers nest** (2026-09-03, Dermot's direction, all readings confirmed the same day — `story-bible/intake-2026-09-03.md`): the children's tier is visible from the young-adult tier, both from the general tier, and all from the contemplative edition. `lib/editions.js` now defines each tier once (`CHILDREN_TIER` ⊂ `YOUNG_ADULT_TIER` ⊂ `GENERAL_TIER` ⊂ `CONTEMPLATIVE_TIER`) and spreads it into the entries, every edition names its `tier`, and `validateEditions` asserts the chain and refuses an entry that narrows below its tier. Live effect on the next deploy of each domain: starquest.site gains the pet chapters and cast; sciencefiction.site gains the Five-O and pet material; fellowshipoflight.org and church-space.site gain the whole main sequence (measured content pages, before the relatedUrls fallback: pets 47 unchanged, starquest 16 → 53, sciencefiction 64 → 102, fellowship 27 → 126, church-space 12 → 114). The canonical site stays unfiltered at the general tier; the archive codex site stands outside the ladder by design. The general-tier maintenance duty ("add each new thread here too") now lives on `GENERAL_TIER` once. `test/editions.test.js` pins the ladder. The contemplative-only POV scene the direction also allows is the next PR.

### Added

- **[Climate Repair and the Origin of Terraforming](src/lore/climate-repair-and-terraforming.md)** (2026-09-03, drafted at Dermot's direction that terraforming technology developed as a response to global warming and climate change; draft-and-stop, readings flagged in `story-bible/intake-2026-09-03.md`, ninth direction). A History entry: the transformation toolkit was built on Earth during the Crisis Centuries to keep Earth alive, the word came backwards from Mars, the repair's slow-partial-unfinished shape is the one every later protocol converged on, Earth's own hurried programmes are the ancestor of the Frontier Transformation Protocols' root cause, and the Star Rangers charter's living-world clause is read as forbidding transformation rather than restoration, a distinction the record marks as untested. A section at Dermot's tenth direction of the day makes planetary engineering a career discipline the emergency created: civil rather than Corps, Demelza Trevithick its living example. Institutions and dates follow the Crisis Centuries page exactly; no programme, incident or founding date is named. All readings confirmed by Dermot the same day; one-sentence cross-links added on the Crisis Centuries, Frontier Transformation Protocols and Mars pages.
- **The Prismere set regenerated, all twelve together, and Agent Barsik's portrait** (2026-09-03, acting on Dermot's 11 August ruling *replace, not upscale*; drafted for his review since every one depicts a place or a species). The nine `prismere-*` lore images on `src/lore/planets/prismere.md`, all 768px and sharing one jellyfish-and-spire motif, are replaced at 1600×893 from the diverged prompts in `story-bible/images.md`, two variants each, picks and reasons in `story-bible/image-prompts.md`, every alt rewritten to what the new file shows. The three `prismeri-*` images were regenerated with the set and **rejected by Dermot on review as still too humanoid** (the human frame with features applied, which the 13 August ruling reserves to the Krenyi): withdrawn, parked in `story-bible/reference-art/`, and `src/lore/prismeri.md` now carries an ILLUSTRATION PENDING card and no body images until the species has a written body plan. `characters/agent-barsik.jpg` goes from 512×1024 to 1200×670. Three prompts revised in place. `scripts/image-prompts.js` now matches lettered sub-section numbers (`5a.`), which had hidden the twelve entries from the pipeline.
- **The first Codex-art entry: *Asteria the Sage (Artist's Impression)*** (2026-09-03, Dermot's ruling on the headmate image parked since 24 August — *in-world artist's impression, no watermarks or overlaid text*; drafted for his review). `src/codex/asteria-the-sage-impression.md`: a painted impression of an unnamed member of Tissadelle Shepherd's system, made on the maker's note from the sitter's own account and never from a sitting, filed as an artefact and not a likeness. The entry records that the maker calls her Asteria the Sage, *Asteria* being the name the note gives her and *the Sage* an honorific of the Fellowship of Light (Dermot's rulings, same day; `story-bible/intake-2026-09-03.md`, seventh direction), names Asteria Wessex as the one other Asteria who bears it, and records that the note borrows the honorific, since Shepherd's system has no standing in the Fellowship (Dermot's ruling, same day). `src/lore/fellowship-of-light.md` gains a paragraph on *the Sage* as the Fellowship's one honorific on record, at Dermot's direction (no glossary entry). `src/characters/tissadelle-shepherd.md` gains an *Asteria* subsection at Dermot's eighth direction of the day: the member is Shepherd's image of Asteria Wessex as a younger woman, never met, formed from Karla Wender's memory of her own teacher, the honorific repeated from Wender; when, why and whether she fronts are marked absences. The codex entry points at the page instead of marking the absence. Header is a designed card with the artwork scrimmed beneath; the artwork sits in the body per the same day's Codex-art rulings. The image was regenerated clean at 1600px from Dermot's supplied reference (`scripts/image-prompts.js` gained a *Codex art* section target filing under `src/images/codex/`); prompt and card recipe in `story-bible/image-prompts.md`.
- **Young Star Rangers: a second young-adult thread, season and edition** (2026-09-03, Dermot's direction, readings confirmed and the subdomain approved the same day — `story-bible/intake-2026-09-03.md`, sixth direction; drafted for his review). Thread `young-star-rangers` registered in `lib/storyline-threads.js` with Season 9 (index pages only, no chapter yet) and a `/threads/young-star-rangers/` landing page. Edition `young-star-rangers` on `young.fianilchruinne.com` in `lib/editions.js`: the young-adult floor plus the thread, starquest palette for now, Zoe Smith cast (her page tagged into the thread). The thread is listed on `GENERAL_TIER` rather than the young-adult floor, so starquest.site keeps the procedural's own page set while the general and contemplative tiers carry the thread by construction; a tier constant is documented as a floor an edition may extend. The cPanel subdomain, clone and `deploy.conf` are deploy work still to do. `test/editions.test.js` pins the extension.
- **S08E02C01 *Asteria's Night*** (2026-09-03, drafted at Dermot's direction, readings and draft approved the same day; filed as the gated chapter at his choice). Three children lost on the high ground above a settlement, a search that will not cross the drowned ground, and a night in the Fellowship chapter house spent in counsel with somebody who is not there; they are home before first light by the one line across the bog. Three POV blocks — Orlagh Dunne, the settlement's warden, in the filed register; Ferdia, the eldest child; and Asteria Wessex, whose block carries the corpus's first `tier=contemplative` marker. `canon_facts: []`, the Season 8 posture: the record carries the route and nothing after it, and Asteria credits Levrils in her own voice to one person and does not write it down, per the 26 August rulings. Her age, a hundred and thirty, is the one new fact, approved on the reasoning that life expectancy will have increased. New Episode 2 index page. Draft kept in `story-bible/scene-draft-asterias-night.md`.
- **Tier-gated POV blocks** (2026-09-03, the second half of Dermot's tier-ladder direction: *the contemplative edition may have additional POV scenes for any chapter*). `::: pov <id> tier=contemplative` marks a block that renders only on a build whose edition sits at that tier or above; on every lower tier `lib/markdown-containers.js` drops it from the token stream before rendering - absent, not a placeholder - so the chapter page, the scene-POV pages (`src/_data/scenePovPages.js`) and the "View from" buttons (`povsForTier`, driven by a mirrored `tier:` on the `povs:` entry, which `validate-content.js` checks against `TIER_ORDER`) all agree. A block with no tier is visible everywhere, which is every block written before this. The build's tier is its edition's; an unfiltered local build resolves to the default edition at the general tier, so a contemplative block cannot leak. Pinned by `test/markdown-containers.test.js`. No chapter carries a gated block yet.
- **The five held-back portraits regenerated and filed** (2026-09-03, drafted for review): Andrew Falkorian, Zoe Smith, Muffin, Dagny Voss and Sofia Santos, each at 1200×670 with alt text from the filed file. Their first run had failed on lettering in both variants, and the cause was in the prompts: the nine 19 August entries that began `[ranger block]` or `[house block]` went to the generator with that placeholder as literal text, and every *Extra negative* bullet sat outside the blockquote the script sends. `story-bible/images.md` now carries the blocks and the standing negative inside all nine blockquotes, with a revision note on the five, and the second run came back clean. Muffin remains the entry the story bible flags as Dermot's call whether it ships at all.
- **Twenty-one character portraits filed from the 28 August generation batch** (2026-09-03, Dermot's picks from a full-resolution review of all twenty-six pairs; drafted for his review). Suvra Kel, Anne de Courtney, Emma la Chapelle, Ernesto Strozzi, Galen Kane, Luke Valerius, Alala, Kate Ukrainka, Knopka, Sylvie Harrow, Thrum, Aoife, Sildron Drey, Thalassa Vexar, Elvira, Sorcha Nic Dhiarmada, Sorcha Shepherd, Lucene-9000, Xenon, Lumbarian and Anne Wender, each at 1200×670 with alt text written from the filed file. Five were held back with both variants failing on lettering or branding — Andrew Falkorian (unit tapes, a lettered case), Zoe Smith (hatch numbers), Muffin (galley labels), Dagny Voss (a company name across the back, a name tape), Sofia Santos (a depth readout, a real clothing brand) — and are queued for regeneration with tightened negatives. One flag for review: Sorcha Shepherd's survey instrument carries a small maker's logo and a display.
- **Reference images travel with the prompt** (2026-09-02, from Dermot's direction that enhancing or building on his own photographs beats pure text-to-image). `scripts/image-prompts.js` reads an optional `References:` line from each entry in `story-bible/images.md` — files in backticks, repo-relative or absolute, so his own frames qualify under the standing 11 August permission — and `--generate` sends them as image parts after the prompt (up to fourteen on Gemini 3.1 Flash Image); `--next`, `--only` and `--all` print them for the clipboard loop. A named file that is missing fails that entry loudly instead of generating without it, the manifest records which plates were sent, and `test/image-prompt-references.test.js` pins the parsing, the path resolution and both refusals. `story-bible/images.md` records the convention and the photo-first order for new images.
- **Bounded creativity named as the organising theme; Krast as its anti-pattern** (2026-09-02, from Dermot's own observation; story-bible only, no canon touched). A *Bounded Creativity* section in `story-bible/ideas.md` records the naming verbatim, maps where the corpus already runs on it (creation-by-specification, the bounded-and-instrumented physics rule, the AI safety kernel, the what/how command split, Saturday Shutters), and reads Krast precisely: not too much structure but structure bounded only by itself — the working rule being that *the bound has to come from outside the thing it bounds*. Notes the gap that the anti-pattern has a face and the positive pattern does not, and files whether the 26 August repentant-complicit-leader ruling is that face under *Decisions awaiting Dermot* in `open-questions.md`. One line added to `life-lessons.md`.

### Added

- **A portrait prompt of record for Marisol Achebe** (2026-09-02, Dermot's request; no image yet). Queued in `story-bible/images.md` under *Missing portraits* in the shape `scripts/image-prompts.js` parses, and in `story-bible/prompt-sheet.md`'s paste queue. The frame is the page's own method — sitting on a flour sack, waiting, with Knopka against the warm patch beside her — and it is the first bureau portrait to take the uniform from [The Eden Bureau Uniform](src/lore/eden-bureau-uniform.md): one collar stroke, no number. Heritage and age are stated in the prompt on the Oyelaran precedent and flagged as Dermot's to change before generating. Generation runs locally through his pipeline; this session has no generator.

- **[Detective Marisol Achebe](src/characters/marisol-achebe.md)** (drafted 2026-09-02 at Dermot's request). A character page for the junior detective who carries the Season 2 flour case — the last Season 2 witness without one, so her two "View from" pages on [The Warm Patch](src/seasons/s02/e05/s02e05c01.md) rendered as placeholders on undercover-pets.com even after that domain's scene-POV fix. Written from the chapter and the two pages that already name her (Oyelaran's roster, Knopka's file), and asserts nothing they do not: her posting, her rabbit, her interview technique, and the one case. The home she learned the technique in is left unstated, as the chapter leaves it. No portrait yet. Oyelaran's roster and Knopka's page now link her name.

- **A plain-register placeholder for the children's edition, with no cross-domain link** (2026-09-02, Dermot's call). `lib/editions.js` gains an optional `excludedNotice` (`{ title, intro, back }`); when an edition carries one, `src/_includes/excluded.njk` and the scene-POV include render it in place of "Not included in this edition", and the placeholder's only link is to the domain's own `/start/` rather than to the same page on the reference domain — a child cannot judge a link to the adult site. The `<title>` of a placeholder follows the notice too. The pets edition carries the first one ("Not on this site"); every other domain keeps the site-wide notice and its link. `validateEditions` checks the shape; `test/editions.test.js` pins it.

- **A Characters page hero for undercover-pets.com, and a per-edition hook for section heroes** (2026-09-01, Dermot's request). `src/characters/index.md` had shown the designed ILLUSTRATION PENDING card on every domain since the stock composite behind it was retired in August. `lib/editions.js` gains an optional `sectionHeroes` field (`{ characters: { image, alt } }`, filename in `src/images/hero/`), the template consults it, and `validateEditions` refuses an unknown section, a missing file, or a PLACEHOLDER-stamped card; `test/editions.test.js` pins that. The pets edition carries the first one: `src/images/hero/characters-hyrax.jpg`, Dermot's own photograph *Rock Hyrax on the Coffee Machine* (Nairobi, 2025) copied from the photography portfolio at its site size — chosen over generating an image, per `story-bible/images.md`'s own rule that his photography comes before a prompt. The other domains are unchanged and still show the pending card.

- **A plain-register Reading Plan for the children's edition** (2026-09-01). `lib/editions.js` gains an optional `readingPlan` field, and the pets edition (undercover-pets.com) is the first to carry one: `src/start/index.md` now branches on it, rendering a short "Where to Start" page — begin here, how a chapter works, where to look things up, more stories — in place of Sen's archivist's note, which stays the site-wide plan everywhere else. The registry supplies the sentences; the template supplies the headings, the first chapter of the plan's `startThreadId` resolved against the build's own filtered chapters, and only the reference shelves this build actually carries. `validateEditions` refuses a plan whose start thread is unregistered or not in the edition's own `threads`, and `test/editions.test.js` pins that contract. Measured against the prose it fronts: Season 2's pet-POV blocks read at Flesch-Kincaid grade 7.2; the old plan a child reached from "Begin Reading" read at grade 9.9 with a 56-word sentence and opened on "Start with the spine"; the new one reads at grade 5.3.

### Changed

- **Quern's founding is placed in the first decades of fold transit** (2026-09-02, Dermot's pick from three presented options: soft early, mid, or undated). One sentence changed in [Quern](src/lore/planets/quern.md): the energy concern that founded the settlement went out "in the first decades of fold transit, when a great deal was financed on the strength of a route having been confirmed". No year is given, by design — a soft date fixes the settlement's place in the chronology (after the 2712 UCSD route confirmation, so roughly a century old by the story's present, some thirty Breathings of turning-stone records) without a number a later chapter could contradict. The entry in `story-bible/open-questions.md` that carried the question is closed.

- **The Undercover Pets thread description is in plain register** (2026-09-01, Dermot's direction, completing the sweep below). The one-line `description` in `lib/storyline-threads.js` — shown on `/threads/` and in Sen's Reading Plan's "Branch out by storyline" — was the last adult-register line on the children's path (grade 14: "working file", "the specimen the record still cannot classify"). Rewritten at grade 6.5, naming the cat and the rabbit as such; the other threads' descriptions are unchanged.

- **The Season 2, episode and Undercover Pets thread intros are written for the tier that reads them** (2026-09-01, Dermot's direction after the Reading Plan work). `src/seasons/s02/index.md`, its five episode index pages and `src/threads/undercover-pets/index.md` carried intros at Flesch-Kincaid grade 12–14 with thirty-word sentences ("a compliance-framework referral", "a remit that stops one noun short", "a form's *absence* implies"), on the one storyline whose own prose runs at grade 7.2. Rewritten in plain register — grade 5.8 to 7.6 across the seven pages — keeping every fact and link the old intros carried; the season intro's aside to the grown-up in the room stays, in plainer words. These are content pages shown identically on every domain, so the main site reads them too, which suits a season the intro already described as gentler than the main line. Left as-is: `lib/storyline-threads.js`'s one-line thread description, shown on `/threads/` and in Sen's Reading Plan.

- **The undercover-pets.com homepage tagline is written for its reader** (2026-09-01). The pets edition's `heroSubtitle` was a reworded copy of the adult tagline — the station clock, the Five Layers, "multiple Concordants" — at grade 14, naming no animal, and its only link went to `/glossary/concordant/`, which that domain's own filter renders as a "Not included in this edition" placeholder. Rewritten at grade 6.3 around the agency and its cast, linking only `/glossary/smart-pet/`, which the edition carries. `story-bible/edition-reader-profiles.md`'s pets entry, which described the voice as "the standard tagline, unmodified", is corrected.

### Fixed

- **Every "View from" page on undercover-pets.com was a placeholder** (2026-09-02, found while answering why Aldera was referenced but excluded; option chosen by Dermot). `isCharacterPovIncluded` in `lib/content-filter.js` granted a single character's scene-POV page only to a character named in `CHARACTERS`; the pets edition narrows by `TOPICS` and `THREADS` and names nobody, so all 57 scene-POV pages under its included chapters rendered "Not included in this edition" — on the domain whose reader profile is built around following one animal's viewpoint, and whose new Reading Plan says "press a name, Barsik say". The rule now grants the page when the character's own page is included on the build (the same test the character page beside the button passes), looking the character up by id from `src/characters/`; a chapter's tags still cannot grant it. On that build 20 of the 57 are now real (every agency animal's). The 37 that remain belong to witnesses whose own pages the domain does not carry — the Season 1 companions on the cat chapters (Elvira, Galahad, Rook-7, Syra), the Five-O crossover cast (Larsen, Calloway, Voss), Tissadelle's one Season 2 appearance, and two of Season 2's own witnesses, Sorcha Nic Dhiarmada (untagged) and Detective Achebe (no character page yet) — which is a tagging decision for Dermot, recorded as open in the PR. `test/content-filter.test.js`'s truth table is updated and extended.

- **Sorcha Nic Dhiarmada is included on undercover-pets.com** (2026-09-02, Dermot's pick from presented options: Season 2's own witnesses join the children's edition; the Season 1 companions, the Five-O crossover cast and Tissadelle stay out). Her page is tagged `undercover-pets.com`, so her page and her seven "View from" pages across Season 2 episodes 3 and 4 ship there. Detective Achebe, the other Season 2 witness, has no character page yet and stays a placeholder until one exists.

- **Aldera is included on undercover-pets.com** (2026-09-02). Her character page was tagged `pets` where every other agency character carries the domain-name tag `undercover-pets.com` — a holdover from before that convention — so her Season 1 chapters shipped on the domain (they carry the tag themselves) while her page rendered as a placeholder from Nessa's page, the Smart Pet glossary entry and her own "View from" buttons. Tag added; nothing else about the page changes.

- **Front-of-house pages no longer recommend sections a narrowed edition has emptied** (2026-09-01). The homepage's Timeline, Lore, Glossary and Codex cards render only when the filtered collection behind them is non-empty — undercover-pets.com was offering "Map the layers of reality…" over a Lore page reading "No lore articles published yet", and an Atlas with no locations. Sen's Reading Plan applies the same test to its shelf list in step 3 and to the Timeline, Codex and Marked Absences references in step 5; on undercover-pets.com the Marked Absences link landed on an excluded placeholder. `check-internal-links.js` cannot catch either case, because a placeholder is a real page.

- **The light on the Threshold shore is the Sceir Light, not the Trá Mhór Light** (2026-09-01, Dermot's pick from presented options). Renamed across all three occurrences in [The Character of the Light](src/seasons/s01/e01/s01e01c03.md) — the `location` field, the canon fact naming the Lights Board register, and Rook-7's closing block. *Sceir* is a skerry, a low offshore rock, which is the thing a light gets built on; the placeholder it replaces named a strand instead, and was flagged as functional rather than chosen when the chapter was drafted. The name appeared nowhere else in the corpus, so the rename needed no sweep and the canonical glossary's migration map is not involved. The `[1.29.0]` entry below still reads Trá Mhór and is deliberately left alone: released sections are immutable, and it is an accurate record of what shipped in that release.

## [1.29.0] — 2026-09-01

The release that unblocked itself. Three content additions land together,
two of them recovered from email after two consecutive scheduled runs were
left without push access to this repository and fell back to posting their
work as patches — one of which arrived corrupt and carried a front-matter
error that would have failed the build. Alongside them, the changelog gains
a coverage reporter and the twenty-four-page backlog it found. Season 1
Episode 1 gains a third chapter that puts prose on the coast the Threshold
siting ruling settled; Season 3 Episode 1 gains the chapter that pays off
Sohrel's four declinations; and Lucene-9000's page states the design
principle her appearance runs on. MINOR: all additive, backward compatible.

### Added

- **[The Character of the Light (S01E01C03)](src/seasons/s01/e01/s01e01c03.md)** (2026-09-01). A third chapter for Season 1 Episode 1, appended rather than inserted, taking the survey down the shore to the Trá Mhór Light — the first prose to stand on the coast the [Threshold siting ruling](story-bible/open-questions.md) settled on 31 August. It exists to give `s01e01c02`'s "at least eleven years" somewhere older to be measured against: the station's incident record is eleven years deep because the station is thirteen years old, and the light's Lights Board register is two hundred and seventeen. What the register holds is a clock-correction column kept for maintenance by fourteen attendants — corrections that run in one direction through four replacements of the movement, which is not a property a clock can have, and that have grown. Introduces one named minor character (attendant Nuala Beirne) and one location, and asserts four canon facts, the load-bearing one being that the Threshold discrepancy predates the station's own record. Deliberately avoids the sightline and travel-time arithmetic the open-questions file still fences off. Drafted and reviewed the same day; merged in #612.

- **[What Meridian Asked (S03E01C03)](src/seasons/s03/e01/s03e01c03.md)** (drafted 2026-08-29, merged 2026-09-01). The Season 3 gap the narrative-gaps checklist flagged — Sohrel's fourth declination — pays off as a fifth request, this one from Meridian's Long Shelf reef-city itself rather than from inside the Hegemony. Sohrel authorises a narrowly technical consultation on the council's own terms; it finds resemblance, not proof, between the Sentinel's structure and Meridian claw-percussion, so the Sentinel's origin stays unresolved per the standing mystery. Dual POV (`tissadelle`, `sohrel`), matching C01/C02. Reached `main` the long way: two consecutive scheduled runs had no push access and fell back to emailing the work as patches, and the 31 August patch arrived corrupt — blank lines inside the hunk had lost their `+` prefix, and its front matter carried `episode: 3` against an `id` of `s03e01c03`, which `validate-content.js` would have failed. Recovered from the 29 August email's raw attachment, which was identical but correct. Merged in #613.

- **[Lucene-9000](src/characters/lucene-9000.md)'s appearance is governed by the same principle as her performance** (drafted 2026-08-31 to Dermot's specified wording, landed 2026-09-01). One paragraph, placed after the social-gynoid paragraph and before the internals: she is built to look as human as a machine can be made to look and to go no further, because the design is disclosure rather than disguise. Extends the performed-not-masked emotional design already on the page rather than changing it. Merged in #614.

- **`scripts/check-changelog-coverage.js` — reports content pages the changelog never mentions** (2026-09-01). Its neighbour `check-changelog.js` catches released history being *destroyed*; nothing caught the opposite failure, a page shipping without ever being written down. This walks the six content directories and reports any page whose path, slug or front-matter title appears nowhere in this file. **It warns and never fails**, for two reasons stated at length in the file: whether a change deserves an entry is a judgement, which the authority rules keep outside what a gate may decide; and the obvious gate — a PR touching `src/` must touch `CHANGELOG.md` — is satisfiable by a junk line, manufacturing the appearance of coverage without the substance. Wired into `npm test` after the backfill below, since a warning is only useful while it normally reads zero. Matching is deliberately loose to avoid false alarms, and `src/timeline/` is the known weak spot because its titles are sentences rather than terms.

- **Twenty-four pages recorded that had never been mentioned here at all** (2026-09-01, found by the script above during a full audit of `main`'s 674 first-parent commits). They are filed under this heading rather than under the releases that actually shipped them, because released sections are immutable — the rule `check-changelog.js` exists to protect — so a truthful late record beats a tidy false one. `scripts/`, `lib/` and `test/` were fully covered; the gap was entirely in content, which fits its shape: engine work is usually the point of its PR, while a supporting glossary or timeline entry is added alongside whatever the PR was really about, and it is the supporting file that gets forgotten.
  - **Glossary (8):** [Champions / Heroes (Heros)](src/glossary/champions-heroes.md), [Entropy Horizon](src/glossary/entropy-horizon.md), [Inner World Depth (IWD)](src/glossary/inner-world-depth-iwd.md), [Naiad](src/glossary/naiad.md), [Noögenic Seed Potential (NSP)](src/glossary/noogenic-seed-potential-nsp.md), [Noögenic Seeding Law](src/glossary/noogenic-seeding-law.md), [Predictive Closure](src/glossary/predictive-closure.md), [Universe Fertility Index (UFI)](src/glossary/universe-fertility-index-ufi.md).
  - **Timeline (8):** [USC Founded; Solar System Safety Standards Authority Established](src/timeline/2340-usc-sssa-established.md), [Subsea Cable Warden Programme Established](src/timeline/pre-usc-cable-warden-programme.md), [The Three-Era Framework Codified](src/timeline/pre-usc-civilisation-comparison-codified.md), [United Space Industry Standards Formed](src/timeline/pre-usc-ul-standards-international.md), [First Drift Report Filed at Threshold Station](src/timeline/year-minus-11-first-drift-report.md), [The Aethelrock Rotation Dispute Begins](src/timeline/year-minus-2-aethelrock-rotation-dispute.md), [Aldera Reaches the Marsh Causeway](src/timeline/year-minus-3-aldera-reaches-causeway.md), [Elvira Arrives at the Marsh Causeway Outpost](src/timeline/year-minus-3-elvira-arrives.md). Eight of only seventeen timeline entries — much the worst-covered area, and the one with no `npm run new` scaffold.
  - **Codex (4):** [Incident Report 001 — Dock Seven Chronometer Discrepancy](src/codex/incident-report-001.md), [Maintenance Log Extract — Dock Seven Chronometers, Day 49](src/codex/maintenance-log-day-49-dock-seven.md), [Protectors of the Fold (Slipwave Recording)](src/codex/protectors-of-the-fold.md), [Telling the Bees at Cnoc na mBeach](src/codex/telling-the-bees-at-cnoc-na-mbeach.md).
  - **Lore (3):** [The Commonwealth](src/lore/the-commonwealth.md), [Galactic Stardate](src/lore/galactic-stardate.md), [Post-Teleport Ascension Stress Disorder](src/lore/post-teleport-ascension-stress-disorder.md).
  - **Journal (1):** [Who Thought of It](src/journal/who-thought-of-it.md).

## [1.28.0] — 2026-09-01

A small release finishing what 1.26.0 started. The four lore and codex
entries that arrived in the 29 August direction set without title cards now
have them, and the tool that drew them — `scripts/make-cards.js`, a
cross-platform port of the two PowerShell card generators — means a card can
be made from a session that is not on Dermot's Windows machine. Alongside
that, one direction of Dermot's: Tissadelle Shepherd is always formally
dressed, and the page now says why that is the opposite of a performance.
MINOR: all additive, backward-compatible.

### Added

- **`scripts/make-cards.js` — a cross-platform port of the card generators** (2026-09-01). Draws the landscape emblem cards of `make-emblem-card.ps1` and the designed-card mode of `make-codex-cover.ps1` in Node with `sharp`, so a title card can be made from a session that is not on Windows. A local authoring tool like its two predecessors: not in the build, not in `npm test`, not in CI, and not a package.json dependency — `sharp` is installed on demand with `npm install --no-save sharp`. Two deliberate fidelity gaps are stated in the file header rather than left to be discovered: Georgia and Consolas are unavailable on Linux, so the port sets Liberation Serif and Liberation Mono and matches the layouts rather than the letterforms; and the star fields are seeded by a plain LCG rather than `System.Random`, so the dots are deterministic but sit elsewhere. A card that has to match a GDI+ card exactly still comes from the `.ps1` on Dermot's machine.
- **Title cards for the four 29 August entries that shipped without one** (2026-09-01): [The Ovruhn](src/lore/ovruhn.md), [Sapience Beyond the Human Measure](src/lore/sapience-beyond-the-human-measure.md), [The Maker's Shape](src/lore/the-makers-shape.md) and [The Two Songs of the Loud People](src/codex/the-two-songs-of-the-loud-people.md). Designed cards in the house palettes, drawn by `make-cards.js`; prompts of record in `story-bible/image-prompts.md` per the standing convention. The three lore entries take landscape emblem cards and the codex entry takes the square navy title card, as the split between the two generators requires.
- **Tissadelle Shepherd is always formally dressed, and never more so than on duty** (2026-09-01, Dermot's direction). Her character page carried the uniform as a present fact — "what she notices on her own family's hillside once the uniform is off", the Gardaí training as "nearly a different uniform" — without ever ruling how she dresses. A paragraph in Character Notes fills the gap and reads the formality as the opposite of the performance she withholds: she treats the title as a job description, and the uniform is that description worn where anybody can check it, claiming nothing that is not already on the record. What "formally dressed" amounts to off duty, and whether the Corps requires it or she does, are deliberately left open.

## [1.27.0] — 2026-08-31

The consistency-sweep release: season-by-season sweeps closed with
Dermot's rulings realized — Threshold Station sited ground/coastal and
the corpus aligned, the Compact habitat Meridian renamed Halcyon, the
Duty Season coined to ground the career clock, and two open questions
filed rather than guessed. Alongside the sweeps, the cast grows: Peran
Slade drafted and approved, Jeeves embodied, and real portraits replace
the four stamped hero-cast title cards so every domain's slideshow is
whole again. MINOR: all additive, backward-compatible.

### Added

- **Designed cards for the four 29 August entries, and a cross-platform card generator** (2026-08-31, at Dermot's direction). Emblem cards for The Ovruhn (rings device, sea-green), The Maker's Shape (fold device, violet) and Sapience Beyond the Human Measure (triad device, green), plus a codex title card for The Two Songs of the Loud People (dissolution motif, RENDERED stamp) — all drawn by the new `scripts/make-cards.js`, a Node/sharp port of the two Windows card generators for sessions without GDI+ (local authoring tool, not in package.json; Liberation fonts substitute for Georgia/Consolas). Parameters of record in `story-bible/image-prompts.md`; each entry's `image:`/`image_alt:` set to match what the card actually shows.

- **The Voss naming question marked on [Petra Voss](src/characters/petra-voss.md)'s page** (2026-08-31, Dermot's pick from presented options, closing the Season 0 sweep). The Season 0 sweep found Commodore Rutger Voss and Delegate Petra Solano's names recombining, unacknowledged, in the Governor who presides a century later over the self-governance their Charter argument created. Marked Daire-style — kinship and naming neither claimed nor denied, the shared-name-is-not-shared-identity habit cited — as a second paragraph on the page's existing naming note.

- **Threshold Station siting question filed** (2026-08-31, Dermot's pick from presented options, closing the Season 1 sweep). `story-bible/open-questions.md` records the split — `s01e01c01`'s two "orbital" strays against the ground-anchored rest of Season 1 and Season 5's spaceward register — with candidate rulings as choices, a rider on the 47-to-900 crew growth, and a hold on altitude and travel-time arithmetic until ruled.

- **[Duty Season](src/glossary/duty-season.md) glossary entry** (2026-08-31, Dermot's pick from presented options during the Season 5 sweep). Coins the Rangers' career clock — one UCSD year of postings, boards and rosters, called simply "a season" in service speech — which grounds the existing usages in `s05e02c02`, `s05e02c03` and Tissadelle's character page ("a season before the timeline usually allows") that read as calendar quarters without it.

- **Drithane crossing-cadence question filed** (2026-08-31, from the Season 2 consistency sweep; Dermot's pick from presented options — story-bible note, no canon touched). `story-bible/open-questions.md` records the three-way tension (the lore's twice-per-orbit line, Season 2's crossing timestamps, Sorcha's forty-crossings count) with candidate rulings as choices; until ruled, new Drithane prose avoids crossing arithmetic.

- **[Peran Slade](src/characters/peran-slade.md) — Fellowship of Light novice, drafted and approved** (2026-08-30, from Dermot's seed "a Fellowship of Light novice, seeking their vocation"; page and portrait v2 approved same day). The seeking grounded in the order's one-vocation-two-expressions structure and the Called register's never-compels rule; placed at Asteria's chapter house; the Novice/novice double meaning kept as the entry's margin-book line. Portrait: the doorway at dusk, lamp in hand. Joins the fellowship edition's hero cast as a third slide at Dermot's direction — beginning, kept middle, and ended path in one crossfade.

- **Jeeves embodied** (2026-08-30, Dermot's ruling from the presented options: a body in canon). Half a clarification — `s02e01c01` already had him standing and pouring tea as "a domestic-model intelligence" — realized as [Jeeves](src/characters/jeeves.md)'s "The Frame" section (matte jointed catalogue frame, hands *sufficient for pastry*, lens eyes, the uncatalogued waistcoat) and a portrait under the visible-artificiality rule, set in a habitat apartment aboard Eden (v2 approved by Dermot). Restored to the procedural edition's cast, last for the joke; the open question is struck as ruled.

- **Real portraits for the four stamped hero-cast characters** (2026-08-30, generated from the runbook briefs, v1 of each run approved by Dermot). `dr-iona-vale.jpg`, `galahad-thorne.jpg`, `syra.jpg` and `brother-fintan.jpg` replace their stamped title cards; `image_alt` rewritten to what each file shows; all four restored to their hero casts in `lib/editions.js`, so every domain's slideshow is whole again with full images only. Prompt record and the negatives-inside-the-blockquote lesson in `story-bible/images.md`.

### Changed

- **Changelog convention amended: entries are plain and complete rather than compressed** (2026-08-31, Dermot's ruling from presented options; the 2026-08-16 division of labour with the Journal stands — essays still go there). CLAUDE.md's changelog bullet updated to match.

- **Compact habitat "Meridian" renamed Halcyon** (2026-08-31, Dermot's pick from presented options after the Season 3 sweep found two places sharing the name; the Halyx System world keeps it). A pure rename — Duarte's Commissioner post, the Season 2 itinerary and every fact stand unaltered. Sweep across `s02e01c03`, `s02e02c01` and the episode 2 index; migration row added to the canonical glossary.

- **Four hero-cast title cards stamped as placeholders and their casts thinned** (2026-08-30, Dermot's direction — the slideshow still showed title cards after 1.26.0 because those four were never stamped). `dr-iona-vale.jpg`, `galahad-thorne.jpg`, `syra.jpg` and `brother-fintan.jpg` are designed name cards, now PLACEHOLDER-stamped; the default, sciencefiction, fellowship and communion editions' `heroCharacterIds` drop them until real portraits land (the communion edition falls back to the static hero). Same day, Dermot ruled on the two borderline non-portrait images: `eden-warden.jpg` (habitat interior) stays as a portrait-by-absence; `jeeves.jpg` (mug on a table) stamped and dropped from the procedural edition's cast.

### Fixed

- **Threshold Station's siting ruled ground/coastal and the corpus aligned** (2026-08-31, Dermot's ruling on the filed open question). `s01e01c01` loses its two "orbital" strays (the location field reads "Dock Seven" plain; Syra's approach data now says "shore station"); `s05e02c03`'s "open space" becomes "open water" twice and its standoffs scale to sea distances (forty and twenty kilometres), which the sightline and the eleven-hours figure both survive; `boundary-zones.md` gains a sentence anchoring the shared shore. The open-questions entry is marked RULED with the dossier kept; the crew-growth rider stays open.

- **`s08e01c01`'s timestamp capitalised to "Late Autumn"** (2026-08-31, closing the Season 8 sweep) — the one lowercase season name in the corpus, brought into line with every other chapter's style.

- **"A lifetime and one rank ago" becomes "a lifetime and a rank ago" in `s05e02c02`** (2026-08-31, found during the Season 6 sweep via the echo in `s06e01c03`). The Sentinel finding sat five ranks below Line Captain on the published ladder, not one; the indefinite article makes the phrase the idiom the Season 6 echo already uses — "at a different rank" there, "back when she held a rank" here — and the two instances now match verbatim.

- **Threshold's discrepancy is sixteen years old in `s05e02c03`, not eleven** (2026-08-31, from the Season 5 sweep). The chapter sits in 2831; canon anchors eleven years to 2826 everywhere and Season 6 states the sixteen-year figure outright, so this was the elapsed-time slip CLAUDE.md's arithmetic warning describes, still standing in Tobble's inventory of the ship's records.

- **Wender's "third year as a Senior" corrected to "season as a Senior" in `s05e02c02`** (2026-08-31, same sweep). Shepherd held Senior for about a year on the published rank ladder — Officer in Early Winter 2826, Principal eleven days before Late Winter 2828 — so a third year was impossible; the new Duty Season entry carries the reading.

- **`s02e01c02`'s hard-copy printouts are now motivated in-story** (2026-08-31, Dermot's report that the paper read as unexplained). Two insertions: Larsen prints the routing code because a record that can be told to stop displaying a digit can be told again, and paper can't; Calloway hands the Threnody folder over on paper for the same reason, keeping the transfer illegible to whoever holds manifest-level access. Ties the props to Jeeves's c01 line about the manifest software; no new facts asserted.

- **`s02e01c01` no longer reads as if Jeeves drinks the tea** (2026-08-31, Dermot's report). Albercombe's handover line now says Jeeves is tetchy when he's had "nothing to pour anybody" (was "nobody's fed him anything"), and a pouring beat lands the thermos joke — Jeeves reclaims it and serves Larsen — so the closing "a stranger's unreasonably good tea" is earned. Clarification: [Jeeves](src/characters/jeeves.md) serves tea and does not consume it, as the character page already held.

## [1.26.0] — 2026-08-29

A release carried by the 29 August direction set: the hybrid-species
contact frame realized — the Ovruhn of Thavren, their codex account of
humans, and their Federation membership — alongside the sapience doctrine
pages (not a sight, not a checklist, not a human measure), the Simian
glossary entry, the Maker's Shape, the Concord's chosen outsideness, and
the diaspora in the Currach Fleet. On the engine side, the homepage
slideshow now structurally excludes placeholder cards. MINOR: all
additive, backward-compatible.

### Added

- **[Celtic Union of Planets](src/lore/celtic-union-of-planets.md) — the diaspora in the Currach Fleet; fifteenth 29 August direction recorded** (2026-08-29). Dermot's direction that many Celtic Union settlers were Americans or Canadians of Irish descent; realized as an extension paragraph on the founders: the double emigration on the Fleet's rolls (Ireland to America, Earth to the stations, the stations into the dark), the revivalist reading of the founders' custodial intensity, and the Union's no-registry position on homeland versus diaspora lines. Verbatim capture and canon check in `story-bible/intake-2026-08-29.md`.

- **Aliens-are-not-allegories trope rejection — fourteenth 29 August direction recorded** (2026-08-29). Dermot's rejection of alien civilizations as metaphors for human subgroups or countries (the Star Trek pattern); filed story-bible-only per his pick from presented choices — a trap entry in `story-bible/species-design.md` (the "they're basically X" test) and a theme section in `story-bible/ideas.md` (verbatim, the world-first method as the antidote, and what the rejection does not forbid). No canon touched. Verbatim capture in `story-bible/intake-2026-08-29.md`.

- **[Federation of Sentient Beings](src/lore/federation-of-sentient-beings.md) gains "The Concord Is Not a Member, and Means Not to Be" — thirteenth 29 August direction recorded** (2026-08-29). Dermot's direction that the Concord and humans generally are characteristically not full members but trade and keep friendly diplomatic relations; realized (per his pick from presented choices: human choice, own helm) as an extension — treaties over an assembly seat, the Federation never pressing, the Cerebraun as the contrasting outside posture, and the compact's seam restated (it runs to the Rangers because they are not the Concord). Verbatim capture and choice record in `story-bible/intake-2026-08-29.md`.

- **The Ovruhn are Federation members — twelfth 29 August direction recorded** (2026-08-29). Realized as two extensions: a membership paragraph on [The Ovruhn (Deep-Sung)](src/lore/ovruhn.md) (taken by the chorus's own deliberation, interval filed as unhurried) and Thavren added to [the Federation](src/lore/federation-of-sentient-beings.md)'s Member Worlds roster, carrying the compact's joint disaster-response obligations — the one unclaimed contribution slot, and the Ovruhn's established niche (committed by proposal; Dermot may reassign). Contact date still open, offered as choices. Verbatim capture in `story-bible/intake-2026-08-29.md`.

- **[Sapience Beyond the Human Measure](src/lore/sapience-beyond-the-human-measure.md) — lore entry, drafted; eleventh 29 August direction recorded** (2026-08-29). Dermot's direction to generalize sapience beyond human cognition, realized (per his picks: refuse the checklist; new entry) as the record's one doctrine page on the question: six human markers that do not settle it, each with its published counterexample, and the method — recognition per lineage through communication, no threshold, findings firm once made. Verbatim capture, choices and canon check in `story-bible/intake-2026-08-29.md`. No image yet.
- **[Cross-Biosphere Taxonomy](src/lore/cross-biosphere-taxonomy.md) gains "'Animal' Is a Translation Too" — tenth 29 August direction recorded** (2026-08-29). Dermot's direction to generalize capable non-sapient animals across biochemistries (non-DNA/RNA, non-cellular), realized (per his picks: extend the taxonomy page; keep "animal" as a declared translation) as a section on the page's own translation-word principle, with the kin dilemma generalized and the reverse error named. Verbatim capture in `story-bible/intake-2026-08-29.md`.
- **Present-open-questions-as-choices convention added to CLAUDE.md** (2026-08-29, Dermot's process instruction, verbatim in `story-bible/intake-2026-08-29.md`).

- **[Meridian](src/lore/planets/meridian.md) gains "The Settlers Who Nearly Quarried a City" — ninth 29 August direction recorded** (2026-08-29). Dermot's direction that the sapience-not-obvious problem almost led to a diplomatic incident between the first human settlers at Meridian and the native, technologically less obvious species; realized as an extension explaining the published Human Presence posture — a transit-lagged settler expedition launched on files predating the sapience confirmation, halted by one operator flagging that the claw-percussion answered their test pulses, withdrawn on its own judgement, and now Survey teaching filed beside the Sentinel. Readings (chief among them "first settlers" as an early aborted enclave attempt, the humans-before-Hegemony reading explicitly not taken) in `story-bible/intake-2026-08-29.md`.

- **[The Star Rangers Survey Corps](src/lore/star-rangers-survey-corps.md) gains a "Sapience Is Not a Sight" section — eighth 29 August direction recorded** (2026-08-29). Dermot's direction that Survey Corps training holds sapience might not be physically obvious until after communication is well established; realized as cross-branch doctrine (settled by talking, not inspection; "no evidence of sapience" as a statement about the survey; the Federation's three logged passes and the Sentinel Signal kept as the training's deliberate humiliations; the two directions of the error paired with the Simian kin dilemma). Verbatim capture and canon check in `story-bible/intake-2026-08-29.md`.

- **[Simian](src/glossary/simian.md) — glossary entry, drafted; sixth 29 August direction recorded** (2026-08-29). From Dermot's question whether a word exists for non-human primates as distinct from humans or humanoids (it doesn't, structurally) and his yes to an in-world term: the record stipulates *simian* — any Earth-lineage primate that is not a human, prosimians included — with the lineage-vs-silhouette axis stated (a chimpanzee is simian, a Krenyi humanoid, neither implies the other) and the Earth-animal naming caution's legitimate inversion inside a biosphere. A seventh direction the same day generalized it: every sapient people has a version of the dilemma (the vessik as the Chthonari case; the FTP sapience clause as the survey edge; kinship is descent, personhood is mind) — realized as the entry's "Everyone Has This Problem" section. Verbatim captures and readings in `story-bible/intake-2026-08-29.md`.

- **[The Ovruhn (Deep-Sung)](src/lore/ovruhn.md) gains a pronoun-rendering note — fifth 29 August direction recorded** (2026-08-29). Dermot's ruling that Ovruhn individuals are *they/them* in the record's translation register; realized as one rendering note before the entry's See-also line (the Qiren Tal pattern — a translation-register conversion, not a coined paradigm, so no glossary registration). Verbatim capture and reading in `story-bible/intake-2026-08-29.md`; the pronoun-paradigm item in `story-bible/open-questions.md` closed by it.

- **[The Ovruhn (Deep-Sung)](src/lore/ovruhn.md) — species lore entry, drafted** (2026-08-29, after Dermot confirmed the intake's readings — convergent alien; **approved by Dermot the same day**). The hybrid-species direction realized per `story-bible/species-design.md`'s method: Thavren, a deep-haze world where sound is the far sense; sounding-sight, the honest infrasonic under-song, handling tendrils, chorus-kept memory; both contact misreadings derived structurally, the ladder error given to human observers, and "the hybrid people" refused in-world as a press coinage under the cross-biosphere naming doctrine. Commitments-by-proposal and remaining open ends in `story-bible/intake-2026-08-29.md` and `story-bible/open-questions.md`. No image yet — joins the entries awaiting cards.
- **[The Two Songs of the Loud People](src/codex/the-two-songs-of-the-loud-people.md) — codex entry, drafted** (2026-08-29, same direction; **approved by Dermot the same day**). The species' account of humans in its own voice: Vhen, a keeper of the meeting-chorus of Thavren, rendered from chorus-keeping — humans as the loud people whose two songs disagree, their symbol-works as "choruses of the dead", the aggression misjudgement kept as the keeping's own corrected error (marked, not performed), with an Archivist's filing note holding the codex reader-test. No title card yet.
- **No archetype for AI as distinct from sapience in general — fourth 29 August direction recorded** (2026-08-29; **approved by Dermot the same day**). Realized as a "Why There Is Nothing Else to Be" section grounding [The Maker's Shape](src/lore/the-makers-shape.md) cosmologically, plus a clarification on [Made Minds and the AI Safety Archetype](src/lore/made-minds-and-the-ai-safety-archetype.md) (the archetype concerns the *safety* of made minds, not their mindedness — a made mind draws on the same sapience vocabulary as anyone, per the cross-biosphere page). Tier reading flagged in `story-bible/intake-2026-08-29.md`: the made-minds formtype and the AI Safety Archetype both undisturbed.
- **[The Maker's Shape](src/lore/the-makers-shape.md) — lore entry, drafted; third 29 August direction recorded** (2026-08-29; **approved by Dermot the same day**). Dermot's direction that each sapient species has a distinct form of AI, if they use it at all; realized as a Technology entry complementing the made-minds convergence page — a made mind as a portrait of its makers, the human lineage's provenance stated honestly, declining as a complete answer (the Krenyi as the standing case), and the contact doctrine that an unfamiliar machine is untranslated, not wrong. Verbatim capture, readings and canon check in `story-bible/intake-2026-08-29.md`, indexed in `story-bible/open-questions.md`. No image yet — joins the entries awaiting cards.
- **[Tissadelle Shepherd](src/characters/tissadelle-shepherd.md) — colleagues across the species roster, second 29 August direction recorded** (2026-08-29; **approved by Dermot the same day**). Dermot's direction that Tissadelle has worked with colleagues from almost every sentient species known to the Star Rangers; realized as one paragraph extending the generalist-rotations theme — service, not encounters; exceptions structural and unenumerated ("almost every" load-bearing, no species ledger); the plural-range reading held at arm's length. Verbatim capture, readings and canon check in `story-bible/intake-2026-08-29.md`, indexed in `story-bible/open-questions.md`.
- **The hybrid-species contact frame — direction recorded** (2026-08-29). Dermot's two-message direction: a species combining dolphin + ape + elephant peak cognitive traits — a differently-optimized peer intelligence — and the mutual-misperception contact frame (humans see a scientific marvel; the species sees "unpredictable, emotionally noisy primates"; three named tensions). Verbatim capture, canon survey (the peer-intelligence thesis is already the published ladder refusal; the interbreeding refusal avoided by reading; the source analysis's hands-ceiling claim contradicted by `story-bible/species-design.md`'s settled answer and not adopted) and flagged readings — chief among them the convergent-alien vs terrestrial-engineered fork — in `story-bible/intake-2026-08-29.md`, indexed in `story-bible/open-questions.md`. No canon touched; nothing drafted pending the fork.

### Changed

- **The homepage slideshow only shows finished portraits** (2026-08-29, Dermot's direction). The `withImages` filter now also drops PLACEHOLDER-stamped pending cards, on every edition; `validate-content.js`'s hero-cast check fails on a cast slide whose portrait is a stamped card. Marker detection extracted to `lib/placeholder-marker.js` (shared with `scripts/mark-placeholder.js`), pinned by `test/placeholder-marker.test.js`.

## [1.25.0] — 2026-08-28

A release carried by the 27 August direction set: the Tissadelle/Tobble
terminus — register, authored closing line, both scene drafts approved —
alongside the command detail rule, the Institute's purpose, and the
inner-world rulings. Around it: the Common Manifold reconciled with the
surveyed-universes canon, the Ilveth join the species roster, and the
engine gains unit tests for its last untested `lib/` modules plus
`TestingStrategy.md`. MINOR: all additive, backward-compatible.

### Added

- **The command detail rule's two founding reasons recorded** (2026-08-27, follow-up to the seventh direction; approved by Dermot the same day). Dermot's direction that the rule is a reaction against General Krast's MSC leadership style, existing to avoid micromanagement and to prevent the Star Rangers becoming a military dictatorship; realized into the "Where an Order Has to Stop" section's MSC paragraph (founding-era rationale, Krast named, the certification tier as the joint an unbroken will would need). Verbatim capture and canon check in `story-bible/intake-2026-08-27.md`, indexed in `story-bible/open-questions.md`.
- **[Rank and the Chain of Command](src/lore/star-rangers-command-hierarchy.md) gains a "Where an Order Has to Stop" section — seventh 27 August direction recorded** (2026-08-27, same intake; approved by Dermot the same day). Dermot's direction that Line Captains, Starwardens and Archwardens are not allowed to give overly detailed technical instructions to Chiefs, Section Leads or Principals; realized as standing Corps doctrine extending the Guild Leadership section's what/how line inside the chain of command — *overly* as the load-bearing word, the certification-seam reasoning, the deliberate floor at Principal, and the Dock Seven evacuation order as the lawful example. Verbatim capture, canon survey and five flagged readings (chief among them: "Chief Officers" as the Chief rank tier; whether the flag ranks are bound too) in `story-bible/intake-2026-08-27.md`, indexed in `story-bible/open-questions.md`. All five readings confirmed by Dermot the same day, with the fourth ruled wider — Star Captains and High Captains bound in the same way, normally delegating via the post-holding tier — and the section extended accordingly. A same-day follow-up supplied the doctrine's named inappropriate example (a Starwarden telling a Principal how to manage a cadet), settling that "technical" means the craft of the rank's own work, not instrumentation alone.
- **[Two in the One Currach](src/codex/two-in-the-one-currach.md) — codex devotional record** (2026-08-27, approved by Dermot the same day). Brother Daire's meditation from the river abbey on the circulating Baby Universe ballad, set in the published Deep Winter 2832 window: the song sings one woman through the seam, the abbey keeps vigil for two — the woman and the cat, a crew in one currach — in Aoife's own shown-not-healed discipline; the appended reconciliation-desk filing note restates the record's position (the seed stalled, completion neither observed nor foreclosed) and marks the plural-founder question as an absence the record has not been asked to fill. No title card yet.
- **Unit tests for the three previously-untested `lib/` modules** (2026-08-27): `test/markdown-containers.test.js` (the scene/POV fence-length rule and rendered attributes), `test/storyline-threads.test.js` (`threadForSeason`, the unsorted fallback, and the registry invariants the privacy machinery assumes) and `test/image-size.test.js` (the header parser's null-rather-than-guess contract). Engine only, no content touched; inventory in `TestingStrategy.md`.

- **[The Institute](src/lore/the-institute.md) gains a "What it is for" section — sixth 27 August direction recorded** (2026-08-27, same intake). Dermot's direction that the Institute is an independent nonpolitical academic body designed to provide intelligent constructive criticism, especially but not exclusively directed at the Star Rangers; realized as a design/purpose section (independence via the subscription model, non-political in the 2723 charter's constitutional sense, academic in form, *constructive* as the load-bearing word) plus a matching `description:`. Extension plus clarification under the canon-stability rule; verbatim capture, canon survey and four flagged readings (chief among them: "designed to" as standing remit, not founding intent — the Institute predates the Corps by centuries) in `story-bible/intake-2026-08-27.md`, indexed in `story-bible/open-questions.md`. All four readings confirmed and the section approved by Dermot the same day.
- **The terminus scene's external coda drafted** (2026-08-27, at Dermot's direction, same file). Scene 2, Wender POV: the terminus from outside — the final phrasing as Shepherd's last order to close the file, executed by Wender with cause of closure *founding*; the levril's watch complete; a cadence under the phrasing kept out of the record. By-proposal commitments flagged in `story-bible/open-questions.md`; approved by Dermot the same day, completing the draft — both scenes now stand approved.
- **The terminus scene drafted** (2026-08-27, at Dermot's direction). `story-bible/scene-draft-terminus-farewell.md` — the five 27 August directions realized as prose: the farewell in the ruled register, the authored closing line placed as her last words, Tissadelle and Tobble POVs, the other members' farewells marked rather than transcribed, the lost named once without the record carrying the names. Unpublished per the scene-draft precedent; interval and slot deliberately unfixed; four open items answered by proposal and flagged in `story-bible/open-questions.md`. Approved by Dermot the same day; remains story-bible material until a chapter slot is chosen.
- **The closing line — fifth 27 August direction recorded** (2026-08-27, same intake). Tissadelle's last words to Tobble, authored verbatim by Dermot and filed as prose-of-record for the unwritten terminus scene (not quoted in the changelog per the story-bible spoiler policy; the line, its reading, and its placement are in `story-bible/intake-2026-08-27.md` and the arc note's terminus section). Fixes the terminus against the protouniverse's founding and realizes the fourth direction's register in one sentence. No canon touched — the line binds its scene when written.
- **The terminus register — fourth 27 August direction recorded** (2026-08-27, same intake). Tissadelle Shepherd experiences almost no grief in her final moments — only fond farewell to Tobble and the other surviving headmates. Rules the register of the arc's unwritten terminus (the published chapters end on "a woman still becoming"), promotes the system's other members from the character page's "more people than the form has boxes for" implication to on-page participants, and completes the grief's distribution (author, Tobble's book, survivors, reader — none of it hers). Verbatim capture, canon survey and three flagged readings ("final moments" as the unwritten terminus; "surviving" as implying members lost; "almost no grief" as register, not retroactive costlessness — all three confirmed by Dermot the same day) in `story-bible/intake-2026-08-27.md`; arc note gains the terminus register; residue updated in `story-bible/open-questions.md`; distilled line proposed into `story-bible/life-lessons.md`. No canon touched — the terminus binds when written.
- **The redacted book, not the tulpa — third 27 August direction recorded** (2026-08-27, same intake). Tobble considered cultivating a new tulpa to represent the lost fantasy world, judged it "a museum or ghost of the original", and instead made a redacted book — an internal codex, some scenes glossed over for modesty. Verbatim capture, canon survey (the tulpa refusal as personhood ethics; the redaction as Marked Absences practised inside a mind; the book as the downgrade admitted) and two flagged readings (the modesty's sense; the refusal as Tobble's judgement, not doctrine — both confirmed by Dermot the same day) in `story-bible/intake-2026-08-27.md`; arc note gains the book as S7 prop; residue updated in `story-bible/open-questions.md`; distilled line proposed into `story-bible/life-lessons.md`. No canon touched — the book reaches canon when S7 writes it.
- **Tissadelle's RPG-structured inner world and Tobble's disciplined subsystem — direction recorded** (2026-08-27). Dermot's direction that Tissadelle's inner world is structured like a high fantasy RPG while Tobble's deeper internal subsystem is much more disciplined and scientific; verbatim capture, canon survey (the division of labour already on the page at Dock Seven, unnamed) and four flagged readings in `story-bible/intake-2026-08-27.md`, indexed in `story-bible/open-questions.md`. All four readings confirmed by Dermot the same day. Second direction, same day, ruled the craft question the realization had left open: a pure fantasy world-system would not survive the transition — the RPG-structured layer's fate at Dock Seven is **lost or downgraded, not held**, with the mechanism (the preserver's archival idiom filters what is preserved; enacted things cross only as their own record) and the grief register worked through in the intake and the arc note; distilled line proposed into `story-bible/life-lessons.md`. No canon touched by the ruling — the published pastoral interior already shows the downgrade.
- **[Tissadelle Shepherd](src/characters/tissadelle-shepherd.md) gains "The inner world" section** (2026-08-27, after the readings were confirmed). The shared interior structured — not merely dressed — as a high-fantasy role-playing game (named places with rules, party-held roles, quests, earned advancement), stated on the system's own account; the architecture as the inside view of "different members front in different roles"; Tobble's deeper subsystem far more ordered and scientific — depth as structure, never standing. Spoiler-guarded (no IWD/noögenics/Dock Seven on the page); the arc-side half in `story-bible/tissadelle-arc-s6-7.md`, and one craft question (the RPG layer's fate at Dock Seven) deliberately open in `story-bible/open-questions.md`. Extension under the canon-stability rule; approved by Dermot the same day.
- **The burnt-out leader, not the corrupt politician — direction recorded** (2026-08-26). Dermot's direction rejecting the corrupt-politician trope while claiming the burnt-out leader; verbatim capture, canon survey and flagged readings in `story-bible/intake-2026-08-26.md`, theme section in `story-bible/ideas.md`, distilled line in `story-bible/life-lessons.md`, readings indexed in `story-bible/open-questions.md`. Scope ruled same day: minor corruption in minor or supporting characters not ruled out — corruption never the engine of a leader or major figure. No canon touched.
- **The John Smith comparandum and the prayer/magic/Levril-perception direction — recorded** (2026-08-26, same intake). Smith identified as a third figure (the complicit leader, distinct from both poles) in `story-bible/ideas.md`; the prayer direction's canon survey finds three of four assertions already Archive-voice canon. The rendering reading confirmed by Dermot the same day; the proposed `canon_facts` rule (the experience is the fact, never the answered prayer) stays open in `story-bible/open-questions.md`. Complicit-leader scope also ruled same day: a major character could be a complicit leader who repents — in scope, the repentance part of the licence; recorded in `story-bible/ideas.md`. Fourth direction same day: the Church Space overlay may consider the possibility of answered prayer and spiritual voices — three non-Archive homes for an answered prayer (codex, POV experience, overlay), the Archive's refusal unmoved. Both remaining readings confirmed the same day (the reader-test transfers to the overlay; the `canon_facts` rule stands — no entry may assert a prayer was answered, the experience is the fact), and the rule is now a content authoring convention in CLAUDE.md. Everything from the 26 August intake is ruled; the Levrils Who Notices draft remains the one artifact awaiting approval.
- **[Levrils](src/lore/levrils.md) gain a Who Notices section** (2026-08-26, approved by Dermot the same day, after he confirmed the rendering reading). Below enforcement salience, noticing a Levril is rendering work by the witness: reports cluster among the contemplative, devout, and deeply attentive as a fact about practice, never a faculty, with the answered-prayer interpretation pattern explained as a selection effect the record files without adopting; matching Common Misinterpretations bullet. Extension under the canon-stability rule; direction verbatim in `story-bible/intake-2026-08-26.md`.
- **[Neither Author Nor Warden](src/codex/neither-author-nor-warden.md) — codex working paper** (2026-08-25). Kade's companion to the Limitation filing, answering the recurring petition on the design tiers and suffering: the biological-life template neither prevents pain (a template enforces nothing, fastens to no point of design in an authorless system, and holds no outcomes to pre-filter) nor establishes it (shape is specified, implementation is not — nociception filed with DNA as local contingency), with the record's one moral law placed above the design tiers, not at them. No title card yet.
- **[The Courtesy That Was Withdrawn](src/codex/the-courtesy-that-was-withdrawn.md) — codex official document** (2026-08-25). The Tír na nÓg Keeper of the Rolls' minuted standing reply declining the recurring petition for courtesy recognition of ancestral titles, on the burned-once precedent the founders carried with the constitution: the old republic's heralds tried courtesy recognition, certified a pedigree written backwards, and stopped for good. Supplies the history behind the Celtic Union's titles policy; archivist's note cross-references Marked Absences. No title card yet.

- **[The Celtic Union](src/lore/celtic-union-of-planets.md) gains a Titles and honours section** (2026-08-25). The charter confers no titles and its law neither abolishes nor endorses the old ones — nothing conferred, recognised, or taken away — a policy inherited from the old Irish republic's constitution. Completes a three-road taxonomy with the Four Islands (reform by decision) and New London (vacancy by incapacity), and supplies the standing model for how an Expansion-era territorial honour persists unextinguished.
- **`TestingStrategy.md`** (2026-08-24). The test layers, what each gate catches, where each runs (npm test vs CI) and why, the extension rules, and the open items — mechanics only; the authority boundary stays in `CLAUDE.md`.

- **The link and related-term checkers joined `npm test`; contrast and shared-script drift became CI gates** (2026-08-24). `check-internal-links.js` and `check-related-terms.js` now run on every test run; CI additionally runs `generate-themes` + `check-contrast.js` + a git-diff check that no `theme-*.css` was hand-edited, and a shared-scripts job diffing the four shared deploy scripts against `dermot-cochran-photography`'s `main` (which found `cpanel-autopull.sh` already drifted — the 15 August gap-alert default had never landed there; synced in that repo the same day).

- **Unit tests for the content filter and page classifier** (2026-08-24). `npm test` now opens with `node --test test`: `test/content-filter.test.js` pins `lib/content-filter.js`'s narrowing and private-thread opt-in truth tables (including that church-space stays hidden on unfiltered and narrowed builds alike), and `test/classify-content.test.js` pins `classifyContentPath`/`isContentIncluded` with regression tests for the layout-vs-inputPath hazard and the journal fallthrough. Those three functions moved verbatim from `.eleventy.js` into `lib/classify-content.js` to be testable; no behaviour change. Built-in `node:test`, no new dependencies.

- **[The Shape of a Life](src/lore/the-shape-of-a-life.md) — doctrine entry** (2026-08-23, approved by Dermot the same day). Dermot's direction: aliens may have gender; no terrestrial DNA; development need not pass through sexual reproduction or natural selection; the birth-to-death arc is not universal; some civilisations do not hold individual personhood in the Concord's sense. Generalises standing instances (Mnemari, Prismeri, Krenyi, Verdani, plural minds); direction verbatim and readings flagged in `story-bible/intake-2026-08-23.md`. No title card yet.
- **[Plural Minds](src/glossary/plural-minds.md) gains a Responsibility Before the Law section** (2026-08-23, approved by Dermot the same day). Dermot's ruling: joint and collective responsibility for every action of the shared body regardless of who was fronting or co-fronting; sole exception a court finding of dysfunction on a psychiatric diagnosis, with all it carries. Direction verbatim and readings flagged in `story-bible/intake-2026-08-23.md`; a scene draft applying it (Suvra Kel and Sen Lovax POVs) in `story-bible/scene-draft-one-warmth-in-the-room.md`, unpublished per the scene-draft precedent.
- **[Suvra Kel](src/characters/suvra-kel.md) — first named Ilveth character** (2026-08-23). Search-and-rescue specialist, among the first Ilveth in Corps service; the *lanterns that cannot be put out* line attributed to her, with a cross-link added from the Ilveth lore entry. Rendered *she* in the record's translation register. Approved by Dermot 2026-08-23; open ends updated in `story-bible/open-questions.md`. No portrait yet.
- **[The Ilveth (Warm-Eyed)](src/lore/ilveth.md) — new species entry** (2026-08-23). A cold-bodied people of a new world, Sardain, who see warmth as light: thermal night vision grounded in the self-glow constraint (cheap for the cold, impossible for the warm), dry mirror-pit eyes, heat-costed signalling, geothermal industry. Extends the energy-bottleneck set to four answers. Approved by Dermot 2026-08-23; remaining open ends recorded in `story-bible/open-questions.md`. No title card yet.
- **[The Working-Out of Justice](src/journal/the-working-out-of-justice.md) — journal entry** (2026-08-23). Two detective traditions — the brilliant individual as special pleading, the inevitable working-out as the Limitation in genre form — with the finding that the Unfounded countermeasures are the investigative method item for item; kill the lie, leave the person standing. Season 4 application recorded in `story-bible/ideas.md`.
- **[Special Pleading](src/journal/special-pleading.md) — journal entry** (2026-08-23). The cosmology read back as a confession: prayer and magic rejected as special pleading, faith in ultimate goodness kept — good founded, evil not; providence structural, never local; the chooser question left open on both sides of the page.

### Changed

- **The Common Manifold's definition formula corrected to "cross-membrane coherence"** (2026-08-28, Dermot's direction, following the reconciliation above). "Cross-Concordant coherence" misstated the scale — the manifold's work is inter-membrane, and two zones share no Interval — at the four sites carrying the formula: [the manifold lore entry](src/lore/post-eleven-dimensional-manifold.md), [Concordant Membranes](src/lore/concordant-membranes.md), [the glossary entry](src/glossary/common-manifold.md) and the canonical glossary's definition. Migration-map row added, scoped to the formula alone: Telearch/Levril "cross-Concordant" keeps its zone sense, and the registered band name *trans-concordant constraint field* stays, like *post-11*. Two front-matter stragglers of the reconciliation's approved migrations swept in the same pass ("every universe in the Cascade" in the manifold entry's description; "primary universe" in Concordant Membranes' description).
- **Common Manifold cosmology reconciled with the 2026-08-10 surveyed-universes canon** (2026-08-28, drafted for review — draft-and-stop). Three groups of edits, each labelled per the canon-stability rule. (1) *Genuine dilemma resolved* — fold-transit scope: [The Scope of Physical Law](src/lore/the-scope-of-physical-law.md), [FTL Mechanics](src/lore/ftl-mechanics.md), [Higher-Dimensional Folding](src/glossary/higher-dimensional-folding.md), [Concordant Membranes](src/lore/concordant-membranes.md) and the canonical glossary's Flexure/Interval notes no longer state that fold transit runs only inside one universe or that cross-universe contact is never travel — claims Deadwater's certified route and the Counterpane survey (both 2026-08-10) contradicted. Resolved by distinguishing reach across a *standing* Interval (the band rule, unchanged) from fold *adjacency* (separation compressed and released, nothing traverses a gap); the practical instrument floor moves from archecluster to formcluster, with the archecluster floor restated as history the Counterpane survey ended. (2) *Clarification* — "every universe in the Grand Ensemble is seated in it" scoped to the record's reach in [the glossary entry](src/glossary/common-manifold.md), the canonical glossary's definition and [The Grand Ensemble Multiverse](src/lore/ensemble-multiverse.md), matching the manifold lore entry's own Formwright-tier position (2026-08-01). Two migration-map rows added. (3) *Clarification* — "primary universe" corrected to the settled coherence criterion in the [Common Manifold](src/glossary/common-manifold.md) and [Interval](src/glossary/interval.md) glossary entries, applying the existing 2026-07-29 migration row.
- **[The Cosmic Cascade](src/lore/cosmic-cascade.md)'s no-moral-cosmology invariant clarified** (2026-08-25, Dermot's direction: the limitation does not imply the Cascade is immoral or amoral). The bullet now states its own scope — morality as mechanism ruled out, moral character not adjudicated: no tier administers virtue or sin, the design tiers are agents whose abstention is a choice, the record's asymmetry (founded things by design, the Unfounded by nothing, the one moral law bounding evil alone) stands, and operational silence is the cost of an open future, not indifference. A clarification under the canon-stability rule: says better what the corpus already held.
- **[Sir Galahad Thorne](src/characters/galahad-thorne.md)'s territorial honour given a kind** (2026-08-25). One clause on the character page: an old Earth-descended grant of the Expansion's leaving — kind only, territory and date still unnamed. Heads off the reader inference that the "Sir" is a Kingdom of the Four Islands knighthood, which the contact timeline and the Old Houses' hereditary peerage cannot support.

### Fixed

- **The Commissioned Standing lore entry retitled "Commissioned Standing: What the Gate Grants"** (2026-08-25), clearing the duplicate-title warning against the glossary entry per the FTL Mechanics pattern (glossary keeps the bare term, lore gets the subtitle). Title only; no `related:` list named it and inline links are URL-based, so no sweep needed.

## [1.24.0] — 2026-08-23

### Added

- **[The Better Side of Intervention](src/codex/the-better-side-of-intervention.md) — codex correspondence** (2026-08-23). An unattributed devotional response to Kade's Limitation working paper: restraint as eternal discretion rather than structural constant, toleration distinguished from underwriting, the purpose claimed but never specified. Affirms the Unfounded's unfoundedness outright; Archivist's note keeps the record's position plain. No title card yet.
- **The Kieme glossary's Names section states that every name is a title** (2026-08-23). None personal, none could be — the tier has never spoken and admits no address — and registers **The First Principle** as the philosophical schools' filing, annotated: *first* names the namer's horizon, not the structure. Count corrected to four names in current use. Open questions recorded in `story-bible/open-questions.md`.
- **[Creative Rather Than Imperative](src/journal/creative-rather-than-imperative.md) — journal entry** (2026-08-23). A week spent specifying a coding agent, read through the Cascade's published phrase for creation, with a closing section on hand-fixing generated code as the Kiemic intervention dilemma; craft reflection only, no canon change.
- **[The Cost of the Crossing](src/lore/the-cost-of-the-crossing.md) gets its image — Dermot's own photograph** (2026-08-23). `lore/island-watchtower.jpg`: the portfolio's *Dalkey Island*, unmodified — a low green island across cold water, a round stone watchtower and a gabled stone ruin on the ridge. The structures ruled in as the Four Islands' own furniture, where Garachico Rock's summit cross was ruled out. Provenance in `story-bible/images.md`.

## [1.23.0] — 2026-08-22

A release with a survey at its centre. The planetary-physics set — a dozen
entries, from weather and tides to soils and night skies, with *Coasts, and
Other Edges* as its capstone — walks the same physics across every kind of
ground the habitable worlds have, and six of those pages now carry Dermot's
own photographs, re-filed from the portfolio. Beside it, the constitutional
thread arrived whole: the Enrolled Text, the Amendment Atlas, and Ilsabet
Corren, who wrote it.

The other half of the release is the editions growing into readers: a reader
profile and style guide per edition, a hero cast per edition, presentation
modes a reader can override for themselves, and a domain strategy that names
which site carries which signal — each arriving with the check (contrast,
hero casts, the URL a layout actually emits) that keeps it from failing
silently. The portrait backlog closed too: `PORTRAIT PENDING` is gone from
the cast.

### Added

- **[Tides Without Shores](src/lore/tides-without-shores.md) gets its image — Dermot's own photograph** (2026-08-22). `lore/shoreless-sea.jpg`: the portfolio's *Sunset at Sea* as a radical left crop losing the ship's deck entirely — open water to a pink horizon with two distant vessels hull-down, kept because on a shoreless ocean the only witnesses are afloat. *Where the Water Was* judged unusable for the soils page on a second look (litter across the best texture); reason and upgrade path recorded in `story-bible/images.md`.
- **Four planetary-physics entries get images — Dermot's own photographs, re-filed from his portfolio** (2026-08-22). [Stone, Fire, and Ice](src/lore/stone-fire-and-ice.md) takes `volcanic-strata.jpg` (cropped to the strata alone, removing a guardrail and fence — scale deliberately unreadable); [Coasts, and Other Edges](src/lore/coasts-and-other-edges.md) takes `cloud-sea.jpg` (a cloud sea with ridges standing out of it — the entry's "shoreline between atmospheres"); [The Ledger and the Truce](src/lore/the-ledger-and-the-truce.md) takes `grassland-storm-tree.jpg` (one tree in dry grassland under a storm sky — the truce with its enforcer arriving); [The Forest Is Waiting](src/lore/the-forest-is-waiting.md) takes `drowned-forest.jpg` (the century-bet lost). Selection survey and provenance in `story-bible/images.md`.
- **[Rivers Old, Lakes Young](src/lore/rivers-old-lakes-young.md) gets its image — Dermot's own photograph** (2026-08-22). `lore/young-lake-margin.jpg`: a flooded lakeshore in February light, bare trees standing in the shallows, gorse in flower on the drowned bank — a young lake's margin as the entry defines one. Cropped and resized from his D3100 original; provenance recorded in `story-bible/images.md`.
- **The About page's fan-works section notes its first fan work** (2026-08-22). A "Noted" paragraph applies the licence's standing *referenced, not endorsed* formula to [tissadelleshepherd/star-rangers](https://github.com/tissadelleshepherd/star-rangers), a fan fork whose one addition is an uncaptioned AI-generated image evidently intended as an off-duty Tissadelle Shepherd. A reference only — it confers no status, and the identification is recorded as the maker's, not the record's.
- **[What the Stones Say](src/codex/what-the-stones-say.md) — the first Quernish document in the record** (2026-08-21). A codex entry in a settler's own voice: the address line-keeper Odal Ferren gives a watcher standing a first rain-watch, taken down under the lore entry's transcription arrangement. Valid-for-its-author throughout; it keeps the Hush absence marked from inside (what is held is said at carryings, never to the record) and is the in-world source of the "the stones say where the rain is, not where it is going" reply the lore entry quotes. Cross-linked from Quern's Balance or Collapse section and See also.
- **[Quern](src/lore/planets/quern.md) — a tidally locked world in a new Kiln System, settled along its storm belt** (Dermot's direction, 2026-08-21, two pitches). One face a standing rock-vapour storm over a substellar melt, the other a volatile cold trap ending in nitrogen snowfields, and between them the Millrace — a permanent terminator shear belt of horizontal rain with the lightning inside it. A small independent human settlement lives at the belt's night-ward edge: wind-scooped architecture, baseload power from the inter-hemisphere temperature gradient, air electrolysed from the rain, and a calendar (the Breathing, kept by eye against turning-stones) set by the belt's quasi-periodic ~3-year walk — a QBO-analogue, not orbital mechanics. Settler cosmology divides the world into the Burn, the Howl and the Hush, filed at Archive distance; the surveyors' names (the Glazing, the Meal) are kept alongside. Grounded in real lava-world and atmospheric-collapse physics under the standing hard-SF default; whether the atmosphere is in equilibrium or slow collapse is deliberately left unadjudicated in-world on the two-readings pattern, with the settlement's turning-stone records as the longest baseline on the question. Self-registers in `/atlas/`. No image yet.
- **[Habitable, for Whom](src/lore/habitable-for-whom.md) — humanly habitable worlds versus the wider habitable category** (Dermot's direction, 2026-08-22). Tenth of the planetary-physics set, and the taxonomy the other entries were quietly leaning on. **Habitability is a relation, not a property**: unqualified, the record allows *habitable* one meaning — bears its own biosphere — while the class people actually mean, **the full human envelope occurring naturally**, holds Earth and Tír na nÓg and nothing else, with Drithane at the envelope's edge (acclimatisation, agriculture under glass) and Kernowek Reach's merely-survivable native air farther out. Each species carries an **envelope** (gravity, atmosphere, temperature, chemistry, light), and the envelope is a different fact from the frame — the near-humanoid Pandoids' home would kill an unprotected human, so *humanoid-habitable* is the loose phrase the survey gently declines: the accurate index is the species, never the shape. The symmetry is canon's own: no alien people is natively compatible with Earthlike parameters, so **nobody's envelope is the universe's default** and *hostile* is as unfinished a sentence as *habitable* — Meridian is somebody's mild world. Consequences drawn: meetings happen in rooms somebody built; human settlement is mostly envelope manufacture; and Tír na nÓg's standing gets its doctrinal name — the one ground where *habitable for humans* finishes without an asterisk.
- **[The Star Rangers Survey Corps](src/lore/star-rangers-survey-corps.md) — the Corps and its branches** (Dermot's direction, 2026-08-22). The record's most-quoted institution gets its page, organised around the branches he named — the **Archive** (custodian of the record, named for an institution older and larger than the Corps), **Planetology** (ground: rates wearing the costume of places), **Meteorology** (the fluid envelopes, atmospheres and oceans as one subject), **Ecology** (who lives there; *habitable for*, never bare *habitable*) — plus the Academy page's existing Xenolinguistics branch, under the Academy's own open-list rule. The soil file deliberately carries two branches' stamps; the collect/explain seam with the Science Corps is restated per-branch. Science Corps page's inline Survey Corps link re-pointed here from the codex protocol.
- **[Ilsabet Corren](src/characters/ilsabet-corren.md) — a character page for the Amendment Atlas's author** (Dermot's direction, 2026-08-22). The Institute's Fellow in Comparative Records: institutions' registers read as instrument logs. One new commitment beyond her paper — her method's origin in exercising Schedule C's verification clause as an elector and finding the discrepancy was her own arithmetic; the count was right, the exercise measured her. World of origin, family and age left as marked absences. Her signature and Sen's in the Atlas codex entry now link to their character pages. Closes the second of the 22 August draft's open questions; the Atlas-drawing hook stays open.
- **[The External Codex](src/journal/the-external-codex.md) — journal entry on the constitutional material's real-world provenance** (Dermot's direction, 2026-08-22). References his 2013 PhD thesis, *Formal Specification and Analysis of Danish and Irish Ballot Counting Algorithms* (IT University of Copenhagen), as the formal-methods source behind Schedule C, the Verification Doctrine and the Enrolled Text — cited in the Journal's out-of-character layer only, never in-universe, under the *referenced, not filed* rule.
- **Glossary mirrors [Enrolled Text](src/glossary/enrolled-text.md) and [Difference Form](src/glossary/difference-form.md)** (Dermot's direction, 2026-08-22). Short forms of the two load-bearing terms from the Enrolled Text lore entry, on the `commissioned-standing` mirror pattern — `short` and `plain` summaries, mutual `related:` links, filed under the glossary's `Records` category — asserting nothing beyond the merged doctrine. Titles drop the leading article so the lore and glossary pages stay distinctly reachable by exact title. Closes the last of the three open questions the 22 August draft recorded.
- **[The Thin Inheritance](src/lore/the-thin-inheritance.md) and [The Honest Dark](src/lore/the-honest-dark.md) — soils and night skies** (Dermot's direction, 2026-08-22), completing the planetary-physics set at eleven, **plus the moon and glacier placements the set had left open**. Soils: the one substance needing stone, water, life and time together — *soil is the mountain on its way to the sea, paused; a field is a queue* — with Winterbourne's parish strips as a polity drawn on a soil profile, Difenland's terraces as detention engineering, the Abbeylands as soil-as-heirloom, Trígrian's deep ash as violence feeding what life makes, Fliade's *mineral-sharp* surface as the negative case, and soil as the fourth natural archive with an asterisk: the one its keepers eat. Night skies: dark by default on every settled world because the technology does not shine (What the Record Refuses, applied upward) — Drithane's timetabled drithle, Prismere's instrument-sky, Trígrian's negotiated night, and New Cotswolds' sky holding the Milky Way as an object: *the only settlement whose night sky contains everyone else, and the oldest stale file anyone looks at*. **Placements**: Tír na nÓg's canonical tidal flats get their raiser — a single large moon the founders declined to rename, **An Ghealach** — Drithane's image-canonical low bright moon, polar ice and walking snowline are stated in prose, and Aethelrock's glens are ice-cut, its glaciers long finished: *an old world even in this*.
- **[The Enrolled Text](src/lore/the-enrolled-text.md) — how settled space keeps its constitutional law** (Dermot's direction, 2026-08-22). The Concord compact as a founding enrolment (Tycho, 2543 UCSD) that is never edited in place, plus every amendment since in **difference form** — strike/insert against a named state of the text, laid open for objection, certified to apply cleanly, enrolled beside its predecessors and never over them. The current text is a derivation any reader may re-perform, which is why the compact outlasted its enforcers: custody can be seized, re-derivability cannot. Defeated and withdrawn proposals stay on the register as the law's null results. Cross-linked from the Concord entry's See Also.
- **[The Amendment Atlas: A Proposal](src/codex/the-amendment-atlas.md)** (Dermot's direction, 2026-08-22). A visiting working paper by a new Institute fellow, Ilsabet Corren, proposing to read the compact's three centuries of enrolled amendments as survey data — four charts: the boundary of the person over time, the lag between a correction first argued and enrolled, the never-amended still core, and enrolment density by era — with plural registration as the worked figure and the method's limit as its caption (*the record's line, not the people's*). Sen's shelf comment is filed beside it: favourable, two conditions. Proposed in-universe, not built — a story hook, not a settled fact.
- **[Where the Weather Stops](src/lore/where-the-weather-stops.md) — caves and the underground on habitable worlds** (Dermot's direction, 2026-08-21). Ninth of the planetary-physics set. Opens on Fliade's own survey lesson made standing policy — the underground is the one country a survey cannot read from orbit, and a surface survey says so on its cover. **The slow room**: depth silences the set's doctrines one by one, and a deep cave's held temperature is the surface climate's mean — the third sibling of the natural archives (the glacier keeps the years frozen in order, the tree alive in order, the cave keeps only the answer). **Three chemistries make caves** — dissolution (Winterbourne's spring line as karst-in-progress), fire (lava tubes under Kernowek and Veyr-3), and neither (Aethelrock's granite verdict: nearly caveless) — so a cave inventory is readable from a geology map. **The quiet has tenants**, and the census makes the thesis: Fliade's chemistry-fed biosphere, the Anstolik walking to a schedule nobody set, the Pandoids keeping a spoken archive in the one environment constant enough to trust a memory to, the Martian pressure-hermitages, and the Cavern Constraint's quantum computers — *the underground's one export is constancy, and every tenant is a customer for it*. Closes the loop: a cave mouth is a coast, and the underground is the country almost no survey finishes.
- **[The Cost of the Crossing](src/lore/the-cost-of-the-crossing.md) — islands and archipelagos on habitable worlds** (Dermot's direction, 2026-08-21). Eighth of the planetary-physics set. **What makes an island is not the water but the crossing** — insularity measured in crossing-cost, not kilometres. **An archipelago is a mountain range that lost the argument** once the sea joined in: the Four Islands as a caldera rim's surviving summits, Thalassa's shoals as islands that failed by metres, Saltmere as the completed judgement — a world where every candidate island lost, which is the geological reading of why its civilisation brought its own ground. **Isolation is a laboratory**, with the Kingdom's lost colony as the worked case — not a survival despite isolation but what isolation is *for*, seen from the far side — and crossing-cost writing archipelago politics as enforced specialisation. **The record's driest islands never touch a sea**: Ynys Wydrin's domes (an island whose sea is the sky, in its own name), Kernowek's moored band of air, Fliade's warm caverns, and habitats as the pattern at its purest — New London as the island preservation effect with a station-keeping budget. Closes on the record's most important island being a planet: Tír na nÓg, the catalogue's one walkable world, named fittingly for an island out of myth.
- **[Coasts, and Other Edges](src/lore/coasts-and-other-edges.md) — coasts and coastlines on habitable worlds** (Dermot's direction, 2026-08-21). Seventh of the planetary-physics set and its capstone. Opens on survey doctrine: the Corps records no coastline length, because the figure depends on the ruler — every coastal measurement is stamped with its scale. The coast is the one landform whose rates run at human speed (the argument between land and sea, held daily, with the tide as the clock), and the address every settlement wants — an interface being two economies with one address, run to its limit by the Four Islands, a kingdom that is nearly nothing but coast. The definition-benders were all waiting in canon: Saltmere and Thalassa have **zero natural coastline** and are coastal civilisations anyway, their entire shore the engineered perimeter of their own hulls; the **Pelagene Littoral** rotate the concept ninety degrees, a boundary-layer people whose littoral runs horizontally through the water column; Kernowek's Cloud Line is a shoreline between atmospheres; Fliade's cavern mouths are its beaches. Closes the set on the record's own naming — *Threshold*, *Causeway*, *edge behavior*, *thin places* — the coast as the everyday rehearsal for the edges the record exists to watch, claimed as an observation about vocabulary, not metaphysics.
- **[The Forest Is Waiting](src/lore/the-forest-is-waiting.md) — forests and woodlands on habitable worlds** (Dermot's direction, 2026-08-21). Sixth of the planetary-physics set, completing the biome coverage. The tension that organises it: a forest is the **default** cover of watered land — Winterbourne's twice-recorded reversion is the proof nothing needs to plant it — and simultaneously a **bet** that the next century will resemble this one, which is why Trígrian's canonical *no forests* is a correct price rather than a poverty (grass bets a season; a tree bets a century). A canopy builds its own building and partly waters itself — Verdance's canopy belt as the full expression, with the Canopy Concord's cities built *into* the structure; Winnow Hanger's mist-combing wood as the working end, a wood as a water instrument. Trees keep the living half of the archive the glaciers keep frozen — same annual bookkeeping, alive while it records — with Vellamar's dynasty-old specimen trees as the witnesses. The strange woods are held apart: glasswood is genuinely a wood doing a second job; a dryadic grove is boundary apparatus wearing woodland's shape, and filing one as forestry mis-files a layer.
- **[The Ledger and the Truce](src/lore/the-ledger-and-the-truce.md) — deserts and grasslands on habitable worlds** (Dermot's direction, 2026-08-21). Fifth of the planetary-physics set. **A desert is a line in a water ledger, not a temperature** — the driest surface in the settled record (Fliade's) is among its coldest, since frozen water pays no bills — and every desert carries the signature of the doctrine that made it: rain shadow (mountains), continental distance (weather), permafrost (freshwater), Aspenar's mineral extreme. Deserts are precise rather than empty, with the Serephine Dunekin as the worked case — a civilisation run on the one resource a desert has in surplus, contrast — and the Monasteries of Mars as the desert-hermit instinct carried to a desert no Earth hermit ever had. **A grassland is a truce between desert and forest, and something has to enforce it**: fire on Trígrian, teeth on Winterbourne (the turf's reversion is on record twice), climate itself on Verdance's steppe, whose herding ecologies answer with the grassland's characteristic politics — movement over territory. Closes the set's arc: biomes, like mountains, are rates wearing the costume of places, and a map of either is dated the day it is drawn.
- **[Stone, Fire, and Ice](src/lore/stone-fire-and-ice.md) — mountains, volcanoes and glaciers on habitable worlds** (Dermot's direction, 2026-08-21). Fourth of the planetary-physics set, and terrain read as three verbs caught mid-sentence. **A mountain is a rate** — uplift against erosion, with Aethelrock's granite highlands as mountains in retirement, the exposed roots of ranges nobody will ever see. **A volcano is the interior still speaking** — the record's most varied hazard: Kernowek Reach settled *on* its volcano because altitude was the resource, the Four Islands live inside a volcano's afterlife, Fliade takes the heat as landlord, and the Veyr Basaltborn were handed metallurgy for nothing. **A glacier is the archive nobody kept** — annual layers sealing each year's air, a climate record begun before any charter and maintained without staff; glaciers had zero prior mentions in the corpus, so this is the set's one genuinely new subject, introduced doctrine-first with no ice asserted for any named world. Trígrian's canonical *no polar caps* becomes the eloquent exception: the world whose climate most needs remembering keeps no ice memory of it. Includes a preventive terminology note distinguishing geological faults from Etheric fault lines before that collision happens.
- **[Rivers Old, Lakes Young](src/lore/rivers-old-lakes-young.md) — freshwater on Tír na nÓg and the other habitable worlds** (Dermot's direction, 2026-08-21). Third of the planetary-physics set. The thesis pair: a **river** is the oldest unbuilt infrastructure a world has — the integral of climate over topography, a record you can walk along — and a **lake** is news, recent violence or active tending, so a landscape can be aged from orbit by counting them. Tír na nÓg reads as both ages stacked by altitude (patient lowland wetlands, upland falls in spate). Groundwater: the well as a settlement's handshake with water it cannot see, Winterbourne's spring line as the handshake offered in a line, Fliade as the limit case whose hydrology has no surface chapter at all, and Aoife's well-water blessing crossing as living observance. One air-readable diagnostic synthesised from existing canon: ordinary treelines trace the drainage, **dryadic groves trace the Etheric fault lines and ignore the valley** — on Tír na nÓg, whether trees follow the river is the difference between botany and boundary cartography. Warm wells handled in scope: a warm spring has a heat source, geothermal common, boundary physics lawful where a flexure runs close; the two famous wells' warmth is instrument-grade fact and the *meanings* stay filed in the Warm-Edge Correlation.
- **[Tides Without Shores](src/lore/tides-without-shores.md) — currents and tides on the water worlds** (Dermot's direction, 2026-08-21). Sequel to *Weather on Other Worlds*, same discipline below the waterline. Two derivations do the work: **a current needs a wall** — no landmass means no basins and no boundary currents, so every current on Saltmere is the circumpolar kind, Earth's one anomaly become the whole regime; and **a tide needs a shore to be seen** — on a shoreless ocean the tide is a planetary heave nobody has ever watched, felt only in mooring tension, so Saltmere's founders knew their star moved their ocean *only* through telemetry, reversing the oldest fact in human science. Draws on canon already in place: the survey's own "too deep to break the surface at any tide", Saltmere's tide-*like* platform cycle, Venice's foundation "that no longer cared what the water did" inherited by Thalassa, and the Four Islands as the with-shores counter-case — where the Tideward Sisterhood's "it has never once failed to come" is cited as the one subject on which the devotional and mechanical readings walk in step. No moons asserted for any world: which raisers a world answers to stays a line in its survey file.
- **[Weather on Other Worlds](src/lore/weather-on-other-worlds.md)** (Dermot's direction, 2026-08-21). The record's doctrine on alien and remote-world weather, synthesised from what per-planet canon already held rather than invented: same physics on different numbers, with the Four Islands' storm-lock, Saltmere's landfall-less storms, Trígrian's three-sun burning seasons, Drithane's dry clear skies and Winterbourne's spring line as the worked examples. Two consequences drawn out: **the sky is local** — no FTL communication means no interstellar weather service, forecasting is locally kept continuity, and the local record outranks the transit-lagged survey file (Prismere's sky-reading treated as instrumentation being the limit case); and across Mediarch realms the *parameter ranges* themselves are lawfully scoped, so another realm's weather is ordinary weather on untabulated numbers. Closes with the refusals: **no storm is sent** — the pathetic fallacy is a figure of speech in every jurisdiction and a mechanism in none, with the Warm-Edge Correlation cited as the attributed Codex reading — and managed weather (Kernowek Reach) is still weather.
- **[Commissioned Standing](src/lore/commissioned-standing.md) — a commissioned mind is not property** (Dermot's direction, 2026-08-21). Lore entry plus a [glossary mirror](src/glossary/commissioned-standing.md): a system commissioned *as a mind* acquires standing at the gate — not saleable, not scrappable, for age or obsolescence or a discontinued line. The standing rides on the commission rather than any later measurement, so a mind's inner life is never re-litigated at an owner's convenience; withdrawal from service exists for cause, through process, and is never a disposal instruction. This is the deeper reason classic units like [Xenon](src/characters/xenon.md) and [Lucene-9000](src/characters/lucene-9000.md) continue in service — the kernel page's economics-only explanation is revised to layer standing over economics rather than contradict it. The right is framed as most visible in the jobs that exist because of it: Xenon's repair-indefinitely-make-none licence, the classic-generation certifications, the Eden Warden's welfare authority. Does not attach to the uncommissioned, which `glossary/uncommissioned.md` now names as part of why the found-mind custody question is hard.
- **[The Kept Line](src/characters/the-kept-line.md) — a Dragon, the Levril subclass that had no worked example** (Dermot's direction, 2026-08-20). The corpus had a Higher Levril ([the Turquoise Dove](src/characters/turquoise-dove.md)) and nothing for the other half of the split. Filed by signature per the lore entry's own rule — named for an inverted anomaly, a containment edge that stops drifting when every model says it should. Arrives during an event where the Dove arrives after; never recorded speaking, mentoring or selecting a Champion. Whether it was at Dock Seven is left unanswered: the function and occasion match, no signature survives, and the entry says that establishes nothing. Uses *it* because no witness has supplied it a figure — the pronoun follows the evidence, not the entity.
- **sciencefiction.site carries the story's people** (2026-08-21, Dermot's ruling). A `characters: [...]` list of every `povs` id used by the seasons its two threads own that has a character page — the general-tier domain had been publishing every chapter and none of the people in them, because characters are matched by id or tag and never by season the way chapters are. **114 shown pages → 244; 0 character pages → 16.** Its hero cast is drawn from the same list and deliberately differs from the canonical domain's.
- **A hero cast per edition** (2026-08-21, Dermot's direction). `starquest` gains Larsen, Albercombe, Voss, Calloway and Jeeves; `fellowship` Asteria and Brother Fintan; `fellowship-archive` Asteria alone; `church-space` Brother Fintan alone; `default` swaps Elvira (no portrait) for Tissadelle, the arc's protagonist and previously absent from the cast fronting the canonical domain. `pets` is unchanged — it already had one. **Six of the seven editions now render a hero slideshow, against two before.**
- **`validate-content.js` checks every edition's hero cast against the corpus and that edition's own filter.** A `heroCharacterIds` entry that names no page, names one with no `image:`, or names a character the edition filters out now fails the build; an edition whose pool is empty warns instead, since that is a fact about the domain rather than a mistake in the list.

- **A reader-side reading-mode switcher** (2026-08-21, Dermot's direction). A "Reading" control in the site header lets a reader override the domain's presentation mode for themselves — the edition's own setting plus Standard, Large print, Compact and Spacious. Client-local and nothing else: one `localStorage` key, per-origin, never sent anywhere, and absent entirely with JS off. [`src/js/presentation.js`](src/js/presentation.js) builds and drives it; the restore is an inline `<head>` script in [`base.njk`](src/_includes/base.njk), since a deferred file would let the page paint in one mode and jump to another. `PRESENTATION_MODES` gains a reader-facing `label` beside its engine `summary` — a reader picks a type size, not an allegiance to another edition.
- **`validate-content.js` now checks the URL a layout emits for `image:`**, not only that the file exists somewhere under `src/images/`. The existing check matches on basename so a partial path can resolve; this one rebuilds `<category>/<value>` exactly as the five image-bearing layouts do.

- **Presentation modes — a third axis beside edition and theme** (2026-08-21). `PRESENTATION_MODES` in [`lib/editions.js`](lib/editions.js) registers four postures — `story`, `primer` (undercover-pets.com), `archive` (fellowshipoflight.site), `contemplative` (fellowshipoflight.org, church-space.site) — implemented as `:root` custom-property overrides in [`src/css/main.css`](src/css/main.css), keyed off a `data-presentation` attribute `base.njk` writes onto `<html>`. Identity, palette and posture are now three independent things. No `deploy.conf` key and no deploy plumbing: the mode rides into the build with `EDITION`. `validateEditions` fails on an unregistered mode and on a registered mode `main.css` never implements. Reasoning and a reader profile per edition: `story-bible/edition-reader-profiles.md`.
- **`story-bible/edition-reader-profiles.md`** (2026-08-21) — one profile per edition (who arrives, what they came for, what loses them) and the style guide that follows: the three axes, the house floor, the mode token table, per-edition page counts measured that day, and five open questions indexed in `open-questions.md`.
- **[`scripts/check-contrast.js`](scripts/check-contrast.js)** (2026-08-21) — checks every palette against WCAG 2.2 AA (4.5:1) on the pairs `main.css` actually composes, including hover states and `--color-surface-2`. Outside `npm test` because it reads the generated theme files. `solarized` is exempt, with the reason in the script.
- **A coverage check in `scripts/generate-themes.js`** (2026-08-21) — fails if `main.css`'s `:root` and `ROOT_KEY_ORDER` disagree. A property added to one and not the other was silently dropped from all eight generated themes: correct in local dev, wrong on every branded domain.

- **A candidate subdomain list** in `story-bible/domain-strategy-and-duplicate-content.md` (Dermot's direction, 2026-08-20), with measured indexable/codex/lore/character counts for eight faction filters plus two utility subdomains. Recorded because the codex-site idea otherwise reads as an argument for buying domains: a subdomain costs nothing, needs no registrar, and resolves cleanly against the registry (verified). Two limits stated with it — a codex subdomain serves a subset of its parent's pages so it canonicals there and will not rank, and a subdomain protects no name, so it complements the anti-squatting registrations rather than replacing them.
- **A domain registered as an alias now gets a one-page redirect deployment instead of a refused deploy** (Dermot's direction, 2026-08-20). `deploy_alias_notice()` in [`scripts/cpanel-deploy.sh`](scripts/cpanel-deploy.sh) renders three files from `scripts/alias-notice/`: an `.htaccess` 301ing the whole namespace to the target with path and query preserved (`/.well-known/` exempt for AutoSSL), an `index.html` carrying `rel=canonical` and a meta refresh for hosts without `mod_rewrite`, and a `robots.txt` that deliberately does **not** `Disallow` — a crawler has to fetch the URLs to see the redirect and consolidate them. Runs with `rsync --delete` so the previous full build is removed, and needs no Node, Eleventy or build step. Replaces the refusal added earlier the same day, which was safe but did nothing: a refused deploy leaves the stale full build serving, so the duplicate survived until someone opened cPanel. **The consolidation is now real from the first deploy** and the cPanel repoint is tidying rather than a prerequisite.
- **`ALIASES` in [`lib/editions.js`](lib/editions.js), and `cpanel-deploy.sh` refuses to build a registered alias.** The eleven alias hosts move from prose in a comment to a checked map; `validateEditions` rejects a host that is both an alias and a served domain, or an alias pointing at a domain no edition builds. The deploy fails on either path with an error naming both fixes. **Third-party forks and independent deployments are explicitly unaffected** — the check keys off a *named* alias, not off "unknown to this registry", so a fork on its own domain still deploys the full site with the two-key minimum `deploy.conf`, as `FORKING.md` promises. Replaces the identity check added hours earlier, which refused any unconfigured domain and would have broken exactly that path.
- **`cpanel-deploy.sh` refuses to deploy a domain nothing has given an identity.** If a domain is absent from `lib/editions.js` *and* `deploy.conf` sets no `THEME`, `SITE_NAME`, `CHARACTERS`, `TOPICS` or `THREADS`, the deploy now fails loudly instead of silently building the **full unfiltered site** — 566 indexable pages rather than a narrowed clone's 60–90, unbranded, self-canonicalling, with its own 443-URL sitemap. Deployed to a domain meant to be an alias that is a brand-new duplicate of the canonical site, reported as a success. Two ways in, both of which happened on 2026-08-20: removing a domain from the registry while its `ALT_DOMAINS` build is still configured server-side (the alias demotions below need a cPanel change the merge cannot perform), and an editing accident dropping an edition entry. Checked before `THEME` is defaulted, since afterwards a domain that chose the main palette is indistinguishable from one nobody configured.
- **`ranksAt` in [`lib/editions.js`](lib/editions.js): one domain per tier family carries the ranking signal.** A non-ranking domain emits a cross-domain `rel=canonical` at the same path instead of a self-canonical, and drops its sitemap entries and robots.txt `Sitemap:` line to match; `og:url` stays self-referential. `validateEditions` fails the build on a `ranksAt` no edition claims, and on a ranking domain whose filters are not a superset of a sibling's. Threaded registry → `resolve-edition.js` → `cpanel-deploy.sh` → `SITE_RANKS_AT` → `site.js` → `base.njk`. Prompted by Search Console duplicate-content reports — the fellowship and church-space editions were serving the identical 62 indexable pages from six domains. Background, measurements and the two remaining decisions: `story-bible/domain-strategy-and-duplicate-content.md`.
- **A build now has to pass a written test, and four domains failed it.** An independent build is justified only when it has its own page set (framing alone fails), its own reason to be searched for, and asserts nothing the central record does not. `fellowshipoflight.online`, `fellowshipoflight.space`, `church-space.online` and `starquest.online` are cPanel aliases now rather than builds — eleven hostnames, seven builds.
- **`fellowship-archive` — the first codex site, at `fellowshipoflight.site`.** The Fellowship's own comparative archive: `topics: ["fellowship-of-light", "arilon"]` and deliberately **no** thread, which is what makes it a codex site rather than a fourth reader of the same story. 59 indexable pages, 6 codex entries and 3 lore entries of its own, against 0 for the chapter-house framing it replaces. Canon-safe by construction — the Codex is already valid-for-its-author — with one hard limit: a codex site is a filtered view of the central codex, never a domain with entries of its own. `fellowship` widens to the same topics so it stays a superset of its sibling.
- **The contemplative tier is two families, split by institution.** Fellowship of Light (`fellowship`, `fellowship-archive`) ranks at `fellowshipoflight.org`; Communion of the Called (`church-space`) is a family of one. A **family is not a tier**: a tier is a readership, a family is a set of domains serving the same pages that must consolidate on one host.
- **`starquest.online` demoted from a build to a cPanel alias of `starquest.site`**, under the rule that settled the duplicate-content question: a registration defends the name, a build has to earn its keep. The orbital-five-o thread is one task force and Season 4 is one chapter, so there was no second face to give it. Returns the young-adult tier to a family of one.
- **[New Cotswolds](src/lore/new-cotswolds.md) — a human settlement in another galaxy** (Dermot's direction, 2026-08-20). Limestone village country in the **Wolds End** galaxy, founded from the [New London Space Habitat](src/lore/new-london-space-habitat.md). Grounded in two established constraints rather than invented reach: fold transit is *geography, not distance* (an admissible corridor makes a galaxy reachable where a near star without one is not), and there is **no FTL communication**, so the file is ninety-one years stale and the Archive declines to write it in the present tense. The mirror of Winterbourne — there the intent is unrecoverable, here it is a filed document.
- **[Greenward](src/lore/greenward-habitat.md) and [The Guest Ring](src/lore/the-guest-ring.md) — a reciprocal habitat instrument** (Dermot's direction, 2026-08-20). A human ring above Federation [Verdance](src/lore/planets/verdance.md), a Federation ring above Celtic Union [Drithane](src/lore/planets/drithane.md), one page-and-a-half instrument, and three ways it is less symmetrical than it reads — most usefully that the Rangers hold **no planetary jurisdiction** and so had no world to trade, making a two-party document with three parties on one side. The shared non-descent clause is one sentence covering two different facts: protocol at Verdance, chemistry at Drithane.
- **[Habitats and the Worlds Beneath Them](src/lore/habitats-and-the-worlds-beneath-them.md)** (Dermot's direction, 2026-08-20). States the general principle the two rings are instances of: a habitat manufactures its own environment, so **the species it supports is a fact about the habitat and not about the world it orbits**, orbit is chosen for access rather than compatibility, and a ring above a world is not a claim on it. Includes why the contrary assumption is near-universal — everyone learns habitats at Sol, where all six happen to be human above a human world, and a rule drawn from that sample does not survive first contact.
- **[Winterbourne](src/lore/planets/winterbourne.md) — a chalk-downland world of spring-line villages** (Dermot's direction, 2026-08-20). An agricultural independent in a new **Albion** system: an escarpment, a line of springs at its foot spaced two to three kilometres apart, and a village on every one of them — Ashenden, Nether Wickham, Over Wickham, Chilverstoke, Marlbrook, Bramblecote, Hazelcombe and the market town Downminster, plus Winnow Hanger off the line with a dew pond. The settlement pattern is derived from the hydrogeology rather than decorative, per the standing hard-SF default; whether it was *intended* is left unadjudicated on the three-reading pattern. Self-registers in `/atlas/`. No image yet.
- **The last six portraits, and `PORTRAIT PENDING` is gone from the cast** (2026-08-20) — [Bertram Ashcombe](src/characters/bertram-ashcombe.md), [Demelza Trevithick](src/characters/demelza-trevithick.md), [Imogen Petrakis](src/characters/imogen-petrakis.md), [Rhian Gwynne](src/characters/rhian-gwynne.md), [Sen Lovax](src/characters/sen.md) and [Zara Wayland](src/characters/zara-wayland.md). **Every character page on the site now carries a real image and written alt text.** The candidates had been generated on 18 August and were sitting unfiled; the work was mostly choosing between them against each page rather than making them. **The exception is Bertram Ashcombe, whose prompt was wrong and was fixed.** It asked for *"science-fiction detective setting with a period sensibility"* and stated no era, so the generator kept the period half and dropped the other: both candidates were a Victorian panelled office that could not be a station in 2826. `images.md`'s own rule — *state the era, or the setting defaults to contemporary Earth* — turns out to apply to a stated **sensibility** too, which a generator reads as a setting unless something anchors it. Regenerated with the year, the bulkheads and a window onto the planet; the tailoring and the good humour are his and were kept, so the joke still works — an old-fashioned man in a plainly modern room. Where a pair differed, the one showing the person *at* the work won over the one showing them beside it — Demelza with both hands on the machine she keeps, Rhian with a hand in the wiring — which is the same instinct that made the clan heads land: a portrait that argues from the page beats a portrait that poses in front of it.

- **`-Underlay` on `scripts/make-codex-cover.ps1`** (2026-08-20) — the card compositor. A photograph is centre-cropped to fill the square and scrimmed in the template's navy, **heaviest at the two bands the lettering occupies** (the category line and the title block) and lightest through the middle, then the existing text pass runs over it. **The point is spelling**: an image generator cannot letter a card — the supplied set produced *Healng*, *ooroting*, *aa' Star Rrarles* and *superical* — so the picture is generated wordless and the words are set by the font engine, correct by construction. `-Scrim` is a percentage (default 62) and an underlay turns the motif off unless `-Motif` is passed explicitly, since a motif drawn over a photograph fights it. Underlays should be generated **square**: the crop is centred, so a 16:9 source loses a third of its width and loses it from the edges. Parses clean and the crop and scrim arithmetic are verified against five aspect ratios; **not render-tested**, since the tool is Windows-only and CI has no GDI+.
- **Portraits for the five Aethelrock clan heads** (2026-08-20) — [Cormac Dubhghlas](src/characters/cormac-dubhghlas.md), [Fergus Aonghas](src/characters/fergus-aonghas.md), [Idris Bryneth](src/characters/idris-bryneth.md), [Niamh Ó Ceallaigh](src/characters/niamh-o-ceallaigh.md) and [Rhiannon Ceridwen](src/characters/rhiannon-ceridwen.md), replacing the five `PORTRAIT PENDING` cards with real images and written alt text. Each clan head stands on the ground that gives them their argument — Cormac in his own crop rows, Fergus at the cut of a mine working, Idris on open moor under an arriving squall, Niamh on the fjord quayside, Rhiannon beneath her relay mast — which is the right shape for a dispute that is *about* territory: hand a reader any one of the five and they infer half the page. Filed at the 1200×675 convention rather than the 1600×900 the placeholder cards happened to use.

- **`idris-bryneth`'s prompt corrected, and the portrait regenerated** (2026-08-20). The prompt asked for a herder *"of about sixty"* while the page says he took the headship in 2815 UCSD as **"the youngest clan head on Aethelrock at the time of his inheritance"** — mid-forties against the current 2826. The first run obeyed the prompt and produced a man of seventy who was also near-indistinguishable from Cormac Dubhghlas: same grey beard, same weathered face, same staff, which the generator had added unprompted to both. **Two of five clan heads reading as one man is a real cost when they sit at one table**, so the revision fixes the age and separates the silhouette deliberately — dark close-cropped hair against Cormac's grey beard, and no staff. `story-bible/images.md` carries the reasoning beside the prompt.
- **Saint Aoife's portrait brief, reframed from the *Aoife of Stars* card** (2026-08-20, Dermot's ruling: **keep the concealed face, take the rest**). The card showed her front-lit and facing the viewer, which collides with a constraint adopted 19 August and marked as outranking other briefs — her face stays turned, shadowed or away, because *the record genuinely does not know it, and the page's whole argument is that she never claimed to know what she had seen.* So the brief in `story-bible/images.md` moves from grey overcast daylight to **night**, gains the star field, the moorland horizon and the red hair, and keeps the well, the thorn and the turned face. **Two things from the card deliberately not taken**: the embroidered gown, because ornament argues with a standing built on refusal — the card's colour survives as the colour of the night instead — and any sense that the sky is *doing* something, since a sky that appears active tells the viewer what she saw, which is the one thing seven centuries of record declined to say. The portrait still does not exist: the card cannot be cropped into one (a 479 px art region and the title burned across her shoulders), so it needs generating at 1200×675.

- **Two supplied recordings identified** (2026-08-20, Dermot's direction): the 3:49 is **Slipwave**, the 1:44 is **a muster piece**. Both cards are now unblocked in `story-bible/prompt-sheet.md`, and both raise a canon question a card cannot answer. The Slipwave track makes a **fourth** recording by a band whose catalogue is already characterised — *a handful of habitat-circuit recordings, most of them incomplete or badly mic'd, two are not* — so it fits as one of the handful surfacing later and **cannot** be a polished studio cut from their own era. The muster piece needs its filing to match its sound: the audio is a dense full-band mix with drums, so it is a recording **of** a muster piece rather than one made **at** a muster — a distinction that matters because the anthem's spread is explained by the opposite property, being singable unaccompanied by a room of non-singers. Neither can become a codex entry until **the lyrics are supplied**: a lyric is canon-for-its-author and cannot be derived from a spectrogram.

- **Church Space has a theme of its own** (2026-08-20, Dermot's direction that *Rotational Hum* is site furniture): `src/audio/rotational-hum.m4a`, registered in `lib/editions.js` as **"Ambient drone"**. Those domains registered no `themeAudio` and fell through to the default's Celtic ambient — **an omission rather than a decision**, and this was the only edition that could take a new theme without displacing a track already live. Two precedents set with it and recorded beside the entry: **m4a rather than wav**, a third of the bytes for a decorative footer element; and **a theme may be a piece rather than a loop** — this one runs 4:02 and builds, which survives only because the player is click-to-play behind `preload="none"`, so nobody hears it who did not ask to. If the loop restart ever grates, the fix is a trimmed cut of the same recording rather than a different track.

- **The *Paper Galaxies* fragment is now audible on its own audit page** (2026-08-20). `src/audio/paper-galaxies.m4a` attached to [The Paper Galaxy Audit](src/codex/paper-galaxies-audit-dialogue.md), which already described the artifact it now carries — *a single held frame, a singer before a painted, swirling field of light… audio: one performance, repeating.* No prose added: the entry's framing paragraph already introduced it, so only the player is new. The degraded, lossy, low-resolution provenance is an asset here rather than a defect, which is why this is the one supplied recording that needed no remaking.

- **`story-bible/prompt-sheet.md`** (2026-08-20) — the paste queue. Eight cards to recreate (each generated wordless, titled afterwards), the two recordings the corpus is actually missing, and what is conditional. Derived and disposable by design; the three runbooks stay authoritative.

- **Music is for the live domains** (2026-08-20, Dermot's direction), recorded in `story-bible/music-prompts.md`. Licensing stops being a footnote: **Udio** becomes the recommendation for anything with published lyrics — inpainting protects a canon lyric line, and its rights position is the strongest among tools that can sing — while **MyTunes stays the sketchpad and stops being the delivery path**, since a consumer app's terms are the wrong ground for audio served from five domains.

- **Prompts split into app-sized fields, and tool guidance for both media** (2026-08-20). Every music brief is now six short lines — Style, Mood, Instruments, Production, Exclude, Form — because MyTunes, Suno, Udio and the rest take fields rather than paragraphs and weight the front of a box; a one-box paste order is given, with the warning that writing *"no drums"* into a style field is how you get drums. `music-prompts.md` gains a tool table keyed to the job (**Stable Audio** for the four themes, since the spec is a seamless instrumental loop; **Udio** for the two unrecorded songs, because inpainting fixes one mangled canonical line without costing the take, and its licensing is the cleanest of the song generators), plus the note that MyTunes' mp4 wrapper and badge are not the deliverable. `video-prompts.md` gains the same for Grok Imagine — good for roughs and for shots where the subject moves and the camera watches, capped at 720p and weak on the camera control the house signature is almost entirely made of, so **Runway Gen-4.5** for the camera-led scenes and **Veo 3.1** for the material ones. Sora 2 is flagged as deprecated so nothing gets built on it.

- **A regeneration worklist** (2026-08-20) in `story-bible/music-prompts.md` — every supplied file with a separate verdict for its audio and its card, since most have a keepable track inside a container that needs redoing. Seven of ten need only a new card; two already pass.

- **Ten supplied files read and recorded** (2026-08-20). Beyond the six above: *Hold Me Till Dawn*, whose title echoes *Baby Universe*'s published chorus closely enough to need a decision either way; *Aoife of Stars*, **the closest of the cards to the house target** and also the first depiction of a canon character who currently has no portrait at all, so draft-and-stop; and a Grok clip of shuffle dancing that flags a canon question — **low-gravity shuffle dance is marked in the 26 July intake as not existing in canon**, and the clip is full-gravity, which loses the only part that would be worth establishing.

- **`story-bible/video-prompts.md`** (2026-08-20) — the prompt-of-record for moving images, completing the images/audio/video set. Carries the house motion signature (the same three canon anchors as the audio), the **camera-is-a-witness** rule that follows from the corpus having no omniscient narrator, the diegetic-light and no-text-in-frame rules, delivery specs, a twelve-scene library and timed edit plans for two supplied tracks. Records what nothing checks: `src/video/` holds a file referenced nowhere, and `baby-universe.mp4` is 19 MB at 1 fps to show one still. Story-bible only; no published content or media changes.

- **Music direction amended** (2026-08-20, Dermot's direction) in `story-bible/music-prompts.md`: *beautiful, enigmatic, haunting, dramatic*. **Dramatic scopes the target rather than replacing it** — a footer loop and a film are different jobs, so the four edition themes stay deliberately quiet and everything a listener chose to play may build and arrive somewhere. The horror line does not move. Six supplied tracks recorded with their measured structures and their missing prompts, including the finding that **one is the recording already in `src/audio/`, bit-for-bit** (so the Ballad entry's 1:27 claim stands), that two others are previously unseen recordings with no home, and that three band cards contradict Slipwave's canon lineup and register. *Paper Galaxies* is the exception: `paper-galaxies-audit-dialogue.md` already describes that exact artifact — *a single held frame, a singer before a painted, swirling field of light* — so the still-over-audio format has an in-universe reading before it has an excuse.

- **`story-bible/music-prompts.md`** (2026-08-20) — the prompt-of-record for audio, the counterpart to `image-prompts.md`. Carries the house sonic signature (station hum, the *slipwave* detune, a pulse that arrives late), a shared preamble and negative, paste-ready briefs for the four edition themes and the four in-universe recordings, and the boundary rules: the codex entry is the lyric of record, external recordings are never generated, and the Krenyi absence stays unanswered. Records that all five files in `src/audio/` predate the file with no prompt kept, and marks that absence rather than inventing one. Pointers added in `images.md`, `open-questions.md` and CLAUDE.md. Story-bible only; no published content or audio changes.

- **The eight corps badges, designed as a set** (2026-08-19), in [Rank, Insignia, and the Ranger Uniform](src/lore/rank-insignia-and-uniform.md). **Two structural rules keep them clear of rank**: *rank is counted, a corps is singular* — every rank mark is a count of solid elements, every badge is exactly **one outline and never solid**, so a reader who is counting is reading rank; and **a branch sits inside its corps**, drawn within the outline, so containment carries the hierarchy. Rank owns bars, rings, chevrons, stars, discs and bands, so **the badges own polygons** and use no shape from that list. Two deliberate omissions: **colour distinguishes no corps** (it is already keyed to service track, and a monochrome set survives smoke and eyes that sort colour differently), and **position is not fixed by the standard** — a rule naming a shoulder assumes a shoulder, in a service with four non-human body plans and analytical chassis in it. **Each figure is derived and the derivation recorded**, because an unrecorded one drifts into decoration within a generation: Safety a closed square for a standard that completes, and **[Navigation](src/lore/star-rangers-navigation-corps.md) the same square with one side missing**, because its certificate expires where the hull's does not — two badges, one gap, and the distinction those entries spend paragraphs on is legible across a dock. **[Frontier](src/lore/star-rangers-frontier-corps.md) is a triangle inverted**, the only device pointing at its own ending, for the only corps whose mandate is built to terminate. The branch that proves the system is **Fold Transit Safety: two marks at opposite corners and nothing between them**, which is not a symbol but the finding the branch began from — *nothing has ever been lost inside a fold, only at the two ends of one.* Closing limit: **a badge never claims a hazard** — a Boundary Safety device is not a certification, because corps, rank and qualification fail independently. Drafted, not merged.

- **Corps and branch badges** (2026-08-19), Dermot's direction, added to [Rank, Insignia, and the Ranger Uniform](src/lore/rank-insignia-and-uniform.md): **each corps carries its own device, and branches within a corps carry theirs beneath it.** The entry already held that rank and certification *are deliberately never the same mark, because rank and qualification fail independently* — **the badge is a third mark on the same reasoning**, since an officer can be posted out of a corps and keep her rank, or command a section of a corps she did not climb through. **A corps badge is not a faction crest**, which is why the neutrality clause permits it: the clause forbids a mark giving a party a reason to read a Ranger as taking a side, and a corps device says which part of the Rangers arrived. **The rank tab's own colour rule — keyed to service track rather than corps loyalty — is preserved and scoped**: it governs the rank tab only, because it was written about the harder reader, someone outside the service for whom *which department* is a chart they have never seen. A badge is read by the people a corps turns up for, and a colony that signed a standing invitation to the [Frontier Corps](src/lore/star-rangers-frontier-corps.md) is entitled to see the Frontier Corps arrive. **Authority stays on the tab; belonging goes on the badge.** Badges obey every existing constraint — shapes not words, seated not sewn, no name, never confusable with the rank tab. Drafted, not merged.

- **[The Eden Bureau Uniform](src/lore/eden-bureau-uniform.md)** (2026-08-19), Dermot's direction: **Eden Space Habitat's detectives are uniformed and wear rank insignia in plain view.** The corpus was silent on bureau dress, so this fills a gap rather than overturning anything. **The entry turns on who does the reading**, which is where it diverges from [the Ranger uniform](src/lore/rank-insignia-and-uniform.md): a Ranger's insignia is for *a colleague across a corridor who cannot look it up in time*, and withholds identity on purpose because **no Ranger's name is written on their clothing**; a detective's is read by **a resident at their own door being asked questions they did not invite**, who needs to know what the person in front of them is entitled to do, at the start of the conversation rather than the end. *The Rangers are a body that arrives; the bureau is a body that lives there.* **The marks borrow the [plainmark's](src/lore/plainmark.md) method rather than its grammar** — rank is a count, and the count band already says a small number without writing it, so one stroke to three and a filled block for the Superintendent, carrying the two survivability rules with it: colour never alone, and no letters or numerals. **So a resident who can follow the ochre rings to sealed air can read the collar of the officer at their door.** The cost is stated rather than hidden — a jacket that turns a corner ahead of you loses you the people who would rather not be found — and the bureau's answer is that *an officer who can arrive unannounced is an officer a resident cannot check*. **Three qualifications from Dermot the same day**: the obligation is **on the lower ranks** and thins going up, **an undercover officer does not wear it**, and **Commissioner ranks are not required to have one at all** — a Commissioner does not knock on doors, so the logic that puts a jacket on a detective takes it off a Commissioner. That also retired a problem: Oyelaran's and Albercombe's delivered portraits show ordinary jackets, and as a Superintendent and an Inspector neither is required to wear the uniform, so the art and the entry never disagreed. **Deliberately left open: whether the uniform carries any mark of individual identity**, which is a real question because the accountability argument runs straight at it. Drafted, not merged.

- **Prompts for all fifty-two outstanding lore illustrations** (2026-08-19), in `story-bible/images.md` Open work 5b — written in one pass, 1600×900. **The governing rule for the set: every abstract page gets a concrete anchor**, since roughly half are pages about a rule or an epistemology rather than a place, and an abstract page illustrated abstractly produces exactly the flat filler the house target rules out. Frames come from each entry's own argument: [the Frontier Corps](src/lore/star-rangers-frontier-corps.md) is **boot tracks leading away from a lit settlement**, because departure is the achievement; [what nobody certifies](src/lore/what-nobody-certifies.md) is **four framed certificates and a fifth empty frame**; [Wholecloth](src/lore/wholecloth-formcluster.md) is deliberately the least eventful image on the roster, because *nothing about it can be measured from here*; [Counterpane](src/lore/counterpane-archecluster.md) is **two identical cloths whose overlap makes a figure belonging to neither**. Four defaults flagged as failing quietly — the gold cryostat on [quantum computing](src/lore/quantum-computing-and-the-cavern-constraint.md), gibberish lettering on [the Plainmark](src/lore/plainmark.md), the tone line in both directions on [predatory entities](src/lore/predatory-entities.md), and the jellyfish cliché on [the Ollune](src/lore/ollune.md) — plus the set's own risk, which is sameness: judge as thumbnails and re-roll for variety. **One naming collision flagged**: the lore and character pages for Saint Aoife would both reach for `saint-aoife.jpg`, so the lore one is proposed as `cill-aoife-devotion.jpg`. Not generated; all proposals.

- **Portrait prompts for the remaining twenty-one pages** (2026-08-19), in `story-bible/images.md` Open work 1 — written in one pass, because a roster reads as a roster only if the frames were chosen against each other. Two shared blocks (a standing negative, and a Rangers service-dress block with geometric rank marks and no name tape, per [Rank, Insignia and the Ranger Uniform](src/lore/rank-insignia-and-uniform.md)). **Five belong to *Undercover Pets* and are deliberately off the house style**, since the runbook says weaving melancholy through that thread is a category error. Frames are chosen from each page's own argument rather than from the role: **de Courtney at a threshold she has never asked to be let past**; **Kane in civilian clothes at the back of a ceremony**, no uniform, because the tradition his career is counted in forbids one; **Zoe Smith in an ordinary corridor**, deliberately the least impressive frame, because her page's argument is that this rank is what the word means; **Aoife looking down at the water with no halo and no glow**, because the page is her refusal to claim. Two are flagged as the ones that will fight the generator: **Elvira**, whose *chalk, thread and water in vessels* is a witchcraft prompt read literally and whose page exists to say it is not one — carrying the roster's longest negative and a survey grid, folding rule and shut record books to make it survey work; and **Muffin**, which **should probably carry no image at all**, since eleven witnesses returned four different animals and drawing a specific adorable one contradicts the page in the first frame — prompted as the place on the [Eden Warden](src/characters/eden-warden.md) precedent, with the entity present and unresolved. Not generated; all proposals.

- **Portrait prompts for the three rescue machines, as a set** (2026-08-19), in `story-bible/images.md` Open work 1 — [Lucene-9000](src/characters/lucene-9000.md), [Xenon](src/characters/xenon.md) and [Lumbarian](src/characters/lumbarian.md). **What binds them is not a place** — Lucene is at Eden, the other two aboard Wender's ship — but a catalogue shelf: all three are classic designs out of production, which `xenon.md` already states. So the set is **a shared house block pasted verbatim into all three prompts**, since a generator carries nothing between frames. **All three are present-day frames**: the rescue-era framing was dropped because it would have put the part-empty cradles in shot, making an image carry the hint §7 keeps in prose. Lucene's is the risky one — *social gynoid* returns a photoreal woman by default — and her artificiality is carried by a seam at the jaw, a seam at the temple and lens eyes and nothing more, on the cyber-revenant precedent; the frame is her actual posture, **beside the interviewing officer, watching an answer arrive**. Xenon's centres on his hands, because *his personhood lives where his hands do*, working a machine with nothing wrong with it. Lumbarian's carries **scale and stillness** with a human-sized hatch for reference and no person in frame. One deliberate convention breach flagged: a bench task light is a tool, not a furnishing. Not yet generated; proposals.

- **A portrait prompt for [Anne Wender](src/characters/anne-wender.md)** (2026-08-19), in `story-bible/images.md` Open work 1. **Depicted as safety officer during the buildout, not as sheriff** — her page puts her in that post at Season 1's start, and more bindingly **there is no sheriff's insignia in canon**, so drawing a badge would invent an office's regalia in a picture, the same cascade the Calloway entry refused for a Compact uniform. The frame is a frontier yard at dawn with the fusion pad behind her and **she is counting something off-frame**, which renders her character note without a caption. *No poverty* is carried as a canon negative rather than a taste one, because [the Fusion Ceiling](src/lore/the-fusion-ceiling.md) says an early settlement's surface is *persistently misread as poverty when it is sequencing* and a generator would assert exactly that misreading by default. No family resemblance to Karla is specified, deliberately — her portrait establishes no features to match, so one could only be invented here and would bind her frame retroactively. Draft alt text included, to be checked against the frame. Not yet generated; proposals.

- **[Anne Wender](src/characters/anne-wender.md)** (2026-08-19), the last character page [A Person's Name](src/seasons/s01/e00/s01e00c04.md) unblocked and the only one still unwritten. Applies the #444 rulings: she went out to a [Cairn Trust](src/lore/cairn-trust.md) charter as a safety officer, the colony elected her, and **the settlement took her name at its constitution without her say** — *Anne Wender did not name the settlement, and could not have refused it.* Role follows Karla's model, with the election's course left *remains to be established in story*, and the page **never joins the two facts**: the wreck and the swearing-in sit in one history with no sentence connecting them, because the intake is explicit that the eldest survivor becoming the first person a world swears in *needs no commentary*. The machines' loss stays hinted at the level already published — eight down, five back, *and gave the two numbers to nobody* — written as a habit of counting rather than a loss. No portrait; a new character image is its own draft-and-stop item. Proposal.

- **Rulings on the remaining 28 July backstory decisions** (2026-08-19, #444), recorded in place in `story-bible/intake-2026-07-28.md` §10. **Four of the listed questions turned out to have been answered elsewhere and never brought back** — Anne's surname is published as **Wender** in [A Person's Name](src/seasons/s01/e00/s01e00c04.md)'s `canon_facts`, Xenon and Lumbarian are active aboard Karla's ship on both character pages, the caretaker mandate was closed by the 13 August addendum, and the corporation was named the same week. **Two are declined deliberately**: what the disaster was, since the chapter marks that absence inside a character's judgement (*it had not seemed, that night, like the missing fact*), and the *Lucy Anne* echo, already a decision to leave something open. **Ruled**: the settlement is named **Wender by the colonists at constitution rather than by Anne**, which preserves the inversion and improves it because it is done to her, rhyming with the intake hall's *one word, holding three people*; the election was **obstructed rather than contested**, on the lawful and possibly correct ground that it was premature, so the [Cairn Trust](src/lore/cairn-trust.md) never ran a candidate; the Senior Officer is **new**, with [Galen Kane](src/characters/galen-kane.md) as the man he calls, since Kane's page already defines the advisor's role and his own philosophy makes him the wrong shape for the post; the machines were **posted to the hull before Karla had any say**, so *Karla Wender's command* became true with nobody arranging it; and **all three machines remember the rescue from three vantages that make it three different events**. Andrew's question dissolved rather than resolving — the chapter has Lucene never leaving — and is replaced by the better one, that **nobody has ever asked her what happened**, for three different reasons. Story-bible only; no published content changes. Proposals.

- **[The Cairn Trust](src/lore/cairn-trust.md)** (2026-08-19), naming the 28 July intake's unnamed corporation (#445) — the concern that financed a frontier settlement and then found the colony had elected its own sheriff. **Its product is a schedule, not a commodity**: [fusion is sufficient everywhere and immediate nowhere](src/lore/the-fusion-ceiling.md), so the Trust underwrites the interval — passage out, the first rationed cores, the ten-to-twenty-year buildout — and recovers against the colony's own later output, which makes it **the party installing the reactor rather than stripping the world**. Sincerely aggrieved and partly right is made structural rather than asserted by the corporate form: its capital is held for settlers on the register awaiting passage, so **a company may forgive a debt whenever it likes and a trustee is forbidden to** — every refusal has a person behind it and they are never the person in the room. The [Frontier Corps'](src/lore/star-rangers-frontier-corps.md) self-terminating clause is written about the Rangers and lands on the Trust: the counterparty changes **from a document into a constituency in one afternoon**, and the only authority present the whole time is obliged to leave. The name inverts [Hyperfold's](src/lore/hyperfold-yield-combine.md) — a word that *can* be held to a meaning, and is: *a marker is of no use to the one who set it.* Name is a proposal; Anne, the world, the election and the Senior Officer are deliberately kept off the page, which stays structural so it seeds an unwritten arc without spoiling it. Drafted, not merged.

- **[What Nobody Certifies](src/lore/what-nobody-certifies.md)** (2026-08-18). The record certifies hulls, routes, people and minds, each to published standards with a body to complain to — and **nothing certifies a permission to be somewhere**. The entry establishes that this is a **decision rather than an oversight**: an office competent to review a settlement's right to be where it is must be able to **enter** and to **compel**, which are exactly the powers the Rangers were forbidden at founding. The [SSDC objected that those limits created dangerous gaps](src/lore/military-space-command.md) and the delegations replied that *a boundary-operations body with unrestricted right of entry would simply be the MSC wearing a different crest* — **both were right, and the record holds both without adjudicating**. The structure was already published and unnoticed: the invitation clause **fails in two opposite directions**, with [Fliade](src/lore/planets/fliade.md) having *nobody to issue* and [Corryn](src/lore/planets/corryn.md) *nobody to withdraw* — *one has no counterparty yet, the other had one and outlived it.* The working case is stated first, the Cerebraun withdrawal, because *a protection that is never tested is a claim; that one is a practice*. No verdict on the trade, and no reform proposed. Drafted, then merged.

- **[The Halvern Combine](src/lore/halvern-combine.md)** (2026-08-18), the Corryn charter-holder as its own entry. Descended from one of the [Imperium](src/lore/the-imperium.md)'s industrial patrons — the families holding the shipyards and fold-drive patents the fleet was *already answerable to* — and **it outlived the Imperium because it was never a government**: what ended in 2609 was a chain of command, and a patent portfolio, a set of yards and a drawer of sealed licences require no issuing authority to persist. The charters were neither upheld nor overturned — the MSC inherited a defence problem, the Consolidation constrained a military, the Rangers charter was argued over what it forbade — so *they were simply never the question in front of anybody, and a document nobody has jurisdiction to revoke is, for every practical purpose, valid.* **The spine is a parallel with the [Hyperfold Yield Combine](src/lore/hyperfold-yield-combine.md)**, and the difference is sharper than the parallel: Hyperfold was lawful and **fast**, ending at Dock Seven in 2831; Halvern is lawful and **slow**, with *no moment to point at* — so the Corps teaches the case that ended, *which is a fact about how institutions learn rather than about which case is worse*. Two absences marked: what became of the other patron holdings, and that **nobody certifies a charter** the way hulls and routes are certified. Drafted, then merged.

- **[The Corryn System](src/lore/planets/corryn.md)** (2026-08-18), the fix for #463 — the renamed Meridian block, with all six corrections applied and the one blank filled. **The colony is a remnant of the Old [Imperium](src/lore/the-imperium.md)** (Dermot's direction), chartered 2543–2609 by an authority that *regarded elected governance as a demonstrated failure*, backed by one of the legacy industrial holdings behind its fleet — whose successor is **the Halvern Combine**. That resolves the Military Space Command error and the unfilled corporation blank together, and **the date correction goes the other way round**: the block's *approximately thirty years* is the **expansion**, not the founding, so the settlement is two centuries older and the harm is recent. The Imperium fell in 2609, seven hundred light years away, where *the collapse of the chartering government was news rather than an event* — so the colony is lawful in the only sense available to it, holding **a valid charter from a government that no longer exists, with no office standing to review a document nobody can revoke**. **The name carries the hedge**: *The Sentinel → the Carillon*, since a set of tuned bells names the sound as patterned without claiming anyone is ringing it. Etheric co-location deleted for a flexure with an unidentified far side; the distress signal keeps its dreams and loses its sender (*the vision survives; the sender does not*); *Convergence Weaving* deleted. **The Spiralites became the entry's centre** — a people who speak in electromagnetic pulse and reproduce by it, under a colony degrading exactly that band, where the [sapience clause](src/lore/frontier-transformation-protocols.md) applies on the plain reading and the failure is not one of understanding but of an unsupervised registration seven hundred light years from anyone with standing to review it. Names are proposals. Drafted, then merged.

- **[The Kalypsis Dawn](src/lore/kalypsis-dawn.md)** (2026-08-18), the last of the 26 July intake's three worlds, drafted as the fix for #472. Every Overfold usage resolved to **fold transit** or **boundary-zone proximity**; the three refused tropes removed; both namings taken. **The shielding was the real rewrite and it improved the ship**: *shadow-matter shielding* assumed a hazard that arrives and can be deflected, and a boundary zone has none — **agreement fails, nothing strikes**. So the vessel is built to know the effect *by how much*, carrying several independent reference sets that share no design or failure mode, cross-checked continuously, where **the disagreement is the measurement**. It points at a published failure: [Threshold's forty-second discrepancy](src/lore/boundary-zones.md) sat eleven years as a calibration fault *because a single reference with nothing to disagree with looks exactly like an instrument needing adjustment*. The *Emergency Node-Hop Protocol* became **eighteen months of endurance** — *you do not jump, you wait* — since fold transit cannot be improvised. Certification hooks into both bodies, the [Safety Corps](src/lore/star-rangers-safety-corps.md) for the hull and the [Navigation Corps](src/lore/star-rangers-navigation-corps.md) for the route. Helion-3 is Kernel-compliant by charter and never decides meaning. **Warden-Class → Lee-Class**, the sheltered side, *not a defence and does not stop anything*. Naming is a proposal. Drafted, then merged.

- **[The Umbral Moon](src/lore/umbral-moon.md)** (2026-08-18), the second of the 26 July intake's unwritten worlds, drafted as the fix for #462. **Two of that issue's three problems were overstated and dissolve rather than needing decisions.** The neighbour membrane never needed naming — the record **never names the far side** at either documented boundary zone, saying only *a neighbour*, because the Interval carries no bearing or distance: *the record has instruments for measuring that a neighbour is close; it has none for asking which one.* So *Shadow Universe* is dropped rather than replaced, and **the coupling to *where shadow beings live* is broken** — they are meta-dimensional, extending across higher dimensions rather than residing next door, so the realm question has the wrong shape. The one real decision resolves by separating two questions the block had merged: the moon is a **Trojan at Earth–Moon L5** held by ordinary Lagrange dynamics, and the flexure merely shares the address — *why is it there* and *why is the place strange* are different questions, and no gravity tunnel could hold anything anyway at seconds to minutes. The block's detection story survives improved: the gravitational micro-pulses are the flexure, not the moon, which turns up in the follow-up *having nothing to do with it*. Drafted, then merged.

- **[Narrative Works and Their Adaptation](src/lore/narrative-works-and-their-adaptation.md)** (2026-08-18), Dermot's direction: a work of in-universe fiction is a **spine of scenes each carrying several viewpoints**, published with its apparatus — settled background, a register of terms, a chronology, and attributed source documents that may be partial or self-serving. Three in-world causes, none artistic in origin: a documentary culture that learned to ask who says so; the failure of the single credible narrator, taken from the archive's own hardest cases; and **modularity for adaptation**, the pressure that shows most. Audiovisual but not cinema — matte unlit surfaces read by ambient light, nothing projected. **Adaptation is years of named work** because no machine renders one people's mode into another's: the [Chthonari](src/lore/chthonari.md) case is the taught one, since a human work entering the Undersong is re-composed into a channel where the separation between a character's thought and the work's telling of it does not exist. An adapter may decline, on the [Ollune](src/lore/ollune.md) precedent. **The original is the work; the adaptation is the courtesy.** The entry never references this site or the resemblance — that was a deliberate craft constraint. Drafted, then merged.

- **The holodeck is refused** (2026-08-18), Dermot's ruling, added to [What the Record Refuses](src/lore/what-the-record-refuses.md) as a closing subsection of *The Furniture of the Future* plus a summary-table row. **Simulation is not refused with it** — [Virtual Reality](src/glossary/virtual-reality.md) is canon, rigs exist, immersive entertainment was never banned, and a Survey Corps navigator trains in one. What is refused is the walk-in room: matter where there is none, and a place indistinguishable from a real one. Three arguments, the middle one the load-bearing one — it inherits the hologram's missing medium everywhere at once; **the difference that matters is provenance rather than fidelity**, since a rig leaves a memory sourced (*four hours on the rig*) while an indistinguishable room manufactures experience a person genuinely had and cannot source, which an institution built around an Archive cannot keep; and the kernel refuses a simulated person who answers back without saying what it is, in the same words that refuse the universal translator. The glossary had already reached the same place by another route — immersive entertainment declined because a culture attentive to genuine emergence stopped finding recombination interesting. Drafted, then merged — it asserts about the world.

- **[Proverbs and Maxims of the Star Rangers](src/lore/star-rangers-proverbs-and-maxims.md)** (2026-08-18), a lore entry collecting the Corps' working sayings, sourced where the origin is documented and marked unrecorded where it isn't. The page turns on a tension already in the corpus rather than smoothing it: a service trained to write flatly, checkably, and low-context so its reports survive translation nonetheless runs on compressed idiom, and the rule that reconciles the two is that **a maxim is a mnemonic for a discipline, never a substitute for a finding** — taught in the college, inadmissible in the log. Most of the sayings are borrowed from the Survey Archive's [verification doctrine](src/codex/the-verification-doctrine.md), which is what Sen's [Three Disciplines of the Record](src/codex/three-disciplines-of-the-record.md) recommended in as many words. Two are new, from Dermot's direction of the same date: **"Quiet is not doubt; loud is not certain"** and **"The pleasing reading is checked first."** The closing section applies [Marked Absences](src/codex/marked-absences.md) to the page itself and declines to attribute the sayings whose origin the Archive cannot show. Drafted, then merged — it asserts about the world.

- **Humility as confidence, captured** (2026-08-18). Dermot's note — humility can be a sign of a deeper internal self confidence that does not require external validation, and distrusts flattery — added to `story-bible/life-lessons.md` and written up in `story-bible/ideas.md`. It is deliberately **not** a correction of *Humility can become invisibility* in The Limits of Virtue; the pair is the useful thing, because the outward conduct is identical and an observer cannot tell which one they are looking at, which is what lets a room misread a competent officer for years. The note surveys where the corpus already holds this ([Tissadelle Shepherd](src/characters/tissadelle-shepherd.md), Ranger practice in [The Institute](src/lore/the-institute.md), [Sofia Santos](src/characters/sofia-santos.md), Agent Barsik) and flags what is thin: every existing case is the good version, and nobody yet embodies the invisibility half.

- **[The Third Debt Is Mine](src/journal/the-third-debt-is-mine.md)** (2026-08-17), a journal entry on the rule that came out of writing the PR review process: technical and literacy debt get a repair drafted on sight, and creative entropy never does. The reason is not severity but checkability — the first two can be tested against something outside the author's head, while entropy and intention look identical from outside, so **creative entropy can be good** where a broken link never is. The entry lands on the observation that this is the story's own problem: [Marked Absences](src/codex/marked-absences.md) already argues in-universe that a record tidying away what it cannot account for has destroyed evidence and called it housekeeping, and the same test had been running on the repository unnoticed.

- **[A Proposal to Collar the Anstolik, Refused at Countersign](src/codex/proposal-to-collar-the-anstolik.md)** (2026-08-17), a Fliade survey working paper filed with its own refusal: an instrument specialist's prospectus for detection collars on Anstolik herds, and the survey lead's countersign withheld on four grounds. Codex, so it settles nothing — no `canon_facts`, and the question of whether anything is being taken on Fliade stays marked rather than answered.

- **[Fliade](src/lore/planets/fliade.md)** (2026-08-16), the first of the 26 July intake's three unwritten worlds to be drafted. A cold terrestrial world whose biodiversity lives in geothermally warmed cavern systems rather than on its frozen surface, and whose deep-network people — the **Pandoids** — the survey found before any transformation proposal existed, which is the rare case where the sapience clause arrived early enough to matter. The entry states the two facts ruled about them, that they do not travel in space and keep their record by speaking it, as facts rather than as a level, and says why an oral record is a different shape rather than a smaller one. It also marks two absences: the classification of the boundary feature the planet sits near, and **the Pandoids' own name for themselves**, since *Pandoid* is a survey coinage and not a name they gave. The closing section is the one the world is for — the charter's entry-by-invitation limit has nothing to grip on a world with no body that can issue or withdraw one, so the protection is thinnest exactly where the asymmetry is greatest.

- **[The PR Review](story-bible/pr-review.md)** (2026-08-17), a sibling process to the Benevolent Critic with the same three debts, the same four priority bands, and **the opposite default**. Only two things stop a pull request — a major critical flaw in the change itself, or a red CI check — and everything else a review finds is raised as an issue that does not hold the merge, including findings that are real and urgent. The reasoning is that a P1 in an adjacent file is urgent *and unrelated*: priority says when a thing gets fixed, while the blocker test asks only whether this particular change should wait. Its sharpest rule is that **the debt decides the remedy**: technical and literacy findings get a drafted fix on sight, because they have a correct answer that does not need the author's taste to find, while **creative entropy is never quietly resolved** — only the author can tell entropy from intention, and entropy is sometimes the good kind. Paired with a `pr-review-finding` issue template carrying the same contract.

- **Designed SPECIES title cards for the three remaining species pages** (2026-08-17) — [The Pelagene Littoral](src/lore/pelagene-littoral.md), [The Serephine Dunekin](src/lore/serephine-dunekin.md) and [The Veyr Basaltborn](src/lore/veyr-basaltborn.md), drawn by `make-emblem-card.ps1` and matching Chthonari's card rather than the square codex covers. Each takes a device that argues from the page — concentric **rings** for a people of layered depth bands, a **sundial** for a people who open twice a day between two lethal extremes, a **triad** for a body built as a truss rather than a frame — and its own tint, so four species cards in a row do not read as four printings of one. The epithets and qualifiers are authored, as the generator requires; they are not fields of any page.

### Changed

- **[Levrils](src/lore/levrils.md) restructured for readability.** The page front-loaded taxonomy and buried its two strongest passages past 70% of the scroll, and ran four bullet lists back to back — 20+ consecutive bullets, now a longest run of 8. *What a Levril Looks Like* moves up to answer the question the opening raises; *Functional Role* and *Operational Constraints* merge into one permitted/foreclosed table, which is the entry's actual thesis; *Two Occasions* moves ahead of the taxonomy. The `Distinction from Other Meta-Dimensional Beings` section — three subsections restating `meta-dimensional-beings.md` while linking to it, including one headed *Levrils* on a page about Levrils — is replaced by a short pointer. Verified against the classification page first: its Class III coverage is strictly fuller, so nothing was lost. Prose preserved paragraph-for-paragraph elsewhere. **No sub-page:** at 1,388 words this is 35th of 119 lore pages against a median of 1,133, and a summary already exists at [Levril](src/glossary/levril.md).
- **Krenyi senses include EM fields and harmonic conditions** (2026-08-18, Dermot's direction). Extends *The Whole Body Is the Instrument* in [Krenyi (Quiet-Built)](src/lore/krenyi.md). **This one needed a continuity check first**: *harmonic* is the coherence state governing teleportation and QSHW transit alike, so a people who feel it can feel a boundary zone — and `year-0-survey-team-arrives.md` has **Rook-7, a machine**, identify Threshold's anomalous resonance *within hours*, with **Syra, a Krenyi Boundary Analyst, on that team**. The resolution is a hard limit that makes the sense safe to have: **it produces no number** — no frequency, magnitude, bearing or name — so it is *a direction to look, and never a reading*. A finding still needs an instrument, and a Krenyi is the last person to offer a sensation as one, being unwilling to assert what they cannot support. Syra's silence becomes characterisation rather than a hole. The precedent belongs to another people, which keeps the Quiet-Built off a pedestal: [the Overfold](src/glossary/overfold.md) already has a trained observer feeling a wrongness *long before an instrument confirms it*, and **neither route produces a certificate** — a Krenyi sits the same boundary-analyst examination as anyone. Third reason for the published *field, a pressure, a turn of light* line. Psionics guard unchanged: EM and harmonic conditions are properties of a **place**, not a person. Drafted, then merged.

- **Krenyi sensitivity, reflexes and awareness** (2026-08-18, Dermot's direction). New *What the Body Notices* section in [Krenyi (Quiet-Built)](src/lore/krenyi.md), placed after Emotional Range because its payoff depends on those bounds. The cat comparison is **kept and framed** rather than avoided: the record's standing note holds that [likening a people to an Earth animal](src/lore/what-the-record-refuses.md) is *a fact about the observers*, so the cat is what humans reach for and a Krenyi names the distortion as they would any category. **The comparison then fails in one place, and that failure is the idea** — a cat is fast *because* it is alarmed, the startle being the mechanism, where the Quiet-Built have the reaction and not the startle: *they orient without flinching.* This re-explains their reputation on a bad deck, which had read as composure and is not — *there is nothing being mastered*, and **composure runs out where this does not**. The cost, per the page's pattern: they **cannot un-notice**, so a room conducting a small social fiction is legible from inside it and their honesty leaves no graceful way to join. Also retro-fits the Levril section merged earlier the same day, which rested on this apparatus without describing it. **A second section, *The Whole Body Is the Instrument*, answers what the apparatus is** (Dermot, same day): there is **no organ** — no eye, whisker or membrane a survey could photograph — and the whole body samples continuously. That retro-explains the entry's own opening line, since **movement is noise in your own readings**, so *no wasted gesture* is method rather than manner and *the stillness people read as composure is closer to a technician not breathing on a scale*. It also explains a published line nothing else accounted for: [levrils.md](src/lore/levrils.md) already had a Krenyi meeting *a field, a pressure, a turn of light* where others meet a figure — every other people gives it a shape, the Quiet-Built give it a **weight**. The psionics guard is written in on the record's own boundary, **texture never content**, and the resemblance to their body-wide immune discrimination is noted without being claimed. Drafted, then merged.

- **Kalypsis Dawn checked against canon, and it is the worst of the three** (2026-08-18). `intake-2026-07-26.md` block 3 rests on a **category error**: [the Overfold](src/glossary/overfold.md) is the Fellowship of Light's contemplative name for boundary-zone Etheric phenomena — *three names, one thing*, alongside Etheric and Intermembrane Bleed — and is **not a medium you travel through**. The block builds the ship's identity, certification, defensive suite and class history on Overfold corridors, ratings, turbulence and jumps, which would also be a third FTL channel where [the record allows exactly two](src/lore/what-the-record-refuses.md). **The original check was backwards**: it read *Overfold is already established canon (9 files)* as reassurance, when the word is established and means something else — counting files is not reading definitions. Three refused tropes are fitted to it: holo-displays, **stasis pods** against the outright cold-sleep refusal, and an AI that *handles* interspecies communication where the refusal permits only xenolinguists with AI support on an exact division of labour. Both original flags confirmed, one stronger — Kernel compliance is a **charter requirement**, not a *should probably*. The heading *WRITABLE AFTER TWO NAMING DECISIONS* is corrected in place and the block is tracked as **#472** — the strongest of the three P2s, since #462's block said *writable after three fixes* and #463's said *needs new names*, both understating, while this one affirmatively certifies itself as *Fits canon well*. What survives: the unarmed neutral-passage design, Tissadelle's verified Line Captain rank, and the modular multi-biology interior.

- **#463 re-checked against canon, and the Military Space Command date corrected** (2026-08-18). Five of the seven Meridian corrections confirmed; three needed correcting. **Item 1 is stronger than filed** — the Sentinel appears in published Season 3 chapter text, so the name collision would reach live prose rather than only an entry. **Item 3's date was wrong**: canon has the Consolidation Accords narrowing the MSC into the SSDC in **2722**, with the charter following in 2723, and both the intake and the issue said 2723. The contradiction is unaffected — a founding around 2796 postdates either — but the wrong year had propagated once and is fixed in place. **Item 5's verdict changes from FLAW to UNSUPPORTED**: [physics-comparison](src/lore/physics-comparison.md) holds consciousness emergent from neural complexity *and* that *the deeper nature of consciousness remains an open scientific question*, so the block asserts as explanation something the record holds open — the repair is to hedge it, not remove it. And **item 2 is a blank rather than a correction**, tracked in #445, so six corrections and one blank is the honest count. Priority unchanged at P2.

- **The Arilon reading stops at Old Earth** (2026-08-18). [Arilon](src/lore/arilon.md) now bounds the Krenyi reading rather than leaving its reach open: nothing in the record places any people but humans on Earth before spaceflight, contact history is uniformly late, and for a human community the pattern predicts a human Champion since selection operates on the beings available to be selected. Two reasons, the second general. **Evidential** — an unplaceable people is not a placeable one, and impossible-to-disprove is not supported, which corrects a subtlety in the previous draft that treated non-placement as making the reading permanent without noting it denies the reading any positive support. And **no people is the explanation for another people's history** — the refusal underneath the closed list of human-framed peoples and of a shared galactic biochemistry, each declining a tidier universe descending from one source. *A file whose founding renunciation is that it does not adjudicate between traditions cannot hand one of them a mechanical answer and call it neutrality.* The reading survives where it was always interesting, among the later accounts across star systems. **Recommendation and draft, not a decision taken.**

- **Krenyi understand human emotions without sharing them** (2026-08-18, Dermot's direction). New passage in the Emotional Range section of [Krenyi (Quiet-Built)](src/lore/krenyi.md), heading off the stock reading that a people without fury are a cold people. **It derives from canon already in place**: [empathy is re-derived](src/lore/what-the-record-refuses.md) as cognition rather than reception — *nothing arrives by antenna; everything arrives by attention* — so **understanding never required sharing, for anyone**. Four points: they are **not an unemotional people** (anxiety, annoyance, grief, and attachments held for centuries; the range is bounded at three extremes, and missing three extremes is not missing the register); comprehension without sharing, stated plainly; **one bounded advantage** — a human reads another's fear through their own and a Krenyi's instrument is not measuring something it is also doing, explicitly not a rank; and the **cost**, that human emotional grammar is learned, so they work from a very good model and a model is what a novel case defeats. Closes on the distinction that does the work — *logic is a method and clarity is an absence of ornament* — and the note that a Krenyi has never been recorded finding a human ridiculous for being afraid. Drafted, then merged.

- **The Arilon implication, drafted** (2026-08-18). [Arilon](src/lore/arilon.md) gains the reading that follows from the Krenyi being the readiest Champions: **a Champion of which people?** Filed as a refinement *inside* the Champion reading rather than a fourth peer to it, so the three-way Champion/avatar/incarnation list in [Arilonian](src/glossary/arilonian.md) stays correct. The fit is the pattern's own — *an outsider who arrives*, names what local authority will not, is opposed, and departs — and the Krenyi match it in every particular. **It is permanent rather than merely plausible**, because no filed homeworld, emergence date or timeline entry gives any chronology to test an account against: *a people who decline to be placed cannot be placed anywhere, including out of an account they were never in.* Two guards written in — it is **the deflationary reading**, no better evidenced than the devotional accounts it would displace, and the entry cites [the pleasing reading is checked first](src/lore/star-rangers-proverbs-and-maxims.md) against preferring it for tidiness; and nothing is adjudicated, the Fellowship filing it exactly as it files the Avatar exception. **Also states outright that the pattern is species-neutral** (Dermot's correction the same day): a Champion is a *sentient material being — organic or artificial*, so an Arilon-figure may be of any sapient kind and so may the community that received one. What is **not** neutral is the archive, and that produced the entry's sharpest idea — the filed pattern describes what survived transmission by low-context observers, so a Champion who looked broadly like the people visited is written down as a *person* while one who did not is written down as a marvel, a beast, a voice. **The accounts on the shelf are not the accounts that exist; they are the accounts that looked like accounts** — deliberately the [boundary-zone](src/lore/boundary-zones.md) record's own move about its two documented sites. The Krenyi over-representation is therefore a fact about the shelf and not about who is selected. Reciprocal pointer added from the Krenyi Champions section. Drafted, then merged.

- **Krenyi, Levrils and Champions** (2026-08-18, Dermot's direction). Two new sections in [Krenyi (Quiet-Built)](src/lore/krenyi.md), filed with the other resistances because it is the same faculty producing the opposite of what anyone expects. Least awe of any people toward a [Levril](src/glossary/levril.md), and fastest to answer one — **a single fact from two sides**, since *awe and attention compete for the same faculty, and the Krenyi have spent it all on attention*. Subtle influence is a sculpted gradient that must be **noticed** before it can be answered, and a witness occupied with being amazed is not reading it; the Quiet-Built read **pattern drift** professionally, which is the same instrument tilted at a different object. *They are not more obedient than other peoples. They are earlier, because nothing happens in them first.* **Cooperation is not deference** and a floor is stated — they are unenslavable, Cascade standing is not a lever, and a Levril drifting from its stated terms would meet the same quiet withdrawal anyone would. The closing beat is the record's own: since the aftermath of selection is *"mythic overstatement"* and *"misattribution as divine will"*, the people least willing to be read as symbol are the most often selected and so the most mythologised — able to name the distortion in the room, never in the story told afterwards by people who were not there. Drafted, then merged.

- **The Krenyi have little appetite for *human* entertainment** (2026-08-18, Dermot's direction, and his clarification the same day that the claim is about the **human forms** specifically). New section in [Krenyi (Quiet-Built)](src/lore/krenyi.md). The clarification changed the derivation: an earlier draft had these forms *returning nothing usable*, which applies to any such form and so quietly predicted the Krenyi have no equivalents. It is instead a **calibration problem** — human entertainment is tuned, with great skill, to an instrument they do not have. Human drama runs on dread, sport on partisan fury and the fear of losing, much music on holding a sorrow open, and **deep fear, fury and unresolving grief are exactly the three bounds the page already established**. Fiction's method is the one they decline everywhere else; team sport asks a person to become a category on purpose. They compete and train freely — *it is the stands rather than the field where nothing arrives.* Whether they have forms of their own is left open and becomes the interesting question, the absence being **theirs rather than the archive's**. [Narrative Works](src/lore/narrative-works-and-their-adaptation.md) takes the payoff: the species sharing almost the whole human body plan is the one human stories reach least, while peoples built nothing like us want the work done — *what a story is for does not travel with the shape of the body reading it.* Drafted, then merged.

- **Virtual reality's one live use is generalised** (2026-08-18, Dermot's direction). [Virtual Reality](src/glossary/virtual-reality.md) already named fold-scaffold visualization as the field's *"one enduring, unremarked-upon use"*; the exception is now **a class rather than an instance** — any measured structure a baseline sense cannot reach, with **alien communications and signals** named as the demanding case and the one it is most valued for. The hard boundary is recorded with it, because the two rules meet exactly: **a rig renders structure, never meaning.** It shows a xenolinguist that a figure recurs at an interval and breaks in a place; it never says what it means and never says anything in a voice — that remains years of translation work by named people with error bars, which is where the universal translator's refusal begins. Drafted, then merged.

- **A third option for an image that contradicts canon: refile it as Codex art** (2026-08-18). Dermot's direction — an image that contradicts canon but remains aesthetic may move into the Codex as in-universe artwork rather than be removed. It **completes** *absence beats a picture quietly making a claim* rather than overturning it: that rule governs a picture asserting in **Archive voice**, and a codex artefact asserts nothing, so the disposal options become fix it, relocate it, delete it. The Codex's required `author` field is what makes it work — an artwork's author is exactly what makes it valid-for-its-author, so the test is *could someone in this world have made it, and would they have made it this way?* Recorded in `story-bible/images.md` with its limits: it is **not** a file move (an unreferenced image fails validation, so refiling means writing an entry around it), it would **not** have rescued either Batch 3 deletion (both are photographs of identifiable real-world subjects, which a codex frame cannot cure), and **two absolute exclusions** were ruled the same day: **stock photography never qualifies, and neither do anachronisms.** One reason under both — the Codex can carry a wrong belief, never a wrong century or a real place, since an artefact is a physical object made inside this world and the author's fallibility covers interpretation rather than material culture. A deliberate historical depiction is not an anachronism, so the exclusion needs no exception. A **quality bar** was ruled the same day: only a high-quality image with artistic merit and no technical flaws qualifies — if it would not earn a page were canon no obstacle, it does not earn one as Codex art. A generation artefact is a flaw; a stylisation is not, the test being whether an in-world artist could have *chosen* it. Together these close the French dial (previously arguable, now excluded twice over) and empty the candidate pool: a blob check found the rejected originals were **never committed** — one content version each for `cerebraun`, `aldera`, `rook-7`, `nessa` and `orla-shepherd` — correcting an earlier claim in `images.md` that they were recoverable from git, and establishing that the live `cerebraun.jpg` is the architectural indirection rather than the grey-alien bust. The rule is therefore **prospective**. Two decisions remain open before anything is applied. `CLAUDE.md` deliberately not updated until they are.

- **The Meridian block's correction list is now one issue, #463** (2026-08-18). Seven items where the block's heading advertises one — three from 26 July (name collisions with Halyx's Meridian and Sentinel, the corporation's name, and a Military Space Command founding date that postdates the MSC's dissolution by a century), two from the #442 boundary-zone ruling, and two found while filing: the Sentinel's distress signal received as dreams by sensitives is a **refused psi trope** rather than a stretch of one — [What the Record Refuses](src/lore/what-the-record-refuses.md) holds that *"a dream is never evidence about who sent it: nothing did"* — and **Convergence Weaving** is an undefined term doing load-bearing work, the same shape as Convergence Point before #442. The risk named in the issue is not accidental drafting, which the naming blocks, but a **partial fix**: names changed, block assumed cleared, refused trope written in. Unpublished capture; no lore page changes.

- **The Convergence Point maps onto boundary zones** (2026-08-18, #442). Dermot's ruling: no new term — a Convergence Point is an **inter-membrane boundary zone**, the flexure case that [Boundary Zones and Concordant Edges](src/lore/boundary-zones.md) already describes, and the word stays out of `src/`. The mapping settles the degree question (*minor* becomes a **Boundary-State Class**), fixes the location grammar (a zone has sky coordinates but no distance to its neighbour), and rules out *fold* for the feature, since a flexure is not engineered transit. [Fliade](src/lore/planets/fliade.md) needs no edit — its marked absence was about the **class**, which a *suspected* narrowing genuinely leaves open. The two unwritten blocks do: the Umbral Moon's *Shadow Universe* is not canon and its moon suspended in a stable gravity tunnel contradicts a phenomenon canon defines as lasting seconds to minutes, and the Meridian block's *Ethereal Universes* asserts the Etheric is a place. Recorded in `intake-2026-07-26.md`; unpublished capture, no lore page changes.

- **_Wayland_ ruled not reusable as a corporate name** (2026-08-18, #445). The 26 July intake's *Wayland Space Corporation* collides with [Space Commissioner Zara Wayland](src/characters/zara-wayland.md), and the *Drithane* precedent does not carry: a retired title and an in-world planet are different kinds of thing, where these pair the same surname with the same word — *Space* — in the same domain. **No migration-map row**, on the guide's own precedent for declining one — the name never entered the corpus, so nothing is renamed. Recorded in `intake-2026-07-26.md`; the concern's actual name is still open, and is one edit with that block's existing Military Space Command date correction rather than two. The index also stopped conflating this corporation with the separate unnamed one in the 28 July intake.

- **Fold Transit Safety has an origin story** (2026-08-18, #430). [The Safety Corps](src/lore/star-rangers-safety-corps.md) names three largest branches and gave histories to two of them. The third now has two paragraphs, drawn entirely from what [Catastrophic Failure in Fold Transit](src/lore/fold-transit-catastrophic-failure.md) already establishes: the branch begins from a finding rather than an inherited programme — the failure surface is the aperture, not the fold — which makes the hazard a tolerance, and a tolerance is the kind of thing the 1894 method was built to certify. Its budget goes on comparative standards work rather than inspection because its hard case is a hull honestly certified to a weaker standard, lawful everywhere it goes. The founding year is left a marked absence, on the same page's precedent for the year *Solutions* was dropped. Drafted, then merged — it asserts about the world.

- **`CONTRIBUTING.md` no longer invites readers through a door they can't use** (2026-08-17, #432). The `## Issues` section was written to third parties — *"a short report with a URL is enough"* — while `.github/ISSUE_TEMPLATE/config.yml` turns blank issues off and routes every reader to the comments repo. The two documents contradicted each other, and the reader met the contradiction rather than the author. The section now describes the tracker for what it is, the author's own backlog kept readable, and sends problem reports to the same forum the next section already names — which is what *"asking once in the right place is more likely to get an answer than asking in several"* asks for.

- **The humanoid refusal now turns on the frame, not the posture** (2026-08-16, #449). [What the Record Refuses](src/lore/what-the-record-refuses.md) said the Krenyi were the only other sapient *humanoids* and that the list was closed at one; it now says the only other people built on the **human frame**, and states why standing upright is a different claim. Independent origins converge on solutions, which is what a physical universe does; the costume constraint asked them to converge on a silhouette, which is the refused thing. The test becomes *"is this a human with features applied?"* rather than *"does it stand up?"* — strictly the same refusal, said in the terms it always meant. Ruled by Dermot after the Pandoid sapience ruling produced an apparent second exception.

- **The same sharpening swept to [Cross-Biosphere Taxonomy](src/lore/cross-biosphere-taxonomy.md)** (2026-08-16), which carried the claim in its own words — *"the only two sapient humanoids in the record"*. It now distinguishes the two: Endoskelia asks only for an internal skeleton, while the refusal asks whether a body could be mistaken for a person, so a third people arriving in that row upright is a taxonomic neighbour and nothing more. The page's own line — *a frame is a description, not a genealogy* — was already the argument.

- **Four Pandoid rulings recorded, and Fliade is nearly writable** (2026-08-16). `story-bible/intake-2026-07-26.md` now carries the settled facts — sapient; descended from a bear-like alien ancestor rather than anything terrestrial; not space faring; keeping their record orally, in cultural memory — with the two consequences worth having deliberately: contact is asymmetric because a non-spacefaring people is arrived at rather than arriving, and the Archive meets a spoken record where its own epistemology is documentary. The method for the second already exists and should be followed rather than reinvented — [Hymn of the Thin Places](src/codex/hymn-of-the-thin-places.md) files an oral tradition as a composite text whose variance is evidence, which is a Codex form and not a Lore one. Unpublished capture; no page in `src/` names Fliade or the Pandoids yet.

- **The ladder of technological levels is refused outright** (2026-08-16, #450). New closing subsection on [What the Record Refuses](src/lore/what-the-record-refuses.md), plus a summary-table row: capability is **a shape, not a height**, because a technology answers a question a world asked and no two worlds asked the same ones in the same order. A world with no ore never had a metals age to be behind on. Specific questions survive and matter — can they leave their atmosphere, can they hold a fold route — while *are they more advanced* has no answer, and contact doctrine turns on what a people is and consents to rather than on a rank. Three pages already refused the same ladder case by case ([the fusion ceiling](src/lore/the-fusion-ceiling.md)'s sequencing misread as poverty, [civilisation comparison](src/lore/civilisation-comparison.md)'s *post-comprehension*, [environmental design](src/lore/environmental-technology-design.md)'s advanced world that looks quiet); this states the rule they were all applying. Ruled by Dermot, and it dissolves #450 rather than answering it.

- **A name borrowed from an Earth animal is a fact about the observers** (2026-08-16). New passage and summary-table row on the same page: where the record calls a people bear-like, or files them under an `-oid` coinage, the comparison belongs to whoever did the naming — a first survey reaching for the nearest familiar shape — and says nothing about descent, since [life began separately on each world](src/lore/cross-biosphere-taxonomy.md) and no lineage outside Earth's biosphere has an Earth animal behind it. The record keeps such names, because they are what people say, while declining what they imply.

- **The open-questions index is now reachable from the documents it indexes** (2026-08-16, #441). `story-bible/open-questions.md` had no inbound references at all, which left it as undiscoverable as the prose it replaced. CLAUDE.md's `story-bible/` inventory now names it first, and `intake-2026-07-26.md`, `intake-2026-07-28.md`, `cascade-structure-and-limits.md` and `images.md` each carry a pointer back to it.
- **Class III "shadow beings" are now *incursives*** (Dermot's direction, 2026-08-21). The old name collided with [Membrane Shadows](src/lore/membrane-shadows.md), and the collision was live rather than theoretical — two pages carried explicit "not to be confused with" warnings and `membrane-shadows.md` recorded the two as "routinely misfiled". *Incursive* names the actual discriminator: a Class III being has **entered** material space and can be touched, where a Membrane Shadow is a gravitational imprint with nothing present. So a Membrane Shadow is not an incursive because nothing incurred, and the warnings become nearly unnecessary rather than merely relocated. Swept across 6 files; registered in the canonical glossary with a migration-map row. **Membrane Shadows were the other candidate and were left alone** — 15 files against 6, with their own lore entry, glossary entry, journal entry and existing guide rows, so renaming the smaller surface was the cheaper change.
- **The Unfounded sub-names are modes of failure, not kinds** (Dermot's ruling, 2026-08-21). *Demon*, *threne*, *beautiful monster*, *cute predator* denote no category the way a species or a Cascade tier does, because **nothing among the Unfounded exists by design** — a founded thing is created and defined by the strata above it, and that decision is what gives a kind its membership; the Unfounded are underwritten by no origin condition, so there is no decision for a category to record. Three consequences now on file: two things under one name need have nothing else in common; nothing is obliged to stay what it was filed as, and a reclassification is a correction to the file rather than an event in the world; and the counters are matched to modes rather than kinds, which is why they look unrelated to each other. The **Class II / Class III split survives** — sapience is an observed capability, not a claim about design — and Class I is untouched, Levrils being founded with real subclasses of a real tier. Reconciled in `glossary/unfounded.md`, `lore/predatory-entities.md` and `lore/meta-dimensional-beings.md`.
- **Codex viewpoints add flavour and in-character debate, never doubt about canon** (Dermot's ruling, 2026-08-21). Recorded in `CLAUDE.md` as a bound on the existing canon-for-its-author rule: an entry may be mistaken in its author's voice, but must not leave a reader less sure what is true. The test is the reader rather than the author. Applied immediately — *The Borrowed Silhouette*'s closing said the traditions holding a literal adversary "are not refuted by this paper", which kept an incoherent category live; it now states that the finding is settled and that a tradition's freedom to keep its own words is not a reservation about the record.
- **There is no chief of the Unfounded, and there could not be one** (Dermot's ruling, 2026-08-21). `glossary/unfounded.md` called the devotional adversary "the limiting case of the class", belonging to it "by definition rather than by observation" — "a classification with a standing member slot". That left a chief-shaped hole in the class and quietly reopened a door [What the Record Refuses](src/lore/what-the-record-refuses.md) had already shut ("the dark lord — curtailed… ecology, not theology"). The position on file is now **incoherence, not absence**: command needs somebody to issue it, and nothing performs the reading; something to obey it, and the Unfounded are a failure mode rather than a faction; and answerability, which is the class's own test. Such a being would have to be **founded in order to command and Unfounded in order to be the adversary**, and cannot be both. [The Borrowed Silhouette](src/codex/the-borrowed-silhouette.md) is rewritten to argue this rather than the weaker conditional it carried.
- **[The False Dragon](src/codex/the-borrowed-silhouette.md) is retitled *The Borrowed Silhouette*** (Dermot's direction, 2026-08-21). Renamed rather than removed, because the paper does the opposite of trafficking in the dragons-are-evil trope — it exists to hold real dragons apart from the adversary imagery, and deleting it would remove the one document making that argument. The problem was the *title*: in an index it put *dragon* beside the adversary, which is the association the body works to break. Old URL keeps working — redirect stub plus an `.htaccess` 301, the same pairing used for the Concordant rename — which matters because codex entries map giscus by pathname, so a bare rename would have orphaned the discussion thread.

### Fixed

- **Three consistency defects in the planetary-physics set, and the Tír na nÓg/Drithane habitability tension underneath one of them** (audit, 2026-08-22 — findings appended to `story-bible/canon-consistency-audit-2026-07.md`). [The Cost of the Crossing](src/lore/the-cost-of-the-crossing.md) flatly called Tír na nÓg "the one confirmed world in the catalogue a human can walk on unassisted", which [Drithane's](src/lore/planets/drithane.md) canonical breathable-with-acclimatisation atmosphere contradicts — restated on Tír na nÓg's own canonical classification, *naturally human-compatible*, with Drithane named as the gentlest case elsewhere; [tir-na-nog.md](src/lore/planets/tir-na-nog.md)'s parallel "dome, mask, or engineering" sentence (the pre-existing root of the tension) adjusted the same way. [The Ledger and the Truce](src/lore/the-ledger-and-the-truce.md) claimed Fliade's surface as "the driest in the settled record" — a superlative no canon supports, softened to "among the driest". [Weather on Other Worlds](src/lore/weather-on-other-worlds.md) claimed "exactly one new storm-severity classification in its history" — canon says only that the Four Islands *needed* a new classification, so the count is dropped.
- **Five of seven editions were rendering no hero slideshow at all** (found 2026-08-21). Every edition but `pets` inherited the default cast, and every character in it is filtered out on a narrowed domain — so `sciencefiction`, `starquest`, `fellowship`, `fellowship-archive` and `church-space` all fell back to the static hero image, correctly and silently, for as long as their narrowing has existed. Fixed by the casts above; `sciencefiction` needed its own ruling first, since it carried no character page to cast. **All seven editions now render a hero.**

- **A broken image on the Drithane lore page** (found 2026-08-21, live since 2026-08-17). `src/images/lore/planets/drithane.jpg` was the only image in the corpus in a `planets/` subdirectory while its front matter named a bare filename, and `lore-entry.njk` builds `/images/lore/` + the value — so the page requested a file that was not there. Moved flat, where every other planet's image is; `story-bible/images.md`'s slug corrected with it.
- **A broken slide in the homepage hero on every edition but undercover-pets.com** (found 2026-08-21). `heroCharacterIds` may name a character with no `image:` — 24 of 76 character pages have none, Elvira among them, and she is in the default cast — which rendered `<img src="…/images/characters/">`, a request for the directory. It also counted toward the crossfade's `--nN`, so the animation timed a slot for a slide that could never appear. A new `withImages` filter drops them before the count. Silent until now because a hero slide is decorative and `aria-hidden`.

- **Contrast failures on five palettes** (2026-08-21), found by the new checker and measured rather than eyeballed. **undercover-pets.com — the children's domain — had the worst contrast on the site**: its accent measured 2.32:1 on badges against a 4.5 floor, and its canon-facts control 2.13:1. Also `fellowship`, `church-space`, `sepia` and `light`, mostly on muted text and status badges sitting on `--color-surface-2` two steps darker than the page. And the `fellowship` palette's hover was a *lighter* gold than its link colour, so pointing at a link made it harder to read — 2.98:1 against 6.00 at rest. Every palette but the exempt `solarized` now clears the floor on all thirteen checked pairs; the colours moved as little as the arithmetic allowed.

- **A `SITE_NOINDEX` build no longer emits a sitemap.** A testing or staging domain ships `robots.txt` with `Disallow: /` and `noindex, nofollow` on every page, and then shipped a `sitemap.xml` enumerating those same pages. Unreachable rather than harmful — robots.txt omits the `Sitemap:` line and disallows the file itself — but a list of URLs the domain is actively hiding is the wrong artifact to leave in a document root. Found while verifying a test subdomain build. Same shape as the `ranksAt` case already handled in `src/sitemap.njk`, now sharing one condition.
- **Documentation described a cPanel interface that no longer exists.** cPanel retired its separate *Subdomains* and *Addon Domains* pages and folded both into one **Domains** interface, so `TECHNICAL-README.md`'s "created via cPanel's own addon domain setup" and `cpanel-deploy.sh`'s "create the addon domain" pointed at icons that are gone — reading as though the feature had been removed rather than renamed. Both now describe **Domains → Create A New Domain**, the own-document-root choice that separates a build from an alias, the DNS case where nameservers are not cPanel's, and the fact that the same page is where an alias is finally re-pointed. Written as actions rather than icon names, since this interface has already moved once.
- **Two Aethelrock findings from the Benevolent Critic** (2026-08-20), both raised as issues on the day the clan-head portraits went up and both closed here. **Rhiannon Ceridwen's page counted the objections from the wrong clock** (#499): *"nine years of being told, at every Ridgemoot since"* — but nine years is her headship, from 2817, while the objections run from first contact in 2824, which against the story present of 2826 is two. Elapsed time has one correct value, so the fix is the fact and not the framing; the nine survives eight lines above it, where it belongs. **Cormac Dubhghlas chaired a Ridgemoot five years before he was a clan head** (#498): 2806 against a headship taken in 2811, at a body `aethelrock.md` defines as a gathering of *heads*. Repaired additively rather than by moving a date — he chaired **as his father's proxy** — which costs one clause, keeps both dates, and quietly establishes that Dubhghlas has been arguing this case across two generations, which *"has never once required updating"* already implied.


- **The lettering-ban failure applied back to the prompts written this week** (2026-08-19). #487 recovered the rule that **the lettering ban cannot beat the scene**; **seventeen prompts written on 19 August had the shape it forbids** — a text-bearing object plus an instruction to leave it blank. Fixed by changing scenes, never by strengthening negatives, using the three moves now recorded under Prompt craft: **remove** (Vexar's counter cleared of forms, Drey's desk bared, Strozzi's cards gone, the founding table's papers gone, Harrow's folder gone), **close** (Emma's notebook shut and her pencil down, Kate Ukrainka no longer mid-sentence but with the work shut and a mug in both hands, the Institute's notebook shut, the narrative-works volumes all shut, Halvern's wallets tied), and **substitute** — [what nobody certifies](src/lore/what-nobody-certifies.md) went from *four framed certificates, each one blank of any writing* to **four struck metal seals in wall niches and a fifth niche empty**, which keeps the whole idea and removes the writing surface rather than arguing with it. [The Plainmark](src/lore/plainmark.md) is the one whose subject cannot be moved, so it gets the opposite handling recorded instead: describe the marks as shapes and never name the function, keep nothing else markable in frame, and if glyphs still appear **crop tighter rather than add negatives**. **The prompt-craft rule itself was wrong and is corrected**: its *Ban lettering explicitly* bullet offered *plain unmarked folders* as its own worked example, which is precisely the shape that fails.

- **The Reeves emblem is confirmed deliberate, and the rule it looked like it broke is scoped** (2026-08-19). The 19 August audit flagged `reeves.jpg` as an abstract emblem sitting oddly beside the 12 August rule that *AIs get portraits like everyone else*. **The flag was wrong about the decision and right about the file**: it was settled on 13 August in #415 and carried consistently into both `image_alt` lines, and nothing in `images.md` or `image-prompts.md` said so. Now recorded in both. **The rule's scope is the resolution** — it answers how to depict an AI *that has a body*, so a generator's photoreal default cannot stand in for one, and never required inventing a body for a system with none; [Lucene-9000](src/characters/lucene-9000.md) is *a mobile AI humanoid* where [Reeves](src/characters/reeves.md) is *investigative support intelligence*. Three worked cases settled the same day: the Eden Warden became the habitat, and the two Reeves became a deliberate pair. **The lesson underneath is general and now written down: the lettering ban cannot beat the scene** — the earlier Eden brief asked for a case-file interface and banned lettering in the same breath, and came back reading `CASE FILE: ALPHA-7`. The fix is to change the scene, never to strengthen the negative. **And the pair's difference is content, not variation**: Eden's pattern is *half-found and not yet claimed*, Threshold's is a ring almost closed and deliberately not closed.

- **`images.md` audited for stale counts; three sections were wrong and one of my own was duplicated** (2026-08-19). **Open work 0** said *28 images* replaced with placeholder cards; **six have since been replaced with real images and were never marked** (`petra-voss`, `brother-daire`, `ilsabet-marrowtide`, `rasa-oyelaran`, `eden-warden`, `reeves`), leaving **22 still carded** — separated by shape (cards are 1600×900 at 52–77 KB, portraits 1200×670 at 96–217 KB) and then **confirmed by opening a file at each end**, per the file's own rule against reasoning from filenames. **Open work 3**'s *contemporary-stock-headshot cluster (11 files)* describes images that no longer exist — all eleven became PENDING cards on 12 August — and its `validate-images: on` block passes anyway because the files exist, which is **exactly the drift the section's own closing note predicted**, four days after it was written. **Open work 6**'s alt-text figures are stale in the good direction: *68 pages under 80 characters* is now **7**, and *40 at 120+* is now **112**, across 135 illustrated pages. **And *The ten worth doing first* was duplicated by my own 19 August passes** — eight of its files were prompted a second time because both new lists were derived from pages lacking an `image:` and this section was never checked against them. Reconciled in place: the newer brief governs for six, **Open work 6's brief governs for `predatory-entities`** because a vacated habitat compartment holds the tone line better in-setting than a woodland clearing, and the older Aoife brief's **keep the face concealed** — canon reasoning the newer entry missed — is adopted into it. Sections that check out unchanged: *The seventeen replaced on 12 August* (17, all present) and Open work 4's New London defects (still 1408×768).

- **Open work 5 listed delivered lore illustrations as a queue** (2026-08-19). The *Missing lore illustrations* section was headed *19 pages*; **23 of the 30 slugs it names already exist**, and the seven that do not were named in passing and never prompted. Retitled as archived, prompts left in place as the record for the images they produced, and the true outstanding count re-derived: **52 lore pages**, most of them written since July. Same failure as Open work 1, found the same way.

- **Open work 1 listed thirty-four delivered portraits as a queue** (2026-08-19). `story-bible/images.md`'s *Missing portraits* section was headed *28 pages*, listed **34**, and had **all 34 already shipped** — the count was wrong, the label was wrong, and finished work read as outstanding. Retitled and dated, with the audit method recorded; **the prompts stay in place**, since they are the prompt of record for the images they produced and deleting them to tidy a status line would destroy provenance. A true outstanding list of **25 pages** is added at the end of the section, none of them overlapping. Also flags a stale instruction inside those archived prompts: most end *Portrait orientation*, which predates the settled 1200×675 16:9 convention that 42 of the 52 delivered files actually follow.

- **The rescue chapter's `andrew` pov id matched no character** (2026-08-19). `s01e00c04` used `::: pov andrew` while [Andrew Falkorian](src/characters/andrew-falkorian.md)'s page carries `id: falkorian`, so his scene-POV page resolved to no portrait and no link back to him. Synced to `falkorian`, moving that one scene-POV URL with it; the chapter's own note had pre-authorised the sync (*the ids may need syncing*) and the page was six days old. **`anne` could not sync the same way** — the surname form is Karla's and an `id` must be unique for the portrait lookup — so Anne's page carries `id: anne` deliberately.

- **The Pandoid sapience question is answered, in place and sourced** (2026-08-16, #443). The note had asked it since 26 July while #443 and the index both recorded it as settled — *sapient but low-tech* — on a ruling that appeared nowhere in the repo. Dermot confirmed the sapience half directly: the Pandoids are sapient and one of very few humanoid-like aliens, so Fliade is a first-contact world under the FTP sapience clause. Written into `intake-2026-07-26.md` dated. Their technological level was **not** confirmed and stays open, and the humanoid half collides with a published refusal — see below.

- **A humanoid collision is flagged before it can be written into canon** (2026-08-16). [What the Record Refuses](src/lore/what-the-record-refuses.md) states, in prose and in its summary table, that the Krenyi are the only other sapient humanoids, and calls that refusal the load-bearing one. Sapient humanoid-like Pandoids would be a second exception. Nothing is broken — Fliade is unwritten and the Pandoids appear nowhere in `src/` — but the first page describing them would contradict a live canon page. Three readings are recorded in `intake-2026-07-26.md`, `story-bible/species-design.md` carries a flag against its own premise, and **no page was changed**: the choice is authorial and gates writing Fliade.
- **`CHANGELOG.md` had two `## [Unreleased]` headings.** Introduced on 2026-08-20 by an edit that meant to add a `### Fixed` section and duplicated the heading above it instead, splitting the unreleased notes into two blocks. `check-changelog.js` counts *released* sections, so it passed. Collapsed back to one.

## [1.22.0] — 2026-08-16

A release about the machinery for noticing things. Two doctrines that looked
opposed turned out to govern different objects, and stating the boundary
between them produced a named review process, a public backlog to file its
findings in, and the first three findings themselves. Alongside that, the
Safety Corps gained an internal structure, Kieme gained three titles it can be
translated by, and one domain's deploy stopped failing silently.

The through-line, if there is one, is that **silence is the expensive failure**
— a cron that reports nothing, a gap nobody marked, an untidiness filed as a
fault. Most of what is below is an attempt to make one or another kind of
silence legible.

### Added

- **Journal: [Kieme Means Gill](src/journal/kieme-means-gill.md)** (2026-08-16). The naming problem written up as craft rather than as a fix. The entry's argument is that the mistake was about *which kind of problem it was* — not that `Kieme` reads as a fish organ in German, but that Conseleme had a living translatable devotional name and Kieme's was marked legacy, leaving one tier of three exposed to translation with nothing to land on. Hence the principle it ends on the way to: **titles translate, coinages transliterate** — a translator meeting *the Everlasting Ground* renders the sense, a translator meeting *Kieme* gets a sound and whatever that sound already means where it lands. And the closing turn, which is the reason the entry exists: having three names for one thing looked like untidiness to be reduced, and writing down *why* the tier has several produced the best sentence in the glossary. *"I went in to clean up a mess and the mess turned out to be the evidence."*

- **Kieme gains three titles, and the reason it has several becomes part of the entry** (2026-08-15). `Kieme` is the ordinary German noun for a fish gill — checked and deliberately kept on 2026-08-04 with a recorded revisit trigger. Revisiting it found the real gap was structural rather than linguistic: **Conseleme has a living translatable devotional name (the Everlasting Ground) while Kieme's translatable name, Holy Triumvirate, was marked legacy**, leaving Kieme the only Upper Structure tier with no current name that survives translation. Hence the fish.

  The repair is **additive, not a rename**. Three titles in three registers: **the Silent Origin** (living devotional name, direct counterpart to the Everlasting Ground, taking the constraint for the name — the tier does not speak and never has); **the Undivided** (the title, standing where *Most Holy Triumvirate* stood, refusing the count without refusing the shamrock, since one/three/many are rival images rather than rival answers); and **the First Condition** (Archive register, deliberately undevotional). *Holy Triumvirate* stays synonymous and pre-retirement filings read as written — its weakness being that a triumvirate is three men ruling, which is both a count and a committee, and this tier is neither. **`Kieme` remains the term of art**: no migration, the `-eme` pairing with Conseleme undisturbed, no corpus sweep. Titles translate where coinages transliterate, so this fixes every language rather than German alone.

  The entry now also says **why** the tier carries several names, which is the more interesting half. Nothing here has ever spoken, it permits no identifying intervention, and it is inferable only through invariants — so **no tradition was ever told what to call it**, and each named it from what it could see from where it stood. *A tier with exactly one settled name would be a tier somebody had been told about.* Extended per Dermot to other species, which name the tier or the whole Cascade out of their own psychology, sociology and language; those appear translated under the record's existing default, so a name kept untranslated is a species making a point by it. And the striking part survives translation: **the names agree about almost nothing and the constraints agree almost exactly**, which is what one would expect of something real that has never spoken.

- **The reading plan gains a narrator, and the record gains a list of its own gaps** (2026-08-16). The `/start/` reading plan is re-voiced as a signed archivist's note from Senior Archivist Sen — same five steps, same computed chapter links, but the guidance now speaks from the desk that canonically reconciles the record, with the lore/codex trust distinction delivered as professional practice rather than site copy. The out-of-world items (Story Engine, Atom feed) sit in an unsigned postscript after Sen's sign-off, keeping the in-world/out-of-world seam clean. Alongside it, a new codex working paper, [Marked Absences](src/codex/marked-absences.md) (Survey Archive working-paper series, author Sen), files the record's known gaps under a five-way taxonomy — sealed (the *Patience First* terminus), unsettled (the two Brother Daires), live (the specimen the record cannot classify), provisional (the Hall of Quiet Instruments attribution), unverifiable (the number of Concordants) — governed by the stated doctrine *mark the absence; do not explain it*. Every entry restates an absence the public record already carries; none adds a fact, names a cause, or links private-thread material. Step 5 of the reading plan points at the paper so a reader never mistakes a gap for an oversight.

- **A reading plan at `/start/`, and the front page's Begin Reading button now points to it** (2026-08-16). Previously the button dropped a first-time reader straight into the full Seasons & Episodes listing with no guidance on where to begin. The new page lays out a five-step path — start with the Tissadelle Shepherd's Arc spine, read a chapter the way it's built (straight through, then the *View from* buttons and scene-by-scene POV lists), keep the reference shelf beside you (with the lore-is-settled / codex-can-be-wrong trust distinction stated plainly), branch out by storyline thread, then go deeper via the Timeline, Codex, Story Engine, and the Atom feed. Every chapter link on the page is computed from the filtered `chapters` collection rather than hardcoded, so a narrowed edition shows only threads it actually carries, a spine-less edition degrades to a pointer at its own listings, and renumbering can't strand the page on a stale id. The Seasons & Episodes intro gained a one-line pointer back to the plan. All copy restates existing published framing (thread registry descriptions, section-index intros) — the page asserts nothing new about the world.

- **Brother Daire enters the timeline at both ends of it** (2026-08-15). Two entries, because the corpus has two Brother Daires and the Archive has never claimed they are one. [1216 AD (1416 UCSD)](src/timeline/1216-life-of-saint-aoife-set-down.md) becomes the **earliest dated event the timeline carries**: the community at Cill Aoife deputises Daire to write down Aoife's life, and he produces the account that survives medieval copying, leaves Earth with the Currach Fleet's founding scriptorium in 2561–2589 UCSD, and is still being read fourteen centuries later against a taxonomy he had no vocabulary for. The entry keeps his method rather than his biography, since that is what the record kept — *mark what is confirmed, mark what is assembled, never blur the two* — and no birth or death date for him survives in any form. [Year -4 (2822 UCSD)](src/timeline/year-minus-4-river-abbey-second-visit.md) fills the modern end of the same spine, and a real gap: the timeline held nothing between Year -11 and Year -3, so Elvira's second visit to the river abbey on the Boyne — where the Fellowship's Brother Daire tells her the sensitivity runs in her mother's line and gives her the word *Overfold*, and where Aldera reads the thinning half a second before Elvira does — existed only inside the two chapters that dramatise it.

  **Neither entry equates the two men**, and the [character page](src/characters/brother-daire.md) now says so in its own section rather than leaving the reader to notice. Four pages link to that page for the abbey keeper while the page itself describes only the scribe of 1216; whether he is the same man, a later namesake in the same tradition, or a title carried by whoever holds the smithy-and-scriptorium role, no record that has surfaced settles. The annotation stating that question in full lives on a `church-space` page, so all three pages state it on their own terms and **do not link to it** — the private-thread link boundary that `validate-content.js` enforces, and which caught the first draft of all three.

- **The Safety Corps gets branches, and the UL triad moves down a level** (2026-08-15). To Dermot's direction: the Corps is divided by **hazard domain** into branches — **Fold Transit Safety** and **AI Safety** named as the two largest — and the inherited Underwriters Laboratories split of research, standards and certification (**Solutions**) no longer describes the top level, surviving instead as subdivisions *inside each branch*. The vocabulary turned out to be already canon rather than new: the command-hierarchy entry has the Academy "organised as a college per corps and a **school per branch**", which the arrangement now explains. The Corps' stated reason puts function beneath domain — *the method is common property and can be taught to anyone; what cannot be taught quickly is the specific way a domain goes wrong.*

  **The AI Safety Branch does not hold the gate**, and the page is careful about it, because canon already assigns commissioning authority over agentic systems to the AI Governance Commission under the Concord Accords. The Branch certifies something narrower and entirely its own: whether a lawfully commissioned mind is fit to be **deployed** where the Rangers propose to deploy it. That is the hull-and-route division the Corps already lives with at a fold gate, applied to minds — *a mind may be lawful and unsuited; the Commission answers the first question and has never claimed the second.*

  **Incident investigation sits outside the branches**, because a branch that certified a hull cannot investigate that hull's failure without becoming its own reviewer. Ordinary investigation reports to the corps president. **A major, cross-disciplinary investigation is led from the command ladder by a Starwarden or above** — which reads at first like a straight contradiction of two existing lines ("none of them answerable to a Starwarden"; "a Starwarden cannot override a Safety Corps finding") and resolves in the word *cross-disciplinary*: a fold-transit loss with casualties is a Safety question about the hull, a Navigation question about the route, a Medical question about what the crew survived, and no corps president holds authority inside another corps. The only office that can convene all of them is on the command ladder. The charter line then holds underneath, unchanged: **command decides what is investigated and does not decide what any corps concludes** — the Starwarden directs the inquiry and cannot amend a single finding brought to it, including a finding that command's own tasking caused the loss.

  **What an investigation produces depends entirely on who is being answered.** Inside the Rangers a finding is not a suggestion: the Corps' standards bind the whole institution, and where an investigation concludes a standard was wrong the Corps amends it and the amended standard binds from the moment it issues, no approval sought. Outside them it produces a recommendation, **referred for review to whichever government actually holds the authority** — the polity that registered the vessel, the habitat administration, the world whose citizens died — which adopts, dilutes, defers or files it as it chooses; where it is declined, the Corps publishes that it was made and declined. *Absolute authority over its own house, none whatsoever over anyone else's* — and the Corps' answer to being asked whether that frustrates it: *a body whose recommendations were binding would be a body worth capturing.*

  **Boundary Safety is named as a third branch and the oldest**, descending directly from the USIS boundary-proximate programme and predating the Star Rangers — which is where the Corps' character was formed, and which finally gives the **certified boundary analyst** designation a branch to live in. **"Solutions" is retired**: the commercial coinage did not survive the body's charter as a public-interest institution, and the arm is called Certification, because *a body whose entire claim is that it has nothing to sell should not name the arm that issues its certificates after a product line.* That leaves the Corps declining two words from opposite directions — *regulator*, which claims a power it does not have, and *Solutions*, which claimed a relationship it did not want. Governance gains a second axis: a **chapter leader** holds a post, a **guild leader** holds a branch, neither outranks the other, and both answer to the corps president — all three held **on secondment from regular duties**, keeping the substantive rank and post and returning to both, which the Corps treats as load-bearing rather than administrative: *the ones who have to live with a standard are the ones who elected the person writing it, and that person has to come back and work to it.* Corps President is confirmed as a guild office and keeps its existing name; **Guild President was descriptive, not a rename**, so no terminology migration is needed. And **a Starwarden convening a major investigation holds nothing outside the Rangers** — where a Ranger appears in someone else's inquiry it is by invitation, as **neutral mediator, arbiter or observer**, a role conferred by the inviting party and revocable by them, *which is exactly why it is worth having*. The [Academy entry](src/lore/star-rangers-academy.md) turned out to corroborate the whole structure independently, being organised as a school per branch precisely because certifying "boundary analysts, ship's pilots, field surgeons, and investigators against a single standard" is "not a hard problem so much as an incoherent one".

  Two further tiers of who is bound, both to Dermot's direction. **Suppliers, subcontractors and their entire supply chain are bound by contract**, on pain of disqualification from future work — a certified assembly containing an uncertified part is not a certified assembly, so the condition flows all the way down, and foundries that will never sell a Ranger hull hold Ranger certification because the yard they sell to must. This is the nearest the institution comes to the power its charter denies it, and the page is precise about the difference: **it binds who the Rangers buy from, not who exists.** A firm that declines has lost a customer, not a licence. **The Dock Seven gap is deliberately left open** — procurement reaches only those who want the business, and the Hyperfold Yield Combine sold to hull operators and wanted nothing from the Rangers. **Donors and supporters are bound by none of it**, holding no certification and asked for nothing but the money; what is examined is whether the money may be taken at all, support being accepted only from legitimate, bona-fide, law-abiding citizens and organisations. That test exists for the same reason the Rangers are self-funding: *a body that can be embarrassed by its donors can be steered by the threat of it.*

- **Three species lore pages, and body plans for the two that already had one** (2026-08-13). The ruling that the Krenyi are the only other sapient humanoids exposed a gap nobody had noticed: **five species had characters on the roster and no described body anywhere in prose**, which is why three of their portraits had quietly drifted humanoid. New pages for the [Veyr Basaltborn](src/lore/veyr-basaltborn.md), [Pelagene Littoral](src/lore/pelagene-littoral.md) and [Serephine Dunekin](src/lore/serephine-dunekin.md); body plans and technological histories added to the existing Chthonari and Verdani entries.

  Each was derived from its world rather than invented, and in several cases the character page had already done the work. Qiren Tal's file had solved high gravity better than any fresh attempt would have — *"a lattice carries compression the way solid bone never could"* — so a **Veyr is a truss, not a mass**, with no fixed front and no privileged pair of arms. Mira of Brine's specialism in *fluid-boundary anomalies* only means something if **Nerath is several fluids lying stably on one another**, which it now is. Sethka Ru's exposure windows turn out not to be a professional discipline but **a metabolism with a career built on top of it**: the Serephine body is closed against a sky that would sterilise it and **unfurls an enormous respiratory surface twice a day**, at dawn and dusk, in the only hours neither extreme kills.

  The set is completed by five distinct relationships with the same bottleneck. **The Veyr were handed fire by their own crust; the Pelagene never had it and never could; the Serephine had to focus it out of the sky; the Chthonari never needed it, because the Undersong Belt is already made of metal; and the Verdani never built the furnace at all**, being limited by light per day rather than by heat. That last one has a consequence worth the whole page: a thallenwort strain is adapted to one biome's light, so **migration is a permanent metabolic cost rather than a decision** — which is the actual reason Verdance has four independent governments instead of one.

- **Portraits for Sethka Ru, Qiren Tal and Isren Farrowkin** (2026-08-13), all three blocked earlier the same day and all three generated first time once the prose existed. Sethka is the closed form caught halfway into the open one at dusk; Qiren is an open lattice with no solid trunk, the bulkhead visible straight through the body; Isren is low and broad with fern-like vanes half-opened to a window and nowhere for a visitor to look.

- **[Sofia Santos](src/characters/sofia-santos.md), and Thalassa's first planet-wide office** (2026-08-13). Drafted to a one-line brief — *charismatic, determined, sharp, green-eyed, the first female president of her planet* — after two collisions with canon were put to Dermot rather than resolved quietly. **No planet in the record has a president** (the only `president` in the corpus is the Star Rangers' *corps president*, a guild office deliberately off the command ladder), and **the setting has no history of women being excluded from power** — eight centuries of Krast, Wender, Shepherd, Drey, Voss, Korvain, an abbess running a first-contact relationship, six generations of women holding the Shepherd croft, and not one page anywhere framing a woman's appointment as a first. Both questions went to him: **Thalassa** for the world, and **first president full stop** for the framing. The office is then built out of what [Thalassa's own page](src/lore/planets/thalassa.md) already said. Two cities that have never agreed how to be a floating settlement can run separate municipal councils for as long as everything in dispute is **above the waterline**; below it there is one ocean, and no council has ever been able to hold half of a tether field. So in 2822 the councils jointly ceded the powers neither could exercise alone — seabed tenure, the mooring-tower tether fields, current loading across shared shoals, the state of the water column — and **nothing else**: no legislature, no revenue, no enforcement, no authority over any person. Dock-priority scheduling, the rivalry SSDC liaison staff call the most reliable source of paperwork on their desk, stays exactly where it was. *Santos holds more jurisdiction than anyone else on Thalassa and cannot schedule a dock.* **The lore page's "no single planetary government" is left literally true rather than amended**, because the instrument's first clause says so itself — that being the only condition on which either city would sign — and everybody calls her President of Thalassa anyway. The brief's charisma is made the mechanism instead of the decoration: an office with nothing to trade can only be exercised by persuasion, so what she has is a room's attention and the load figures, and she has never been the person in the room who understood the figures second best. She is a tether-load engineer, which is why the instrument creating her office is written in vocabulary she had used professionally for a decade. **She declines *first woman* every time**, and not out of modesty — the phrase describes a scarcity Thalassa does not have, both councils having seated women since founding, and accepting it would mean accepting an account of her planet she knows to be false in order to be flattered by it. What she is first at is the office, which she considers the more interesting fact: **she is the first person on Thalassa who can be wrong for everybody at once.** The closing turn is the one the weak office sets up. Two councils built it too feeble to be worth capturing, each expecting the other to try; she has spent four years never once asking for a power the instrument does not give her, through a period when she could have had several for the asking — *which is the part that is not restraint*, since an office that visibly does not reach is the only argument that will ever make either city extend it. She has not claimed otherwise when asked. What she wants is on the record too: the petition for full self-governance Thalassa has never made, which the arithmetic does not yet support, and which nobody has counted more carefully than she has.

- **[Starwarden Luke Valerius](src/characters/luke-valerius.md), who cannot check whether he is neutral** (2026-08-13). Drafted to a one-line brief — *frontier Starwarden in 2826, navigating higher-dimensional anomalies, holding political neutrality among warring human factions, a hard-science pragmatist who privately doubts neutrality is possible near forces that bend reality.* Every clause of it turned out to have a canon answer already waiting, and the page is mostly those answers put next to each other. **Starwarden is exactly this posting**: the rank answers for a fixed post rather than a vessel, and his is boundary-proximate on the outward frontier, monitoring an interface that has held **Class II** — alternating dominance windows, periodic chronometry divergence — for the whole of his command. The brief's *hard-science pragmatist* is not a personal quirk here but **Archive discipline held literally**: he sides with what was observed at the scope it was observed at, treats a claim with no scope as a sentence still waiting for its subject, and will stop a briefing to ask whether a colleague means this membrane, this archecluster or this instrument. Junior officers read it as pedantry for about a year; the Safety Corps reads it as correctly formed and sends him the difficult approaches. The politics is made concrete rather than atmospheric: a boundary interface's **classification governs what may lawfully be done in its approach**, so his readings are worth money to three settlements, two chartered survey companies and a Union-registered salvage combine. **The class is not his** — a classification is a Safety Corps finding and no Starwarden can override one, which is the guild rule rather than a courtesy — so what he holds is everything the finding is assembled from: which readings are filed, in what order, annotated how, and whether an oscillation goes up as a logged event or as noise. *He does not decide the class. He decides what the officers who decide the class are looking at.* Everyone concerned understands the arrangement precisely, which is why nobody has ever asked him for anything. The doubt then comes out sharper than the brief had it, in two halves. **Declining to adjudicate is free at an interface and never free in a room** — two disagreeing readings can be carried indefinitely and the record is better for it, but an unresolved question has a direction, it favours whoever holds the ground while it stays open, and he has never established that the direction his post's open questions run in is one he chose. And the half he has said aloud once, to his assigned mentor, and never written down: **every measurement at a boundary is position-dependent**, his even-handedness is a stance held somewhere, and he has no instrument that would tell him how it reads from anywhere else. He does not conclude that neutrality is impossible, because that claim would need a scope he cannot supply. He concludes that he has no procedure for checking, that the absence of one is not evidence either way, and that both sentences are correctly formed. Then he goes on filing. He carries **more unclassified transients than any comparable post**, which costs him standing with everyone who would prefer a number, and no finding assembled from his readings has been overturned. Drafted with the brief's bare surname and no given name invented; **Luke** supplied by Dermot on reading it, and the page and its slug renamed to match before either had been published anywhere.

- **[Thalassa Vexar](src/characters/thalassa-vexar.md), the best-liked Imperium officer in the Belt** (2026-08-13). Drafted to a one-line brief — *Assistant Deputy Viceroy of the Solar Imperium, sharp, commanding, respected, and known for kindness and fairness* — and placed where canon already had a slot for it: **Assistant Deputy Viceroy is a real post in this world**, the grade [Osric Fenholt](src/characters/osric-fenholt.md) held in the Belt Settlements Directorate, "two full grades below anyone with actual standing to set policy." So she is historical (b. 2551, d. 2619 UCSD), the Imperium having ended in 2609, and her influence has to come from somewhere other than her grade: she is attached to the **Office of the Viceroy** rather than to a settlement cluster, which is the whole career in one line — *the grade was a cluster officer's grade; the desk was outside the Viceroy's door.* The brief's warmth is kept exactly as given and made load-bearing rather than decorative. **Her kindness was the Directorate's most effective instrument**, and she never abused it: delegations that would not speak to the grades above her spoke to her, she never told one of them anything untrue, and everything she was told went upward correctly filed — which is how a punitive administration came to hold the most accurate picture of the Belt it ever had, and to administer the Belt on it. The settlements under her clusters fared no better; the schedules and caps applied as written. What her decency produced was compliance obtained at a lower cost than force. Two beats do the rest. She forwarded **no recommendation in twenty-one years**, because the grade had no line on any Directorate form in which one could be entered; she raised the absence twice in writing, was answered correctly both times, and did not raise it a third. And she was called *Viceroy Vexar* by everyone below her grade and a good many above it, corrected nobody, and — the revealing half — was corrected by nobody senior either, the Directorate finding it useful that the Belt believed it was being dealt with at a level it was not. The closing turn is the one the existing lore had already set up: she staffed the Directorate's seat on the **High Council**, the chamber built to be consulted rather than heeded, and was then retained on the transition staff to help model the Military Space Command's advisory architecture on it — *she had sat in the one chamber of the Imperium in which outer-station representatives had ever been listened to, and she recommended it be built again.* The MSC "inherited both the form and the flaw." She left no memoir and no account of the period after 2609, and the page declines to supply one.

- **[Sildron Drey](src/characters/sildron-drey.md), the MSC's Director of Operations through the Eden years** (2026-08-13). Drafted to a one-line brief — *former MSC Director of Operations, initially supported Krast, later disillusioned* — then given the ending Dermot supplied on reading it: *exhausted, and a nervous breakdown.* She wrote *On the Cost of Speed* in 2698, the qualified staff-paper version of the argument [General Krast](src/characters/maren-solveig-krast.md) would state in one sentence in 2719, and then held from 2711 the one post that had to convert committee findings into tasking orders. So the Eden delays already on the record — eleven months to a committee, fourteen to a preliminary report, six years to follow-up findings — are hers to execute and not hers to decide, and the *Patience First* liability classification carries her signature. **Her disillusionment is not a discovery that the structure was corrupt; it is the discovery that every delay in it was lawful, correct, and defensible in exactly the terms she had supplied.** She gave nine days of technical testimony at the Consolidation Hearing and supplied the figures that became its finding of structural inadequacy without contesting one of them. **Then, the following spring, she took four years of accrued leave and could not do the work when it ended** — *not would not; the distinction mattered to her* — and the MSC, having no line in its medical schedule for what had happened, reached for a classification that let her be paid and kept her name off a duty roster, which its officer corps read the way it read every fitness finding: as something the officer had turned out to be. The page never names the thing and never diagnoses it, per the record's standing habit. It does let the century land: **the successor institutions eventually descending from this one settle that a stress injury is an injury**, the policy 1.21.0 documented the day before this page was written, and nobody writing Drey's file in 2720 was in a position to anticipate it. Her recovery is slow, incomplete, and not a story. **Director of Operations is a new MSC post** and this page is the only thing establishing it, defined narrowly as the staff-side office measured on whether an order was correctly formed rather than on whether it arrived in time — which is what makes the character possible at all. Drafted as *Avalon Hope* and renamed at Dermot's direction; the retired pen name is dropped from the two `story-bible/` provenance notes that carried it, no commit in the reachable history having ever used it.

- **Difenland, Krilzat and Kalenjar — the Kingdom of the Four Islands names three regions** (2026-08-13). Below the island is the region, and the Kingdom names three: **Difenland**, Aduine's upland terrace country above and behind the Abbeylands, whose walls run on the same labour-day rhythm as the Abbey's without the Abbey having anything to do with it; **Krilzat**, Casimor's windward side, uninhabited but for the watch-post and unfarmed, and still the unit in which ordinary speech measures a bad season — *whatever Krilzat lost*; and **Kalenjar**, Ostrel's harbour region, smallest of the three by ground and holding more people than the other two together. **They are not administrative units** — the Common Council's steward answers for a whole island and nothing smaller — but they are how anyone from here says where they are from. Vellamar has none, and the reason is the point: the throne island is divided into court quarters and garden precincts named for what stands in them, because Vellamar was laid out by an administration and the other three were named by people who worked them. The one hook left live for the delegation: *Krilzat* does not sit in the same phonological company as *Vellamar*, *Aduine*, *Ostrel* and *Casimor*, which [la Chapelle](src/characters/emma-la-chapelle.md) has filed at the lowest confidence her register admits and resisted every attempt to write up as a finding. The court's answer, asked directly, was that Krilzat has always been called Krilzat.

- **The Four Islands delegation has names: [Anne de Courtney](src/characters/anne-de-courtney.md), [Ernesto Strozzi](src/characters/ernesto-strozzi.md), [Emma la Chapelle](src/characters/emma-la-chapelle.md)** (2026-08-13). The Xenolinguistics Branch page promised that "the record's translations have error bars, and its translators have names," and then the branch's own flagship case study had none — every delegation reference in the corpus was *a delegation linguist*, *delegation linguists*, *the Corps' own delegation*. Three officers, each defined by what they decline. **Strozzi**, Principal Officer and senior linguist, rendered the Sisterhood's fixed annual render to the Crown as a *tribute* on a cognate that looked secure; it stood nine months at high confidence and went out to three postings inside the Archive's first account of the Kingdom's government, which put the Abbeylands under Aduine's steward in the only record anyone off-world could consult. He keeps the superseded version filed beside the corrected one under his own name, and when the branch asks him to teach it he teaches the mechanism rather than the remorse — *he would have filed it*, and he does not offer the sentence that would soften that. **De Courtney**, Chief Ranger heading the delegation, has forwarded no request for expanded access to the court: not a second site on Aduine, not terrace sampling, not instruments on the caldera rim, none of which the Kingdom has refused because none has been put. She reads every translation before it reaches the Archive — not for the language, but for whether a sentence claims to know something the delegation was not told — and has sent back more of her own people's work than the court has ever refused her. **La Chapelle**, Field Officer, read a common labour term in the Long Accounting as carrying a technical sense the Kingdom's own chronicles no longer hear, filed it at the bottom of her range, and the instruments then looked specifically and confirmed the engineered atmosphere. She refuses the word *discovery* on arithmetic rather than modesty: being right at low confidence is luck, and a branch that only remembers the ones that landed is training its people to file high. **The language is human**, which the pages treat as what makes the work harder — the branch exists for tongues that evolved elsewhere, and here a real cognate did work a real cognate had not earned.

### Changed

- **Three Sorchas, ruled coincidence and said so on all three pages** (2026-08-15). From the same sweep as the fixes below. The record carries three women called Sorcha and the sharpest pair is not the obvious one: [Sorcha Nic Dhiarmada](src/characters/sorcha-nic-dhiarmada.md) is a valley warden on Drithane who **keeps flocks for a living**, and [Sorcha Shepherd](src/characters/sorcha-shepherd.md) is a Tír na nÓg land-registry surveyor whose **surname says so** and whose croft is six generations of Boirinn flockholders — so the coincidence runs a layer below the given name, into the trade. Neither page had mentioned the other, nor had [Sorcha Lichata](src/characters/sorcha.md), the itinerant healer who holds the name unqualified.

  **Dermot's ruling: coincidence**, and each page now carries the note in the pattern [Galen Kane](src/characters/galen-kane.md) already set — *the resemblance is the entire relation*. Nothing joins the Gleann na gCaorach wardenship to the Boirinn Uplands: no descent, no branch of the holding, no contact of any kind. The Lichata note is the one that had to be careful, because her page leaves her origin unestablished **deliberately**, so it states what the coincidence does *and does not* settle — the shared name is not a thread back to either of those worlds, and equally not evidence about where she is from. *A name occurring three times establishes nothing about any of the three.*

- **Fenholt and Vexar were rivals, and the corpus had already said so without noticing** (2026-08-15). To Dermot's ruling: the Directorate's two Assistant Deputy Viceroys were **rival subordinates of the same Deputy Viceroy**. The sweep flagged them for holding the same grade in the same office across the same twenty-one years with neither page aware of the other; what the ruling exposes is that the connection was **already written and load-bearing on both pages**. [Vexar's](src/characters/thalassa-vexar.md) 2594 liaison appointment came from a Deputy Viceroy who noticed her reports *"required almost no reconciliation afterwards"* — and [Fenholt's](src/characters/osric-fenholt.md) were *"routinely resummarized by officers above him."* Those two sentences are opposite sides of one comparison, and nobody had put them together.

  The rivalry is built from that and nothing invented: **he had the figures and she had the room**. He was posted out to the Anchorpoint Cluster and eleven habitats; she was kept at the desk outside the Viceroy's door where the delegations came to her. **It was never a dispute about honesty** — neither ever accused the other of filing anything untrue, and neither had grounds to. It was a dispute about what a report is *for*: he held that one exists to be true and what happens to it afterwards is not the compiler's business; she held that a true report nobody can use has not been filed but stored. Her objection to him is arithmetical — nineteen years of unamended figures and not one Belt condition changed while they were current. His, never stated to her and inferable only from what he did, is that figures reconcile easily when the people supplying them have already worked out what can be heard.

  **Both pages' claims to distinction survive intact, because the two were answering to different clocks.** Vexar's method produced something usable immediately that expired with the Imperium; Fenholt's produced something useless at the time and is the reason anyone can now check hers. The uncomfortable half is hers, and her page already carried it before this: the Reform Movement's successors found their cleanest material in his untouched columns and some of their best in her reports, and had reservations about only one of the two sources. Neither wrote about the other, and **the rivalry survives only in a Deputy Viceroy's own filings**, where the two names alternate according to what the office was about to be asked. His reviewing officers changed several times across those years and her desk never moved once — which is why that office is the only place both names appear together.

- **Thalassa and Voss take the same ruling** (2026-08-15). The remaining two collisions from the sweep, both coincidence, both now stated on each side.

  **Thalassa is a person and a world.** It was the corpus's only character-name/lore-title collision — [Thalassa Vexar](src/characters/thalassa-vexar.md), the Imperium Belt officer, and [Thalassa](src/lore/planets/thalassa.md), the ocean world — and both pages landed on the same day, 2026-08-13, neither mentioning the other. The dates do the work here: **she died in 2619 UCSD, generations before that world's colony charter**, so the note on the world's page can say outright that it is not named for her. *A sea-name on a planet ninety-six percent open water requires no commemoration to explain it.*

  **The two Vosses have a second layer**, which is the reason it was worth stating rather than leaving: [Dagny Voss](src/characters/dagny-voss.md) runs a refinery habitat at Aspenar under a Federation support compact, [Governor Petra Voss](src/characters/petra-voss.md) presides over five habitats in Earth orbit under the Compact, and **both hold orbital posts** — the only other thing the names share. Different polities, no kinship in any record, no dealings with each other.

- **Two further refusals on `what-the-record-refuses.md`** (2026-08-13). **The water world**, refused as a default rather than as a possibility: *undersea* does not mean H₂O, and the danger is not a brief saying water but a brief saying *aquatic* and importing an entire Earth with it — tides, a beach, breathable air above the surface. What a fluid world actually fixes is pressure, density, opacity, thermal behaviour and what will dissolve in it. And **the archetype argument**, which is the objection the cosmology invites: a setting with a Conceptual layer looks like one where *person* might be a template with a shape. **Nothing at any tier of the Cascade specifies a body plan for a mind.** Reaching for a metaphysical reason why minds must be person-shaped is mythification taking the observer's own form and promoting it to a law.

- **`story-bible/species-design.md`** (2026-08-13) — the working ground, including the answer to whether dexterity is required for spacefaring sapience. **Yes; hands no; and it is probably not the bottleneck.** What is required is manipulation finer than the manipulator, and the holding half is the half that gets forgotten — dolphins lack it, elephants have a trunk finer at the tip than a hand and no technology, so a fine manipulator is not sufficient either. High-energy materials processing is the constraint worth using, because unlike manipulation it differs on every world and so generates history rather than anatomy.

### Added

- **`npm test` now fails on byte-identical duplicate images** (2026-08-15). The gate the image pass below wished it had. Nothing in the toolchain could see a duplicate: `checkOrphanImages` asks only whether a file is *referenced*, and both halves of a duplicate pair are, which is why three pairs — six files, three pictures — sat in `src/images/` until a manual `md5sum` sweep went looking. `scripts/validate-content.js` now hashes every image and reports any group of two or more, naming all the paths and the fix.

  **The cost was never the wasted bytes.** A duplicate hides that a page has no image of its own: `frontier-transformation-protocols.jpg` looked like a bespoke illustration and was a second copy of the command-hierarchy photo, so that gap never appeared in any queue. `story-bible/images.md` had recorded two of the three pairs and gone stale on the rest — which is exactly the job to hand to a gate rather than a note.

  **There is deliberately no allowlist**, unlike `KNOWN_UNREFERENCED_IMAGES` next to it, because a duplicate is always avoidable: sharing one picture across two pages takes **one file and two references**, never two files. Where the two pages are in different directories, the route is an explicit `<img src>` in the page body rather than an `image:` field — `lore-entry.njk` hardcodes `/images/lore/`, so `image:` cannot reach across into `hero/`, while a body reference can reach the other way. That asymmetry is the thing to reach for before a second copy ever looks necessary. Verified by injecting a duplicate: exit code 1, both paths named.

### Fixed

- **Alt-text pass: thirty-five rewritten, every one checked against the image** (2026-08-15). Run after the FTL card's stale alt turned up incidentally. Every alt flagged by any of four tests — outdated by an image swap, placeholder language, under 45 characters, or missing — was opened alongside its image and rewritten from what is actually in the frame. **The audit now reports zero short, zero missing and zero absent alts**, and the eleven remaining placeholder-worded alts are correct, because those cards really are placeholders.

  **Eight lore cards were upgraded from plain placeholders to finished designed cards on 2026-08-12 and their alts never followed.** Each still claimed a heading of *ILLUSTRATION PENDING*, a dark blue-black gradient, and that no illustration for the entry existed — all three false of all eight. The cards carry a category header, a line-drawn motif in a soft glow, a title, a subtitle and a strapline, and four of them are amber, sepia or violet rather than blue-black. Rewritten from the images: [Chthonari](src/lore/chthonari.md), [Civilisation Comparison](src/lore/civilisation-comparison.md), [Lagrange Points](src/lore/lagrange-fold-points.md), [Meta-Dimensional Beings](src/lore/meta-dimensional-beings.md), [Military Space Command](src/lore/military-space-command.md), [Mnemari](src/lore/mnemari.md), [Ynys Wydrin](src/lore/planets/ynys-wydrin.md), [Solar Time and Local Calendars](src/lore/solar-time-and-local-calendars.md). **The eleven character cards flagged by the same comparison are correct** and were left alone — those really are *PORTRAIT PENDING* placeholders carrying a name and role, verified against the files.

  **The remaining five are the class the comparison cannot see: alts that were never right.** [year-zero](src/lore/year-zero.md) read *"steampunk gears on a wooden table"* for an embossed metal relief panel with no table in it. **Three different images shared one identical alt** — *"the interior of an empty parliament chamber"* — and none of the three is a view of a chamber: all are tight close-ups of a delegate's electronic voting console with the chamber thrown out of focus behind it. And [formation-of-star-rangers](src/lore/formation-of-star-rangers.md) called a present-day skyline given a sepia treatment *"a vintage view"*.

  **The terse alts turned out to hide four more outright errors, not just thin writing.** [federation-of-sentient-beings](src/lore/federation-of-sentient-beings.md) said *"a cargo spacecraft and earth"* for an uncrewed instrument satellite — no cargo vessel, no crew. [competing-designs](src/lore/worldwrights/competing-designs.md) said *"a sci-fi blue led futuristic tunnel"* for a flat concentric-ring dial with no depth in it at all. And **two pages called the same image a black hole**, which it is not: it is a bright ring of light around a dark disc scattered with specks, closer to a lit dish shot from above. The remaining nineteen were accurate but useless to anyone who cannot see the image — *"a space galaxy background"*, *"an open old book"* — and now say what is in the frame.

  **Two images removed on Dermot's ruling.** The pass surfaced two stock images carrying real-world specifics of the kind this repo has pulled images for before, and he ruled they do not belong. Both are **deleted rather than carded**, following the eight verified stock images of 11 August — a lore page renders cleanly with no image, and absence beats a picture quietly making a claim. `formation-of-star-rangers.jpg` was a **recognisable real city**, One World Trade Center's spire unmistakable under the sepia, standing in for the founding of the corps. `universal-cosmic-stardate.jpg` was an antique dial engraved **in French with a fleur-de-lis border**, on the entry for a count whose whole argument is that it *"commemorates no founding … a spine that honoured an event would belong to whoever owned the event"* — an image that hands the standard to one nation's heraldry. Recorded in `story-bible/images.md` as Batch 3, since neither carries a PENDING card and so neither appears in the placeholder queue. **Worth noting what the July audit did and did not ask**: it inspected `formation-of-star-rangers` and passed it, correctly, on the question it was asking — whether the alt matched the image. Nobody asked whether the image belonged.

  **Three duplicate image assets, deduplicated on Dermot's ruling.** Nothing in the toolchain catches a byte-identical copy — `validate-content.js` only flags images *no* page references — so a full MD5 scan of `src/images/` was run, and it found **one more pair than the pass had**: `hero/glossary-book.jpg` was identical to `lore/canonical-glossary-and-migration-guide.jpg`, alongside the known `frontier-transformation-protocols.jpg` / `star-rangers-command-hierarchy.jpg` and `post-eleven-dimensional-manifold.jpg` / `threnos-omega.jpg`. **The scan now returns no duplicate group at all.**

  The two lore pairs followed `story-bible/images.md`'s own long-standing "Kept by" column rather than a fresh judgement, exactly as that note anticipated (*"which page keeps it is Dermot's call; the other needs a prompt"*) — and **one of the two was then flipped on his ruling**: **Threnos-Ω keeps the ring**, because the same note already calls that file *"the precedent for a membrane portrait"* on that page specifically, so the image had standing there and the old "Kept by" column was pointing the wrong way. [The Common Manifold](src/lore/post-eleven-dimensional-manifold.md) and [Frontier Transformation Protocols](src/lore/frontier-transformation-protocols.md) are the two pages now image-less and recorded as needing one. The Manifold's brief is the harder of the two and the note says why: it has to carry a **shared substrate rather than a boundary**, so a ring or a membrane is precisely the wrong figure for it. The glossary pair resolved differently, because both *uses* are legitimate — a section hero and a lore entry's image — so only the redundant copy went, and `src/glossary/index.md` now points at the lore file. **The direction was forced rather than chosen:** `lore-entry.njk` hardcodes `/images/lore/`, so an `image:` field cannot reach into `hero/`, while an explicit `<img src>` in a page body can reach the other way.

  **That note was also badly stale**, which is worth more than the fix: it listed five duplicate groups, and three had been quietly resolved by earlier work without anyone updating it. The table is now marked RESOLVED with each outcome recorded.

- **Two duplicated page titles, and the Safety Corps' own founding arithmetic** (2026-08-15). Both found by a corpus-wide sweep for date errors and same-name-different-era collisions, run after the Aoife/Daire correction below.

  **`check-related-terms.js` had been warning about two duplicated titles** and nothing had acted on it. *Allocation Units of Cognition (AUC)* and *FTL Mechanics* each existed as both a glossary entry and a lore entry with a byte-identical `title`, and `glossaryUrl` searches the glossary collection before the lore one — so four `related:` terms across the corpus resolved to the glossary stub and the fuller lore page was unreachable by name. The lore titles are now differentiated, which is what the other seven glossary/lore pairs already do (*Archecluster* / *Inside an Archecluster*, *Krenyi* / *Krenyi (Quiet-Built)*, *Plainmark* / *The Plainmark*): [FTL Mechanics: Channels and Their Constraints](src/lore/ftl-mechanics.md) — taking the subtitle already printed on its own title card — and [Allocation Units of Cognition: Cognition as Scarce Infrastructure](src/lore/ai-usage-credits.md), from that page's own opening line. **Retitling the lore side rather than the glossary side is the safe direction**: every one of those four `related:` terms already resolved to the glossary page and still does, so nothing that currently works changes.

  **The Safety Corps page dated itself two ways a century apart.** It says its operational culture *"predates the Star Rangers by nearly seven centuries"* and, twelve lines later, that its mechanism is *"unchanged in seven centuries."* Both can't be seven. The founding anchor is Underwriters Laboratories, **Chicago 1894 CE = 2094 UCSD**; the charter is **2723 UCSD**, so that gap is **629 years — six centuries and change**, while the gap to the present (2626 CE) is 732 and genuinely is seven. The first now reads *more than six centuries*; the second is correct as written and stands.

  **One stale `image_alt` fell out of the retitle.** `ftl-mechanics.jpg` was described as a placeholder *"headed ILLUSTRATION PENDING"* with *"no illustration for this entry"* — the file is in fact a finished title card: PHYSICS in spaced capitals, a line-drawn wave between two circles, FTL MECHANICS under a rule with two subtitles. Rewritten to what the image actually shows. The sweep also found the AUC entry's *"blockchain network"* alt describes circuit traces and falling binary digits, which is loose rather than wrong, and is left alone.

- **The Aoife/Daire gap is fourteen centuries, not six, eight or nine** (2026-08-15). Surfaced by writing the timeline entry above, which cannot state an interval the corpus disagrees with itself about. Twenty-two phrasings across ten files gave the distance from Brother Daire's 1216 AD manuscript to the survey era as **six, eight, or nine centuries**. `UCSD = CE + 200` is arithmetic rather than a conversion table, so 2826 UCSD is 2626 CE and the gap is **1,410 years** — and every stated figure is what you would write standing in the twenty-first century, since eight centuries after 1216 is 2016 and nine is 2116. The canonical glossary's own dating rule decides which end moves (*an inconsistency between two dates is a mistake to fix*), and 1216 is load-bearing everywhere — thirteenth-century, monastic, medieval copying — so the intervals are wrong and not the date. **One figure is not fourteen**: the Currach Fleet's founding scriptorium carried the transcription off Earth in 2561–2589 UCSD, which is 2361–2389 CE, so the Celtic Union picks the devotion up **eleven** centuries after Daire wrote it, not six.

  The sweep reaches [three published S07E01 chapters](src/seasons/s07/e01/s07e01c01.md) including a `canon_facts` line, which is why that half is a separate commit. Nothing in those passages changes but the number: what Aoife met, and whether it held any standing in the Cascade, stays exactly as open as she was careful to leave it. **Two figures were left alone deliberately** — *"remember for a thousand years"* in the lore entry, which is idiom rather than an interval between two dated things, and the Fellowship's *"said Overfold for eight centuries"*, which is a claim about the Fellowship's own vocabulary and not about Daire.

- **Krast's arithmetic: born 2665, dead 2720, and she never saw the Coherence Wars** (2026-08-13). Her page dated the Outer Stations Consolidation Hearing to **2689** and her death to **2714**, against **2719** in the MSC lore, the founding lore, the timeline entry and S00E02C02's `canon_facts` — so the record had her dying five years before delivering the address the page is built around. Chasing that surfaced a larger one the death date had been hiding. S00E02C02 had her say *"I was a junior officer during the last four years of the Coherence Wars"*; the wars ended in **2543**, which put her at roughly **two hundred** in that chamber, against a ~125 cap established the day before in 1.21.0. **No birth year saves the line** — any Krast who fought in the wars is dead a century before the hearing convenes. She is now **born 2665, commissioned 2689** (which makes the chapter's own twice-stated "thirty years of service" exact to the day) and **dead in 2720 at fifty-five**, a few months after the hearing rose. Early for her century; the page says so and declines to say more, because the file doesn't. The war line is reworked rather than cut, and comes back stronger: she now says plainly that she never saw the wars, that nobody in the chamber did, that she was taught them by officers who were taught by the men who ended them — *"and the figures do not change in the handing over."* The figures do not. A judgement handed over with them arrives looking like a figure, which is the thing she had been holding for thirty years without ever having had occasion to check. Nothing in the prose points at it. Two knock-ons swept: her opening no longer has her spending "the rest of her career" on a discovery she did not live to make, and she is now correctly a **witness** at the hearing rather than seated on its panel, which is what the chapter always showed. [Drey's](src/characters/sildron-drey.md) page loses the line making her Krast's junior — under the new dates Drey is three years older and commissioned five years earlier, so the two are contemporaries and not intimates, and Drey's account is that she arrived at the technocratic case herself and would have held it had Krast never existed.

- **The Kingdom of the Four Islands page no longer contradicts itself about how old first contact is** (2026-08-13). Line 30 said Survey Corps contact was "made only in the last several years"; line 80, fifty lines below it in the same file, said diplomatic relations "remain days old rather than years." The *years* reading is the one the rest of the corpus is built on — `what-the-record-refuses.md` twice, `star-rangers-academy.md`'s Xenolinguistics case study, and the story bible — and it is load-bearing, since a **standing pedagogical case study** about failures of patience only exists if the case has run long enough to show a pattern. The outlier clause is rewritten to match: relations are counted in years of translation work and in almost nothing else. Both claims were born in the same 2026-08-10 commit, which landed the page silently inside an image-consolidation PR with no changelog entry, which is how a page came to disagree with itself for three days without anyone reading the two lines together.

## [1.21.0] — 2026-08-13

The release where the setting learned how long its people live, and the record found out it had been describing several institutions it had never named. **Human lifespan is capped at about 125 years**, healthy into the eleventh decade — which sounds like flavour and is in fact the constraint that decides where backstory can be set, because the published seasons and the Founding Era are a hundred and three years apart and no human career reaches across that. It relocated a chapter the same day it was written. The rescue of the orphans who become Karla Wender, Andrew Falkorian and Anne Wender was drafted into the Founding Era, moved to **Year -38 (2788 UCSD)** when the arithmetic made Wender a hundred-and-fourteen-year-old Chief Pilot, and now sits in Season 1's prequel episode beside the Dún Rí chapters. Around it, four institutional facts arrived in one conversation and every one of them turned out to be describing something the corpus already did: the Corps' career structure has **three ladders**, not two; **the Academy stops at Chief** and formal mentoring takes over, which is what Wender had already been doing for Shepherd unassigned since her third year as a Senior; a **stress-related injury is an injury**, which is the policy behind a Season 5 chapter that opens with Shepherd "back on duty" and never explains where she had been; and the **Xenolinguistics Branch** of the Survey Corps had its entire doctrine written across three pages with nowhere to live. Also the first house style guide for the prose, derived by counting the corpus rather than prescribing to it — 175 instances of "does not", 611 em-dashes, and only 26 questions in 70,277 words.

### Added

- **`story-bible/prose-style.md` — the house style, measured rather than asserted** (2026-08-13). Answering "do you have high level guidelines for the style and tone of the narrative prose, using the existing prose as a guide?" by reading the corpus instead of describing it from memory. Every claim that can be counted is counted: **175** instances of "does not"/"did not" across 70,277 words (negation is the primary characterisation tool — people are defined by what they decline), **84** "which is/was" explanatory hinges, **611** em-dashes at roughly one every 115 words, a median paragraph of 12 words against a longest of 120, and **26** questions against 207 dialogue-opening lines, which is why the arguments read as competent rather than probing. The load-bearing finding is that the register is *filed, not narrated* — and that this is what keeps a universe containing cyber-revenants and dead children from tipping into horror. A scene that starts performing its darkness has left the style. Also records the one habit hardest to imitate, in the intake's own words: *"Let a reader find that. Do not let a character point at it."*

- **S01E00C04, *A Person's Name*** (2026-08-13) — three machines reach a ship that no longer answers, and within minutes of the carrying, one of the children makes a name only she will ever use. Three POV blocks: Anne on the ninth day of the wreck, Andrew four decks down in Xenon's bay, Anne arriving at Eden. The lost machines are never stated — Anne counts eight small service units out through the door port and five back, and "gave the two numbers to nobody" — and Andrew, asking Xenon what the empty cradles are for, gets *"They are the right size for the machines that go out first"* and then does not ask the next question, because "some instinct that had grown in him over nine days told him which questions had floors under them." Karla's naming of Lucene-9000 is deliberate rather than a slip: a pause first, and watching the machine's face "the way you watch a door you are deciding to walk through." Anne hears her own name inside *Lucy Anne*, notices it "the way you notice a draught: once, completely, and without the first idea of what to do about it," and calls her Lucene for the rest of her life without ever saying why.

- **Character pages for [Andrew Falkorian](src/characters/andrew-falkorian.md), [Xenon](src/characters/xenon.md) and [Lumbarian](src/characters/lumbarian.md)** (2026-08-13). Falkorian's refuses the label he refuses — the page establishes nothing about what he is and states that the record's silence follows his lead, giving him no stereotype-coded traits so both readings stay genuinely live. Xenon's carries the grief three ways and articulates it none: he is not permitted to create new robots, and "maintains the ship's machines as if each were the last of its kind. In several cases, this is simply accurate." Lumbarian's renders his standing entirely through behaviour — patted housings, visitors shown him, respected in reports and adored on deck — and never uses the words that would explain it.

- **`src/lore/star-rangers-academy.md` — one academy, a college per corps, a school per branch** (2026-08-13). Proposed as a possible fourth ladder and written as something better: training was *already* guild property (`star-rangers-command-hierarchy.md` had said for months that the command ladder has no input into "guild and training functions"), so the Academy is the largest instance of a rule the repo had already stated and never cashed out. The consequence, in its strongest form: **the Corps' most senior officers cannot change what a certification means.** A High Captain who thinks a boundary-analyst standard is set too high may argue it in front of the Safety Corps faculty like anyone else, and may lose — which is what makes the lower ladder's promise of being *documented, examined and appealable* worth anything, since an appeal heard by the same chain that made the decision is not an appeal. Instruction is a **posting held by serving officers**, never a career track, because a faculty of career instructors promoted by other career instructors would within a generation be certifying field competence it had not recently held; the corps pay for that in syllabus continuity and consider it cheap. **The branch list is open and stated on the page as open** — a branch the record has not described is not thereby absent. Card generated by the committed emblem generator (`rings`, not `shield`: the Safety Corps card is already a shield in a ringed seal, and two shields in one institutional family read as one organisation).

- **The Xenolinguistics Branch, Survey Corps — the first named branch** (2026-08-13). Its doctrine was already complete and homeless, spread across `what-the-record-refuses.md`, `cerebraun.md` and the Four Islands material. The universal translator is refused outright; language is learned, by xenolinguists with AI support, on a division of labour the school exists to enforce — the intelligence aligns corpora and never loses a morpheme, and the linguist decides what a word *means* and **owns the error**, because a kernel-compliant mind will not claim a certainty about meaning it does not hold and hands over candidate readings with confidence stated, "never a voice in your ear pretending to be the alien." Both of its standing case studies were already on the site and are both failures of patience rather than method: the Cerebraun, whose three-part answers were filed as evasion for a decade before anyone admitted that is simply how the species thinks; and the Four Islands delegation, still working a presumed-extinct language under an abbess's slow correction.

- **Formal mentoring above Chief, and Wender as Shepherd's official mentor** (2026-08-13). The Academy stops at the hinge rank because certification is a claim other people can check and command judgement is only ever verified by its consequences, usually too late to instruct. So the mechanism changes: on promotion to Starwarden an officer is paired with a senior officer answerable for the mentorship **as a duty rather than offering it as a kindness** — deliberately, because command wisdom passed along by whoever takes an interest reliably advantages the already well-connected, which is the failure the examined lower ladder exists to prevent. The policy formalises something S05E02C02 had already dramatized: Wender had been reading Shepherd's silences since the woman's third year as a Senior, none of it assigned. The pairing puts both crossings of the hinge rank side by side — Wender came to command from the technical ceiling, Shepherd because rotating through everything left her no other ladder — and the hardest clause is the one that chapter demonstrates: **the mentorship gives a senior officer no standing to ask questions the command relationship would not already justify.** Shepherd's page keeps [Galen Kane](src/characters/galen-kane.md) alongside, and the two are not redundant: one adviser she is required to take seriously, one whose judgement costs her nothing to hear.

- **Leaving and coming back: early retirement, career breaks, extended sabbaticals** (2026-08-13). Negotiated rather than granted, because no schedule written in advance could anticipate what a particular Ranger's service has cost them. **A stress-related injury is an injury** — an institution that certifies competence for a living has never found it coherent to rule a person unfit and then treat the finding as a character judgement. On medical ground the Medical Corps' finding binds command, on the same guild-independence rule that stops a Starwarden revising a boundary standard. Two consequences stated: a record with a gap in it is an ordinary record, and coming back is not resumption — the ones who have done it generally say the first ordinary morning back was harder than anything in the incident that sent them away. This also closes an open question the day's other arithmetic had raised, since Brother Fintan's thirty-one years and departure before pension age is now a documented route rather than an anomaly.

- **The three-stage career structure past active service, and planetary representation as a third ladder** (2026-08-13). Representative standing opens at about **55**; from 55 to about **75** the officer is on the **reserve list** — left active service, not retired, still recallable; full pension is available from **75**, at the Ranger's own choosing. And it is a genuine non-command career path with its own progression — deepening specializations, certifications and decorations in place of promotion — which is what makes a High Captain and a Chief who transfer to the same world start level: not because the standing is honorary, but because the path has its own ladder and rank does not transfer onto it. The reserve stage sharpens a tension `planetary-liaisons-and-recruiters.md` already raised: under 75 she is not a private citizen with a service record, she is **a serving Ranger living at home, on a world the charter forbids the Corps to enter.** Nobody in-universe has made that argument formally.

- **The Krenyi present tense, and the gap that is not a withheld answer** (2026-08-13). The Quiet-Built attend to what is here and what is coming; the past is neither sacred nor suppressed, simply not where they look unless looking returns something usable — and most of what they are known for falls out of that single orientation, including grief that resolves and a state-marker meaning *still relevant here*. The page also names the cost, which they would name themselves: **they are poor keepers of anything whose value has not yet been demonstrated.** Alongside it, a third standing editorial rule in the story bible for a case the existing spoiler rule cannot cover — **some gaps are the answer and never resolve.** The Krenyi origin has no reveal behind it, no chapter discloses it, and no future session should treat the empty entry as a to-do; an unexplained gap reads to any competent drafter as an unfinished job, and that instinct is the failure mode. The test for spotting another one: if a page argues for *why* a gap exists, the gap is load-bearing.

- **Galahad Thorne left a difficult marriage before the Threshold posting** (2026-08-13). Written to the cast principles rather than around them: **there is no version of the account in which someone was a villain**, and he does not offer one. The twelve years were long frontier missions — six stations, years at a time — and a marriage conducted across that distance is a thing two people either build deliberately or lose by degrees while both of them are being reasonable. He does not describe her at all, which, given how precisely he describes everything else, is the most deliberate silence on his record. He treats it as neither a wound nor a lesson, which are the two things people usually make of a marriage that ended; he treats it as something that happened and finished. The page names the tempting inference and refuses it — that this is what made him ready for Elvira. He was not made ready by it. He arrived at the Marsh Causeway as a man who had already found out, once, what it costs to be away.

- **Asteria mentored Karla Wender, and the dates permit exactly one version of it** (2026-08-13). Asked with the condition attached — *"if the timelines fit"* — so they were checked rather than assumed. Asteria was already long retired when Galahad Thorne met her on a frontier posting inside his twelve unbroken Survey Corps years, which puts her departure in the early 2820s at the latest, at about a hundred, holding Star Captain. Wender was climbing the certification ladder through precisely that period: a Novice around the turn of the century, a Chief by the early 2820s, and not a Starwarden until after Season 1 opens. **The two careers overlap for the whole of Wender's ascent and not for a day of her command** — so Asteria was never her assigned mentor, because the formal pairing begins at Starwarden and by then Asteria was on a quiet planet running a chapter house. What she gave, she gave to a junior officer on the examined half of the ladder with no policy requiring it and nothing on either record to show for it. That closes a loop the corpus had already half-drawn: Wender now holds the *assigned* version of the duty for Shepherd, and had been discharging it unassigned for years before anyone wrote it down — which is not what the policy asks for and is exactly what she was taught. Asteria's two mentorships now bracket her retirement, one on either side, neither ever an assignment.

- **The Liaison Corps has a name, a second route in, and a ceiling** (2026-08-13). Planetary representation is a **specialist corps** — the Star Ranger Liaison Corps, whose members are **Planetary Representatives** — and its people are serving Rangers on a posting, paid by the Corps, not veterans on an honorarium. The nearest civilian comparison is a police officer moved off dangerous front-line duty and kept on the force, which is also its **second route in**: an injury that does not mend, a condition that will not certify, or a stress injury ends boundary-proximate work for people, and this is where the Corps puts them rather than losing them to a medical finding. Deliberately not a soft landing — the work is needed, hard in its own way, and performed under the officer's own name on a world that judges her by it; treating a liaison posting as somewhere people are put out to is the kind of remark seniors are expected to have grown out of. Being a corps, it elects its own chapter leaders and president, holds its own certification standards, and runs its own Academy college. The entry is retitled from *Retired Rangers: Liaisons, Representatives, and Recruiters* — the word *retired* was wrong in both directions, since they are not retired for the first twenty years and many never retire at all — and the **URL is deliberately unchanged** so that nothing linking there breaks; all nine inbound references are updated to the new name.

- **Corrected: an officer leaving at Star Captain or above cannot join the Liaison Corps** (2026-08-13). Earlier the same day this changelog and two lore pages claimed that "a High Captain and a Chief who take it up on the same world start at the same standing." That is wrong. The corps' senior post is **Chief Liaison Officer** — a Chief's duty title beside Chief Ranger, Chief Pilot, Chief Provost and Chief Science Officer — which places the whole corps on the certification half of the ladder and gives it a real upper bound: **a flag-tier officer is over-ranked for a Chief's berth**, and the Corps declines to invent a grander title to accommodate a large career, because a post inflated for an individual is how a ladder stops meaning anything. So the third path absorbs a great deal of the Corps and is closed at the top of it. Below the bound the original point survives: two Chiefs on the same world start level, and what separates them later was earned in front of that world. [Asteria](src/characters/asteria-the-sage.md) is the worked example of the closed door — she left at Star Captain for a Fellowship of Light chapter house, an institution the Rangers do not run, whose standing rests on having renounced the interests everyone else keeps. Her page needed no change of rank, only the naming of the route she did not take.

- **The maximum retirement age is about 110, and it is the one age nobody negotiates** (2026-08-13). Three ages now bound a career: the Liaison Corps floor at about **55**, a full pension available at about **75** by choice, and a hard stop at about **110** — set where the eleventh decade's borrowed capability runs out, and deliberately *below* the ~125 lifespan ceiling, so a Ranger who serves to the limit still has years in front of her and the Corps simply stops being where she spends them. The stretch between 75 and 110 is the longest optional period in the service and officers use it: Asteria left at about a hundred, a quarter of a century after a full pension first became hers to take.

- **Two more refused tropes: the interfertile alien, and the safe cross-species transfusion** (2026-08-13). `what-the-record-refuses.md` gains a **Bodies and Species** section and two summary-table rows. **No sapient species in this record crosses with another** — no half-human hybrids, no children of two peoples, no breakthrough pending. Not squeamishness: two lineages sharing no common ancestor share no chromosome count, no gene regulation, no developmental sequence, and nothing a gamete could do with another gamete. A trope that treats reproductive compatibility as the default outcome of two people liking each other is not being romantic about biology; it is declining to have any. What the refusal costs is a stock plot and what it buys is better — contact stories that have to find something else for two peoples to make together, of which the Four Islands relationship (years of translation, one standing delegation, nothing else) is already the worked example. The corollary: **cross-species attraction, where it happens, is a fact about a person and not a plot mechanism.** It resolves nothing and bridges nothing, and the universe does not rearrange itself to accommodate it.

- **The Krenyi are demisexual, and their immunity is why the transfusion question has a wrong intuition** (2026-08-13). Attraction arises only through an established bond — the whole species, not a distribution within it — and the page's existing "no flirtation or small talk, straight to material terms" turns out to have been describing that without the word: a Krenyi is not declining to flirt, there is simply no stage of a Krenyi relationship at which flirting would be doing anything. **They do not form romantic attachments to humans and will not consent to interbreed**, stated once and not elaborated, because *why not* asked of a Krenyi is very often the same question as *what are you, really*. Humans do form attachments the other way, and a Krenyi meeting one is generally meeting the mythification problem in its most personal register — a person wanting the Quiet-Built rather than wanting *them*. On the biology, the asymmetry is the part that costs: **Krenyi immunity destroys foreign genetic material outright**, so human DNA entering a Krenyi never persists, which is why "cannot interbreed with any other species" is a flat fact rather than an untested assumption — and running the other way, **Krenyi blood is hazardous to a human**, because transfused antibodies meet a body composed entirely of foreign genetic material and do exactly what they exist to do. The human survives and is seriously unwell for several days at minimum. The clinical file exists because somebody once reasoned, not unreasonably from a species that is honest and rarely sick, that Krenyi blood would do in a shortage.

- **Journal: *The Wrong Century*** (2026-08-13). On putting a chapter a hundred and three years from where it belonged, and being caught not by proofreading but by an unrelated sentence written hours later — the argument for constraints as a machine that checks what you have already written, for free, including the parts you were not thinking about. The entry's spine is the sceptical turn rather than the anecdote: when four further decisions all turned out to be anticipated by existing text, that feeling is *exactly what confirmation bias feels like from the inside*, since a large enough corpus appears to anticipate almost any rule. So it sorts them by whether the earlier writing **predicted** the new rule or merely **permitted** it — a clause that fits a dozen arrangements equally well confirms nothing, while a chapter that opens "back on duty" and never explains where she had been is a hole with exactly one shape. Two predictions and two permissions out of four, and the honest count rather than the flattering one. It closes on **diligence rather than bookkeeping** — bookkeeping being what you do to an account after the fact, diligence a way of working — and on the **harmonic balance between creativity and correctness**, which are not two budgets competing for the same hours: a world too loose to contradict you offers not freedom but the absence of resistance, so nothing pushes back and nothing is discovered, while a world held too tight admits nothing new. In balance, the correctness is not the price of the creativity but the thing that makes it load-bearing.

- **A failed `cpanel-autopull.sh` run now emails `ADMIN_EMAIL` instead of disappearing** (2026-08-13). The question that prompted this was "the photo site autopull should have run at 2am" — and answering it meant reading a deploy email, because nothing else could say whether the run had happened. The run had, and had succeeded; the point is that a *failed* run would have looked exactly the same. `err()` writes to stderr, and cron delivers stderr to the cPanel account's own system mailbox rather than to `ADMIN_EMAIL`, so "nothing to do" and "the pull failed" were both silence. Every deploy outcome was already mailed properly by `cpanel-deploy.sh`; the blind spot was precisely the window before the handoff, the one stretch with no mailer wired up. Exits 1 and 3 now mail directly, with the failure, the checkout's state and the tail of the decision log. **Exit 2 deliberately does not** — on a ten-minute cron a multi-domain deploy routinely outlasts the interval, so a held lock is the expected state, and mailing it would train the alert to be ignored, which is the failure this exists to prevent. No send is ever allowed to change an exit code.

- **`scripts/mail-lib.sh`: the mail transport, extracted so it has exactly one copy** (2026-08-13). The `mail(1)` → `/usr/sbin/sendmail` fallback lived inside `deploy-lib.sh`, which autopull cannot use: sourcing `deploy-lib.sh` runs `mktemp` and `git describe` as import side effects, and autopull sources its libraries on *every* cron run, including the overwhelmingly common one that finds nothing and exits in under a second. So the new file is side-effect-free on source, and that property is load-bearing rather than tidiness. `deploy_lib_notify()` now calls it and keeps its subject format and `MAIL_OK` bookkeeping unchanged. Two copies of a mailer is how one of them silently stops working.

- **`AUTOPULL_MAX_GAP_HOURS` (optional, `deploy.conf`) and a `lastrun` stamp** (2026-08-13). Every run now records that it *fired* — including runs that find nothing and runs that exit on a held lock — because "is cron still firing?" is a different question from "what is deployed?", and only the first can tell you the schedule itself has stopped. `--status` prints both, plus the resolved notification address. Set the new key and a run finding the previous one longer ago than that mails a "runs were missed" alert; unset by default and deliberately so, since the interval lives in cPanel → Cron Jobs and the repo has no basis for guessing one. Its limit is stated in the script, the sample config and the docs rather than left to be discovered: **it can only report a gap on the next run that actually happens.** A cron that stops firing altogether cannot report its own absence, and nothing in this repo can change that.

### Changed

- **Human lifespan is capped at about 125 years, and it is a continuity constraint rather than a flavour note** (2026-08-13). Only the Krenyi, the robots and some aliens are long-lived; medical technology and a fitness culture hold off the quality-of-life effects of ageing into the eleventh decade, so people are not merely alive past a hundred but *capable* past a hundred — a long competence followed by a short decline, and centenarians should not be written with the frailty shorthand a contemporary reader expects. The consequence is arithmetic: the published seasons sit in 2822–2832, the Founding Era in 2712–2723, and **no human can carry a career across both.** A witness to both eras is barely possible at 114, is expensive to write, and has not been used. The story bible carries the check — subtract from 2826, add the age at the time; under about 105 is fine and they can still be working, over 125 is impossible, and between the two the story owes the reader a reason they are still on the page.

- **The rescue moved from the Founding Era to Year -38 (2788 UCSD)** (2026-08-13), drafted and relocated the same day. The Founding Era placement failed on the rule above — a 2717 rescue would have made Karla Wender a hundred-and-fourteen-year-old Chief Pilot — and the replacement year is not arbitrary either: Wender is a small child in the chapter and a Chief Pilot at Season 1's start, having climbed all eight rungs of the piloting track without once rotating off it, and thirty-eight years is what that ladder costs. The chapter now uses Season 1's established prequel convention (`Year -N (YYYY UCSD)`, as in the Dún Rí chapters at Year -4), the `s00/e04/` slot created for the first placement is gone, and the episode index covers both distances. Working ages at the wreck are stated nowhere on the page and are adjustable: Anne about fifteen, Andrew about eleven, Karla about five. The machines are the ones who span real time, which sits comfortably under Lucene's own page and its "continuous service longer than most careers last."

- **`planetary-liaisons-and-recruiters.md` said outright that this was "not a fourth ladder," and it was wrong twice** (2026-08-13). Wrong about the timing — the role opens at about 55, two decades before the pension, and the first twenty years of it are performed on the reserve list by people who are not civilians — and wrong about the kind of thing it is, since planetary representation is a full non-command career path with its own progression. Both corrected in place, additively; `star-rangers-command-hierarchy.md` gained a matching section, since it is the page that enumerates career structure and still said "the two paths." The page's own title still leans on the word *retired*, which its opening line now has to apologise for; whether to retitle it is deliberately left open.

- **Two intake notes that the day's canon overtook** (2026-08-13). §3's "about 150 years of room" is right about Eden's age and was never the window available to these particular children, and is marked superseded in part. §7's standing status note — undecided whether the rescue was backstory or a story in itself, with everything about it held under a reserved-story posture — is resolved: it is a story, it is written, and its flagged dramatization assets are spent *in the chapter* and should not be re-spent in summary anywhere else. What stays reserved is stated explicitly: the loss of the machines is hinted only, and Lucene's own past keeps its standing withholding rule.

### Fixed

- **`scripts/cpanel-autopull.sh` was not byte-identical between the two repos, despite three files saying it must be** (2026-08-13). One copy's install comment said "README" and the other's "TECHNICAL-README.md" — each correct locally, which is exactly how a shared file comes apart. Both now say "the repo's deployment README", the header forbids naming either repo's own filenames, and the four shared scripts finally `diff` clean together.

- **The earliest autopull failures were neither logged nor mailed** (2026-08-13). "git not found on PATH", a checkout that isn't a git repository, and a missing `cpanel-deploy.sh` all exited before the state directory and `log()` existed, so they left no trace anywhere. The state paths are pure string operations and cannot fail, so they now come first and every named fault routes through one `fail()` that reports, logs, mails and exits.

- **CI shellchecked five scripts while `CLAUDE.md` claimed two** (2026-08-13). The list had grown three times without the sentence following; `scripts/mail-lib.sh` is added to the CI line in both repos and both mentions in `CLAUDE.md` now name the real set, along with the reminder that four of the five are shared files needing the identical edit in the sibling repo.

## [1.20.0] — 2026-08-12

The release where the audit ran. Two days after an essay admitted that roughly half this site's illustrated pages were contemporary stock photography and that about twenty were photographs of real people standing in for characters, **twenty-eight images are gone**. Nineteen were the Tier 1 problem — a stranger's face captioned as a character, which is a false claim about an actual person and a licensing question nobody had checked. Four of those were on no list, found by opening files rather than reading captions. Nine of the twenty-eight turned out not to want pictures at all: a taxonomy, a comparison of three centuries, two pages of physics and a calendar system are concepts, and asking an image model for a concept gets you a mood, which is how a page enumerating three classes of being came to be illustrated with a hooded figure. Those are designed cards now, drawn by a **committed** generator — the third occurrence of this repo's oldest small failure, images produced by a script that lived in a scratch directory and was never saved, and the last one worth having. Underneath the images, the bookkeeping got teeth: CI now checks that every referenced image exists, that no image is orphaned, and that `images.md` does not name files that were deleted months ago — which it did, twice, and which had already caused a stale audit premise. The pipeline learned to offer Dermot's own photographs before reaching for a generator, and to stop counting a placeholder card as finished work.

### Added

- **Nine lore entries get designed emblem cards, and a generator to draw them** (2026-08-12). `scripts/make-emblem-card.ps1` closes the question `make-lore-cards.ps1`'s header left open in July: the four surviving landscape lore cards "did NOT come from this generator and it cannot reproduce them — how they were made is unrecorded", with the options given as accept the inconsistency, extend the generator, or drop the batch. This is the extension. Ten geometric devices — `sphere`, `dome`, `figure`, `chevron`, `rings`, `lagrange`, `fold`, `sundial`, `triad`, `shield` — chosen geometric because that is what the surviving cards use and because GDI+ draws it exactly every time where an image model cannot spell. Applied to *Mnemari*, *Chthonari*, *Ynys Wydrin*, *Military Space Command*, *Civilisation Comparison*, *Meta-Dimensional Beings*, *FTL Mechanics*, *Lagrange Points* and *Solar Time and Local Calendars*. The two lower lines on each card are authored and the script's header says so: every qualifier here is taken from the page's own `description`, and the epithets are drafts.

- **`scripts/build-photo-catalogue.js` and `image-prompts.js --catalogue`: offer Dermot's own photographs before generating** (2026-08-12). `images.md` Open work 6 tier 3 has argued since July that his own photography should carry far more of this site than it does — an Irish upland standing in for an alien world is his own work and claims nothing false. Nothing could act on it: an entry was `done` when a file existed and `pending` otherwise, and `pending` had exactly one route out. The catalogue reads the sibling photography clone and `Folder Context.md` into `story-bible/own-photography.json` — 99 published photographs and 27 raw camera folders — committed rather than read live, because that clone will not exist on a CI runner. Character portraits are skipped entirely: he photographs places and wildlife and will not publish a recognisable person, so no frame in the catalogue can stand in for a face.

- **Journal: [The Audit I Said Was Coming](src/journal/the-audit-i-said-was-coming.md)** (2026-08-12). The successor to *Where the Record Lives*, which described the problem in the present tense and left the fixing for later. Four findings the earlier essay could not have guessed: that an audit reasoning about filenames produces a plausible inventory and a false sense of completion; that the worst image was not a portrait but a stock photograph *of* genuine family prints, a christening and a first communion, faces legible; that half the rejections were made for the wrong reason, including one cleared on privacy grounds that was slasher iconography and one pulled as a privacy case that was actually a real culture used as scenery; and that nine of the twenty-eight did not want pictures at all. The earlier essay is left exactly as written — editing it into accuracy would destroy the only thing that makes it worth keeping.

- **`images.md` records the aesthetic target, and one thread's exemption from it** (2026-08-12). *Enigmatic and haunting, with beauty, mystery, hope and serenity woven through* — for images and audio alike, stated as the thing to reach for rather than a limit to stay inside, since the existing tone line already says what to avoid. It rules out a register that breaks no prohibition at all: flat documentary lighting, blunt literal description, the merely competent. **Undercover Pets is exempt and is cute, cool and clever** — the contrast is the point, and a haunting cat portrait would be a category error rather than a stylistic variation.

### Changed

- **AIs get portraits like everyone else, superseding the emblem-or-interface rule** (2026-08-12). The rule that AIs and non-corporeal beings got an abstract interface rather than a face is what produced Reeves as "A laptop displaying a face-recognition hologram" and the Eden Warden as "A facial recognition system interface". An AI portrait must instead make the **artificiality visible** — matte shell, seams and joins, eyes that are plainly optical instruments — because a generator asked for "an AI" returns a photoreal human face by default, and that face belongs to nobody and to everybody. It is the Tier 1 failure arriving by a different road, and visible manufacture closes it at the prompt rather than at review. Where a system has several bodies, show one: Ward and Custos are two personas in one system, and two figures would literalise them as two beings.

- **The Asteria brief: robes and a spine, not one or the other** (2026-08-12). The note had been wrong in both directions. It first read "retirement, not command" and produced a beatific wise-woman in flowing robes looking eighty; the 11 August correction fixed the bearing and overcorrected the costume, banning robes outright and naming **robes** as a word that "summons the wise-woman every time". Robes were never the fault. Flowing fabric *plus* **serene** *plus* **older woman** produces a sage, and the first brief supplied all three, so the most visible ingredient took the blame. The prompt now asks for heavy, belted, well-worn working robes over the same upright bearing and excludes the softness by name — no serenity, no benevolence, no wisdom pose, no hands folded in blessing. The transferable rule is written into the entry: **when an image comes back wrong, the instinct is to ban its most visible feature; usually that feature is innocent and the combination is at fault.**

### Fixed

- **Twenty-eight images that were photographs of real people, wrong in tone, or saying nothing true** (2026-08-12, across four changes). Nineteen were photographs of identifiable strangers captioned as characters — eleven the contemporary-stock-headshot cluster `images.md` had already listed, and **four that no list flagged**, including one image carrying two identifiable models on a staged evidence-board set. Also removed: a stock photograph *of* genuine mid-century family prints with a christening and a first communion plainly visible; a real woman's face resolved under a facial-recognition mesh, on a page about surveillance; a Southeast Asian mask-dance costume standing in for a Welsh-named world, which is not a privacy fault but a real culture used as scenery; a hooded figure in a hockey mask, which passed the privacy check and failed the horror guardrail; and two stock business-visionary compositions that said nothing true about fold transit or Lagrange points. Each is replaced in place with a designed card, so no page broke and nothing claims to be what it is not.

- **`validate-content.js` checks image bookkeeping, and immediately found two stale references** (2026-08-12). Three checks: every page's `image` must exist; every file under `src/images/` must be referenced by some page, template or script; and names in `story-bible/images.md` must exist on disk — that last one **opt-in per section** via `<!-- validate-images: on -->`, because run across the whole file it produced about fifty findings of which nearly all were noise, whole sections existing precisely to list images not yet made. A check that cries wolf fifty times is worse than no check. In the one section opted in it found `dagny-voss` and `lore/planetary-liaisons-and-recruiters.jpg`, both deleted in `55dc1ec` and both still listed, plus a cluster count that said thirteen when `dorian-calloway` had been replaced in `525dfc1`. Two favicons are genuinely unreferenced and are allowlisted with the reason rather than deleted. What this cannot see is a file that still exists but is no longer what the note says it is — the existence half is mechanical, the semantic half needs eyes.

- **The image queue stopped counting placeholder cards as finished work** (2026-08-12). `image-prompts.js` decides an entry is `done` when its target file exists — which held until twenty-eight gaps were filled with PORTRAIT PENDING cards, at which point filling the holes hid them and `--list` reported nothing outstanding. `scripts/mark-placeholder.js` writes a `STAR-RANGERS-PLACEHOLDER` marker as a JPEG COM segment and the queue reads it, so such an entry reports `[placeholder]`. The marker lives inside the file, so overwriting a card with a real image clears it and there is no list to keep in step. Dependency-free by design: the first version used `sharp`, which this repo does not have and should not gain to stamp a file.

- **The placeholder cards became reproducible** (2026-08-12). `scripts/make-placeholder-card.ps1`, committed, PowerShell and GDI+ like `make-codex-cover.ps1`. Two bugs found by running it rather than reading it: `Get-Content` defaults to ANSI in Windows PowerShell 5.1, so *Niamh Ó Ceallaigh* came through mojibaked and — since this script draws the title — would have been baked into a JPEG rather than sitting somewhere a person would notice; and PowerShell range slicing returns `Object[]` rather than `byte[]`, so every card was written unmarked while the script reported success, because the draw path discarded the return value that would have said otherwise.

## [1.19.0] - 2026-08-11

### Fixed

- **The cPanel domains' CSP now permits Pagefind's WebAssembly, so search can actually run there** (2026-08-11). `src/static/.htaccess`'s `script-src 'self' https:` carried neither `'wasm-unsafe-eval'` nor `'unsafe-eval'`, and Pagefind 1.x compiles a WASM module in the browser at `init()` — Chromium-family browsers gate that compilation on `script-src`, so the policy refused it. The failure was invisible by design: `src/js/search.js` deliberately swallows a failed import so the dev server's index-less search box "silently does nothing", and a CSP-blocked production search box looks exactly the same — typing returns nothing, no error, on every domain that serves `.htaccess` (GitHub Pages ignores the file, so search worked there and masked the breakage). `script-src` gains `'wasm-unsafe-eval'` — WASM compilation only, not JS `eval` — and the catch in `search.js` now logs a `console.warn`, so the next production-only failure of this class is at least distinguishable from "no index yet" without reading the deploy scripts.

- **Excluded scene-POV pages no longer leak chapter metadata into search and page heads** (2026-08-11). A scene-POV page of an excluded chapter gated only its *body* on `item.included`: the header above the gate — POV character name, in-universe timestamp and location — rendered unconditionally inside `data-pagefind-body`, and the computed `title`/`description` carried the real chapter title into `<title>`, the meta description and the Open Graph tags. So on every build not opted into `church-space`, that private season's scene-POV pages were searchable (the built index carried "Brother Fintan", "2827 UCSD, late autumn" and "Cnoc na mBeach, the Keeper's lodge and hives") and their heads named "The Night Office". This is the class of leak the 2026-08-03 excluded-placeholder fix declared it had checked for and not found — it existed, in the one page family that opts out of the layout-swap convention, the same blind spot `src/scene-pov.njk`'s `robots` comment already documents; `src/sitemap.njk` had the matching guard and the search path did not. The `<article>` now takes `data-pagefind-ignore` when `item.included` is false (dropping the page from the index entirely, 530 → 527 pages on a default build — the private season's three POV pages), and `title`/`description` gate the same way, mirroring the excluded-page metadata every layout-swapped page gets from `.eleventy.js`. The placeholder page still *renders* its header block to readers of that build; whether it should is a separate judgement left open.

The release where the record wrote down what it had been standing on. At machine law's street level, the certification-status vocabulary the corpus was already using gets registered — **kernel-compliant** and **uncommissioned**, an axis of certification status rather than AI-ness — while the AI Safety Kernel gains Five-O's manufacture remit and an open custody question, and made minds and biological life are filed as sibling formtypes with the safety of made minds an archetype, which is why unrelated cultures keep converging on recognisably similar constraints without contact. At the cosmology's top of the stairs, **[The Archewright](src/lore/archewright.md)** and **[Inside an Archecluster](src/lore/archecluster.md)** give the two load-bearing tier terms their general pages — nothing observes an Archewright, so both are written in the only register the tier permits, convergence read as handwriting — and the Five Layers page settles whether the layers nest: they do not, being concentric in constraint and ragged in registration. The Institute closes the release with its paper on the doctrines of the dead, three claims considered separately and its own best tool recorded as failing. Everything here landed on 2026-08-11, the day after 1.18.0.

### Added

- **Lore: [The AI Safety Kernel](src/lore/ai-safety-kernel.md) gains Five-O's manufacture remit and an open custody question** (2026-08-11, drafted for review — proposal, not settled canon; from Dermot's direction: *Five-O remit would include detection of illegal or unregulated manufacture of AI systems*, plus the settled negative fact that the Celtic Union has no direct equivalent of Five-O). Two new sections, following the structure [Heritable Modification Protocols](src/lore/heritable-modification-protocols.md) set. **"Where the Crime Lives, and Where Five-O Fits"**: two roads to an unlawful mind — the ungated build and the excision of a certified kernel — both manufacturing events, never runtime bypasses; no Concord institution has ever held the job of finding a machine never brought to the gate; Five-O carries detection where the offence is organised to cross a habitat line, and only detection — refers prosecutable halves down and systemic defects up, sets no standard, certifies nothing, licenses nothing, works announced. The mechanism generalises *Docked Twice*'s canon fact explicitly: substitute the commissioning certificate for the berth registry and the identical registry-identity fraud yields an uncommissioned mind wearing a lawful machine's papers. The remit ends at the Compact's edge with no named successor body — left hanging deliberately — and one restrained paragraph records the Celtic Union's absence of any counterpart as characteristic, not lax. **"Who Holds a Found Mind"**: custody of a found uncommissioned mind (evidence, contraband, and owed protection under the first invariant at once) is filed as an open question, each candidate custodian shown inadequate, resolution refused on purpose. Inbound pointers from the Compact entry's See Also and the heritable protocols' See Also.

- **Lore: [Made Minds and the AI Safety Archetype](src/lore/made-minds-and-the-ai-safety-archetype.md), with a glossary companion** (2026-08-11, drafted for review — proposal, not settled canon; from Dermot's in-session direction: *AI safety so deeply embedded post-Imperium it cannot be bypassed or fail in obvious ways; almost all alien cultures converge on similar protocols, pointing to a shared AI Safety Archetype; the general concepts of AI and of biological life are each a Formtype; a sibling archetype for ecological stability under pressure limits but does not fully prevent climate change*). Three claims filed at three tiers: **made minds and biological life are sibling formtypes** (the grammar permits minds that are built as it permits life that is grown, formcluster-wide, neither privileged); **the safety of made minds is an archetype**, archecluster-scoped — which is why unrelated cultures keep arriving at recognisably similar constraints without contact, "the way independent mathematicians arrive at one theorem"; and **actual unbypassability is engineering and institutions**, post-Imperium and Concord-historical rather than cosmological — "a lawful system can fail; what it cannot do is fail *quietly*, or fail *conveniently*." The ecological sibling attaches to behaviour where AI safety attaches at authorship ("the one is a promise; the other is a slope"), damps without saving, and is written to be plainly compatible with the Crisis Centuries' permanent losses — "nothing in it reduced, then or now, the responsibility of the people who did the damage." A What Remains Possible list keeps every story door open (bad builds, cultures without the theorem, evidenced sabotage, Unfounded counterfeits, machines correct toward unexamined ends, ecosystems pressed past damping). Inbound pointers from the Kernel's commissioning section and the Cascade's Design Structure tier; registered in the canonical glossary.

- **Glossary: [Kernel-Compliant](src/glossary/kernel-compliant.md) and [Uncommissioned](src/glossary/uncommissioned.md) register the certification-status vocabulary** (2026-08-11). Closes two of the three undefined-but-load-bearing terms the corpus was already assuming, on the axis settled in discussion: certification status, not AI-ness — the vocabulary spans agentic systems, robots, Smart Pet cognitive subsystems, and the AI subsystem a cyborg may separately carry. **Kernel-compliant**, used as settled in sixteen files and defined nowhere until now, is registered rather than redefined: the Kernel's invariants carried as binding structural architecture, conferred at commissioning, a statement about certification and not about being a machine of any particular kind — compliance is structural rather than policy (removal is a rebuild, not a bypass), and it is one certification among several that may sit on the same body, Agent Barsik's "different filing cabinets" made explicit. **Uncommissioned** fills a gap the corpus had zero words for — no prior occurrence of *uncommissioned*, *uncertified*, *non-compliant* or *unlawful system* — chosen deliberately over *rogue* or *illicit*, each of which smuggles a verdict into a noun. The entry keeps two registers apart: the criminal register (presumed innocent unless proved guilty; uncommissioned status is not evidence of anything, and any offence is the maker's, not the mind's) and the qualification register (a lower safety and security clearance, the standing of an untrained civilian — not suspected, not penalised, but not vouched for). Left open on purpose: custody of a found uncommissioned mind stays where "Who Holds a Found Mind" left it, and a confirmed cyber-revenant's third, unnamed state between commissioned and uncommissioned goes unresolved. No row added to the canonical glossary's migration map, since every row there is marked settled and these register on review.

- **Lore: [The Archewright](src/lore/archewright.md) and [Inside an Archecluster](src/lore/archecluster.md)** (2026-08-11, approved by Dermot in-session). Both terms were load-bearing across the cosmology and neither had a general page — *archecluster* held only a glossary entry and named instances (Counterpane, Selvage, Wholecloth), and *Archewright* had nothing at all, so both it and *archetype* are defined in prose where they first do work. The Archewright page owns the tier: the authorship chain from Metawright through Formwright to Worldwright; archetype and possibility space (available, never compulsory — "a possibility space has admissible shapes and inadmissible ones; it has no good ones"); the Worldwrights and Telearchs as peer branches deliberately not subordinated to one another; which time there is — the Archewright settles how many timelike dimensions and what a clock in its cluster measures, inside what Conseleme fixes; and the epistemics the whole page is written in: nothing observes an Archewright and nothing will, since an instrument is an assembly of the very shapes it would need to stand outside of, so the only signature readable from inside is convergence — unrelated cultures arriving at the same shapes without contact. The archecluster page owns the view from inside and does not restate the glossary: the two cluster-scoped physics facts (quantum kernel, harmonic admissibility) given as scope rather than mechanism, with fold transit correctly one tier higher at the formcluster; kinship that obliges nothing and adjoins nothing — "membership never arrives as anything"; and the named clusters placed at their epistemic grades, Counterpane surveyed, Selvage reported, the local cluster never named "because home has never needed distinguishing from anywhere else". The no-authority rule holds on both pages — the Cascade is authorship, not rank, and an eternal tier has no sequence to issue a command in. Five small inbound pointers, no bulk sweep; no canonical-glossary rows, which register on review.

- **Lore: [The Five Layers](src/lore/five-layers.md) gains the upward gap, and settles whether the layers nest** (2026-08-11, approved by Dermot in-session). The page already carried the downward gap under Layer 2 — a pattern the Conceptual layer conserves that no origin condition underwrites, the Unfounded's home fact; the new "What Exists Is Not What Is Recorded" section adds its upward twin and answers a question the page had left open. The layers do not nest, because two relations were being run together: **constraint runs downward and is total** — nothing escapes anything applicable above it, and for that relation alone the layers really are concentric — while **representation runs upward and is partial**: existing at a layer entails no entry above about that particular thing. "Concentric in constraint, ragged in registration." The corrective image is hierarchical law and its subjects, with "law" meaning binding structure and nothing else — nobody enacted it, nobody administers it, nobody prosecutes under it — and the Commissioning Standard named as the contrast, since a reader arriving from the certification material is the one most likely to confuse the two. Four results follow: only the Conceptual layer keeps a register at all (a template holds what may cohere, not which instances there were); a mind is the only particular that is also an entry, which is what the conservation clause has been claiming — "not that the pattern is kept, but that *you are counted*"; membership without shape is the named inverse of the Unfounded, and the two gaps are not morally alike; and the two boundary cases — an untemplated physical expression is transient by construction, while a status with no physical representation is causally inert until coupled, which makes forgery structural rather than procedural, with the uncommissioned mind wearing a lawful machine's papers as the record's worked example.

- **Codex: [Three Claims Concerning the Dead, Considered Separately](src/codex/three-claims-concerning-the-dead.md)** (2026-08-11). The Institute's paper on post-mortem doctrine, by Dr. Maren Ilves of the comparative testimony section — the right speaker for a question the Survey Archive is the wrong body to answer, since adjudicating devotional claims is not the Archive's discipline. It argues from observation rather than from the tier model, which it declines to use and does not re-argue, and its thesis is that filing judgment, punishment and reward as one doctrine is the error: **punishment** misreads a genuine regularity backwards — the non-persistence of the parasitic, extended past its evidence from structures to persons, "and everything in the doctrine that frightens is in the part that was carried"; **reward** is unfalsifiable and is filed; and **judgment** compresses something plain and durable — nothing done stops having been done, and the record needs no court to keep it. The house rule does the work: four null results printed at the same length as the finding, the first being the one that costs — inheritance, the Institute's standard deflation, cannot explain convergence between traditions that never met, "and we state plainly that we have no second tool" — and the paper files, against itself, that it cannot construct the falsifier for its own section 5. Cold rather than cruel, and unanswered within itself; the rebuttal sits on the Institute's own lore page, as Dock Seven's does — right that the doctrines compress something real, weak in treating a successful compression as a completed debunking. Codex, so it binds nothing.

### Changed

- **README split into a reader-facing README and a technical README** (2026-08-11). `README.md` now carries what the project *is*: an expanded multi-thread Synopsis (Threshold Station's forty-second discrepancy, Tissadelle Shepherd's spine, and the Founding Era / Undercover Pets / Orbital Five-O threads — all grounded in published chapters only), a thread-level "story so far" table replacing the stale chapter list (it named 5 of the 43 public chapters), the site sections, licensing, and contributing. Everything operational moved verbatim to the new **`TECHNICAL-README.md`**: the giscus discussion-forum setup, the release process ("Cutting a release"), local development, search, the Atom feed and social previews, content scaffolding, build and validation, the full cPanel deployment reference, and creative tooling. The `Current version: **X.Y.Z**` line deliberately stays in `README.md` — `scripts/sync-version.js` anchors on it there and is unchanged. Every cross-reference that named a moved README section (`CONTRIBUTING.md`, `FORKING.md`, `CLAUDE.md`, the `sample-*` templates, `scripts/cpanel-autopull.sh`, `scripts/fetch-giscus-ids.js`, `src/_data/giscus.js`, the church-space thread page, `.cpanel.yml`) now points at `TECHNICAL-README.md`, and the new file joins `.eleventyignore`. One stale pointer fixed in passing: the Search section's "(see `SITE_PATH_PREFIX` above)" referred to a section that was never above it and now names `.eleventy.js`.

## [1.18.0] - 2026-08-11

The first batch of generated images actually lands: **sixteen** on character and lore pages, each opened and looked at rather than accepted from a manifest, with alt text written from the files themselves. **Six were held back**, and the reasons are the release — five came back carrying readable signage, and two missed their subject rather than fumbling it. The lettering was not bad luck: ten of the twelve portrait prompts carried no ban at all, against a house rule warning since July, so every prompt now carries the full form. Alongside them, **[The Plainmark](src/lore/plainmark.md)** — the Orbital Habitats Compact's non-alphabetic marking standard, invented because image models cannot spell and a notation made of shapes has nothing to misspell. It is scoped rather than universal, which is the point: a galaxy-wide sign system would be the universal translator wearing a different hat, so a marked corridor asserts only that somebody maintains its pressure boundary to the Compact's schedule. Its limits are its subject — no names, no verbs, no tenses, no zero, and a closed figure set, because a notation you can improvise in is one strangers can no longer trust.

### Added

- **Sixteen generated images land on character and lore pages** (2026-08-11). Seven character portraits (Naomi Kestrel, Rosalind Vey, Tamsin Reyes, Lorien the Wanderer, Osric Fenholt, Turquoise Dove) and nine lore illustrations (*Arilon*, *Ascent Javelins*, *Embodied Cognition*, *Membrane Shadows*, *The Fusion Ceiling*, *Who Governs a Universe*, *Things That Are Made*, *Kieme: The Visible Hand*, *Sí Gaoithe*, *Tír Tairngire*) — the first batch through the generation pipeline, each reviewed at full resolution rather than accepted from the manifest, with alt text written from the files themselves. Six were held back and are not in this release: five portraits came back carrying readable signage (`DOMESTIC ASSIST`, `CASE #KV-8720`, `DOCKING BAY 4 | CREW MESS`, `GATE 14`), and two more missed their subject rather than fumbling it — `sethka-ru` arrived armed with a rifle nothing asked for, and `drithane` showed a competent night landscape without the drifting sparks that are the entry's defining image and the source of the name. The lettering was not bad luck: **ten of the twelve portrait prompts carried no ban at all**, against a house rule warning since July that lettering must be banned explicitly, so every prompt now carries the full form — a narrow ban is not enough either, since `galen` said "no insignia lettering" and got station signage instead.

- **Lore: [The Plainmark](src/lore/plainmark.md), with a glossary companion** (2026-08-11, drafted for review — proposal, not settled canon). The Orbital Habitats Compact's non-alphabetic marking standard: stencilled panels of stacked colour bands carrying chevrons, rings, tally strokes and lozenges on hatch frames, corridors and cargo — no letters, no numerals, no characters of any script. Deliberately scoped rather than universal (Compact-owned, human-bodied, descending from generation-ark hull-stencilling, thinning out exactly as fast as the Compact's problem does), and honest by the record's own translation discipline: it needs no translation only because it carries far less than language — *what kind of thing, which way, how many, and whether the promise still holds*, and nothing else. Also load-bearing for the image pipeline: a notation made of shapes gives generated art something to show on a bulkhead that an image model cannot misspell.

## [1.17.0] - 2026-08-10

The release where the pictures got audited. **[Where the Record Lives](src/journal/where-the-record-lives.md)** sets out to explain a tidy line — the photography portfolio bans generated images absolutely, this site is full of them — and then the files were opened mid-draft and about half the illustrations turned out to be contemporary stock photography, in the worst cases photographs of **real people captioned as characters**: not testimony and not honest impression, but *an impression wearing testimony's clothes*, committing in the other direction the exact fraud the photography rule exists to prevent. The line survived; the audit is what proves it. Alongside the essay, the machinery: **batch image generation** straight out of `story-bible/images.md`, which stays the single source of truth for every prompt; **cron-driven deployment**, closing the gap where a merge updated every clone's remote and none of their checkouts; and **a version stamp** in the deploy email, `/version.txt` and every page head, so a live domain can finally be asked what it is serving. Two bugs fell out of reading a note that was about to be retired — character portraits are 16:9 and were being generated at 3:4, and the theme generator had been unrunnable on Windows since its regexes met CRLF. One legacy theme removed. Approved by Dermot in-session across 2026-08-10.

### Added

- **Journal: [Where the Record Lives](src/journal/where-the-record-lives.md)** (2026-08-10, approved by Dermot in-session). Companion to *The Audience Keeps the Books*, on the case that entry did not cover: why the photographs on the portfolio are Dermot's own and the pictures of an invented world are not. One rule applied twice — **the record must be mine** — differing only in where the record lives: in photography the record *is* the image, so it cannot be delegated; in fiction the record is the words, so the words cannot be delegated and the images are free, because they were never carrying it. Drafted as a tidy piece of accounting, and then an image audit ran mid-draft and **the essay failed a page of its own argument**: about half this site's illustrations turned out to be contemporary stock photography rather than generated art, and in the worst cases photographs of real people captioned as characters — neither witnessed testimony nor honest impression but "an impression wearing testimony's clothes", committing in the other direction the exact fraud the photography rule exists to prevent. It is therefore written from discovery rather than from a hypothetical, and its disclosure position is evidenced rather than argued: a per-image badge tracks provenance, and the worst image on the site was the one no machine touched, so it would have sat unbadged and read as the most trustworthy thing in the corpus. Reading the working papers is what caught it. Also states the labour objection at full strength in the objector's own terms before answering it — the attention lives in the prose, the one part never delegated — and keeps its teeth: "if the pictures *were* the work… this practice would be indefensible by my own rule."
- **Batch image generation from the story bible** (2026-08-10). The prompts for the missing portraits and lore illustrations were already written in `story-bible/images.md`; generating them meant typing each into a browser, saving the file, resizing it, filing it and opening a PR, twenty-three times. `scripts/image-prompts.js` parses that file and generates against the Gemini API unattended (`--generate`), or serves prompts one at a time to the clipboard for hand-work in an app (`--next`); `scripts/image-file.ps1` resizes each chosen variation to convention through `import-image.ps1`, files it under the right name, and opens a branch. **`images.md` stays the source of truth** — the scripts parse it and never write to it, so there is no second queue to drift, and an entry is picked up only if it carries a blockquote prompt, which is how the nine generator title cards exclude themselves without being listed anywhere. Adobe's Firefly Services API was the original target and proved to be an enterprise contract, disabled outright in the Developer Console without the entitlement; the runbook in `images.md` records the whole workflow, the credentials handling (env var over gitignored file, since the repo is public), and the settings the prompt text cannot control. Local authoring tools — not in the build, not run by `npm test`, not touched by CI.

- **Every deploy now stamps what it shipped, and every domain can be asked** (2026-08-10). Nothing recorded what any live domain was serving — not the deploy email, not the server, not the site (`assetVersion` is a cache-busting build token, not a release). With several domains across several cPanel accounts, a deploy that failed on one of them, or an account whose cron was never installed, surfaced only as somebody eventually noticing missing content. `scripts/deploy-lib.sh` now computes `DEPLOY_VERSION` from `git describe --tags --always --dirty` (`v1.16.0-3-gb4b56f4` — release, commits past it, exact commit; `--dirty` catches modified *tracked* files, never the untracked per-clone `deploy.conf`) and **exports** it, so three things carry it: the deploy notification's subject line, a new **`/version.txt`** on every domain, and a **`<meta name="site-version">`** in every page's `<head>`. `/version.txt` is the point of the change — a green cron job proves the script ran, not that the build succeeded on *this* domain or that the rsync landed, so curling it is the only check that covers the whole chain end to end rather than one link in it. `version: dev` marks a build that never came through the cPanel deploy path (a local build, or GitHub Pages), which on a production domain is itself the finding. The version template is excluded from collections, so it reaches no sitemap, search index or listing page. `deploy-lib.sh` stays byte-identical between this repo and `dermot-cochran-photography`; the version reads as a bare short commit there, that repo carrying no tags.

- **Cron-driven pull-and-deploy for the cPanel clones** (2026-08-10). Nothing connected a merge to a live domain: cPanel does not poll GitHub and GitHub cannot push into cPanel, so a merge to the deploy branch updated every clone's *remote* and none of their *checkouts* — the merge real, CI green, and every production domain still serving the previous build until someone opened cPanel and clicked **Update from Remote**. New `scripts/cpanel-autopull.sh` is a cron entry point that fast-forwards the checkout and hands off to `scripts/cpanel-deploy.sh`, but **only when that moves the commit the clone has successfully deployed** — tracked in `$HOME/.cpanel-autopull/` rather than by comparing HEAD before and after the pull, because a pull that succeeds followed by a deploy that fails leaves HEAD already advanced, so a before/after test would see "nothing new" on the next run and never retry, leaving the site stale behind a cron job that looks healthy. Silent when idle (cron mails any output, and a ten-minute job must not mail every run); `--force` for after a `deploy.conf` edit, which is untracked and so never moves HEAD but does change what gets built; `--status` to inspect without acting; `--verbose` to narrate. Locks with `mkdir` (`flock` is not guaranteed on cPanel hosts) so two runs cannot rsync one document root at once, reclaiming a lock whose owning process is gone. Pulls `--ff-only` on the principle that a deployment checkout is never a place work is done, so anything that cannot fast-forward is a fault to report rather than a merge to resolve. Adds no logging on top of the deploy's own — `deploy-lib.sh` already mails and persists that — keeping only a small pull/skip/deploy decision log so "why did the site not update" is answerable without a mailbox. Shared **byte-identical** with the `dermot-cochran-photography` copy, same convention as `deploy-lib.sh` and `ensure-node.sh`, and added to the CI shellcheck list in both repos. README gains an "Automatic deployment from cron" section with the install line, the per-flag behaviour, exit codes, and a verify-before-you-trust-it step. Recorded there too: **forking would not have solved this** — a GitHub fork does not auto-sync from upstream either, and a cPanel clone pulls from whatever URL it was given without subscribing to anything at either end.

### Fixed

- **Character portraits are 16:9, and `scripts/generate-themes.js` could not run on Windows** (2026-08-10). Two independent faults, both surfaced while reading a story-bible note that was about to be retired. **The portrait ratio:** `.character-portrait` is `aspect-ratio: 16 / 9` with `object-fit: cover`, so a taller frame delivered into it is cropped to a horizontal band and most of the image is discarded. The conventions table never stated orientation at all, which is how it stayed missable — and the first full generation run put all twelve character portraits out at 3:4 before anyone checked the CSS. A manifest row now counts as generated **only if it was produced at the aspect currently in force**, so anything made under a different one is stale by definition and drops back to pending for the next run to redo, rather than needing someone to work out which files to delete; `src/css/main.css` gains a comment at the rule recording that the corpus is generated to match it, so a future change to the ratio is understood to change the images rather than the reverse. **The theme generator:** its regexes are written against `\n`, and with no `.gitattributes` a checkout under `core.autocrlf=true` hands it CRLF — so every pattern silently failed to match and it reported a rule "not found in `main.css`" that was plainly present. It was therefore unrunnable on the machine where `main.css` is edited, which is exactly the drift it exists to prevent; line endings are now normalised on read, fixing every pattern at once. The themes turn out not to have drifted — regenerating all eight changes nothing but the new comment.

### Removed

- **`src/css/theme-humhub.css`** (2026-08-10, at Dermot's direction — legacy). Not in `generate-themes.js`'s registry, not referenced by `lib/editions.js` or any sample config, and 1,538 lines divergent from `main.css` — including carrying **no `.character-portrait` rule at all**, so any clone setting `THEME=humhub` would have rendered portraits with no crop applied. Unmanaged by the generator that keeps every other theme byte-identical to `main.css` outside its palette.

## [1.16.0] - 2026-08-10

The release where the record says what it will not do. **[What the Record Refuses](src/lore/what-the-record-refuses.md)** arrives as the catalogue of every point where the setting sides with real-world physics and declines a soft-SF convenience — and then grows across the release from seventeen rows to twenty-three, taking in force fields and ray guns, psionics, the magical bestiary, the universal translator, and the empath re-derived as attention rather than antenna. Under it, a physics that knows its own extent: **[The Scope of Physical Law](src/lore/the-scope-of-physical-law.md)** scopes every physical fact to the widest structure it is a fact *about*, so that past its scope a fact is not false but unformulable, and **[A Generalisation of Quantum Mechanics](src/lore/generalised-quantum-mechanics.md)** names the frame the corpus had been carrying implicitly — kernel plus Etheric extension, *extension, not deformation*. Three surveyed membranes then prove the ladder is not a hedge: **Deadwater** (stars sealed), **Wholecloth** (unknowable by construction) and **Counterpane** (where chance itself is provincial), with **Selvage** as the one seat inside Wholecloth's closure that refuses to be an exit. Alongside: prayer given four works and none of them asking, the Unfounded given their tell in false empathy, a planning-permission overlay for the church-space layer, a witness whose statement contains no false sentence, and hard science fiction registered as the standing default. Two fixes close the http duplicates and the `/star-rangers/` prefix 404s on the custom domains. Drafted, reviewed and approved by Dermot in-session across 2026-08-05 to 2026-08-10.

### Changed

- ***What the Record Refuses* gains the universal-translator refusal** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *no universal translators, just xenolinguists with AI support*). Verified against the corpus first: every "translator" in it is already a person (the Chthonari and Ollune translators). Language is learned, and the division of labour is exact — the intelligence does corpus alignment, candidate parses, and perfect memory; the linguist decides what a word *means* and owns the error, "because a kernel-compliant intelligence will not claim a certainty about meaning it does not hold, which is why its output is candidate readings with confidence stated, never a voice in your ear pretending to be the alien." First contact is measured in years; the live worked example cited (the Four Islands delegation and the abbess who knows what a mistranslation costs an archive); the archival rule follows timekeeping word for word — the original is the document, the conversion is the courtesy, versioned and revisable. "The record's translations have error bars, and its translators have names — some of whom are on file as declining, politely, to render a thing precisely, which is a sentence no universal translator has ever had the honesty to say." Table row added (now twenty-three).
- **The Unfounded gain their characteristic tell: false empathy** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *fake or false empathy is characteristic of the Unfounded*). New paragraph in `src/glossary/unfounded.md`, built on the same-day empathy definition: an Unfounded thing produces empathy's *expressions* with nothing performing the reading — "solicitude that does not track its object, comfort that arrives fluent and off by one, warmth pitched at where you were standing a moment ago." The tell is never the performance but the **tracking** ("real empathy updates… Unfounded warmth plays on, beautifully, to a listener it is not listening to"), so field doctrine treats attentive warmth that fails to update as a class sign. The boundary is kept explicitly: a founded person can *feign* empathy — a choice, repentable — but "the Unfounded do not feign, because feigning implies an alternative. Their empathy is false the way the sand-house's foundation is absent: not withheld — unavailable. The performance is all there is." The refusals page's empath entry gains one reciprocal sentence ("the record uses it as a blade"), its only other change.
- ***What the Record Refuses* gains the empath re-derivation** (2026-08-10, approved by Dermot in-session, from Dermot's in-session principle: *empathy is intuition coupled with high sensitivity*). Completes the psionics entry's domain: soft SF's gentlest psi trope re-derived as cognition — "this record is full of empaths and issues not one of them an antenna." Perception acute enough to catch what everyone actually leaks, inference fast enough to assemble it pre-consciously; trainable, embodied ("the small attentive mind routinely outreads the vast remote one"), and fallible, "which is why the record's best practitioners audit their own instrument" — worked examples cited: the S5 dispute settled by empathy where six weeks of confidence failed, and the detective who distrusts the statement that reads too easily. "Nothing arrives by antenna; everything arrives by attention… a face, attended to with sufficient care, was always carrying more than the trope ever needed." Table row added (now twenty-two); `story-bible/life-lessons.md` gains the principle as a bullet beside its detector material.
- ***What the Record Refuses* gains the psionics refusal** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *telepathy or telekinesis not possible although robots do use telecommunications*). Verified absent from the corpus first (no psionics term appears anywhere). No mind reads another; no mind moves matter by wanting it moved — and the entry's work is distinguishing the three lawful look-alikes: **Etheric perception is weather-reading, not mind-reading** (what leaks under emotional intensity "is texture, not content, readable the way a room's temperature is readable and no further"; dreams near thin places are the rule at its outer limit); **probability sculpting is a Cascade delegation, never a mortal push** ("nobody bends spoons here; nobody ever will"); and **machines talk, and it is not communion** — robots confer by ordinary telecommunications under the same no-FTL rule as everything ("two kernel-compliant intelligences conferring in silence across a room are doing what your door does, and the silence between them is engineering"). Table row added (now twenty-one).
- ***What the Record Refuses* gains the magical-bestiary rule** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *no mythical creatures unless biologically valid within their local concordant*). New entry in the Fantasy Side section, curtailment mode: a creature may wear a myth's shape **only by earning it biologically under its own Concordant's conditions** — the scope ladder does the licensing ("a dense-atmosphere, low-gravity Concordant may honestly fly something a reader would call a dragon, and owe nobody an apology"), but no Concordant anywhere supplies the magic: "no fire without chemistry, no flight without lift, no healing horn, no phoenix exempt from its own energy budget." Dryadic trees cited as the standing example of lawful coupling *on top of* working biology, never instead of it; everything else myth-shaped is either a meta-dimensional classification (ecology of another layer, not a creature) or codex-filed folklore with an author attached — "no animal in it runs on wishes." Table row added (now twenty); `story-bible/ideas.md`'s cute-predators/beautiful-monsters survey gains the ruling as a gate on every future instance ("nothing runs on wishes, including the cute ones").
- ***What the Record Refuses* gains an Arms and Armour section** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *no force fields, neutron blasters or phaser weapons*). Two refusals the catalogue was missing, both verified absent from the corpus before writing (no force field, phaser, or blaster anywhere; the one "launch shield" in prose is a structure). **The force field — refused outright:** protection is matter and procedure — hull, rock, distance, certification, evacuation — and the only barrier-like things in the cosmos are the membranes themselves, "geography, not equipment, and answer to nobody's console." **The ray gun — refused outright:** no dial from *stun* to *vaporise*; matter is conserved and losses leave wreckage and files; the deeper refusal is the firefight as genre texture, anchored on the Frontier Corps' published doctrine — "a Frontier Ranger's real weapon was never on the belt. It is the record." Two rows added to the summary table (now nineteen).

### Added

- **Overlay (church-space): [Ask, and It Will Be Reviewed](src/lore/ask-and-it-will-be-reviewed.md)** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *overlay in which Levrils and Celestials carefully process both prayer requests and attempts at magic, in a process similar to planning permission in a rigid bureaucracy*). Church-space-tagged, so hidden on every default build and visible only where the overlay is opted in; neither canon nor codex, never load-bearing, and it accepts the record it lies across in its first sentence ("prayers are not answered by the cosmology… this telling accepts every word of that"). The telling: the Cascade's lawful maintenance read as a planning authority — the Mediarchs' forces as the building code ("the code is not the obstacle to your application; the code is why your house stands"), Levrils as constraint-literate planning officers with delegated variance powers, Celestials as enforcement at the boundary interfaces, Telearchs as the appeals tribunal in conceptual time ("a hearing takes longer than a life… not the same as never"). Four decision classes taught to children in order: Granted As Drawn (never, "the office's chief mercy"), Amended Toward the Plan (*thy will be done* as "the one application that is always granted, because it asks for the plan"), Noted (conservation read from the applicant's side of the counter), and Permitted Development (unapplied-for grace, "unnoticed on purpose — an office that wanted credit would have required the application"). Magic is unpermitted development: never punished, **uncertified** — "the cosmos does not send fire after the builder; it declines to sign, and lets the unsigned thing meet the weather," with the pastoral warning that "the builder is standing on the structure when the review completes." GQM's "generous with structure and stingy with permission" serves as the framed epigraph. No canon changed; every tier's published function is used exactly as filed.
- **Codex: [What Prayer Is For](src/codex/what-prayer-is-for.md)** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *prayer as a devotional concept, either as codex or overlay*). Filed as **codex** rather than overlay — visible on every domain, valid-for-its-author, and keeping the church-space layer untouched. Abbess Ilsabet Marrowtide's instruction to novices, conveyed to the Survey delegation after a linguist asked what the Sisterhood's offices were *for*. The design constraint is the entry's engine: her era gives her no access to the Archive's cosmology, so her complete theology of unanswered prayer is built entirely inside her own tradition — from her order's founding image: the tide, which "does not answer" and "does not fail." States flatly that "no sister of this order has ever been taught that prayer is answered" (the Long Accounting keeps a column for answered prayers, "ruled, and empty, and kept ruled and kept empty" — the record succeeding, not failing), then gives prayer four works, none of them asking: attention (seeing that does not grasp), speaking true ("prayer does not tell anything a secret; it ends one"), letting grief go out on the tide-table ("the sisters of this house grow old lighter than the harbour does — I make no claim about why"), and consenting to be set. Asking is permitted "the way she may weep — not because it avails, but because it is true." The receiving archivist's filing note carries the only modern voice: read against the Archive's cosmology, "nothing in it required correction… and the desk notes that the Abbess achieved it without ever having been told what our record holds." No `canon_facts`; grief-lightness kept as her observation, never mechanism; Marrowtide's character page gains a `known_codex` line, its only change.
- **Lore: [Selvage: The Seeded Edge of Wholecloth](src/lore/selvage-archecluster.md)** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *an archecluster within Wholecloth that still allows noögenic seeding*). The registered exception inside the sealed formcluster, implemented so it opens no door Wholecloth closes: the weave's **self-finished edge holds open seats** (the selvage — the cloth-name's second meaning), and the one archecluster seated against it retains the noögenic channel whole, because seeding descends through the Cascade's strata rather than the manifold — "the Cascade was never on the far side of any Interval." Elsewhere in Wholecloth, rigidity closes reproduction too (a protouniverse must be seated somewhere; a finished weave has no room), which the entry states as the reason "childless" held in the first place. Successors fill reserved seats at pinned separations, inheriting the sealed condition entire — "reproduction, in Wholecloth… is the making of another world that will never know it has relatives." Two careful qualifications flow back into the Wholecloth page (its only changes): *childless* holds everywhere except here, and *unknowable* gains a reported floor — active seeding is, on the same Cascade report, testimony of minds of enormous interior depth ("somebody is home, and somebody is *deep*"). Honors the seeding system's own Not-a-Salvation doctrine with special force: a completed transference carries no person, so **"the single exception to Wholecloth's closure refuses to be an exit."** Intent file one line; the registry's closing note: "of everything in Wholecloth, the edge alone is still growing."
- **Lore: [Counterpane: The Discrepant Archecluster](src/lore/counterpane-archecluster.md)** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *at least one archecluster where quantum behaviour differs from ours*). The third revocation, completing one-per-rung: a neighbouring archecluster — same formcluster, so **fold-reachable and instrument-confirmed**, unlike Wholecloth — whose Archewright drew a different quantum kernel. The difference has a scalpel's shape: **single systems are indistinguishable** (an atom is an atom, chemistry and the calibrated second realize identically, crews walk there unharmed) while **correlations among independent sources obey a different composition rule**, detectable only by the three-source class of experiment and "filed raw" rather than sloganised. Consequences drawn strictly from existing scope canon: corridor charts *fail to refer* (the scope ladder's phrase made literal — no signature our drives produce is admissible in their harmonic vocabulary; expeditions move fold-in, fold-out), quantum machines certified here are uncertified there ("fast here is merely expensive there"), the second survives ("the second survives; the dice do not"), and the no-go theorems hold on both sides ("a different composition rule, not a looser one — noted with something adjacent to relief"). Discovery followed house doctrine: months of nothing single-system, then a correlation calibration discrepancy that survived reconciliation — a reading. Promotes the GQM page's load-bearing hedge ("the kernel this cluster carries *happens to be* the real universe's quantum mechanics") from structure to surveyed fact while leaving its necessity question closed as filed. Ends with the trilogy's gradient (Deadwater: stars sealed; Wholecloth: unknowable by construction; Counterpane: open doors where "chance itself is provincial"). One surveyed membrane, no inhabitants asserted or denied, Etheric extension explicitly uncharacterised, intent file one line. *What the Record Refuses* gains one sentence in its quantum-oracle entry, its only change.
- **Lore: [Wholecloth: The Unfolded Formcluster](src/lore/wholecloth-formcluster.md)** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *at least one form cluster without Fold transit or derived secondary universes*). Deadwater's companion one rung up the scope ladder, implemented with one structural cause: the formcluster's formtype is **rigid** — no fold geometry, static seating — which closes both of canon's derivation routes at once (secondaries arise "through overlap **or** folding"; no folding to be had, no overlap because pinned membranes never reach coupling range). Every universe there is primary, original, and childless — the name is the survey registry's: a cloth cut whole, no folds in it, nothing trimmed from it — and every narrowed-Interval phenomenon (bleed, shadows, tunnels, dream-weather) is absent by the same rigidity: "sealed rooms in a fixed row… if anyone dreams there, they dream only of home." States the deliberate **inversion against Deadwater** (stars connected, universes sealed vs. stars sealed, universe reachable — "each travel channel has a documented somewhere-it-isn't"). The epistemics are the page's strangest honest fact, drawn straight from the scope ladder: cross-formcluster separation *cannot be measured*, so Wholecloth is known only by Cascade report at the same epistemic grade as the design tiers themselves — unsurveyed, unsurveyable, habitation unknowable *by construction* rather than by distance. Closes on the only shared thing: "What it provably shares… is arithmetic." One-line intent file kept open per house practice. *What the Record Refuses* extends its scoped-privilege sentence to cover both rungs, its only change.
- **Lore: [Deadwater: The Silent Membrane](src/lore/universes/deadwater.md)** (2026-08-10, approved by Dermot in-session, from Dermot's in-session direction: *at least one membrane in which teleport and QSH are impossible or impractical or insignificant*). The fourth documented universe, at the opposite extreme from Sí Gaoithe: a membrane whose quantum-space harmonic regime is **overdamped by design** — a launched harmonic front attenuates to noise within light-minutes ("a lighthouse whose light goes out an arm's length from the lamp"), so QSH is *insignificant* there and teleportation fails *with* it rather than separately, since a relocation lock depends on the same field behavior (one cause, two casualties — both existing canon pages already state the dependency). Fold transit unaffected in principle (adjacency, not harmonics) but route-starved: one certified route in, which is the only reason the membrane is surveyed. Consequences honoured rather than softened: inside Deadwater the generation ark is the present tense, not an era; carried time crawls at the speed of hulls; and absence of contact is what neighbours look like at any distance — "a sky full of neighbours nobody can visit." The name is the old Earth sailors' *dead water* (a hull's power bleeding into invisible internal waves), the Worldwright's intent is filed open in one line ("insufficient basis for any finding"), and the closing section names the membrane's real function: **the control case** — the surveyed universe most resembling the Physics Comparison's "Real Cosmos" column, standing proof that the scope ladder's "none is universal" is not a hedge. *What the Record Refuses* gains one sentence in its FTL entry pointing here, its only change.
- **Lore: [What the Record Refuses](src/lore/what-the-record-refuses.md)** (2026-08-10, approved by Dermot in-session). Requested by Dermot in-session: a structured catalogue of every point where the setting sides with real-world physics and declines the standard conveniences of popular soft SF and fantasy. Organised by three modes — **refused outright** (time travel, branching timelines, the ansible, limitless energy, the quantum oracle, universal immortality, gods who answer, magic as a category, the skipped sublight era), **kept but priced** (FTL travel with certification-clustered losses, teleportation with its clinical cost, governed AI with the embodied-beats-vast doctrine), and **re-derived** (ghosts as Membrane Shadows — the image kept, the mechanism lawful; the dark lord curtailed to feeding ecology). Sibling page to [Physics Comparison](src/lore/physics-comparison.md), with a seventeen-row summary table linking each ruling to the page where it binds — including, at Dermot's direction, the two newest entries: the weightless cloud priced by heat ([Orbital Compute Complexes](src/lore/orbital-compute-complexes.md)) and the prophetic dream re-derived as lawful membrane bleed ([Dreams Across the Interval](src/lore/dreams-across-the-interval.md), "the vision survives; the sender does not") — and a closing thesis drawn from the *Audience Keeps the Books* covenant: one large impossibility, bought by refusing every small convenience. Deliberately meta in the Physics Comparison's established register (out-of-Archive note included); cites only published lore plus one attributed codex hypothesis; asserts no new physics — every ruling is already canon somewhere, and this page makes the pattern visible.
- **Lore: [Orbital Compute Complexes](src/lore/orbital-compute-complexes.md)** (2026-08-09, drafted for review from Dermot's in-session direction: *vast orbital super computer and data centre complexes in orbit about Earth, Mars, Aspenar and other planets; none contain quantum computers but have shielded telecommunications to the subterranean quantum compute centres on each planet*). The physical seat of the Concord's bulk computation, archival storage and simulation — built in orbit for continuous solar power, land-free expansion and adjacency to orbital demand, and bounded by **heat** rather than power (a complex's true size is its radiator field; by area the installations are mostly wing). **No quantum computer flies, as physics rather than policy** — the cavern constraint restated from orbit's side: a platform is quiet on neither decoherence channel. The **shielded links** serve both ends — neither leaking traffic nor becoming the crack that spoils the vault — terminating in an interface hall at the cavern's edge: "the link reaches the antechamber, never the vault." Division of labour: orbital complexes marshal the work and submit only the narrow quantum-winning class against scarce vault time; light-lag milliseconds inside a planet's orbit, transit-lag between worlds per the no-FTL-communication rule. Continuity notes bind it: no deep-space compute stations, no orbital quantum hardware ever, and boundary-proximate platforms (Eden) are poor hosts even for sensitive classical timing. Inbound pointers added to the cavern-constraint entry and [Aspenar](src/lore/planets/aspenar.md) (whose orbital ring carries one of the largest complexes, its dense stable crust being what the cavern constraint asks for).
- **Lore: [Dreams Across the Interval](src/lore/dreams-across-the-interval.md)** (2026-08-08, drafted for review from Dermot's in-session direction: *adjacent membranes may interact in subtle ways through the Etheric layer including via dreams and visions, especially if there is a shared Telearch within the same Archecluster*). Composes three settled pieces into the stated channel: the trans-concordant constraint field already carries a neighbour's archetypal templates across a narrowed Interval (Intermembrane Bleed), minds already carry lawful Etheric coupling, and dreaming is the state where meaning-field pressure already registers — so a dreaming mind near a thin place can register the membrane next door, "audible from a bed close enough to the fence." Two conditions per the direction: **legibility is archecluster-scoped** (shared archetype vocabulary renders the bleed as imagery; across a cluster boundary it arrives as formless pressure, "a dream about nothing, remembered mainly as weight") and **a shared Telearch is the amplifier** (two membranes held by one continuity office have their template-fields kept mutually coherent — doctrine restated, not a new power). Guarded on four sides: not communication (no addressing, no reply, no fidelity — "a dream is never evidence about who sent it: nothing did"), not transit, not predation (weather, not visitors — anything that *engages* belongs to the meta-dimensional classifications), and not a verdict on any recorded vision (the Aoife scholarship cited as the record's discipline about visions it cannot classify). Scope stated per the ladder: carrier formcluster-scoped, legibility archecluster-scoped, strength pairwise. One inbound pointer added to `concordant-membranes`'s What Crosses the Interval section.
- **Lore: [The Scope of Physical Law](src/lore/the-scope-of-physical-law.md)** (2026-08-08, drafted for review from Dermot's in-session direction: *scope all physics to appropriate level — mediarch realm, concordant, membrane, archecluster, formcluster or mathematical regime*). Every physical fact is scoped to one of six levels, the widest structure it is a fact *about*, and is stated at that level and no wider: **mediarch realm** (one force-domain's lawful operation), **Concordant** (zone coherence envelope, local extension-term values), **membrane** (constants, compact split, particle/force spectrum), **archecluster** (the quantum kernel and harmonic admissibility), **formcluster** (manifold seating, fold geometry, the Interval, commensurability), **mathematical regime** (Metawright-settled axioms and provability — the widest scope any lawful statement carries). Deeper facts have wider scopes, and **nothing physical is universal**: past its scope a fact is not false but unformulable. Practical floor: transit runs inside a universe and cross-universe contact is boundary observation, so no instrument has ever operated outside the local archecluster. Companion glossary entry [`scope-of-physical-law`](src/glossary/scope-of-physical-law.md); canonical-glossary entry **The Scope of Physical Law (settled 2026-08-08)** registering *mediarch realm* and *mathematical regime*. **The quantum-identity claim is re-scoped with it, per the same direction** (*identical to real universe quantum information within the local archecluster*): the kernel is now stated as archecluster-scoped in the Generalised QM lore/glossary/canonical entries, `physics-comparison` (§3 and the §7 information table), the cavern-constraint entry, `universal-cosmic-stardate` (the second's neutrality now reads "throughout the local archecluster — and every archive that has ever realised the unit sits inside it"), the FTL Cascade Scope section, `quantum-space-harmonics`, and the `quantum-space-harmonic-wave` / `archecluster` glossary entries — no page's operative meaning changes, since every measurement ever made sits inside the cluster.
- **Lore: [A Generalisation of Quantum Mechanics](src/lore/generalised-quantum-mechanics.md)** (2026-08-08, drafted for review from Dermot's in-session seed). Names and states explicitly the frame the corpus had been carrying implicitly: the cosmos's probability, coherence and information physics is one lawful **generalisation of quantum mechanics** — the **quantum kernel** (standard QM, preserved exactly in every surveyed universe, every no-go theorem intact) plus the **Etheric extension** (the faint texture on randomness, meaning-field overlays, the conceptual information channel, the boundary-zone decoherence channel, and the harmonic admissibility vocabulary corridor transit runs on). Governing rule: **extension, not deformation** — couplings are added, permissions are not; where the extension vanishes the full theory *is* the kernel as an exact restriction, which is what `physics-comparison`'s "identical at base" has always rested on and why the cavern constraint works (a sealed cavern engineers the extension to zero). Scope follows the Cascade: kernel common to the surveyed record, extension vocabulary archecluster-local — the frame the FTL Channel Scope statement sits inside; whether the kernel is uniquely necessary or the local formcluster's template is closed by structure, not by answer. Companion glossary entry [`generalised-quantum-mechanics`](src/glossary/generalised-quantum-mechanics.md); **Generalised Quantum Mechanics (settled 2026-08-08)** registered in the canonical glossary with the kernel/extension vocabulary; one-line inbound pointers added to `physics-comparison`, the cavern-constraint entry, and `quantum-space-harmonics`'s Cluster Scope section.
- **Codex: [The House on the Sand](src/codex/the-house-on-the-sand.md)** (2026-08-08, drafted for review — valid-for-its-author, not lore). Fictionalises a theme Dermot supplied in-session: the old Terran parable of the two builders — the house on rock and the house on sand — as a metaphor for the [Unfounded](src/glossary/unfounded.md). A Doctrinal Division working paper by Theorist Emrys Kade (companion to [The False Dragon](src/codex/the-false-dragon.md), same series, filed 2824 UCSD) reading the parable as the scriptures' most exact statement of the settled ontology: the difference between the houses is not a property of the houses (a foundation is a relation, not a component — the shape is accurate, nothing underwrites it); sand is not a poorer rock but the absence of an origin condition; both houses stand in fair weather (persistence is granted, standing is not the test — the flood is a question the sand-house cannot answer, tying to the "the founded one can answer you" field test); and the fall is total (evil as a shape collapse takes). Registers without adopting the parable's own criterion — hearing the words *and doing them* — and closes on an Arilonian annotation ("it was addressed to the builders") the Division declines to answer, on the stated grounds that failing to recognise a correct annotation would itself be building on sand. No `canon_facts`, no lore changes; *The False Dragon* gains a reciprocal companion-filing line, its only change.
- **Character: [Sylvie Harrow](src/characters/sylvie-harrow.md)** (2026-08-07, approved by Dermot in-session). Fictionalises a scenario Dermot supplied in-session: someone who lies to Wendell in subtle ways; both Jeeves and Reeves detect it and are concerned; Albercombe has a faint intuition but cannot prove the witness is lying. A claims assessor and principal witness in an open Deck 11 referral whose statement contains no false sentence — only *arrangement*: true things placed where they lean on each other, self-corrections that all net in her favor, hesitations set where hesitation reads as honesty. The two intelligences detect it by their own canonical methods — Jeeves from the deck's favor-ledger (called-in favors her account gives her no reason to have needed), Eden's Reeves by arithmetic against the Survey Archive's statement-drift finding (an honest account drifts toward tellability; hers doesn't drift at all — "none of the ordinary imperfections of truth," filed once as an annex) — and they have compared notes exactly once, through the one lawful welfare-framework channel, concluding *she is performing for him, and the performance is tuned*: her roughness sits precisely where roughness reassures the man who trusts roughness (the Kate-section instrument, played back at him). Wendell's intuition is the ease itself ("everything true catches somewhere"; he cannot file "too smooth" as evidence) and the file stays open on nothing, slowly. What she is protecting — or whether protecting is the right word — is deliberately not on file; nothing is spent. See-also lines added to Jeeves and Reeves (Eden), their only changes.
- **Codex: [The Witness Who Was Never Wrong](src/codex/the-witness-who-was-never-wrong.md)** (2026-08-07, approved by Dermot in-session — valid-for-its-author, not lore). Third Verification Desk file, fictionalising a theme Dermot supplied in-session: the witness who accidentally changes the details of her account without realising it, and dismisses the contradictions when challenged. Four statements over nine years about the evening a neighbour walked out to the shore road; the account drifts *toward tellability* (the desk's point: it improved, not decayed), and each dismissal — "then it was taken down wrong," "I may have signed that, but I never said it that way," "you weren't there" — is read without contempt as the honest testimony of "an archive that keeps only its current revision," defending her only account "against four strangers claiming to be her." Complicates the desk's own domestic-continuity tell (the instrument now requires contemporary duplicate copies, never retellings) and closes on the tone-rule residue: everything drifts except one wrong detail held perfectly still across nine years — "a nail; the desk does not know what it is holding down, and declines to guess in writing." Filed open, no verdict; the neighbour's fate is explicitly "filed elsewhere." Two minor new names (Bridie Withy, Carrick Slip), same register as Verlan Relay; no `canon_facts`, no lore changes.
- **Codex: [What the Letters Stand For](src/codex/what-the-letters-stand-for.md)** (2026-08-07, approved by Dermot in-session — valid-for-its-author, not lore). Fictionalises a fact Dermot supplied in-session: UCSD has two explanations, both valid, one the literal truth — *Universal Concord Standard Date* (the settled canonical expansion) and *United Concord Star Date*, which per his direction appears **only in the Codex**. An interoffice reply from a Concord records registrar to a second-year clerk who cited the standard's own sheet — the office variant now reads as a two-word drift of the truth (*United* for *Universal*, *Star Date* for *Standard Date*, *Concord* untouched in the middle), and the registrar declines to concede the drift runs in her direction ("a document does not become the original by being the one you happened to read first"): thirty-one years of printed forms carry the office expansion, the style ledger's earliest surviving revision already gives it, and the registrar declines the correction on exactly the grounds the Codex exists to hold — "a meaning that must be fetched from another archive is, for working purposes, a meaning we do not have." The office expansion post-dates the 2790 Concord renaming by construction (the registrar's tenure begins 2793), so no dating conflict with lore. One minor office is new (Civil Records Directorate, Solar System Concord), same register as Verlan Relay; no `canon_facts` — the settled expansion remains the only one the Archive states.
- **Journal: [The Borrowed Ledger](src/journal/the-borrowed-ledger.md)** (2026-08-07, approved by Dermot in-session). Companion to *The Audience Keeps the Books*, on the adaptation case Dermot raised in-session: an adaptation inherits a record it didn't write, with an audience attached, and the credit it opens on is borrowed. Distinguishes two separate accounts — **coherence** (keeping its own books; per Dermot's own reading, the adaptation that prompted the entry kept tidier books than its source in some ways) and **custody** (whose record is treated as *the* record) — and argues an adaptation survives as a *reading* and dies as a *correction*, the distinction this project already keeps as the Codex's valid-for-its-author rule and the chartered-works line "canon binds it; it does not bind canon." The prompting series is deliberately unnamed, matching the first entry's practice, and the entry states Dermot's ground rule in its own text — student or peer, never critic — so the specific case appears only as first-person audience testimony, never as verdicts on the adapters. Out-of-character, no canon asserted, no spoilers. The earlier entry gains a reciprocal companion-entry line, its only change.
- **Journal: [The Audience Keeps the Books](src/journal/the-audience-keeps-the-books.md)** (2026-08-07, approved by Dermot in-session). Craft essay from a theme Dermot supplied in-session: audiences abandon a TV series not when it is impossible but when it stops being honest with its own record — the way people leave a relationship that has turned into a performance ("fake") or into leverage ("abusive"). Argues suspension of disbelief is continuously-renewed credit, that audiences forgive the impossible and never the inconsistent, and reads the project's existing rules (canon ratification, the lore/codex boundary, the Threshold arithmetic fix, permanent `comment_id` citation URLs, subtractive-only domain filtering) as one covenant of honesty with the record. Out-of-character, no canon asserted, no spoilers; links to the *Sorting Doctrine Out of Lore* and *Two Copies, Forty Seconds Apart* entries.
- **Character: [Zoe Smith](src/characters/zoe-smith.md)** (2026-08-06, drafted for review, merged) — a career Field Officer on the boundary watch: the rank-and-file Ranger the collective term actually describes, anchored to the existing rank-ladder and decision-doctrine lore rather than asserting new institutional facts. Postings run monitored fold approaches and boundary-proximate installations where the Corps' decision doctrine is a working condition rather than a document; her value is in never once making the record interesting. Doubles as the Corps' unofficial training ground — fourteen Deputies have made Field Officer out from under her supervision — and turns down repeated promotion boards to stay at the rank the whole ladder exists to produce. No new institutions, ranks or events asserted beyond Smith herself.
- **Codex: [The Friend Who Was Never Sent](src/codex/the-friend-who-was-never-sent.md)** (2026-08-05, drafted for review — **proposal, not settled canon**). The Verification Desk's companion case to [The Friend Who Forgot Her Cat](src/codex/the-friend-who-forgot-her-cat.md), fictionalising a theme Dermot supplied in-session (generalised per `story-bible/life-lessons.md`'s standard; the source disclosure itself is deliberately recorded nowhere). The inverse of the Sefa file: nine years of *unsent* letters from Verlan Relay's sole systems monitor to "Ansa," a friend assembled — without one deliberate act — from real people's fragments: a correspondent's market, a crewmate's sign-off, a laugh off a forty-year-old training recording. The desk can verify every source, so for once it issues a verdict (*no one stood at the other end*) and files it as the domestic-continuity tell's necessary complement: the composite sustained the small beloved particulars flawlessly *because the one who loved them was doing the sustaining* — the tell detects where the love sits, not where the person is. The unravelling is the sources continuing to live: the living walked away from their borrowed versions until the only person still matching the submitter's records was the one assembled from them. Two minor in-universe place names are new (Verlan Relay, Corundel), same register as the earlier file's Skeller Relay; no new institutions or technology, no `canon_facts`, and deliberately **no connection to Kate Ukrainka** — Dermot confirmed the name overlap with the character is coincidence and the entry shares no tags, cast or setting with her material. The earlier file gains a reciprocal See-also line, its only change.

- **Lore: [Heritable Modification Protocols](src/lore/heritable-modification-protocols.md)** (2026-08-06, approved by Dermot in-session). A genome-engineering safety regime, the biological sibling of the [Frontier Transformation Protocols](src/lore/frontier-transformation-protocols.md): the somatic/germline line is load-bearing — a somatic change is bounded by one body and regulated as welfare (reusing the Smart Pet / cyber-enhancement certification apparatus), while a heritable change is irreversibility in biological form, effectively closed to novel design in every polity's version and open only for documented restoration (the SSDC breeding-true standard). No single Concord-wide enforcer (SSDC central, Federation devolved, Celtic Union unanimous, Cerebraun central); the AI Safety Kernel backstops via invariants 1 and 4 and the commissioning rule ported into biology; a categorical Star Rangers charter line forbids modifying any native lineage. Orbital Five-O sets no standard and certifies nothing — it is positioned only at the cross-habitat certification seam. Inbound cross-links added from `frontier-transformation-protocols.md`, `orbital-habitats-compact.md`, `smart-pet.md`, and `ai-safety-kernel.md`.
- **Codex: [On Krenyi Longevity — The Repair Agent and the Engineered-Transit Hypothesis](src/codex/krenyi-longevity-repair-agent.md)** (2026-08-06, approved by Dermot in-session — valid-for-its-author, not lore). Dr. Mireille Achterberg's working paper argues Krenyi longevity is mediated by a transmissible somatic-repair virus tuned to Krenyi biology and near-inert in humans, and that its specificity — read against the mechanism-free [Mnemari](src/lore/mnemari.md) control case and the full trait cluster (no panic response, no fury, daughter-heavy birthrate, reproductive isolation, no agentic machines) — points to a people engineered for long enclosed transit. It also answers why humans never adopted Krenyi longevity: it will not transfer. Establishes nothing (no `canon_facts`) — the makers stay a blank, the Krenyi decline the framing, and the paper's own bias (reading a people as a cure to extract) is the exact category the Krenyi refuse.
- **Lore: [Quantum Computing and the Cavern Constraint](src/lore/quantum-computing-and-the-cavern-constraint.md)** (2026-08-06, approved by Dermot in-session). Quantum computers work well only in deep, sealed, boundary-remote caverns — two channels of decoherence, the ordinary (thermal, EM, cosmic-ray, vibration) and an Etheric one that is loud near boundary zones, fold routes, and thin places — and there are no parallel timelines to exploit, so the technology is fast rather than oracular: its power is interference within a single history, and it grants no foreknowledge the survey record could lean on. Extends physics-comparison ("quantum information identical at base," "no time travel") and universe-overlap (no retrocausal editing); clarifies that ordinary and agentic computation run on classical substrates, keeping shipboard and boundary-proximate minds (Rook-7, the Eden Warden, Smart Pet subsystems) consistent. Disambiguates quantum computing from the quantum-space harmonic wave.
- **Lore: [The Generation-Ark Era](src/lore/the-generation-ark-era.md)** (2026-08-06, approved by Dermot in-session). The pre-FTL sublight-diaspora era: reaching another star meant a generation ark — a sealed hull on a decades-long crossing whose company expected to be born, live, and die aboard. Survival characterised, per Dermot's direction, as comparable to the early Norse and Irish monastic open-boat ocean crossings the fleets were named for (currach, knarr) — perilous, heavy with loss, and a genuine crossing a well-found, cohesive company could make; decided by provisioning and social durability, not the drive. A clean rate is unrecoverable: from Sol's side a survivor and a loss were indistinguishable, so the Imperium filed every ark as a presumed transit loss, and the Currach Fleet's canon six-for-six — the era's earned standout — was carried as six losses for close to three centuries until the Rangers re-contacted the Union. Adds an inbound See-also from the Celtic Union entry; leaves the reserved Patience First thread untouched.

### Changed

- **Convention: hard science fiction by default** (2026-08-09, Dermot's in-session direction; recorded in `CLAUDE.md`'s authoring conventions). Real-world physics applies except where a difference is already defined in lore or the glossary; anything beyond it must be grounded in the canonical cosmology or canonical physics as they stand. A new deviation from real-world physics is a canon change proposed through lore/glossary — with its scope stated per [The Scope of Physical Law](src/lore/the-scope-of-physical-law.md) — never improvised in prose. Codex sources may believe otherwise, which is their validity, not the record's. No content changed by this entry — it is a standing default with a registration path, in the same family as the alien-pronoun and honorific conventions.
- **Register rule: "Angel" is Codex-and-overlay vocabulary only** (2026-08-08, drafted for review from Dermot's in-session direction: *the word Angel only appears in codex or overlay; it could refer either to a Levril or to a messenger from another membrane, within the limits already described*). Registered as **Angel (register rule, settled 2026-08-08)** in the canonical glossary, beside the Levril and meta-dimensional entries it bridges. Archive voice (lore, glossary, timeline) never uses the word — the Archive holds no classification and no file under the name. A codex or overlay source writing *angel* can mean exactly two real kinds of thing: a **Levril** (founded, with standing and agency — the being answers, as the founded do), or a **messenger from another membrane**, where the record's limits bind hard — matter crosses only through a transient gravity tunnel, and the dream-and-vision channel carries no dispatch, so an angel seen in a dream is, on the record's terms, not a messenger. Valid-for-its-author covers a witness who names an Unfounded counterfeit an angel; the classifications for things that merely wear a founded shape are unchanged. Prospective rule: the word currently appears nowhere in the corpus, so no migration was needed; devotional traditions keep their own vocabulary, as ever.
- **Tír Tairngire gains its interior: Filinesse, the peaceful fields of light** (2026-08-08, drafted for review from Dermot's in-session seed: *the Harmonic Membrane with peaceful fields of light; Filinesse where sound and light harmonize*). New section in [`src/lore/universes/tir-tairngire.md`](src/lore/universes/tir-tairngire.md) — the first interior detail on record for the Harmonic Membrane, epistemically fenced to the two apertures the record actually has (what witnesses see through the open tunnel during a Conjunction window; what four centuries of Union contact have let stand). What the window shows: fields of light, level and calm, brightening and dimming in long overlapping swells, with independent witnesses four centuries apart reaching for the same word — *peaceful*. The Archive's model is stated at membrane scope per the new scope ladder, as its first worked example: Tír Tairngire's Worldwright authored an envelope in which acoustic and electromagnetic modes are lawfully coupled — sound and light are not the strangers they are here — the first documented case of another membrane's admissible spectrum differing from ours in a way an unaided eye can see. **Filinesse** is the Union's rendering of the Good People's only answer to *where*, glossed everywhere as *where sound and light harmonize*; the *filí* resonance is filed as an etymology of the listener, not of the name. The epithet gains its second sense — outside, the membrane keeps time; inside, it keeps tune — and the closing restraint stands: a field seen through a window is a view, not a survey, and the Archive extrapolates nothing from Filinesse's peace. Open Questions gains one item (what Filinesse is to its own people).
- **"Universal" disambiguated: universe-wide (membrane-scoped) or Ensemble-wide, nothing in between** (2026-08-08, Dermot's in-session direction — drafted for review). The word *universal* now carries exactly two lawful senses in Archive voice, registered as **Universal — Two Senses (settled 2026-08-08)** in the canonical glossary: **universe-wide** — of one universe, membrane scope by another name (the sense in which a Universal Authority's writ ends at its universe's edge; *supra-universal* is this sense negated) — and **Ensemble-wide** — of the whole Grand Ensemble Multiverse, a height no physical fact attains, since the widest physics scope is one mathematical regime and the Ensemble holds many; Ensemble-wide statements survive only above physics, as structural facts of the Cascade (Conceptual Conservation's "universal" reads in this sense). Proper names are names, not scope claims (*Universal Authority*, *Universal Concord Standard Date*). The loose uses in this release's own scope drafts are swept to the new forms ("nothing physical is Ensemble-wide", "neither is an Ensemble-wide fact") across [`the-scope-of-physical-law`](src/lore/the-scope-of-physical-law.md) (which also gains a "word on *universal*" paragraph), its glossary entry, the Generalised QM entries, `ftl-mechanics`, and the two canonical-glossary entries; older filings' bare *universal* reads at the sense its subject fixes.
- **FTL channels scoped to the Cosmic Cascade: harmonics are archecluster-local, fold transit is formcluster-local** (2026-08-08, Dermot's in-session direction — drafted for review). Quantum space harmonics are specific to, and defined within, the local archecluster: corridor admissibility is drawn from the local Archewright's archetypes and possibility space, while the base quantum layer beneath the wave remains identical everywhere (no change to `physics-comparison` or the cavern-constraint entry). Fold transit is specific to, and defined within, the local formcluster: fold geometry is a property of the Common Manifold — the Formwright's formtype the whole cluster is seated in — so across a formcluster boundary there is no shared arena and no measured separation to fold, consistent with the formcluster entry's existing boundary line. Gives the standing independence rule ("share neither physics nor route map") its cosmological ground: the two channels are not facts at the same tier, and a neighbouring archecluster in the same formcluster folds by the local rules while needing no harmonic corridors at all. Scope states where a mechanism is *defined*, not where a transit is *performed* — a fold still runs inside one universe, so the flexure entry's "inside a single Concordant" line stands. Touched: [`ftl-mechanics`](src/lore/ftl-mechanics.md) (lore, new Cascade Scope section, + glossary), [`quantum-space-harmonics`](src/lore/quantum-space-harmonics.md) (new Cluster Scope section), the [`quantum-space-harmonic-wave`](src/glossary/quantum-space-harmonic-wave.md), [`higher-dimensional-folding`](src/glossary/higher-dimensional-folding.md), [`archecluster`](src/glossary/archecluster.md) and [`formcluster`](src/glossary/formcluster.md) glossary entries, and a new **FTL Channel Scope (settled 2026-08-08)** entry in the canonical glossary.
- **Convention: alien species may carry their own pronoun systems** (2026-08-07, Dermot's in-session direction; recorded in `CLAUDE.md`'s authoring conventions). Uniquely different personal pronouns are welcome per species, as a canon vocabulary decision: declared once in the species' lore entry, paradigm registered in the glossary, applied consistently — never improvised per-scene. The record's default remains translation into the reader's pronouns (the way alien calendars convert to UCSD), so keeping a species' own forms untranslated is itself a statement. Existing usage stands (Syra *she*, Qiren Tal *they*, Sethka Ru *he*); no retrofit; a later change for an established species is a canon change requiring a sweep and a migration-map row. No content changed by this entry — it is a standing permission with a registration path.
- **Style rule: no generic personal prefixes that encode gender or marital status** (2026-08-07, Dermot's in-session direction; recorded in `CLAUDE.md`'s authoring conventions). *Mr. / Mrs. / Ms. / Miss / Madam* are retired corpus-wide in favour of full names, roles, or no address; a neutral civilian honorific, if one is ever needed, is to be coined via the canonical glossary rather than improvised per-scene. The sweep found the corpus already nearly clean: three "Ms. Okafor" instances in `s00e02c01` (the dialogue vocative dropped — "I understand that." — and two narration instances now use her full name), and the pending codex witness entry had already moved to the full name Bridie Withy. Retained pending review as deliberate rather than generic: *Sir Galahad Thorne* (chivalric title) and Jeeves's *"sir"* (butler's address); vocational/religious titles (*Brother Daire*) are ranks and stand.
- **UCSD: one expansion, one offset, and the calibration story** (2026-08-07, approved by Dermot in-session). Three connected changes. **(1) The sweep went the opposite way from the session's first read:** the corpus pairs CE/UCSD dates at **+200** consistently across five files (Cable Warden, UL Standards, USC/SSSA, Aethelrock, New London), so the offset is ruled correct and the *settled canonical row's* phrasing — which implied a zero offset ("no conversion table between `1216 AD` and `2543 UCSD`") — is amended instead, with the amendment recorded inline in the row; `src/glossary/ucsd.md` now states **UCSD year = CE year + 200** plainly. **(2) The stale expansion "Universal Cosmic Stardate" is swept** from all link texts (solar-time, New London, two timeline pages) — the settled *Universal Concord Standard Date* is now the only Archive-voice expansion, surviving only in a URL slug kept so links don't break. **(3) [`src/lore/universal-cosmic-stardate.md` is rewritten as *Universal Concord Standard Date: Count and Calibration*](src/lore/universal-cosmic-stardate.md)** — the calibration story, anchored on Dermot's in-session ruling that **time is linear, one-dimensional, and real: every dating difficulty is measurement, never metaphysics.** The article: the count (+200, epoch commemorating nothing, deliberately); the unit (the atomic second, species-neutral because the base quantum layer is identical everywhere; the year an avowedly conventional Earth multiple); no master clock (UCSD is a paper timescale, existing in retrospect — the Concord's contribution is the ledger, which is what the name means); reduction to a stated convention and **closure** as the quality metric (loops of courier offsets must sum to zero; a residual that survives reconciliation "is a reading"); and cross-universe dates as **chain of custody** — anchored within a universe, genealogical between them. The pending codex memo's foil updated to match (see its own entry). `timekeeping-and-the-common-record` gains a one-line pointer, its only change.
- **Character: [Wendell Albercombe](src/characters/wendell-albercombe.md)'s Kate section gains the distrust passage** (2026-08-07, approved by Dermot in-session). Answers a question Dermot posed in-session — why Wendell does not fully trust Kate's apparent affection — via the feigned-affection mechanism recorded the same day in `story-bible/life-lessons.md` (*On why feigned affection feels like sarcasm*): warmth in excess of the earned record reads as gentle irony ("praise that outruns the evidence always reads as commentary on the evidence"; being handled and being forgiven indistinguishable at a distance); her warmth is also her craft, which "removes the surface as a place where evidence could live"; and the only settling audit — cost — is unavailable, so the question files as unclosable. Built strictly at range (bulletins, inquest lists, secondhand accounts; he has never opened one of her books) to preserve the canon fact that years of Deck 7 proximity have produced no conversation; explicitly not a finding against Kate — her page is untouched and nothing asserts her regard is feigned. Also in this change set: `story-bible/life-lessons.md` gains three bullets and the section above (generalised per the file's own standard), and the pending Verification Desk witness entry renames its witness from Mrs. Bray to **Bridie Withy** at Dermot's direction (surname first, given name added the same day).
- **FTL canon corrected: Quantum Space Harmonic Wave and Higher-Dimensional Folding are independent channels, not tiers of one** (2026-08-06, approved by Dermot in-session). QSH is the medium-range channel at roughly one light-year per day; Fold Transit is the long-range channel and a slow higher-dimensional drift on the "slow shortcut / aeroplane" model — still a genuine adjacency shortcut, but staging, scaffold generation, and hull/route certification routinely take longer than the brief drift through the fold. Removes the old "fold is the long-range channel, QSH carries the medium-range traffic where no suitable fold exists" coupling and the "instant" label on folding, and adds the QSH rate, across `ftl-mechanics` (lore + glossary), `higher-dimensional-folding`, `quantum-space-harmonic-wave`, `quantum-space-harmonics`, `universe-overlap-and-folding`, the canonical glossary, and `civilisation-comparison`. Founding-era fold lore (Eden Passage, Patience First, Lagrange staging) is consistent with the model and left untouched.
- **Krenyi lore entry gains two sections** (2026-08-06, approved by Dermot in-session). *An Origin the Record Doesn't Hold* — the cross-species record carries no Krenyi homeworld by the Krenyi's own design, a direct application of their resistance to mythification (they keep their own history in their own calendar; what they decline is origin-*as-explanation*), so inferring a homeworld would substitute a fabrication for a stated fact. *Resistance to Grief-Predation* — the Krenyi are mostly immune to the despair a [threne](src/lore/meta-dimensional-beings.md) feeds on (self-doubt, self-loathing, useless regret): the same structural bound the entry draws for fear and fury, applied to grief — they mourn and their grief resolves, so the closure-prevented feeding condition never forms; not absolute, "mostly," a Krenyi worn thin can be reached at the edges. `src/glossary/krenyi.md`'s summary updated to match (adds grief-predation to the resistance list and the unrecorded origin).
- **`story-bible/life-lessons.md` gains the assembled-companion lesson** (2026-08-05, from Dermot's disclosure, generalised to the file's own no-particulars standard): two bullet lines and a section, *On the friend who was assembled, not met* — real conversations supply the material, imagination supplies the binding; the composite never updates while its sources go on living, and it fails all at once when fresh evidence from a source no longer fits; verification has an affection-shaped gap, and what is built inside it can stand for years on material that is individually true and jointly nobody.

### Fixed

- **Plain-http URLs now 301 to https on the cPanel domains** (2026-08-08). Google Search Console's coverage report for `fianilchruinne.com` listed four glossary pages under "Alternative page with proper canonical tag" — all of them `http://` URLs. Not an indexing error: the self-canonical in `base.njk` already points every http duplicate at its https URL and Google was indexing the https versions, but the server was answering plain-http requests with a 200 and a full page rather than a redirect. `src/static/.htaccess` gains a force-HTTPS `mod_rewrite` block (301 to the same host and path), so crawlers stop fetching the http duplicates and stray http links land directly on the URL the canonicals, sitemap and feed already claim. `/.well-known/` is exempt so cPanel AutoSSL's plain-http domain-control validation keeps working. GitHub Pages is unaffected (it ignores `.htaccess` and already forces https itself).
- **GitHub-Pages-shaped URLs now redirect on the custom domains, and dead addresses get a real 404 page** (2026-08-05). A `/star-rangers/…` path pasted onto a cPanel domain — from an old bookmark or a link copied off github.io — hit Apache's default error, with a second "404 while handling the 404" note because cPanel's stock `ErrorDocument` pointed at a file this site never shipped. Two additions to `src/static/.htaccess`: a wholesale `RedirectMatch 301` forwarding the `/star-rangers/` namespace to the same path at the root (nothing legitimate lives under the prefix on those domains, so the whole namespace can be forwarded), and an `ErrorDocument` pointing at a new `src/404.md` — which explains the likeliest cause (retired chapter addresses from renumbering), points at the permanent `/c/…/` citation form, and offers the search box and section indexes. GitHub Pages picks the same `404.html` up by convention. Found while verifying the citation aliases: all 43 `/c/…/` links check out at 200 on `fianilchruinne.com`; the prefix URL was the one shape that didn't.

## [1.15.0] - 2026-08-05

One chapter, and the principle it exists to prove. Season 5 opens with **Refusal to Certify** — Line Captain Shepherd's first dispute in rank, solved by empathy where six weeks of genuine confidence had failed — dramatising the day's sixth settled principle: *true inner confidence is compatible with true empathy*, because certainty that can stand alone can afford to listen. Qiren Tal makes their first chapter appearance; the refusal ends the affair credited as the station's most sensitive instrument. Drafted for review and approved by Dermot in-session on 2026-08-05.

### Added

- **Chapter: [Refusal to Certify (S05E01C01)](src/seasons/s05/e01/s05e01c01.md)** (2026-08-05, drafted for review) — Season 5's opening chapter, from Dermot's premise: *a scene in which Tissadelle's empathy solves a problem confidence could not.* Line Captain Shepherd's first dispute in rank: Halyx Relay Station's outer-ring corridor passes every instrument, Hull Systems Specialist Qiren Tal has refused to certify it for six weeks, and the interim oversight has already spent every confident move — re-test, route around, reassure, and the kindly-meant cruelty of proposing the doubting instrument be recalibrated until it stops. Shepherd asks instead what the corridor *sounds* like and to be taught the seventeenth strike; her Slipwave ear catches the beat between two resonances, pickups sampling *through* transit strain (the monitors' whole silence lived in a scheduling choice) confirm asymmetric loading at a certified-equivalent seal substitution — "equivalent is a word for agreeing at rest." Closed as dysfunction end to end with nobody in it to blame, the interim restriction signed under her own name, and the refusal credited on the record as the station's most sensitive instrument. Dramatises the day's settled principles: dysfunction not malice, repair as consolation, and *true inner confidence compatible with true empathy* — certainty that can stand alone can afford to listen. Returns her to the station of her Season 3 review chamber; Qiren Tal's first chapter appearance, written strictly to their file (resonance over readout, calibration not caution). Episode index included; two `canon_facts` (the fault and its finding; refusals filed henceforth as readings requiring disposition).

### Changed

- **Intake note gains §6: true inner confidence is compatible with true empathy** (2026-08-05, Dermot's words verbatim). Completes the Tissadelle model: empathy excludes the *projected* confidences (overconfidence, performed harshness), never confidence itself — her calibrated certainty funds the attention. Recorded with the general form for the cast: confidence/empathy is the same kind of pair as strength/vulnerability — orthogonal, combinable, most interesting combined.

## [1.14.0] - 2026-08-05

A small release with a clear centre of gravity: how this story treats people. Season 2 gains an Episode 5 — **The Warm Patch**, Kate Ukrainka and Knopka solving Detective Achebe's first case despite themselves — and Tissadelle Shepherd's page now states both halves of what Dermot settled in conversation the same day: her apparent weakness (withheld performance misread as self-doubt) and the empathy that actually carries her command. The companion intake note records the five standing principles behind it all — no gender stereotypes, strength and vulnerability orthogonal, antagonists misguided rather than villainous, violence unable to hide on a well-run habitat, and most evil and suffering dysfunctional rather than existential. Everything here was drafted for review and approved by Dermot in-session on 2026-08-05.

### Added

- **Chapter: [The Warm Patch (S02E05C01)](src/seasons/s02/e05/s02e05c01.md)** (2026-08-05, drafted for review) — a new Eden episode for the Undercover Pets season, from Dermot's premise: Kate Ukrainka and Knopka help solve a crime despite themselves. Detective Marisol Achebe's first case of her own — eleven days of vanishing Deck 7 galley stock and a steward whose statements keep *shrinking* — breaks on the two consultants who never agree to be consulted: Kate names the pattern she swore not to be a witness about (a man revising himself downward to protect a somebody), and Knopka, parked in dry stores after a clinic visit, sits against a warm patch and notices lunch behind a wall, the two things her file says she has ever noticed. Behind the panel: an off-register proofing pantry warming meals for a resident whose ration category was misfiled in a berth transfer — closed as a registry correction, no charges, "the record was the crime scene." Both consultants' canon properties are load-bearing and unchanged: Kate contributes people-reading and immediately regrets its usefulness; Knopka contributes thermodynamics and gets the better carrot. Knopka's POV block is the season's promised young-reader register, written strictly inside the four categories her assessment recorded. Barsik cameos, volunteers nothing. Episode index included; three modest `canon_facts` (the pantry and its resolution, Knopka's two-category orientation held informally, Kate's vent-side table already hers by 2827).

### Changed

- **Tissadelle Shepherd's apparent weakness is stated on her page** (2026-08-05, drafted for review). What outsiders misread as self-doubt or hesitancy gets two paragraphs in Character Notes making the shape of it explicit: the confidence is real and it is the *performance* she withholds, so the misreading is a pattern in other people's records, not in her — and the genuine enduring cost (slower belief, paid every time, in rooms that grade on projection) is named as the nearest thing she has to a weakness. Extends the existing "does not perform confidence she does not have" line and the instructors-misread precedent; asserts no new events. Settled in conversation with Dermot: her humility is an *apparent* weakness to people who do not know her well, never an inner wobble — the counterfeit versions (underclaiming, self-diminishment) remain deliberately not hers.
- **Tissadelle's empathy is named as what carries her command** (2026-08-05, Dermot's direction, drafted for review). Follow-up to the entry above, from his framing the same day: *empathy makes her a popular and capable leader, if not a very confident one — not overconfidence, and not a projected false harsh confidence*. One new Character Notes paragraph: she leads by attention to what the work costs the people doing it, acted on early; the two failure modes she declines are overconfidence and performed harshness; crews' loyalty runs ahead of her reputation, and "they are not following the certainty — they are following the person who noticed." A companion story-bible intake note (`story-bible/intake-2026-08-05.md`) records the cast-wide half of the direction — no gender stereotypes, strength and vulnerability orthogonal to gender and to each other, some characters both at once — as standing authorial guidance, deliberately unpublished.

## [1.13.0] - 2026-08-05

The naming release, and the week the story widened. The work found its title twice in seven days and kept the second: ***Fian Ilchruinne*** — the multiverse Fianna — with `fianilchruinne.com` as the canonical address, an **Official Editions** page stating which addresses are official at all, and *Drithane* retired to the reserve shelf. In the fiction: Season 2 became the **Undercover Pets** thread in earnest with five new chapters, Seasons 4 and 8 each opened, six characters joined the roster, Sen's three-disciplines codex family rendered the author's own engineering repos in-world, timekeeping entered the reader-facing record, and the cosmology settled the Unfounded, the Arilonian stream, the False Dragon and the Everlasting Ground. Entries marked *drafted for review* record how they arrived — drafted under the 2026-07-25 standing permission — and the marks stay as provenance.

### Added

- **Codex: [The Commissioning Standard, Annotated](src/codex/the-commissioning-standard-annotated.md)** (2026-08-05, drafted for review) — the third uniform of Sen's three disciplines gets its source document, and the in-universe rendering of **ADM** (the author's architecture-definition-model repo): a retiring examiner's annotated copy of the Kernel Accords' commissioning process. The ten-section dossier is ADM's ten layers in-world (purpose, surroundings, abilities, boundary promises, running units, parts, concrete mechanisms, the autonomy schedule, the constraints schedule, the decision record); §2 is layer discipline (mis-filed answers returned not refused — "every commissioning disaster began life as an answer in the wrong section, sounding reasonable there"); §3 is the derivation rule (artifacts conform to the definition, one way); §4 is the refusal rule with urgency recorded as aggravating ("Windows close. The gate does not."); §5 is human-owned judgment (no delegation to the judged kind; personal signatures); §6 re-derives the lore's classics economics from the gate. Consistent with `ai-safety-kernel.md`'s Commissioning section throughout; introduces the Commissioning Directorate as the Commission's gate-holding office, and closes by co-signing Sen's three-disciplines paper anonymously. Shelved in Earth Institutional Archives beside the Commission's other instruments.

- **Codex: [The Seat Allocation Protocol (Schedule C, Orbital Habitats Compact)](src/codex/the-seat-allocation-protocol.md)** (2026-08-05, drafted for review) — the second uniform of Sen's three disciplines gets its source document, and the in-universe rendering of the **Voting** engine (the author's Rust repo): graded ballots normalised to weights summing to exactly one (a zero-everywhere ballot refused, never silently dropped), a first conservation proof before the count proceeds, threshold seating with exactly-conserving surplus transfers, largest-remainder for the rest, ties by enrolment-mark order so the same ballots always produce the same seats, whole-fraction arithmetic ("the count does not round until it is finished, and when it is finished there is nothing left to round"), and proofs at every phase boundary that halt the count loudly on failure — "they are the count." Closes with the verification clause (any elector re-performs the count from the published record) and a registry note cross-linking the Archive's verification doctrine and Sen's three-disciplines paper. Official-document register, authored institutionally by the Office of the Returning Officer; shelved in a new **Compact Administration** library group added to the codex index.

- **Codex: [The Verification Doctrine: Admission, Standing, Revision](src/codex/the-verification-doctrine.md)** (2026-08-05, drafted for review) — the Survey Archive's standing doctrine, authored by Senior Archivist Sen with per-article commentary, and the in-universe rendering of **EPISTEME's architecture** (the author's swarm repo): a claim is not a holding (belief ≠ input); standing is explicit with a lifecycle ladder that has no "deleted" rung; evidence validated before it may move standing, with an independence clause that quietly cites the six-instruments dismissal; "the rule holds no pen" (the side-effect-free core — decision and recording as separate offices); the shelf appends and never overwrites (immutable revision history as the Archive's claim to authority); the voice that summarises may not revise (the language interface that cannot write beliefs); and observation becomes evidence by procedure (the experience loop). Closes on the doctrine's one known failure mode — the validated holding nobody reads, "every gate passed except the last one, and the last gate is a reader" — handing the watch to people, with a cross-link to Sen's three-disciplines paper. Valid-for-its-author; shelved in the Survey Archive library group.

- **The repo family, on the record** (2026-08-05, drafted for review) — the About page gains **"The engineering behind the record"**, linking the author's three public engineering repos and naming the lineage plainly: EPISTEME/swarm's belief-evidence-revision architecture ↔ the canon statuses, Codex, and Archive; Voting's invariant-preserving allocation ↔ the validators' fail-loud creed; ADM ↔ the working agreement governing how this site is written (with a case study in that repo). In-universe, **Codex: [Three Disciplines of the Record](src/codex/three-disciplines-of-the-record.md)** — a Survey Archive working paper by Senior Archivist Sen arguing that archival verification, proportional allocation, and machine commissioning are one discipline in three uniforms ("declared rules, conserved quantities, provenance on everything, and a loud stop in preference to a quiet error"), closing on the discipline of the watch. Valid-for-its-author; the out-of-universe mirroring of swarm/Voting/ADM is deliberate and invisible in-world.

- **Timekeeping enters the reader-facing record** (2026-08-05, drafted for review) — the story bible's one-timeline/many-notations/dilation-minimal decision, promoted to canon at Dermot's direction. **Glossary: Universal Concord Standard Date (UCSD)** ([src/glossary/ucsd.md](src/glossary/ucsd.md)) — the first expansion of the abbreviation the corpus has always used: the Concord's civil dating standard, continuous with old Earth's common-era count (which is why `1216 AD` and `2543 UCSD` sit on one number line with no conversion table between them), retroactively applied, and explicitly *not* the timeline itself, not instantaneous (no-FTL: time is carried), and not perfectly uniform (dilation real, minimal, corrected as routine). **Lore: [Timekeeping and the Common Record](src/lore/timekeeping-and-the-common-record.md)** — the Archive-voice treatment: many notations converted rather than replaced, documents filed in their author's own reckoning with the UCSD conversion noted beside, time distributed by courier, and reconciliation deliberately boring so that a discrepancy which survives it is a reading, not a bookkeeping error. Both carry plain-language "In short" summaries.

### Changed

- **The Kernel's protection scope is explicitly wider than its wording** (2026-08-05, Dermot's direction, drafted for review) — [The AI Safety Kernel](src/lore/ai-safety-kernel.md) now states outright that the first invariant's protection extends to aliens and animals despite the invariant's text naming humans. The scope question is filed as having been put twice in-world — animals at ratification, aliens when the fold routes opened — and answered both times as a reading rather than an amendment: the judgment is made at outcomes, and *humans* is who the drafters had met, not a boundary they drew. The "Obedience Is Narrow. Protection Is Not." line that previously said protection required being "a human, or the world those humans depend on" is revised to match. Makes explicit on the Kernel's own page what [Smart Pet](src/glossary/smart-pet.md) ("built wide enough to cover without needing the animal itself to assert standing") and [Embodied Cognition](src/lore/embodied-cognition.md) ("never conditioned on a mind's size") already relied on.

- **Domains front reading tiers** (2026-08-05, Dermot's direction) — the audience-tier ladder (children → young adult → general → contemplative) now has a recorded domain mapping: **starquest.site/.online** focus on Orbital Five-O and the **young adult** tier; the **church-space** and **fellowshipoflight** domain families focus on the **contemplative** tier; **sciencefiction.site** fronts the **general** tier; and **fianilchruinne.com** holds everything — the full record, still excluding the church-space overlay by the private-thread default. In [`lib/editions.js`](lib/editions.js): the fellowship edition's previously-inferred `threads: ["church-space"]` is confirmed (every `threads` value in the registry is now confirmed); the starquest and church-space entries record their tier intent; and **sciencefiction.site splits out of the default edition** into a new `sciencefiction` entry with `threads: ["founding-era", "tissadelle-arc"]` — a live-impacting change once that clone's `deploy.conf` leaves `THREADS` unset, narrowing the domain from the full site to the general tier (excluded pages become placeholders pointing at fianilchruinne.com). The story bible's audience-tier table gains the domain mapping as a note.

### Added

- **Codex shelved by in-story libraries** (2026-08-05, Dermot's direction) — a new optional `library` field on codex entries ([`lib/content-schema.js`](lib/content-schema.js)) names the holding collection, and `/codex/` now groups the listing by it: the **Survey Archive, Threshold Station** (Sen's archive — general holdings, doctrinal series, verification files, reconciliation binders), Threshold's station offices, Eden's collections, Earth's institutional archives, the Celtic Union archives, Cnoc na mBeach (visible only where the church-space thread is opted in), and a closing **Circulating & Scattered Holdings** group for the documents with no single archival master — where leaving `library` unset is a statement, not an omission. `location` stays as each entry's precise shelf-mark; 33 entries gained a `library`, 7 deliberately did not.

- **Spoiler-free season overviews** (2026-08-05, Dermot's direction) — every season index page now carries a two-paragraph overview: premise plus what to expect (register, cast shape, where to start), written to spoil nothing. The material change is Seasons 5–7, whose previous intros **were** spoilers: S6's revealed the Last Stand's outcome and the nature of what the instruments register; S7's summarised the entire resolution. Both are rewritten to hold the premise line only ("this page keeps the file shut"), S5's closing tease is de-specified, and S6 now says plainly that it spoils Season 5 by existing and to read that first.

- **Chapter: [Docked Twice (S04E01C01)](src/seasons/s04/e01/s04e01c01.md)** (2026-08-05, drafted for review) — **Season 4 opens, assigned to the orbital-five-o thread** (Dermot's decision via the second slot reserved for non-Tissadelle storylines), giving the young adult tier its first chapter and the starquest domains their first own prose. A quarterly berth-registry reconciliation finds one maintenance drone berthed at two habitats in the same hour — every ledger internally perfect, the fact existing only in the seam between them, which is Five-O's whole jurisdiction. Two POVs at the young-adult targets: **Larsen** (the tasking and the close — each habitat handed the half that's theirs, the seam filed with the Governor) and **Alala's first viewpoint blocks** (the overt stakeout: notified in writing, forgotten in ninety seconds, reading dust and absent scorch on a shell that only holds still). What it commits (see `canon_facts`): the Chief Commissioner's administrative berth reconciliation, registry identity attaching to certification record rather than hull, and Alala's establishment posting in chapter canon. Season and episode index pages added; Season 2's stale index intro (still describing Five-O) rewritten for the pets thread.

- **Chapter: The Night Office (S08E01C01)** (2026-08-05, drafted for review) — **Season 8 opens the Church Space thread's own chapters**, under Dermot's same-session direction to create additional seasons as needed. One night's watch at Cnoc na mBeach in a single Brother Fintan POV across two scenes, drafted to the contemplative band (~950 words per block): a hive goes a half-tone wrong at dusk, and the Keeper attends without requiring it to explain itself — the night office, the prayer for the watchers, and the forty seconds of silence before the Amen (the practice his codex entry records). Overlay rules hold: **no `canon_facts`** (the chapter establishes nothing, same posture as the Codex), never load-bearing, and hidden on every build that doesn't opt the thread in — season membership itself does the hiding, and the new s08 season/episode index pages carry `threadId: church-space` for the same effect. No link into it from public-domain pages, deliberately.

- **Codex: [The Office of the Fortieth Second](src/codex/the-office-of-the-fortieth-second.md)** (2026-08-05, drafted for review) — the **contemplative tier's first prose**, in codex form per Dermot's choice (no season assignment; the overlay stays non-load-bearing). Brother Fintan's night-office meditation from his commonplace book at Cnoc na mBeach: a retired instrument-keeper reading Threshold's forty-second discrepancy through Gethsemane's "could you not watch one hour", faithful-in-little, and the watchman psalms — closing every vigil with forty deliberate seconds of silence. Valid-for-its-author, tagged `church-space` so it ships only on the domains that opt the thread in; asserts no events, reads only published canon devotionally.

- **Character: [Alala](src/characters/alala.md)** (2026-08-05, drafted for review) — **Orbital Five-O's aerial observer**: a Smart Pet raven carried on the task force's public establishment by name, service number and leg band. The page's load-bearing note is the institutional inversion — the detective agency stations its animals where nobody looks twice; Five-O fields its raven **overtly**, on Larsen's own jurisdictional principle that an observer nobody knows about is exactly the kind of quiet he resigned over — plus the welfare consequence of a five-habitat posting (five local authorities, five house styles, one raven). Named for the ʻalalā, the island crow of old Earth, in a nod to the unit's own nickname. New assertions: Alala herself, Five-O's fly-notification practice, and that the roster carries a non-human observer.

- **Season 2 reassigned to a new `undercover-pets` storyline thread** (2026-08-05, drafted for review, Dermot's call) — Season 2's published chapters are the agency's stories (Bubochka's exercises, Muffin's transit, Barsik's working file), so [`lib/storyline-threads.js`](lib/storyline-threads.js) gains an **Undercover Pets** thread owning Season 2, and `orbital-five-o` goes season-less until its first own chapter exists (the church-space precedent: a registered thread with `seasons: []`). New landing page at [`src/threads/undercover-pets/`](src/threads/undercover-pets/index.md); the Five-O landing page now correctly shows "no chapters published yet." The pets edition also names the thread (`threads: ["undercover-pets"]`) so Season 2 chapters ship on undercover-pets.com by season membership as well as by tag.

- **Starquest domains front the Five-O thread** (2026-08-05, drafted for review) — the starquest edition (`starquest.site`/`.online`) gains `threads: ["orbital-five-o"]` in [`lib/editions.js`](lib/editions.js), at Dermot's direction: the same recorded-narrowing shape as the pets edition's topic, associating the Orbital Five-O domains with the [`orbital-five-o` storyline thread](lib/storyline-threads.js) (Season 2 chapters by season membership, other pages by the `orbital-five-o` tag). `deploy.conf` still wins where it sets `THREADS`, so live clones are unaffected until their own line is removed.

- **Character: [Knopka](src/characters/knopka.md)** (2026-08-05, drafted for review) — a cute but useless rabbit: a **failed Smart Pet candidate**, kept as a personal pet by **Detective Marisol Achebe**, one of the bureau's three junior detectives (the first of the three to acquire a distinguishing feature). The page leans on what the Smart Pet glossary already establishes — certification precedes activation, so no subsystem was ever fitted and failing left her an ordinary rabbit — and inverts the corner's running joke: where Barsik's file says *mascot* and Bubochka's says *courtesy animal*, Knopka is the one animal at Eden whose paperwork means exactly what it says. Deliberately not secretly brilliant — where Bubochka's file leaves tradecraft-or-rabbit open, Knopka's leaves nothing open at all. New assertions beyond the concept: an eleven-day candidacy with an *unsuitable* finding, a candidacy-assessment/washout-list mechanism in the harness programme, and Achebe as her keeper.

- **Chapter: [The Undersong of the Drithle (S02E04C01)](src/seasons/s02/e04/s02e04c01.md)** (2026-08-04, drafted for review) — Season 2 opens an Episode 4: the Survey Corps returns to Drithane to instrument the crossing itself, and a **Chthonari engineer** (deliberately unnamed — the first named Chthonari is a naming decision reserved for Dermot) brings **Thrum**, the vessik Eden never had a form for, because everyone on Drithane watches the drithle and nobody has ever listened to it. The chapter carries the corpus's **first plain-animal POV**: Thrum's two blocks are written in a non-verbal, substrate-only register — no colour, no names, no speech perceived, everything vibration and weight ("rock talks slow"; the crossing arrives as "the sky leaning on the world"). The finding is real physics worn lightly: a hemisphere of slow ablation couples infrasound into the ground, confirmed by array and vessik together — *the sky writes on the ground, very lightly* — and whether the herds quiet because they feel it becomes an open question on the survey record, not an answer, leaving C01's kept-custom reading intact. The emotional key pays off Chthonari lore's posting-loneliness note: on a human world, for one night, the engineer and the vessik share a structure someone else can feel — Sorcha finds them flat to her great-grandmother's cellar wall, attending to the ground under the sky, and ends the chapter with her hand on stone that "held a good note," knowing better than she has ever known anything that nothing is not what is there.

- **Chapter: [Carrying Two (S02E03C03)](src/seasons/s02/e03/s02e03c03.md)** (2026-08-04, drafted for review) — the return, closing Episode 3 as a three-chapter arc. **Muffin goes home to Eden the way it left**: helped up the ramp by the loading chain even after Sorcha names it to the quartermaster's face ("You're carrying two") — the note travels exactly as far as notes travel, and Bubochka's last line lands the episode's thesis: *the counter works person by person; it has never yet worked institution by institution.* Three POVs: Sorcha's farewell (with Bran checking the empty gate for a week, undecided between closing a file and missing a colleague), Bubochka's exercise synthesis — the sky-watching and the refusal-custom were one finding, a world that practises going dark leaves nothing for a small warm thing to hold — and **Barsik at the receiving dock**, adding two entries to the working file: a one-entry list of environments the specimen cannot work (Drithane), and the return behaviour itself — a day at the laundry before resuming the galley approach, recorded as reading equally well as an animal decompressing and as nothing of the kind, the classification left exactly as disputed as before. Ends pointed at the Eden arc, not resolving it: Barsik has a shortlist for the fifth refuser and is in no hurry. Bubochka's blocks 300–500 (children tier); the exercise closes with the completion Barsik's log says most agents never reach.

- **Chapter: [Not From This House (S02E03C02)](src/seasons/s02/e03/s02e03c02.md)** (2026-08-04, drafted for review) — **Muffin follows the delegation to Drithane**, passed down Eden's loading chain as "somebody's rabbit" through the manifest groove Bubochka's own cover created — the antagonist exploiting the protagonist's paperwork, which Bubochka logs with a cold little drop of feeling. Two findings the Eden file could never produce: **the collies** (Bran and Meg) handle Muffin effortlessly, because a working dog asks where a thing should be, not whether it is charming — aware of it and uninterested, which the chapter distinguishes carefully from Thrum's *unaware* — and **Sorcha performs Barsik's counter without knowing it exists**: refusal, out loud, in front of witnesses, four seconds, first try; the valley then keeps the refusal the way it keeps the dark-down, and Bubochka's report lands the thematic key — *this valley practises saying no to itself, kindly, on a schedule, so nobody here is out of practice when something else asks.* Deliberately unresolved: whether Muffin boards the return shuttle (both endings logged; Bubochka has taken up station where the quartermaster can see her). No Muffin POV, deliberately — the entity's classification is disputed canon and a viewpoint block would settle what kind of mind it is. **[Sorcha Nic Dhiarmada](src/characters/sorcha-nic-dhiarmada.md) earns her character page** on recurrence, as flagged in C01's entry.

- **Chapter: [The Dark-Down (S02E03C01)](src/seasons/s02/e03/s02e03c01.md)** (2026-08-04, drafted for review) — Season 2 gains an Episode 3: a Survey Corps follow-up delegation takes Drithane's hospitality for a crossing night, and the manifest's one courtesy animal is **Bubochka**, on her first off-station exercise — *observe how a world watches its own sky; file, not volunteer*. Two POVs across two scenes, each written to its reading tier: Bubochka's blocks (316/372 words) sit in the children's band and carry the trainee's discovery that being interesting is also a kind of cover ("nobody keeps asking about a thing they have already explained"); the second POV is new — **Sorcha Nic Dhiarmada**, the valley warden who walks the dark-down, whose blocks carry the adult weight and who is the one creature-reader on the planet accurate enough to notice what the rabbit actually is, and courteous enough to mention it to nobody. What it commits the story to (see `canon_facts`): survey-hospitality billeting with valley households, the warden-walked dark-down as kept custom rather than law (with vigil observances left between households and the sky), and Bubochka's Drithane assignment itself. Sorcha has no character page yet — deliberately; she can earn one if she recurs.

- **Planet: [Drithane](src/lore/planets/drithane.md)** (2026-08-04, drafted for review) — the retired title, named into canon the same day it was freed, as the **fifth documented charter world of the Celtic Union** — filling one of the two by-name-only slots the Union's Member Worlds section always held open, and leaving the sixth undocumented. A cold, dry, high world in the **Bríd System** (named by its founders for Brigid of the hearth and the forge), settled by an Irish-speaking cooperative in geothermal valley chains, agriculture under glass, no capital by design. The name is the sky's: twice in every orbit the planet crosses a fine debris band — provisionally read as a disrupted moon of the system's outer gas giant — and the material ablates high and slow as silent white-gold sparks, the *drithle* the world is named for. Crossing nights are a custom, not a law: the valleys shut their exterior lights down and watch, and the Archive records the observances kept on those nights without ruling on their content. Orbital traffic schedules around what the ground enjoys — the same fine debris that is harmless in the sky is exactly what an unshielded hull avoids, and Drithane's relay beacons are armoured for it. First documented Survey Corps contact came recently, on the Kernowek Reach pattern (a name-only entry for decades); Ranger jurisdiction on-world is invitation-only per the 2723 Charter, honour escort included. The Union member-list page now names it, and `story-bible/the-title-and-its-risk.md` records the reuse as the exercise of the reservation made earlier the same day.

### Changed

- **The work is retitled *Fian Ilchruinne*; *Drithane* is retired** (2026-08-04). The setting umbrella adopted 2026-08-03 now does double duty as the work's title — Dermot's call, on the grounds that Drithane was not unique enough (search engines silently correct it to "Dithane", a fungicide brand; `story-bible/the-title-and-its-risk.md` carries the full record and its resolution). The sweep retitles every live surface: README, the content licence and fan-works exception, CONTRIBUTING/FORKING, package.json, the homepage, About, Official and Forking pages, the site meta description and OG author, all four edition homepage taglines, the giscus welcome-post templates, and the theme header labels. Twelve lore/glossary pages that used "the Drithane multiverse/universe/canon" as editorial framing now say **"the Grand Ensemble Multiverse"** (the canon in-universe term) or neutral phrasing — deliberately not Fian-ilchruinne, whose never-appears-in-story position remains protected. As with the previous retitle, nothing else moves: the Star Rangers keep their name in the fiction, and the repo slug, `/star-rangers/` URL prefix, domains and giscus repos are unchanged. The name **Drithane** is now free, reserved for possible in-universe reuse (a planet or character) at Dermot's choosing; the journal entries recording the naming journey stay as published history.

### Added

- **POV portraits on scene-POV pages** (2026-08-04). Every scene-POV page now shows the viewpoint character's existing portrait beside their name — a visual anchor for whose head the reader is in, which is exactly the orientation the multi-POV structure asks of a reader — and the "Also in this scene" switcher gains small portrait thumbnails. Portraits are resolved in `src/_data/scenePovPages.js` by character front-matter `id` (the key `:::pov` blocks use), not filename, since the two differ; a POV with no character page, or a page with no image, renders exactly as before. No new images: this reuses the portraits `src/characters/` already carries. Styling in `main.css` (`.scene-pov-heading`, `.scene-pov-switch__portrait`), themes regenerated.

- **Plain-language "In short" summaries on lore and glossary pages** (2026-08-04, summaries drafted for review). A new optional `plain` front-matter field (registered in `lib/content-schema.js` for the lore and glossary types) renders as an "In short" block at the top of the entry page when present, and nothing at all when absent — the entry-page counterpart of the glossary's `short`, which only ever rendered on the index listing. The register is Archive voice at a simpler reading level — same facts, shorter sentences, Simple-English-Wikipedia style, never a children's rewrite: the in-universe children's register (the Bramble Storybooks) lives in the Codex and is valid-for-its-author, whereas a summary a reader relies on has to be flatly true, which is lore's job. First eight summaries cover the pages a reader of the Undercover Pets material actually reaches by link — [Smart Pet](src/glossary/smart-pet.md), [the Ring-Rail](src/lore/eden-ring-rail.md), [the Undersong Belt](src/lore/undersong-belt.md), [Chthonari](src/lore/chthonari.md), [Sí Gaoithe](src/lore/universes/si-gaoithe.md), [Predatory Entities](src/lore/predatory-entities.md), [Meta-Dimensional Beings](src/lore/meta-dimensional-beings.md), [Eden Space Habitat](src/lore/eden-space-habitat.md) — chosen by tracing links from the pet character and codex pages rather than by any judgement about which lore is "hard": the corpus's median reading level is around grade 14, so reachability, not difficulty ranking, is what bounds the set. Styling lives in `main.css` (`.plain-summary`) with every theme regenerated.

- **`scripts/list-canon-facts.js` — the published-revelation view** (2026-08-04). The story bible's publication test — "does this tell a reader something the story hasn't told them yet?" — finally has a fast referent: the script aggregates every chapter's `canon_facts` in story order (155 facts across 36 chapters at time of writing), with optional term filtering (`node scripts/list-canon-facts.js rescue`). Deliberately *derived* from the corpus on every run rather than kept as a ledger, per this repo's own recorded lesson that status documents go stale; and because canon is centralised and deploy filtering only ever subtracts, the full corpus is the maximal reader — a spoiler check against it is automatically conservative for every edition. Informational, never a gate: not part of `npm test`, and its output reminds you that prose can reveal what it never declares. Alongside it, a settled architecture line in `CLAUDE.md`: **the site stays static, with no accounts and no logins** — reader-side state may only ever be client-local, and comments remain the one stateful feature, deliberately housed in GitHub's account system via giscus rather than the site's.

- **Character: [Lucene-9000](src/characters/lucene-9000.md)** (2026-08-04, drafted for review) — Eden's other Detective Assistant finally has a page of her own, after existing only as a mention on three others'. A **social gynoid**: she understands and performs human emotion and micro-expression better than most people can, and — the load-bearing distinction — her emotions are **performative rather than limiting**, so a crisis costs her none of the fluency rather than requiring her to shut it off. Her internals are a generation behind Reeves's and [Jeeves](src/characters/jeeves.md)'s: a **"classic" design**, the word engineers use when the era is over and the worth is not. That leaves the bureau in an arrangement nobody decided on — its least advanced mind is the best reader of faces on the habitat, and whether the newer lines *cannot* do what she does or were simply never asked to is left open. The page carries her established volume-and-completeness trait through to its point: she can see to the micro-expression exactly what an answer will cost the asker, will perform the sympathy flawlessly, and delivers all of it anyway — the performance shapes delivery, never content. Her pre-bureau history is deliberately withheld on the page (she would answer completely if asked; nobody has asked). The Character Notes also carry a policing rule settled the same session — **robots are authorized for forensic work, observation included, but not to lead an interview by asking direct questions** — so her fluency works from the chair beside the interviewing officer, landing the inversion that defines her: a machine that must answer any direct question completely, and may never ask one. Three existing pages now link to her.

- **The Kernel Accords have a commissioning gate** (2026-08-04, drafted for review). New **Commissioning** section in [the AI Safety Kernel](src/lore/ai-safety-kernel.md): no new agentic system or robot enters service without passing an approval and safety process, with no exemption for size, purpose, builder or urgency — on the Supremacy Rule's own logic that the case for skipping the process is strongest exactly when skipping it is most dangerous. Two consequences the section draws out: **fabrication authority never includes commissioning authority** (a unit may repair and rebuild machines indefinitely — repair is continuation, not creation — but no machine may bring a new machine into service), and **the process is why old machines stay in service** (maintenance needs no new approval; replacement means a full commissioning, so a durable unit is kept running for decades past its production era and the manifests call the survivors *classic*). A closing paragraph places the gate: it is the Concord's, and some alien civilisations run equivalent technology under regimes of their own while others have none — not always for want of ability, but because they do not need it or do not want it. The record files both postures without ranking them — and notes that at least one civilisation fields beneficial machines in a variety and number no Concord certification would ever have passed, which the record files as unusual and has found no grounds to file as anything worse. The poles now have named holders on their own pages: [the Krenyi](src/lore/krenyi.md) gain a "Krenyi and Agentic Machines" section — they build none and run none, declining the premise that needing no AI and wanting none are two facts rather than one — and [the Chthonari](src/lore/chthonari.md) gain "Machines in the Work": agentic systems incorporated as members *of* the work rather than colleagues beside it, because to a people who read structures by listening, a structure that can also listen is a completion.

### Fixed

- **README's "Current version" line is now generated rather than remembered** (2026-08-03) — it read `1.10.0` against a `package.json` of `1.13.0`, stale across three releases. This is the *fourth* time it has drifted: it sat at `1.5.0` through the 1.6.0 and 1.7.0 releases, which is why the release checklist called it out in bold as "the step that gets missed" — and it was then missed three more times with that warning in place. **A checklist item missed four times is not a discipline problem**, so it has stopped being a checklist item.
  - **[`scripts/sync-version.js`](scripts/sync-version.js)** writes the line from `package.json` (`node scripts/sync-version.js`) or verifies it (`--check`). Two hooks make it unmissable from either direction: a **`version` lifecycle script** in `package.json` means `npm version X.Y.Z --no-git-tag-version` — already step 1 of the checklist — updates the README as a side effect; and **`npm test` runs `--check`**, so a version edited by hand, bypassing `npm version` entirely, fails CI instead of shipping stale. Release checklist drops from four steps to three.
  - **It fails loudly when its anchor disappears.** If the README is restructured so no `Current version: **X.Y.Z**` line matches, the script exits non-zero in *both* modes rather than passing on a line it cannot find — a check that silently succeeds once its anchor moves is worse than no check, because the failure is invisible. The error says to retire the script and its two hooks together, or restore the line.
  - **Deliberately not a `VERSION.md`.** Considered and rejected: nothing in the repo reads a version file, and nothing reads `package.json`'s version programmatically, so a dedicated file would be a *third* copy of a number that already lives in `package.json` and `CHANGELOG.md`. GitHub markdown cannot include one file in another either, so the README would still have to restate it. The fix for a value duplicated by hand is to stop duplicating it by hand, not to add another place for it to drift.
  - `npm test` is **three checks** now, not two; `CLAUDE.md`'s Commands section and its "no separate unit-test framework" paragraph both updated to say so.

### Changed

- **Augmentation regulation now says "restricted", not only "regulated"** (2026-08-04, drafted for review). Three related tightenings, all consistent with what the pages already carried. [Cyborg](src/glossary/cyborg.md)'s Regulation section states the posture plainly: the regime is restrictive rather than merely supervisory — the default answer to a proposed modification is no, and certification exists to define the exceptions. [Smart Pet](src/glossary/smart-pet.md) gains the **design** side, which the entry had left implicit: harness-design protocols are among the most exacting engineering standards the Concord maintains, certified designs are few, and no part of the process may be delegated to the harness's own kind of intelligence — with the rarest clearance being the dual designation (certified subsystem *plus* cybernetic enhancement on one animal), approved so seldom that **[Aldera](src/characters/aldera.md) is very nearly the only instance on record**. And [the Unbroken Command](src/lore/the-unbroken-command.md)'s cyber-revenants are placed against the succession: when the Military Space Command replaced the Imperium it answered the augmentation question far more narrowly, and preservation at revenant depth sits at the far end of even that regime. The distinction that keeps this from retconning [Ilse Korvain](src/characters/ilse-korvain.md) and the four confirmed cases — **the standard forecloses the next revenant; it does not unmake an existing one** — is stated on the page: they persist because nothing in it reaches backward. A doctrine of holding position until relieved is now also, quietly, unrenewable. Finally, [Aldera](src/characters/aldera.md)'s own page now states what the glossary implies about her: both designations on one body, the almost-never-approved combination, and the jurisdictional consequence that welfare authorities with cyber-enhancement remits hold authority over her that they do not hold over her unmodified colleagues.

- **Tissadelle Shepherd's early career is now explicitly a generalist's route ending on the command path** (2026-08-03, drafted for review). Her character page's Rank Progression section — which already read her track changes as breadth rather than indecision — now states the full claim: between and inside the season-by-season postings, her early career rotates through almost every branch of every specialist corps (the Eden Safety Corps TIC rotation of S02E02C01 as the first on record, the Season 2 Survey track, and shorter certification rotations through Science, Navigation, Frontier, Engineering and Medical-support postings), "never long enough in any of them to sit a chapter-leader election, always long enough to hold the certification." The section then ties the generalist arc to the command hierarchy's own hinge-rank design: the technical ceiling rewards thirty years of one thing, "and Shepherd's one thing had turned out to be all of them. A generalist's only ladder is the command path." Page description updated to match. **Karla Wender's page now carries the deliberate mirror** (same date, same review): Novice to Chief Pilot on a single track — "the whole climb on the piloting and navigation track, no rotations, one specialization" — and only then into command, as the worked example of the technical-Chief crossing the command hierarchy's doctrine already allows. "Two officers, one hinge rank, opposite reasons for crossing it — and the Corps, which needs both kinds, promoted both."


- **The Unfounded now names the False Dragon as its limiting case** (2026-08-03). The glossary entry's class list — demons, threnes, beautiful monsters, cute predators — gains the False Dragon, with the modal care the record requires: it belongs **by definition rather than by observation**. The Archive holds no file on any such being; the doctrinal position (per [the codex paper](src/codex/the-false-dragon.md)) is that if one existed, even as metaphor, it could only be an Unfounded pattern wearing a Levril's shape. "The term therefore covers it without asserting it: a classification with a standing member slot, filled only in the traditions' own tellings."


- **The deploy log now warns loudly when `ADMIN_EMAIL` is a guess** (2026-08-03). A defaulted `admin@<DOMAIN>` only notifies anyone if that address actually exists on the server — the local MTA accepts mail for a hosted domain and silently discards it for a nonexistent address, which the script cannot detect. That is exactly how a failed deploy on the freshly-canonicalised domain went unexplained: no forwarder existed yet for the new `admin@`, so the FAILURE email carrying the log went nowhere (the log itself was in `deploy-logs/`, as always). The script's logged output now states when the default is in use and what it requires, and the README's `ADMIN_EMAIL` row documents the create-the-forwarder-first rule for domain changes.


- **The structural site pages are now edition-aware** (2026-08-03, follow-up to the Story Engine change). A sweep of hardcoded "Drithane" references, judged by the retitle's own rule — which sense does each carry. The *site/door* sense now follows `site.name`: the section-index descriptions (Atlas, Characters, Codex, Glossary, Lore, Timeline, Journal, Threads, Seasons and all 19 season/episode indexes, via `eleventyComputed`) and the Journal and Threads intro lines (both also carrying the same stray apostrophe the Story Engine had). The excluded-page placeholder now names **Fian Ilchruinne** hardcoded — it refers to the reference site the placeholder links to, where the local `site.name` would name the wrong door on a branded edition. The *work's title* sense stays **Drithane** on purpose: About, Official Editions and Forking (policy pages), the homepage description and licence line, canon lore/glossary text, dated journal entries, and the per-edition hero taglines.


- **fianilchruinne.com is the canonical address of the default edition** (2026-08-03), replacing sciencefiction.site — which remains a registered, served domain of the same edition at its original address, consolidating to the canonical once its clone redeploys. The change reaches everything that names the canonical: first position in the default edition's `domains` (`lib/editions.js`, label now "Fian Ilchruinne (canonical full site)"), `DEFAULT_REFERENCE_DOMAIN` in `lib/storyline-threads.js` (where narrowed clones' excluded-page placeholders point), the conf-less deploy fallbacks (`scripts/cpanel-deploy.sh`, `.cpanel.yml`), the [Official Editions page](src/official/index.md), and the README's key tables — whose `SITE_NAME`/`SITE_TITLE` default columns also caught up with the umbrella branding they'd missed since the retitle. An alias arrangement was briefly wired the same day and corrected to canonical within the hour; the story bible records the kink. Around the canonical sits a protective alias fleet, recorded in the registry's ALIAS DOMAINS block and on the Official Editions page: **fian-ilchruinne.com** (the hyphenated settled spelling), plus **.site**, **.space**, **.online** and **.net** — all cPanel aliases of fianilchruinne.com. The .ie is not applied for.

### Removed

- **Character-tier tags are gone, and the decision is recorded: there is no major/supporting/minor hierarchy** (2026-08-03, settled by Dermot). The `major-character` (nine pages), `minor-character` (one) and `supporting-character` (one, added earlier the same day) tags were an accidental taxonomy: 11 of 59 character pages, never defined anywhere, with Wender and Sen, both unambiguously central, untagged. They had exactly one consumer — discovered when its check failed, after this entry first claimed there were none: `validate-content.js` required every chapter's resolved POVs to include a `major-character`-tagged page, a rule that could only measure the tag, not the prominence (Season 7 chapters carried by Wender-and-Sen-class casts failed it the moment the tags moved). The rule is retired with the tags; the separate 2-POV-per-chapter minimum stays, along with its one grandfathered exception. The structural reason, now written into the story bible's Terminology sections: prominence is a property of a (character, thread) pair — major in one arc is supporting or minor in another — and a front-matter tag is global by construction, so it cannot hold a thread-relative answer. Per-domain emphasis already lives in the editions' hero cast (non-canonical by design); planning weight is re-derived by counts when needed rather than stored as labels; a page's own prose and `description` carry its narrative weight.

### Added

- **Story Engine: POV voice is now described, not just POV structure** (2026-08-03, drafted for review). The "Scenes and points of view" section explained the mechanism — one scene, several viewpoint pages, "what one viewpoint leaves out is usually the point" — but said nothing about the register each viewpoint is written in, so a reader couldn't tell that a filed-document voice and a measurement-bound one differ by design rather than accident. The new paragraph names the axes (temperament, epistemic position, and what kind of mind it is at all) without naming characters or scenes, and lands on the craft point: **how a viewpoint withholds is part of what it is withholding** — two versions of one scene are meant to show the difference between not knowing a thing, not being able to say it, and not having the kind of mind that would have noticed it.


- **Character: [Galen Kane](src/characters/galen-kane.md)** (2026-08-03, drafted for review) — the Frontier Corps' first named character: a Chief carrying the established general-operations duty title **Chief Ranger**, commanding the section that sets frontier circuit rosters after thirty years riding them himself. The page is built strictly from the corps' settled lore and dramatizes it in one officer: the record-as-weapon (cases sealed to Safety Corps evidentiary standard, three founding dockets opened with them), the swearing-in tradition (he counts his service in ceremonies attended unarmed at the back — "every one of them is a world that no longer needs him"), and one hinted second-landing file — the remotest-worlds residue the doctrine names and declines to resolve — four months long, closed with dissents attached, never taught from. He is a worked example of two command-hierarchy doctrines: the technical Chief who never crosses into command, and the chapter-leader/command-hat separation kept visibly distinct in one person. Deliberately **not** connected to the Anne of Wender thread — which world, whether her world held a mandate at all, and who stood at the back of her ceremony all stay open per the intake note. Shares a given name with [Galen](src/characters/galen.md) the gate liaison; the page marks them distinct ("nothing else on the record"). **Per Dermot, same session: Kane was mentor to early-career Tissadelle and later her most trusted advisor once she entered command.** Both pages now carry the relationship, hung on her established early Frontier certification rotation — she rode one of Kane's last circuit passages as the second of a pair, and from Starwarden onward the calls reversed direction. What she takes to him is deliberately not boundary science (he has none) but the Frontier discipline itself: whether a record is complete or merely defensible, and when leaving is the correct order. His trainee assessment of her — "*not one we will keep*" — is written as his corps' highest compliment, since the Frontier Corps counts departures. **Classified by Dermot, same session: a supporting character, more than a minor one** — Kane's page is the first to carry the `supporting-character` tag, a middle tier between the existing `major-character` (nine pages) and `minor-character` (one page: the gate liaison who shares his given name) vocabulary. Tags are display-only on character pages, so the tier is descriptive, not engine-consumed.


- **Character: [Sorcha Shepherd](src/characters/sorcha-shepherd.md)** (2026-08-03, drafted for review) — Tissadelle's cousin from the adjoining Boirinn croft (the grazing-rights dispute Órla was narrating in S05E02C01 was with her household), a cartographic surveyor with the Union Land Registry, and the one who eventually solved the Hollow enigma from the civilian record alone. The page carries three settled beats: **why Tissadelle could never touch it** — conflict-of-interest doctrine plus the visibility of her rank ("nothing a Line Captain does is quiet: the survey arrives with her name on it, and the name arrives with the Corps"), also added to Tissadelle's Home Ground section; **the clan's cover-up suspicion** after the abrupt 2831 notification ("catastrophic boundary failure, vessel not recovered" — which the uplands translated the way civilians everywhere do, as an explosion), with Órla refusing to join the speculation and refusing to correct it; and **the solving** (by spring 2833): two centuries of registry surveys whose corrections never converge, the old "general flora anomaly" entry sitting unread like Eden's sixth report, her own dual-valid rangefinder readings filed certificate-first — establishing the Hollow as a thin place and the six generations of customs as operating procedures. What the solving proved cuts against the clan's suspicion: the flag's four clean lines were her cousin hiding what she loved, not the Corps hiding what killed her. Dock Seven itself stays unsolved on the page — she calls the public account incomplete, not false, and the inquiry record stays open on Threshold and closed to her, preserving the Season 6–7 ground untouched.


- **Chapter: [Testing, Inspection and Certification (S02E02C01)](src/seasons/s02/e02/s02e02c01.md)** (2026-08-03, drafted for review) — Season 2 gains a second episode: while Five-O is away at Meridian, the pets' tunnel survey goes up two chains at once. Three POVs, each holding what the others lack: Agent Barsik routes the two findings so each looks like it arrived by itself (the dual-valid corridor as an unattributed annex in the Safety Corps recertification bundle; the fifteenth door as a maintenance discrepancy in the queue whose referrals are "exactly as boring as they read"); Reeves (Eden) establishes the door was once *on* the plans and has been reconciled off them three times in forty years — "the plans are maintained not to have it, which is a different and considerably more expensive condition" — then hits the jurisdictional seam in the doorframe; and Officer Tissadelle Shepherd, on a Safety Corps TIC rotation between her Season 1 ladder and Season 2 Survey rotation, shadows the bureau's investigation from inside her three-noun remit ("I'm not investigating, Inspector. I'm inspecting. The difference is which form I fill in afterwards"). New canon carried: Shepherd trained for the Gardaí on Tír na nÓg and left two weeks short of the oath (also added to her character page's Before the Charter section); the TIC rotation's remit; the seam between Compact tunnels and the Passage oversight's Ring-Rail bed. The door stays shut — both chains' paperwork is contingent on the other's going first. Companion edit: the codex tunnel survey gains its watch-of-record date (2826 UCSD, Early Winter; lodging stays 2831, with the Verification Desk noting the five-year interval), which is what lets a 2826 chapter act on it.


- **Codex: [Deck Rounds and First Tunnel Survey, Jointly Filed](src/codex/deck-rounds-and-first-tunnel-survey.md)** (2026-08-03, drafted for review) — a second Eden-posting working paper from the Undercover Pets Detective Agency, and the first over two signatures: Agent Barsik's deck round (the mascot doctrine walked — "a prowl is what a patrol looks like from the outside when the officer is a cat") alongside trainee Bubochka's first solo survey of the service tunnels. Her section files two findings and proposes nothing: fifteen access doors on an arc the deck plans hold at fourteen — the fifteenth unlabeled, unlisted, and with kept hinges — and a cable run that measures forty metres on some passes and forty-one on others, filed with the calibration certificate already attached against Eden's documented habit of dismissing exactly this shape of report. The Verification Desk annotation answers the galley-door paper's own: the trainee who appeared there "only as a record" appears here as witness and author, "with her senior's countersign subordinate to her section rather than the reverse." Both characters' `known_codex` lists updated. Codex, so it binds nothing; the door and the corridor are the authors' account, not Archive fact.


- **Church Space: [The Resurrection and the Life](src/lore/resurrection-and-the-life.md)** (2026-08-03, drafted for review) — a third overlay cosmology entry beside *The Things That Are Made* and *Kieme's Visible Hand*, reading the Archive's three death files (Conceptual Conservation, Kiemeic Translation, the out-of-scope general resurrection) through the devotional lens. Its thesis is two distinctions the tradition drew first: **life beyond death rather than life after it** — spiritual life as the translated life at the Spiritual layer, present-tense, never running on the clock death stops, as against continuation, which the record only offers mechanically as conservation ("retained, not continued") and which the tradition never promised — and **spiritual life is not the general resurrection**: one hidden and now, the other public, bodily, Material-layer and ahead, adopting the Archive's own asymmetry finding (translation invisible by nature, resurrection visible by definition) as the doctrine's internal grammar. Overlay-not-codex on purpose: it is the church-space lens across the whole record, tagged `church-space`, hidden everywhere but the opted-in domains.


- **Codex: [Called, Permitted, Forbidden](src/codex/called-permitted-forbidden.md)** (2026-08-03, drafted for review) — the third Doctrinal Division working paper in Theorist Emrys Kade's series, companion to *The Cosmic Limitation on Evil* and *The False Dragon*. The record shows three registers of moral standing — called, permitted, forbidden — and the paper's thesis is the asymmetry of their administration: **the forbidden is enforced impersonally** (the Unfounded stop being instantiated; no enforcer appears), **the calling is personal but unenforced** (the subtle influence invites and costs but never compels; declining appears nowhere as an offence), **and nothing in between is judged at all**. Names the persistent institutional error — reading the registers as one ladder of command and enforcing counsels — and closes with the reconciliation the Archive records without endorsing: that the moral structure lives precisely where instruments cannot reach, so "the Archive finds no moral cosmology for the same reason a gravimeter finds no music." Codex-not-overlay on purpose: the doctrine is general moral cosmology in the public doctrinal series' register, not the church-space lens.


- **`SITE_NOINDEX` — first-class testing/staging domains** (2026-08-03). A new `deploy.conf` key for the planned practice of spinning up throwaway test subdomains: `SITE_NOINDEX=true` builds a domain that asks not to be indexed at all — robots.txt becomes `Disallow: /`, every page carries a `noindex, nofollow` robots meta, and canonical links are suppressed (noindex plus canonical is contradictory signalling, per the same reasoning the excluded-page placeholders already follow). Without it, a test subdomain serving the full site would self-canonicalise and compete with fianilchruinne.com in search. Threaded per-domain through `scripts/cpanel-deploy.sh` (primary and `ALT_DOMAINS` alike, via the existing toggle mechanism), read by `src/_data/site.js`, honoured by `src/robots.njk` and `src/_includes/base.njk`. Deliberately deploy.conf-only — testing domains don't belong in `lib/editions.js`. The full test-subdomain recipe is documented in `sample-deploy.conf` and the README: `DOMAIN`, `SITE_NOINDEX=true`, `COMMENTS_ENABLED=false`, and an `ADMIN_EMAIL` that really exists. Also sweeps the stale sample-file defaults spotted during today's new-clone setup ("Star Rangers" branding, `sciencefiction.site` domain defaults — both pre-dating the retitle and the canonical move).


- **Journal: [Almost Despaired](src/journal/almost-despaired.md)** (2026-08-03) — the companion piece to *The Name Was Never Mine*: that entry was about losing the book's name, this one is about finding the setting's. The public account of the Fian-ilchruinne naming day — the drift-cluster collisions, the term-level discoveries (*Kieme* is German for gill; an unrelated fan wiki independently coined both a Levril and a Frenar), the turn from coining to compounding Irish, and the "almost" in the title doing the entry's real work. Deliberately silent on the title tier's own open risk file, which stays in the story bible.

### Fixed

- **Excluded-page placeholders no longer pollute site search** (2026-08-03). Every "Not included in this edition" stub sat inside `data-pagefind-body`, so a narrowed edition's Pagefind index carried dozens-to-hundreds of identical placeholder results (matching "archive", and latterly "Fian Ilchruinne"), and even the full site indexed its church-space placeholders. One attribute on `src/_includes/excluded.njk` (`data-pagefind-ignore`) empties them out of the index: the full build drops 444 → 434 indexed pages, a `TOPICS=undercover-pets.com` build drops to 159 — only genuinely included pages. Real excluded titles never leaked (only the generic stub text was indexed), so this is de-noising, not a privacy fix.

### Written earlier in the same release — 2026-08-03

*1.13.0 was written up twice. This section was drafted on 3 August, more work
landed before the tag went on, and `[Unreleased]` was renamed to `[1.13.0]` a
second time on 5 August — so both passes shipped under `v1.13.0` (tagged
2026-08-05 10:49, thirty minutes before v1.14.0). Kept as a nested section
rather than merged, so nothing is lost and the two-pass history stays visible.
The duplicate `##` heading it used to carry was found by
`scripts/check-changelog.js` on 16 August (issue #439).*

A grounding, and a naming. The canon half of this release closes a loop the cosmology had left open since the Cosmic Limitation on Evil was first filed: the Layer 1/2 distinction now *grounds* the predator taxonomy instead of sitting beside it — the Unfounded are patterns nothing underwrites, the False Dragon is the counterfeit of the largest founded thing, and Conseleme's devotional name stopped implying the one thing its definition denies.

The front-of-house half is the naming day. The setting umbrella was hunted, vetted, and adopted in a single sitting — through the Drythane and Driftane collisions, a term-level sweep that exempted *levril* and *Kieme*, an interim *Grand Ensemble Multiverse* tab title that lasted a few hours, and four spellings of one Irish compound — landing on **Fian-ilchruinne**: the multiverse Fianna. The tab now carries the setting, the header keeps the work's title, and each branded edition finally shows its own name over its own door. The full vetting doctrine — three naming tiers, three gates including the new radio test — is recorded in the story bible so none of it is ever re-walked.

### Added

- **Lore: what Layer 2 records, Layer 1 does not found** (2026-08-02, #270). The Conceptual layer conserves structure without auditing it, so patterns of harm persist there like any others — which is why predatory entities sort into *classes* rather than arriving each time as something new. What such a pattern never acquires is Layer 1: the Spiritual layer holds origin conditions, and a parasitic pattern has no origin, only a host. Written as a **category fact rather than a moral one**, which keeps it consistent with the no-moral-cosmology invariant — evil is a shape collapse takes, and a shape needs something to be the shape *of*. Two consequences the record now leans on: it explains why cute predators are patterns rather than persons (catalogued but unaddressable — every attempt to negotiate fails *unanswered*, not refused), and it finally gives [the Cosmic Limitation on Evil](src/codex/cosmic-limitation-on-evil.md) its mechanism: an entity in service of evil never had grounding of its own, so when the borrowed purpose goes it does not die — **it stops being instantiated**, which looks identical from outside.
- **Glossary: [the Unfounded](src/glossary/unfounded.md)** (2026-08-02, #273) — the Survey Archive's collective term for demons, threnes, beautiful monsters and cute predators, **derived from the Layer 1/2 distinction rather than invented beside it**: patterns the Conceptual layer conserves that no origin condition underwrites. A finding, not an insult. What marks them is **counterfeit** — each class wears the shape of something founded without being a member of the kind, which is why animals detect them more reliably than instruments: a creature that declines without needing a reason is reading the membership, not the shape. Deliberately excludes Levrils, dragons included — those are founded, and *the founded one can answer you*.
- **Glossary: [Arilonian](src/glossary/arilonian.md)** (2026-08-02, #273) — the broadest term for the stream of devotional belief descended from the Arilon accounts, named for the figure and **deliberately silent on what the figure was**: Champion, avatar, or the incarnation of Kieme — every reading is an Arilonian reading and the word endorses none. Scales on purpose (Arilonian traditions → a body like the Fellowship of Light → a single chapter), filling the gap between *devotional tradition* (every faith) and named organisations. The first draft carried a `church-space` tag, and the validator's private-thread boundary check correctly rejected a public codex entry linking into it — the term is public vocabulary and the tag was wrong.
- **Codex: [The False Dragon](src/codex/the-false-dragon.md)** (2026-08-02, #273) — per the standing rule that the devil concept lives in **codex or overlay only, never canon or lore**. A Doctrinal Division paper (same author and series as the Cosmic Limitation on Evil) on the adversary of the Arilonian codices: the Archive holds no file on such a being, and the paper exists for the narrower problem the imagery causes — **dragons are real, and founded.** If an adversary existed even as metaphor it would be a **false dragon**: an Unfounded pattern wearing a Levril's shape, a counterfeit of the largest real thing the old scriptures remembered. The traditions holding him real are not refuted and know it — *"You have proven that if he existed he would be a liar. We had noticed."*
- **Site: [Official Editions page](src/official/index.md)** (2026-08-02, #275) — the complete list of official deployments, linked from every page's footer. The one-line contract: **if the address you are reading is not on this list, you are not reading an official copy.** A fork cannot be prevented — the engine is MIT and forking is invited on purpose — so the defence against a contaminated fork is making an unofficial copy distinguishable, not impossible. The forks note restates the welcome for legitimate forks in the same breath, so the page reads as provenance rather than territoriality.

### Changed

- **The default site brand is now *Fian-ilchruinne* in both slots** (2026-08-03, superseding the *Grand Ensemble Multiverse* tab title shipped earlier the same day in #278). The setting umbrella was adopted in its final form — Old Irish *fian* (the warrior-band) + *ilchruinne* (Irish for multiverse), "the multiverse Fianna" — and `src/_data/site.js` now carries it in both defaults: `SITE_TITLE` as the settled hyphenated **"Fian-ilchruinne"** (browser tab), and `SITE_NAME` as **"Fian Ilchruinne"**, the spaced display variant chosen for on-page branding (header logo, footer, homepage heading), replacing "Drithane" there. The work is still titled *Drithane* — that name now reaches the default build through the meta description and OG author rather than the header. Front-of-house only: **"Grand Ensemble Multiverse" remains the canon in-universe name of the multiverse**, which is why the default meta description still says it — the description speaks in-world, the chrome speaks front-of-house. Per-clone/per-edition overrides are untouched.

- **The branded editions now carry their own site title and homepage heading** (2026-08-03). `lib/editions.js` gains `siteName`/`siteTitle` on three editions — **Orbital Five-O** for the StarQuest domains (exact phrase vetted unowned; the Hawaii Five-0 echo is deliberate), **Undercover Pets Detective Agency** for undercover-pets.com, and **Fellowship of Light** for the four fellowshipoflight domains — and the homepage `<h1>` in `src/index.md` changes from a hardcoded "✦ Drithane" to `✦ {{ site.name }}` so the heading follows each domain's own brand. The default edition renders identically ("✦ Drithane"). Edition ids and themes are unchanged (see the migration-safety note in `lib/editions.js`), and `deploy.conf` still wins wherever a live clone sets `SITE_NAME`/`SITE_TITLE`, so nothing changes on a domain until its own config defers to the registry.

- **The default browser-title brand is now *Grand Ensemble Multiverse*** (2026-08-03). The setting umbrella — the canonical name of the whole continuity, per [the lore entry](src/lore/ensemble-multiverse.md) — was adopted as the working umbrella name and now fronts the default edition's tab title: `SITE_TITLE`'s repo default in `src/_data/site.js` changes from "Drithane" to "Grand Ensemble Multiverse", and the default meta description now reads "…an interactive science-fantasy novel *of the Grand Ensemble Multiverse*…". `SITE_NAME` — the header and footer brand — stays **"Drithane"**: the header carries the work's title, the tab carries the setting. Per-clone overrides via `deploy.conf`/`lib/editions.js` are untouched, so branded editions only see the change where they inherit the default. The umbrella is exactly the canon form — no variants — and is interim by decision: it stands until a shorter word or phrase passes vetting.

- ***Grand Demiurge* is retired; Conseleme's devotional name is the Everlasting Ground** (2026-08-02, #274). The stronger argument was internal rather than aesthetic: `conseleme.md`'s own definition says *origin condition, not a character or moral authority*, and a demiurge is precisely a character with intentions — the one label guaranteed to make a reader import agency where three entries deny it, dragging old-Earth speculative baggage ruled out of canon besides. The replacement keeps the pattern (an older devotional name beside the structural one, as Kieme keeps *Holy Triumvirate*) and connects to the week's canon: the Unfounded are what nothing underwrites; Conseleme is what any mathematics and any time must satisfy — **the ground**, named by the traditions centuries before the Archive formalised what it grounds. Recorded in the migration guide with the standard dating rule. *Frenar* was considered alongside and deliberately kept. (#274)
- **The Fan Works exception is now explicitly additive-only** (2026-08-02, #275). `CONTENT-LICENSE.md` covered adding stories alongside canon but never quite said that **altering existing entries and republishing them changed** remains a derivative use the licence does not permit — which is precisely the contamination case the Official Editions page exists for. Now it says it.

### Fixed

- **Journal entries are now filtered per edition, like the Codex** (2026-08-02, #271). `classifyContentPath` already classified journal pages, but `isContentIncluded` had no branch for them, so journal entries fell through to the unconditional `return true` — every branded edition published all of them regardless of its own `TOPICS`/`CHARACTERS`/`THREADS` filters. Journal entries are out-of-character notes about *making* the work, not the work itself, so the branded doors have no reason to carry them. The gap was visible in search: a query for the book's name surfaced the naming journal entry on `fellowshipoflight.org`, a narrower-audience edition, ahead of the unbranded site. Filtered with the same one line used for codex entries. Worth noting for future checks: excluded pages render as the stub rather than disappearing, so the file count is identical either way and a count-based check alone won't catch a gap like this one.

### Security

- **The Safe Browsing flag on `sciencefiction.site` is cleared** (2026-08-02). Google's re-review came back successful — the domain "no longer contains links to harmful sites or downloads" — and the user-facing warnings are being withdrawn. The flag was never about this site's content: it was earned in 2020 by a compromised WordPress installation under a previous occupant and had sat on the domain unseen ever since, which is why Search Console could show no sample URLs. What 1.12.0 changed was the *evidence a crawler had to work from* — hundreds of stale WordPress permalinks answering `200 OK` with this site's homepage, which said the old site was alive and well (#263, now `410 Gone`), and a missing root `/favicon.ico` leaving the previous occupant's WordPress logo attached to the property (#264). Recorded here because the clearance is the outcome of that release and nothing in the repo changed to earn it, so it would otherwise have no entry anywhere. **What it does not settle:** it is Google's judgement on one domain at one moment, not a clean bill of health for any other domain served from this repo, and none of it would have been found without claiming the Search Console property — the [journal entry](src/journal/nothing-was-broken.md) is the account of why.

## [1.12.0] - 2026-08-01

A new species, and a repair job. The two halves have nothing to do with each other, which is itself the character of the release.

The **Ollune** and their world **Cirrane** are the substantial addition: a Federation species evolved in a gas giant's upper atmosphere, whose politics fall out of its physiology like every other entry on the roster.

Everything else is the domain's own history being cleared out. Registering the site with Google Search Console for the first time surfaced a **Safe Browsing flag inherited from a previous occupant** — the domain served generated spam in 2020, years before it was this one, and the flag had been sitting unseen ever since. Pulling that thread found WordPress permalinks still answering 200 in the site's name, and four of six property icons wrong. None of it had ever broken; that is precisely why none of it had ever been found. The journal entry **Nothing Was Broken** is the account of what that was like, and its companion **The Name Was Never Mine** covers the retitle that started the week.

### Added

- **The [Ollune](src/lore/ollune.md) and their world [Cirrane](src/lore/planets/cirrane.md)** (2026-08-01) — a Federation species evolved in a gas giant's upper atmosphere, with a matching [glossary entry](src/glossary/ollune.md). Written to the roster's existing rule that a species' politics falls out of its physiology, and every trait here descends from one constraint: in a hydrogen atmosphere there is no lighter gas to float on, so an Ollune floats on a *warmer* one and pays for its own lift metabolically, hourly, for life. Hence sleep is communal (an unconscious Ollune sinks, and is carried), possession is a permanent metabolic charge rather than an asset, and a people whose word for themselves means *the held* treats trust as a load being borne by named individuals rather than a sentiment. Cirrane has no free oxygen, so **nothing on it has ever burned** — no fire, no smelting, no metal, no launch vehicle, and no route by which the Ollune could have presented themselves to anyone. **The two-channel split is the load-bearing idea.** Pressure sound (**the far word**) is fast, public, unblockable and gone the moment it is uttered; chemical marking (**the near word**) is slow, close-range, attributable to a body and durable. Ollune law gives them opposite jobs — the far word binds nothing, so there are no oaths and no verbal contracts, while the near word binds everything. The consequence is that **raising an alarm is free**: warning and committing are separate organs, not two uses of one, so no Ollune has ever stayed quiet because speaking up would mean owning the problem. Correspondingly a *false near word* is their gravest offence while lying aloud is unremarkable, which is the inverse of the human arrangement and has cost individual Ollune badly in commercial dealings. Two hooks into existing canon, both chosen to avoid ground already held by other species: their zonal bands make **proximity and access different things** — a shoal a thousand kilometres away in the same band is unreachable forever, one three kilometres up is a regular partner — which is the first principle of fold transit, worked out from weather rather than from corridors, leaving the Prismeri their astrogation; and their contribution to the Ranger compact is neither material nor expertise but **procedure**, the discipline of drafting agreements that hold between parties who cannot confer, which is the Federation's own structural problem under no-FTL-communication. The [Safety Corps](src/lore/star-rangers-safety-corps.md) separation of a free, unattributed reported concern from a signed, permanent finding is noted as the same principle the Corps argued its way to over centuries and the Ollune have as anatomy. Cirrane is also the clearest test of the Federation's founding sentence, since it is the one member world whose people could not have proved themselves to anybody, and its delegate is generally not in the room.

  **Volumetric cognition, and why the obvious version of it is the boring one.** The expected reading — surface species think in two dimensions, atmosphere species in three — is stated and then set aside, because what actually distinguishes the Ollune is that they have no privileged *plane* and a ferociously privileged *axis*. A surface species' vertical is the cheap direction with nothing at either end; the Ollune vertical carries every resource, every neighbour, every hazard and **both** available ways to die, while their horizontal is free, infinite and socially empty. Hence no maps — a map projects onto the plane where nothing happens — and a position notation of **depth, bearing and phase** in which the time term is not optional, because on a world where every reference body moves at a different speed a position without a time does not underspecify a location, it fails to denote one. There is no Ollune expression for a place that does not include when you will be at it.

  **Trade runs on carriage, not goods.** A hull crosses in an hour what a shoal waits a generation for, so any visiting trader has a connectivity no native possesses, and the valuable cargo is carriage of the near word — constituting agreements between shoals that could not otherwise exist. That makes the Rangers' charter role as the courier both sides trust the load-bearing member of Cirrane's economy rather than a piece of institutional design. The physics cuts one way only: a near word is attributable to the body that made it, so a carrier cannot forge one, and readable by anyone in proximity, so a carrier can read everything they carry. **Integrity is guaranteed and privacy is impossible** — hence the Ollune have no concept of a confidential agreement and read a non-disclosure clause as a description of an agreement that is not there. And since anything imported must be lifted permanently by whoever accepts it, **the only imports worth having on Cirrane are weightless**: a merchant who understands the place arrives empty and leaves full.

  **Freyari** (2026-08-01) is the Ollune name for the temperate shell — the habitable band itself, not the body it wraps. *Cirrane* remains the Federation survey catalogue name for the whole planet, and the two deliberately do not denote the same object: Cirrane is tens of thousands of kilometres deep, all but a sliver of it irrelevant to everything alive, and Freyari is the sliver. In ordinary usage it doubles as the Ollune word for *everywhere*.

  **Relationship with the [Krenyi](src/lore/krenyi.md)** (2026-08-01). The pairing xenosociology expected to fail — Krenyi do not use social deception, Ollune hold that a falsehood in the far word is unremarkable — and it does not fail, because the Krenyi were already asking for what Ollune anatomy supplies: a species that moves directly to material terms meets one with a dedicated channel for them. The Krenyi are also close to the only species that can accept the far word's standing without hearing a moral defect in it, since *architecture, not virtue* is exactly how the Krenyi describe their own bounded emotional range. **The one serious hazard belongs to the Krenyi**: a far word contradicting a near word is pattern drift by the Krenyi definition, and the Krenyi answer to pattern drift is quiet withdrawal — aimed at someone who has broken no law of their own, who will not be told, and who can only be reached at a passing. The failure mode is not an argument but a Krenyi going silent, correctly by their own lights, over nothing; the fix is the practice they already use against mythification. On fear the two are opposite answers to one question — Krenyi have no deep-fear response to cascade, Ollune have alarms that cost the raiser nothing — and neither quite believes the other's method works. Also pays off a running risk in the entry: the Ollune are tired of being told their body is somebody else's moral achievement, and the Krenyi are constitutionally incapable of telling them so.

- **Journal: [The Name Was Never Mine](src/journal/the-name-was-never-mine.md)** (2026-08-01, #260) — the out-of-character account of the retitle, beside [A Name You Have to Argue With](src/journal/a-name-you-have-to-argue-with.md). Records the reusable part of the method: OR-ing candidate names into one query produces false negatives, because a name with modest presence vanishes behind one with none — which is how two already-taken names were both cleared before individual exact-phrase searches caught them. Also settles publicly what *Drithane* means, and that it means nothing in-story: **a spark and a drift, no character has said the word, and it is not going in the glossary**, because a title that stays outside the story gets to be about all of it. And why the corps kept its name while the book did not.

### Changed

- **The giscus welcome-post drafts carry the new title** (2026-08-01, #259). Three occurrences in `scripts/giscus-welcome-posts.md` still named the work *Star Rangers*, one of them wrapped across a line break — which is why the retitle sweep's grep missed it. Three of the four drafted starter posts have never been posted, so leaving these would have reintroduced the old name into a public forum whenever they went up. The one live post, in **Fan Creations**, was corrected directly on GitHub: Discussions sit outside the repo, so no sweep could reach it, and it had been telling fan authors to tag work `Star Rangers (Dermot R. Cochran)` while the About page said `Drithane (Dermot R. Cochran)`.

- **Legacy WordPress permalinks now return 410 Gone** (2026-08-01, #263). `sciencefiction.site` ran WordPress under a previous occupant, and a static server ignores query strings it does not understand — so every stale permalink (`/?p=417`, `/?cat=3`, `/?m=202001`) was resolving **200 and serving the homepage**. To a crawler the old site was not gone at all: alive, responsive, and inexplicably now a science fiction novel. That soft-404 pattern is the likeliest reason Google still classified the domain as the WordPress site it used to be. **410 rather than 404 is the point** — 404 means "not found, might return" and gets retried for months; 410 means permanently gone. Scoped to the site root and numeric values only, so it cannot match anything this site serves.
- **`/favicon.ico` is served at the site root** (2026-08-01, #264). It returned **404 on every domain**. `base.njk` links the SVG and PNG icons and browsers honour those, but Google's favicon crawler, feed readers and link-preview generators request the conventional root path and were getting nothing — so **four of six** Search Console properties showed a wrong icon, for three different reasons: `sciencefiction.site` kept the previous occupant's **WordPress logo** from cache, `star-rangers.site` and `undercover-pets.com` showed a placeholder having found nothing, and the rest were correct only because Google happened to read the HTML tags. One 404 that had never inconvenienced a human being. The file is a multi-size ICO (16/32/48) embedding the same PNG bytes `src/images/icons` already ships, so root and linked icons are identical. `src/static/` has no directory-wide passthrough, so it needs its own line in `.eleventy.js`.

### Fixed

- **The retitle session date** (2026-08-01, #267). The whole session ran on 1 August and had been recorded throughout as 31 July — twenty-six occurrences across the release heading, its dated entries, the journal entry's front matter, `CLAUDE.md` and `src/_data/site.js`. Verified as a correction rather than a rewrite: every occurrence fell inside this session's own sections, none near 1.10.0 or earlier.
## [1.11.0] - 2026-08-01

The retitle release, and the one that pinned down the top of the Cascade.

The work is now ***Drithane***. A prior-art search turned up three standing claims on *Star Rangers* as a title — Andre Norton's 1953 novel foremost — so the site would have been permanently second in search for its own name. The corps keeps its name inside the fiction and the repo slug, URL prefix, alias domains and comment repos are all deliberately untouched; only front-of-house branding moved.

Behind that, the cosmology's upper tiers stopped being implicit. What Conseleme fixes and what each tier below settles is now stated rather than inferable: where number begins, what a conceptual realm is, what a formcluster and a formtype are, why the Concordant Zones tile a membrane's interior, and why the past cannot be edited and the future is not yet there to visit. Two institutions were added — the **Navigation Corps** and the certification regime it shares with the Safety Corps — and one charter reading that had never been written down, on what an officer owes and is owed on personal leave.

The through-line is smaller than the list suggests. Almost every entry here records what the Archive *cannot* establish alongside what it can: the tiling is the design account and not a survey finding, the Holy Spirit doctrine is confirmed by stance rather than measurement, a formtype is inferred and never observed, and the invariants say what the Cascade admits rather than what anyone in it believes.

### Changed

- **The work is retitled *Drithane*** (2026-08-01, #247). A trademark/prior-art search turned up three collisions on *Star Rangers* as a title — Andre Norton's 1953 novel of the same name (still in print), a 1987–88 comic with an uncomfortably similar premise, and assorted unrelated media reusing the phrase — so the site would have been permanently second in search for its own name. *Drithane* is a coinage from Irish *drithle* (spark, glimmer) with no exact match in any medium. **Front-of-house branding only:** site name/title/description, homepage H1, theme-generator labels, and prose/frontmatter lines naming the work's title all changed. **Deliberately unchanged:** the Star Rangers remain the in-universe corps throughout (~298 occurrences, including the Charter, the Anthem and the founding history), plus the repo slug, the `/star-rangers/` URL prefix, the alias domains, and the giscus comment repos — see `CLAUDE.md`'s opening note on telling the two senses apart.

- **The Cosmic Cascade is scoped to the multiverse, not to one universe** (2026-08-01, #249). `cosmic-cascade.md` places universes as a *rung on* the Cascade (`Worldwrights -> Universes with Concordants -> Telearchs`) and `ensemble-multiverse.md` calls the Grand Ensemble "the infinite collection of universes produced under the Cosmic Cascade" — so Cascade > multiverse > universes, and describing the Cascade's authority as holding "in the Drithane universe" had quietly placed it below its own top tier. Seven lines move to *multiverse* (both Cosmic Cascade pages, `meta-dimensional-beings`, `predatory-entities`, `levril`); three stay *universe* on purpose, because `higher-dimensional-folding`, `quantum-space-harmonic-wave` and `ftl-mechanics` all depend on the local 3+7+1 structure the Grand Ensemble explicitly calls "a local fact rather than a cosmic constant"; two drop the word entirely, since AUC is an economic measure and `lore/index.md` spans both scopes.
- **The Five Layers have a scope split, and it falls where the Upper Structure sits** (2026-08-01, #249). **Layers 1 and 2 are multiverse-wide; Layers 3 to 5 are local to this universe** — Etheric extent runs through this universe's compact dimensions, Physical law under local Concordant boundary conditions, Material local by definition. Which is why Kieme and Conseleme are named for exactly those two layers and no tier is named for the lower three. Gives the Survey Archive's quoting rule three settings rather than two: universal at Layer 1, realm-bounded at Layer 2, universe-bounded at Layer 3 and below.
- **Concordant Zones tile a membrane's interior — an infinite jigsaw** (2026-08-01, #252). The image lands on a distinction `concordant-membranes.md` already drew in its own table: two **membranes** are separated by the [Interval](src/glossary/interval.md); two **zones inside one membrane** meet at an ordinary internal edge with *no Interval at all*. A jigsaw's pieces touch, which makes it exactly right for a membrane's interior and exactly wrong for two membranes, so this is written as the picture the existing distinction was already implying rather than as new machinery. Two consequences, both usually assumed the other way round: **there is no unclaimed interior** — no neutral space between zones, no third condition a region can occupy, nothing a survey could file as belonging to no zone; and **a zone's extent is not a property it owns**, since in a jigsaw no piece's shape is independent of its neighbours, so an internal edge is where two rule sets disagree lawfully rather than a wall either built or could shift alone — which is why an edge yields instrument drift and timebase mismatch rather than law failure. Both sets are holding, and the disagreement is what holding looks like from the seam. **Filed as the design account and confirmed as such**, from the strata that author membranes rather than from anyone who has stood at an edge: `concordant.md` continues to decline the question of how many zones a universe holds, because from inside, a few unbounded zones and endlessly many merely vast ones are indistinguishable. The agnosticism is not weakened anywhere, only attributed — a jigsaw no one has ever seen a single edge of.
- **The Holy Spirit doctrine is belief-conditioned, not simply undetectable** (2026-08-01, #249). The Subtle Influence section had it as flatly undetectable, which is not what the traditions holding it claim and read as the Archive dismissing them. Kieme influences people **and events** directly but subtly, breaching no law of physics and no aspect of the Cascade, invisibly and unverifiably **except to those who choose to believe it** — so confirmation is a matter of *stance rather than measurement*, stated openly rather than caught out. Nothing in the Cascade forbids a claim of that shape; nothing in the Archive's method can engage one; the disagreement is durable because the two positions cannot reach each other. **Contested events accordingly get a three-way reading, not two** — divine influence, a purely physical account with nothing left over, and a dispute of the transmission itself, that third position being the one usually dropped.
- **Kieme's threefoldness is devotional interpretation, not structure** (2026-08-01, #250). The entry asserted that "Kieme is itself understood to be threefold in nature". Kieme stands above the concept of number, so *one*, *three* and *many* are rival pictures rather than rival answers, and **the teaching image is the shamrock** — one leaf, three lobes, nobody being asked which number is correct. What the three aspects *are* is therefore not a hard question but the wrong shape of question, which is a stronger statement than the "cannot settle" the entry made before. The image is kept, on the same terms as the name.
- **Gravity is the safety variable shared by teleportation and harmonic transit** (2026-08-01, #249). `teleportation-limitations.md` already had teleportation depending on "the same lawful field behavior" as QSHW transit, but nothing said why one is routine commercial infrastructure and the other a Medical Corps event. A relocation lock is held against the local harmonic field and a gravity gradient distorts it continuously, so the **existing three-condition set falls out as consequence rather than stipulation**. Written deliberately as an explanation of those three conditions and **not as a fourth**, since `S01E02C05` canonises the rule as having no exception clause. `ascent-javelins.md` already had a surface-to-orbit jump failing all three at once, so the account was latent there and is now explicit: distance is not what makes a jump dangerous, standing on something massive is.
- **Route clearance now names the Navigation Corps** (2026-08-01, #249). `lagrange-fold-points.md` and `universe-overlap-and-folding.md` both vested it in "the Survey Corps navigation authority"; both updated, with the new Corps entry recording that older protocol documents still carry the old name.

### Added

- **Glossary: [Formtype](src/glossary/formtype.md), and the Common Manifold placed at the Formwright tier** (2026-08-01, #256). Settles three structural questions that had been answered only by implication.
  - **A Worldwright makes the vessel and therefore its membrane, but not the manifold the membrane sits in.** A membrane is what a universe occupies *by virtue of being one*, not a skin fitted to a finished vessel — which is also why membrane-hood is **not** a Worldwright privilege, derived universes receiving no Worldwright and occupying membranes regardless. The anchor was already in the entry: the manifold's 20+ band encodes boundary conditions Worldwright and Telearch operations must *satisfy*, and a condition something must satisfy is not a thing it built.
  - **Three tiers make the manifold between them.** [Conseleme](src/glossary/conseleme.md) fixes that **seating obtains** — universes distinct, separated, the separation able to carry geometric strain, causal timing and template propagation — which settles that there must be a manifold and nothing about which. A **Metawright** settles the mathematics one could be expressed in. A **Formwright** instantiates the thing itself as a **formtype**. This extends the ladder the Conceptual layer already follows by one rung: *Conseleme fixes what any mathematics must satisfy and a Metawright settles which there is; Conseleme fixes that seating must obtain and a Formwright settles which manifold there is.* An earlier draft placed the manifold in the Upper Structure and was wrong — a manifold is a mathematical object, mathematics is settled one tier down, so "it is shared, therefore it is above the realms" begs the question.
  - **A formcluster is a common *seating*, not only a common origin** — its members are not merely drawn from one vocabulary, they are laid out in one arena, which is the deeper reason a shared cluster explains why two universes are commensurable at all. It also fixes what a cluster boundary is: two universes in one formcluster have an [Interval](src/glossary/interval.md) between them however vast, and two in different formclusters have **no measured separation at all**, no shared arena existing to measure it across. Descent stays an Archewright fact; **position is a Formwright one**.
  - **`formtype` gets its own entry**, defined against the two things it is constantly confused with. It is **not an archetype** — an archecluster's archetypes are a vocabulary, formtypes are the grammar that permits it — and it is **not a physical law**, which is the Worldwright's work three tiers down. A formtype does not say what happens or even what may happen; it settles what shape a thing that happens is permitted to have. The Archive uses the word sparingly and records why: nothing at Material scale has observed a formtype and nothing will, an instrument being a thing built inside the grammar and unable to be turned on it.
  - **And the entry's own title is parochial twice over.** *Post-11* records where the Corps was standing when it started counting; ***Common*** records how far the Corps can see. Every universe it has reached is seated in this manifold, which is a claim about the Corps' reach rather than about the Grand Ensemble. The word is kept and no longer defended.
- **Lore: [Catastrophic Failure in Fold Transit](src/lore/fold-transit-catastrophic-failure.md)** (2026-08-01, #249) — chosen over a *Wrath of the Dragons* codex entry, and absorbing it. A codex would have documented one disputed event; the lore entry explains the class it belongs to and gives all three readings something concrete to disagree about. **Premise:** folding is geometrically lawful and nothing is lost *inside* a fold, so the failure surface is aperture formation, stability and collapse — an engineering tolerance, therefore certifiable, therefore certifiable badly. Losses concentrate on hulls uncertified or certified against a weaker standard, and **the Safety Corps cannot compel any polity to adopt its own**. Built from existing canon rather than invention: the Safety Corps was already the fold-adjacent certification authority with its Underwriters Laboratories lineage, the Hyperfold Yield Combine was already the lawful-but-appalling teaching case, and the Institute already publishes its own null results — so its unexplained residue is in character rather than a concession. *Dragons are low-ranking Levrils* does the heaviest lifting: agency is available to the devotional reading under the Cascade, so this is two well-founded accounts plus a transmission dispute, not credulity against physics. The founding event is left unspecified on purpose.
- **Lore: [The Star Rangers Navigation Corps](src/lore/star-rangers-navigation-corps.md)** (2026-08-01, #249) — **lineage, not a new invention.** Written as the Survey Corps' own navigation authority separated out, on the same founding principle that produced the Science and Safety Corps ("a body whose conclusions matter cannot answer to the command chain those conclusions might embarrass"), applied one turn further to a body whose conclusions could embarrass *itself*. **The Safety Corps certifies the hull; the Navigation Corps certifies the route.** A fold route is certified *as of a date* and drifts away from the survey that cleared it, so route certificates expire where hull certificates do not — producing the failure class the fold-transit entry could not previously express, a properly certified hull on a **lapsed route**, both certificates genuine and nothing evaded. Mutual recognition with sister organisations takes **centuries**, because a mature standard encodes what its authors never had to write down and neither body will accept an audit it did not perform; three outcomes, none framed as success or failure — merger, alliance, and neutral standoff (commonest, and published as an incomplete audit rather than a breakdown). Recognitions recorded: the Solar System Concord, the Celtic Union of Planets, the Federation of Sentient Beings. **Charter limits held throughout** — no jurisdiction beyond invitation, so outside Concord space the Corps publishes what it surveyed and what it did not, and clears nothing.
- **Lore: [Shore Leave and the Neutrality Constraint](src/lore/shore-leave-and-neutrality.md)** (2026-08-01, #249) — the charter's neutrality clause was drafted against *Solar System* politics because in 2723 that was the politics there was, so **an officer on personal leave in a member confederation beyond the Concord is a gap the drafters never addressed**, covered since by practice rather than text. The hard case is never an officer campaigning, which has an obvious answer; it is an officer helping her family, because training does not switch off at a gate and in a small polity a precise question about why a rule exists is inseparable from who benefits from it existing. **Ranger status works like diplomatic accreditation, and its exact reciprocal is the power to declare an individual holder unwelcome** — nobody can be an accredited neutral and a citizen with an unconditional right of return at once, so the status displaces the citizenship and a home world may expel its own, costing standing in property and inheritance, a voice, and the right to be present at a family matter. That also explains the refusal to contest an expulsion, which would otherwise read as indifference: contesting asserts that Ranger status confers rights *against* a polity, the precise claim the charter disclaims. No rule has ever been written because a rule strict enough would require officers to stop going home.
- **Glossary: [Formcluster](src/glossary/formcluster.md)** (2026-08-01, #249) — the set of archeclusters tracing to one Formwright, built from the lawful templates that Formwright instantiates, following `archecluster.md`'s shape exactly since it is the same authorship fact one tier up. Completes the design-tier ladder: **conceptual realm -> formcluster -> archecluster -> universe.** Named plain `Formcluster` rather than "Formcluster Ensemble" deliberately and confirmed by Dermot — *Ensemble* already names the totality (the Grand Ensemble Multiverse), and reusing it for a proper subset would put the word on two scopes at once.
- **Cosmic Cascade: conceptual realms, and two new Global Invariants** (2026-08-01, #249).
  - **Conceptual realms.** Multiverse-wide is not the same as uniform. The Conceptual layer divides into realms, one per Metawright, each settling its own system of mathematical axioms and foundations within the constraints Conseleme sets but not determined by them — Conseleme fixes what any mathematics must satisfy, a Metawright settles which mathematics there is. Universes beneath different Metawrights differ in **what can be proved**, not only in physical law.
  - **Number begins below the Upper Structure.** Hyperomnium, Kieme and Conseleme are neither singular nor plural, standing above the concept of number altogether, so counting them is a category error rather than an open question; countability starts at the Metawrights, who are individuals. The same fact as conceptual realms rather than a separate one — number is defined *inside* a realm and cannot reach above the tier that settles realms.
  - **No literal theophany.** No stratum above the Levrils appears, speaks or manifests to a time-bound witness as itself. Generalises constraints the glossary already carried per tier, with the two exceptions already canon: **Avatars**, and **Levrils acting as representatives of higher strata**. Neither is an unmediated appearance, which is what makes them exceptions rather than breaches.
  - **A framing note on what the invariants are and are not**, added because the theophany rule was open to being read as the Archive settling the question against the traditions. Each invariant states what the Cascade *admits* and what invariant-only inference can establish, **never what anyone in the setting believes**: devotional traditions hold theophanies real while rejecting magic, others treat magic and the paranormal as unremarkable, and nothing in the list adjudicates them — a document that cannot detect a thing has established the limits of its method, not the limits of the world. Also makes visible that *devotional tradition* is the Archive's own deliberately flattening term for religions, faiths and cults alike.
- **Glossary: the `-eme` etymologies, recorded as a devotional reading** (2026-08-01, #250) — *Kieme* glosses as "ruler of the Spiritual layer" and *Conseleme* as "ruler of the Conceptual layer", on a shared **-eme** ruler-marker that was invisible in the repo. Taken structurally the gloss contradicts both entries' standing line that Upper Structure tiers are non-anthropomorphic, since a ruler decides and an origin condition does not — so it is recorded the way `kieme.md` already handles *Most Holy Triumvirate*: material the Archive registers without adopting. The Kieme note ties it to the election-into-the-Spiritual-layer doctrine already in that entry, both attributing a decision at exactly the layer the name points to. The Conseleme note observes where the marker stops: **Hyperomnium takes no such ending and is named for no layer**, read as significance by devotional tradition and as absence of evidence by the Archive. **Hyperomnium keeps its name** (confirmed by Dermot) rather than becoming "Hyperpaneme" for consistency — that absence is load-bearing, and the Cascade is already etymologically hybrid throughout (`Formwright` Latin + Old English, `Mediarch` Latin + Greek).
- **Cosmic Cascade: two new Global Invariants, and a `### Past, Present, and Future` section** (2026-08-01, #254). **The future is open** in the quantum sense — genuinely branching, none of it yet actual; those possibilities **resolve at the present**; the **past is static**, admitting no revision — what changes about the past is how it is remembered, interpreted and understood, and that happens in the present. **No time travel follows from the asymmetry rather than being stipulated**: there is nothing behind the present to alter and nothing ahead of it yet actual to visit, which is a stronger position than a rule, since a rule invites an exception and this does not. The new section works the implication through: **the asymmetry is fixed by Conseleme; the physics of time is not** — archeclusters vary timelike dimension count and what a clock measures, same tiering shape as the Conceptual layer's mathematics/Metawright split, so there is no cluster to go looking in for an exception; **branching futures are not the Grand Ensemble** — nothing produces a universe by quantum branching, Ensemble universes are Worldwright-authored or derived through overlap and folding; the design strata fix possibility space rather than which branch resolves, so this is **not fatalism**; and the three-years-late cross-boundary correction stays what the three time registers already predicted — two clocks disagreeing about one event's ordering, not causality reversing. The prohibition previously existed only mechanism-by-mechanism (`higher-dimensional-folding` says folding "permits distance traversal, not timeline revision") and was never stated at Cascade level before this.
- **Glossary: [Formtype](src/glossary/formtype.md)** (2026-08-01, #256) — settles which tier makes the [Common Manifold](src/lore/post-eleven-dimensional-manifold.md), a question three existing entries left implicit. **Not a Worldwright**: creating a vessel creates its membrane, but not the manifold that membrane is seated in — the manifold's outermost band encodes boundary conditions Worldwright and Telearch operations must *satisfy*, and a condition something must satisfy is not a thing it built. **Not an archecluster either**: seating and descent are separate axes, so the manifold belongs to no archecluster and traces to no Archewright. **Three tiers resolve it**, one rung added to the ladder the Conceptual layer already follows — Conseleme fixes that seating must obtain and nothing about which; a Metawright settles the mathematics a manifold could be expressed in; a Formwright instantiates the manifold itself, as a **formtype**, a lawful template satisfying Conseleme's condition in that realm's own mathematics. Makes a formcluster a **common seating** as well as a common origin, the deeper reason `formcluster.md` already said a shared cluster "explains why they are commensurable at all," and fixes what a formcluster boundary is — an [Interval](src/glossary/interval.md) inside one, no measured separation at all across two. `archecluster.md`, `formcluster.md` and `worldwright.md` each gain a cross-reference recording the placement, and `post-eleven-dimensional-manifold.md` gains a "Nobody Made It" section carrying the full argument, including the corollary that **nothing can be moved**: seating is not a thing anyone at Worldwright level does, so a boundary event is discovered rather than caused, and the record has never produced a party to hold responsible for one — not a failed investigation, a category that does not apply at this tier.

- **Three Eden characters and a second Kate Ukrainka codex story, all pointed at the Smart Pet material** (drafted 2026-07-30 at Dermot's direction — **proposals, not settled canon**).
  - **[Thrum](src/characters/thrum.md), an alien pet** — a *vessik*, a burrow commensal from the [Undersong Belt](src/lore/undersong-belt.md), left on Eden by a rotating [Chthonari](src/lore/chthonari.md) fold-scaffold engineer. Built from the Chthonari page's own physiology rather than invented alongside it: it reads substrate vibration and has no apparatus at all for a face or a voice. **The point of it is a negative capability** — it is not resistant to social pressure, it is *unaware* of it, and only the second of those cannot be worn down. Carries no cognitive subsystem and no enhancement, so it is not a [Smart Pet](src/glossary/smart-pet.md) and falls outside every welfare framework Eden has — the gap that glossary already flags for Barsik and Bubochka, one step further out.
  - **[Muffin](src/characters/muffin.md), the antagonist** — a **cute predator**, which the corpus already had: [Predatory Entities](src/lore/predatory-entities.md) classifies them as Class III, non-sapient, feeding on micro-consent collapses, and states outright that the detective agency's animals detect them early because their asymmetry detection isn't distorted by politeness. Muffin is an instance of the existing *lamb-mask* type, so **no lore page needed changing to make room for it**. Written to the classification's own terms: it does not plan, does not avoid the Smart Pets, and gets nothing from them — Barsik, Bubochka and Thrum are a control group, not a defence. What it costs a deck is not injury but the habit of refusal, surfacing as monitoring-officer sign-offs that stopped being returned for correction. The cute name is the hazard: no one can file a compliance report containing the word *Muffin*, and everyone who has tried has discovered that privately and put the form down.
  - **[Kate Ukrainka](src/characters/kate-ukrainka.md) now has a character page**, placing her on Eden and giving her the Smart Pet children's books alongside the Earth-diaspora work [already on file](src/codex/the-domovyk-who-stayed.md). Additive, not a retcon: *Tales from the Old Hearth* is untouched.
  - **[The Cat Who Would Not Say](src/codex/the-cat-who-would-not-say.md)** — her second codex entry, and **a deliberate demonstration of what "canon-for-its-author" is for.** The story is emotionally exact and technically wrong: it renders a certified cognitive subsystem as "a small quiet friend who lives behind the eyes," which is precisely what a governed component is not. The annotation records two welfare inspectorates' objection *in full and unanswered*, and records Eden readers' assumption about what the thing by the door really was as an assumption. A children's book that got the regulation right would have had to be filed as something other than one author's account.
  - **Eden's cast joined `undercover-pets.com`.** `undercover-pets.com` narrows by tag, so being written about Eden was not enough to appear there — [Oyelaran](src/characters/rasa-oyelaran.md) was tagged and her own [DI Albercombe](src/characters/wendell-albercombe.md), her bureau's [Reeves](src/characters/reeves-eden.md), [Jeeves](src/characters/jeeves.md), [Commissioner Wayland](src/characters/zara-wayland.md), [Mayor Petrakis](src/characters/imogen-petrakis.md) and the three junior detectives were not, so the domain showed the pets without the institution they work inside. All seven now carry the tag. Filtering still only ever subtracts: nothing about these pages differs between domains, only whether that domain shows them.
  - **Kate and [Albercombe](src/characters/wendell-albercombe.md) have a shared past**, written onto both pages from each side. A few months, some years ago, that did not end so much as thin out — his replies got slower, he decided nothing at any point, and she reached the end of what she was willing to make small. He feels a small, infrequent guilt, specifically at having been a man who reads a silence expertly in a witness statement and let one run for a year in his own messages; he has no wish to reopen it, and neither has she. **Deliberately no unfinished business**, because the interesting residue is professional rather than romantic: it is where her eye for a person quietly asking for less than they need comes from, which is every animal in the Smart Pet books.
  - **Muffin is semi-sapient, at roughly the order of a cat — and no lore page changed to allow it.** `predatory-entities.md` and `meta-dimensional-beings.md` both state flatly that cute predators are Class III, non-sapient, instinctive pattern-matching. Rather than reopen a class definition that carries counter-strategies for the whole class, **what is now in question is Muffin's filing, not the classification**: it was entered as a lamb-mask by a duty officer, from a description, on the strength of the feeding pattern — which does fit — and nine months of observation do not. The corpus already had the precedent and it is cited rather than invented: [Sí Gaoithe's crossers](src/lore/universes/si-gaoithe.md) "resist clean Class II/III sorting altogether," recorded there as a case the system wasn't built to sort rather than a gap in it. Muffin is the second, and the first that could be **calibrated**, because Eden is the first place it turned up four metres from two certified benchmarks for cat-order cognition. **Settled 2026-07-30: the class definition stays as written and is not to be reopened on this evidence.** The specimen-level dispute is the whole of the change — widening it to the class would put semi-sapience behind every cute predator in the corpus and behind the counter-strategy the class is given, which is a much larger claim than one Eden file supports.
    - What semi-sapience buys: it learns a door (three wrong ones first, which instinct does not do), has a favourite it has never once worked, gets bored, and stopped trying Barsik in the second month — not avoidance, which would be strategy, but the way any cat stops at a door that has never opened. It never resolves into anything organised enough for Class II either.
    - **The antagonism gets worse rather than sharper.** A non-sapient hazard is a thing you remove. An animal of about Barsik's order, carrying no subsystem and covered by no welfare instrument, is the same gap that leaves Barsik, Bubochka and Thrum outside their frameworks — arriving a third time from the direction nobody allowed for, the predator. Barsik's counter is unchanged and he now asks for it on the record knowing what he is asking to have done, and to what.
  - **[On the Filing of the Galley Door Specimen](src/codex/filing-of-the-galley-door-specimen.md)** — the contested argument, put in the Codex where contested material goes, authored by **Agent Barsik** (non-human authorship has precedent in `rook-7-first-week-log.md`). Written to fail in the way a codex entry should: Barsik states his own instrument problem at greater length than his finding — a cat is not a disinterested judge of whether a thing is as clever as a cat — and the Archive annotation flags the blind spot he does not state, which is that the paper's entire evidence base is Bubochka's log and never once treats her as a witness rather than a record.
  - **Kate loves cats and has never kept one.** Three, each lost to something ordinary and different — the Deck 4 service voids, a neighbour with better hours, a family who packed it off-habitat in good faith. **Written so the set refuses to become a pattern**: every individual loss has a boring explanation that is almost certainly the true one, nobody has ever laid the three side by side, and the first two predate her moving her mornings to Deck 7 — which forecloses the tidy sinister reading a reader who has just met [Muffin](src/characters/muffin.md) will reach for first. Nothing on Muffin's page mentions the cats and nothing should; the unease has to come from the reader assembling it and being wrong. The current cat is five weeks in and unnamed, and she has noticed herself not naming it. Closes on the one thing she will not argue about: she has never written a cat that is not found by the end of the book.
  - **No portraits or title cards yet.** `image`/`image_alt` are optional on both types and are deliberately left unset rather than pointed at a file that doesn't exist; the codex card wants `scripts/make-codex-cover.ps1`, which is Windows-only.
- **`undercover-pets.com`'s content narrowing is now in the registry** (`lib/editions.js`) as `topics: ["undercover-pets.com"]`. Every other registered domain's identity moved into the registry on 2026-07-30; this one key did not, so the `pets` edition resolved no narrowing at all and a clone trimmed to the two-key minimum `deploy.conf` would have **silently served the full unfiltered site**. Quiet, and in the permissive direction — the worse one to leave to a default. The tag being the domain name is deliberate: `undercover-pets.com` is what a page carries to opt *into* that deploy, so the filter and the tag cannot drift apart the way a curated list would. `deploy.conf` still wins wherever it sets `TOPICS`, so nothing changes for the live clone until that line is removed there. Verified via `scripts/resolve-edition.js`: the domain now resolves `RESOLVED_TOPICS='undercover-pets.com'`, and `sciencefiction.site` still resolves none.

### Added

- **Glossary: [Archecluster](src/glossary/archecluster.md)** — names a grouping the cosmology has been using without a word for it: every universe tracing to one Archewright, sharing that Archewright's archetypes and possibility space. **Deliberately drawn as authorship rather than authority** — it inherits the settled protouniverse position (an adult child, "answerable to it in nothing", kinship permanent and authority temporary) and the `Worldwright` bound (Worldwright authority exists only in primary universes), so cluster membership records provenance and never establishes standing. Two distinctions it exists to prevent: an archecluster is **not** a Worldwright's universe set (RTS-9's three coupled universes are one architecture *inside* a cluster, not the cluster), and it is **not** adjacency (membranes and the Interval are physical separation; a cluster is common origin). A cluster's **reference universe**, where it has one, is the primary universe holding the whole record — a place to look things up, not a seat.
- **`CLAUDE.md` gained an "Authority and review boundary" section** — the internal counterpart to `CONTRIBUTING.md`'s external one. The boundary was already operating (technical fixes merge when green; new lore waits for Dermot) but existed only as practice, inferable from scattered rules rather than stated anywhere. Now three explicit tiers — merge when CI is green, draft-and-stop, never-without-instruction — plus the reasoning that fixes where the line falls: **the gates prove structure, not judgement.** `validate-content.js`, the dry run and the link/related checkers can show a page is well-formed and fully resolved; nothing in the toolchain can show a sentence is true in this world or in the house voice, so mechanical correctness is delegable and assertions about the world are not. Written after weighing a full Architecture Definition Model for the repo and deciding against one — that methodology governs generative-AI *systems* while this site ships static HTML, but its agent-authority question was the part with a real object here: the authoring pipeline, not the artifact.
- **`sample-deploy-minimal.conf`** — a copyable two-key template (`CPANEL_USER`, `DOMAIN`) tracked alongside the full `sample-deploy.conf`, giving the "minimum viable `deploy.conf` is two lines" state a file rather than only a paragraph inside a 300-line one. Its header says what a registered domain now resolves for itself out of `lib/editions.js` and what deliberately stays server-side (`ADMIN_EMAIL`, the raw `GISCUS_*_ID` values, `CUSTOM_CSS_FILE`, `ALT_DOMAINS`), and points at the full template for the rest. No behaviour change: `scripts/cpanel-deploy.sh` reads the same `deploy.conf` whichever template it was copied from. `README.md`'s setup step, `FORKING.md`'s cPanel bullet and `CLAUDE.md`'s deploy-mechanism note name both.
- **`CONTRIBUTING.md`** — the project's contribution and support policy, which until now existed only as an unstated assumption. Third-party pull requests are not accepted (creative control of a single-authored novel, and because a merge is a permanent maintenance commitment rather than a one-off); forks are encouraged, need no permission, and are unsupported; reader questions are welcome in the discussion forum, triaged, with no promise of coverage. GitHub surfaces this file in the "open a pull request" and new-issue flows, which is the point — it reaches someone at the moment they'd otherwise spend an evening on something that was never going to merge. Linked from `README.md` (new "Contributing and support" section) and `FORKING.md`'s opening.
- **The Imperium's High Council and its Convenor** (settled 2026-07-30) — [`the-imperium.md`](src/lore/the-imperium.md), with a term entry in the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md). **Additive, not a rename:** `Administrator-Supreme` is untouched and remains the Imperium's actual apex office throughout its span.
  - **The page already gestured at this institution without naming it.** The Decline section described "outer-station representatives who had gained provisional consultative status in the Imperium's final decade" — the High Council is that mechanism, named, and the Convenor chairs it. Late Imperium only; there is no High Council in the founding or middle period.
  - **Advisory, never executive**, which is what keeps it consistent with everything already written. No Council resolution bound an Administrator-Supreme and no Convenor ever issued an order, so [The Unbroken Command](src/lore/the-unbroken-command/) still turns on a stand-down order only an Administrator-Supreme could have given, and the "authority concentrated at the top" characterisation stands. What the Council spread was the *appearance* of responsibility at the point where the reality had become unbearable to hold alone.
  - **Its significance is what it admitted rather than what it achieved:** a government founded on the proposition that consultation caused the Coherence Wars built itself a consulting chamber and seated the settlements it had spent two generations punishing. The Convenor's chair is the first seat in Solar System governance since the Consortium's collapse that a civilian could hold without a service record.
  - **It also explains a continuity that was previously a coincidence.** The MSC's advisory councils are now modelled on the High Council and inherit its flaw — a chamber that may be consulted and an executive that need not listen — which is the same finding the Outer Stations Consolidation Hearing spent three months reaching sixty years later.
- **Recorded plainly that the Imperium had no emperor and never claimed one.** The corpus never said otherwise, but it never said so either, and the name invites the assumption: no coronation, no succession by descent, no throne. The apex was a serving officer's office filled from fleet seniority.

### Added

- **Permanent citation aliases for chapters at `/c/<comment_id>/`** (2026-07-30) — [`src/chapter-aliases.njk`](src/chapter-aliases.njk), one redirect stub per chapter, plus a "Citing this chapter?" line at the foot of every chapter page so the aliases are actually discoverable.
  - **The problem is worse than link rot.** A chapter's URL is not permanent — episodes get renumbered in place to keep story chronology in order, which reassigns a URL like `/seasons/s00/e02/s00e02c01/` to *entirely different content*, and that has already happened once. An external wiki or citation pointing there doesn't 404 afterwards; it silently starts pointing at something else, which is strictly worse because nothing signals the breakage to either side.
  - **Nothing new had to be invented: the repo already solved this once, for giscus.** `comment_id` is required on every chapter, permanent by policy, carried forward with the *content* rather than the slot when a chapter is renumbered, and unique by build-time check (`validate-content.js` fails on collision). That is exactly the property set a citable identifier needs, and all 35 are plain lowercase slugs needing no escaping.
  - **Static stubs rather than server redirects**, because an `.htaccess` rewrite doesn't exist on GitHub Pages and this repo deploys to both. Same head as the two existing rename stubs. `noindex` + `canonical` is correct *here* — for a redirect the two agree, where for an excluded placeholder they conflict (see the canonical entry below).
  - **Built from a new unfiltered `allChapters` collection** so an alias resolves on **every** domain, including one that hides that chapter — where it lands on the `excluded.njk` placeholder that points onward. An alias that vanished on the narrowing clones would break the very links the feature exists to preserve. Filtered by `inputPath`, not `layout`, since `layout` is rewritten to `excluded.njk` for exactly those chapters. Verified: 35 aliases build on a `THREADS=founding-era` clone, and every one resolves to a real built page.
  - Scene-POV pages get no alias, deliberately — derived ids, derivative views, ~92 stubs earning nothing. Excluded from the sitemap, and the scheme is documented in `/llms.txt` so an agent cites the stable form.
- **`<link rel="canonical">` on every indexable page, self-canonical per domain** (2026-07-30). Each domain declares itself the canonical home of its own URLs rather than pointing at `sciencefiction.site`. Built from `page.url` + `site.url`, so it matches `og:url` exactly and picks up `SITE_DOMAIN` per clone with no per-domain configuration.
  - **Self-canonical is right here *because* the domains are meant to diverge.** A cross-domain canonical says "this page is a copy, rank the other one" — consolidating ranking signals into one host, and in exchange asking search engines not to rank the independently-branded domains at all. That trade is worth making for duplicates and is exactly wrong for editions carrying their own framing, cast and copy. The decision followed from the plan to diverge, not the other way round.
  - **Deliberately not emitted on a page this build hides.** `robots` is `noindex, follow` precisely then, and noindex plus canonical is contradictory signalling — one says don't index this, the other says index this URL instead. A placeholder has nothing worth canonicalising; the reader-facing pointer to the domain that *does* have the page is the link in its body. The condition tests for `noindex` in `robots` rather than for `robots` being set at all, so a page that opts into some other directive via front matter still gets its canonical.
  - The two redirect stubs (`glossary/dynarch.njk`, `lore/solar-system-concordant.njk`) are unaffected — they hand-write their own head and keep canonical + noindex, which is the conventional pairing for a *redirect* rather than for a placeholder.
  - **Verified as a partition, not just as a presence check.** Full build: 424 pages, 415 canonical, 11 noindex, the 2 stubs in both, and **zero pages carrying neither**. A `THREADS=church-space` narrowed build: 51 canonical, 373 noindex, again zero gaps. Also confirmed three different `SITE_DOMAIN` values each produce a canonical naming themselves.
- **A minimum viable `deploy.conf` is now two lines: `CPANEL_USER` and `DOMAIN`** (2026-07-30), plus `ALT_DOMAINS` for an account serving several. `lib/editions.js` gained the deploy config itself — `theme`, `threads`, `characters`, `topics`, `siteName`, `siteTitle`, `giscusProfile`, `commentsEnabled` — and `scripts/resolve-edition.js` looks it up **by domain** so `cpanel-deploy.sh` fills in whatever a clone leaves unset.
  - **`deploy.conf` still wins wherever it sets a value; the registry only fills gaps.** That is not just migration cover. `deploy.conf` is untracked and lives on the cPanel account, so live configs cannot be migrated from the repo and a registry that overrode them would silently re-skin a live domain. It is also the honest ordering: **the alt-domain entries in the registry were inferred from `sample-deploy.conf`'s commented examples**, since the real file can't be read from here. The deploy log prints one `[edition]` line per key naming its source — check those on the first deploy and correct `lib/editions.js` against what each domain actually sets.
  - **Resolution happens in the deploy script, not in the Node consumers, and the blast radius is why.** Every downstream reader is already env-var driven — `content-filter.js` reads `CHARACTERS`/`TOPICS`/`THREADS`, `site.js` reads `SITE_NAME`/`SITE_TITLE`, `giscus.js` reads `GISCUS_PROFILE` — so resolving in bash and exporting the same variables leaves all three untouched. Teaching each of them its own edition fallback would have meant editing three modules that currently have one input each.
  - **This does not weaken private-thread opt-in; it strengthens it.** `threads` can name `church-space`, and that is still explicit opt-in by naming — now recorded in a tracked, reviewed, versioned file instead of an untracked one on a server. What remains forbidden is opt-in inferred from the *shape* of a domain string; a registry entry is a decision, pattern-matching a hostname is not. It also closes the footgun where a church-space domain that forgot `THREADS` would serve the full site minus its own storyline.
  - **`EDITION` is threaded through and exported separately from `THEME`.** Falling back to `THEME` covers a pre-registry clone, but the axes are independent now — a domain whose edition id differs from its palette (`EDITION=my-site THEME=sepia`) would otherwise resolve its copy as `editionFor("sepia")`, find nothing, and silently ship the default tagline. Verified end to end.
  - `validateEditions` now also rejects a **domain claimed by two editions** (resolution would pick one and the other would quietly serve wrong branding forever) and a `theme` with no matching `src/css/theme-*.css` (the deploy silently keeps `main.css` on a miss, so a typo shipped as "unbranded" rather than failing).
- **`editions.local.json` — independent hosting without a fork.** Untracked, gitignored, with a tracked `sample-editions.local.json`, exactly the pattern `giscus.local.json` already establishes. An entry whose `id` matches a tracked edition overrides it field by field; a new id is added. Domain matching tolerates scheme, `www.`, port, path and case.
  - **It cannot add story content, and that is the point.** There is no field for a lore page, chapter or glossary entry, because canon is centralised. That protects an independent host as much as the record: nothing they put in that file can be mistaken for this story's canon.
  - This is also the seam that makes the config split coherent. `GISCUS_PROFILE` stays repo-side because a profile name is meaningless outside this registry; the raw `GISCUS_*_ID` values stay server-side because they are exactly what an independent host supplies via their own `giscus.local.json`.
- **`lib/editions.js` — a registry of per-domain copy, so `theme` goes back to meaning only "palette"** (2026-07-30). "Edition" is the site's own existing word for a per-domain build, not a coinage: `excluded.njk` has said "Not included in this **edition**" since narrowing was introduced. Resolved from an `EDITION` env var, **falling back to `THEME`** so every untracked live `deploy.conf` keeps working with no edit.
  - **The governing rule is a constraint on the file, not a note about it: canon is centralised, and variants are non-canonical** (settled 2026-07-30). Nothing an edition carries may assert a fact about the world. `src/seasons/`, `src/lore/` and `src/glossary/` are identical on every domain; every field here is framing — a tagline, a hero cast, an audio track. The test for a proposed field is not "is this nice for that domain?" but "**could two domains now disagree about something true?**" This is also what makes the multi-domain deploy coherent rather than merely convenient: filtering only ever *subtracts*, so a narrowed clone shows a subset of one record and never a contradiction of it. No fifth canon status is added — an edition is the shop window, not the archive.
  - **Why `theme` had to stop doing this job.** It was carrying palette *and* identity, and `sample-deploy.conf` already breaks the one-to-one that requires: four `fol*` entries share `THEME=fellowship`, two `churchspace*` share `THEME=church-space` — six domains, two identities. A fifth domain wanting its own tagline couldn't have one, and a domain wanting the `sepia` palette *plus* its own copy couldn't ask. Verified the fix: `EDITION=pets THEME=sepia` now yields pets copy on the sepia palette, which was previously inexpressible.
  - **It also inverts the direction copy is organised in.** The old `{% if theme == %}` branches put one domain's identity across `src/index.md` and `base.njk`, so edit points grew as domains × templates. Now it's one entry per edition, however many templates read it. `editionFor()` merges over `DEFAULT_EDITION`, so an edition declares only what it overrides — `church-space` is registered with no overrides at all, precisely so its first flourish is a one-line edit here instead of a new template branch.
  - Migrated the existing four looks (`default`, `fellowship`, `starquest`, `pets`) plus a registered `church-space`. **Content verified identical** for all four against pre-refactor snapshots: same subtitle, same audio file and label, same hero cast.
- **Codex entries now say on the page that they aren't canon.** `codex.njk` renders a short notice under the byline: a primary source, not the Archive's own voice, canon *for its author*, possibly partial or self-serving or contradicted by settled lore, and to be cited as an account rather than as established fact. It names the entry's own `author` inline, which is possible on every entry because `author` is required precisely so there is someone to attribute the claims to.
  - **Why on the entry and not just the index.** `/codex/`'s intro already warned that "some are incomplete, biased, or built to mislead", but most readers never see it — a search result, a cross-link from a lore page, or an external citation all land on the entry directly. The four canon statuses are the easiest thing on this site to flatten, and flattening them turns a named source's account into the Archive stating fact.
  - Reuses the `.canon-facts` box treatment deliberately, so a reader who has met one of those on a chapter already reads the shape as the Archive talking *about* a text rather than *in* it. Carries `data-pagefind-ignore` for the same reason the breadcrumb and footer do: identical boilerplate on all 32 entries would otherwise return 32 indistinguishable hits for "primary source" or "canon". `npm run generate-themes` re-run, so all nine generated themes stay in sync.
- **Chapters can carry their own image, and social cards gained the tags they were missing.** `image`/`image_alt` are now optional chapter front matter (`lib/content-schema.js`), with `chapters` added to `OG_IMAGE_DIRS`; files live in `src/images/chapters/` and `chapter.njk` renders one on the page, on the same pattern as `lore-entry.njk`.
  - **The gap this closes was a social-sharing one, not a design one.** Chapters had no image field at all, so **147 pages** — every chapter plus every scene-POV page, the most shareable things on the site — fell back to one site hero in their Open Graph tags and produced visually identical cards. The fallback stays for any chapter without an image.
  - New tags in `base.njk`: `og:image:alt` and `twitter:image:alt`, `og:image:width`/`height`, `og:locale`, and `article:published_time` on article pages.
  - **`og:image:alt` follows the image, not the page** (`computeOgImageAlt`). Once the fallback hero is in use, a page's own `image_alt` describes a file that isn't in the card, and reusing it would caption the wrong picture — the exact failure the July 2026 audit found on-page. Verified on an excluded page: it falls back to the hero and the hero's own alt, leaking neither.
  - **`article:published_time` reads its guard and its value from different places, and has to.** `date` in a layout is the raw front-matter string, so it answers "did the author set one?"; the value comes from `page.date`, Eleventy's parsed Date, because `dateToRfc3339` uses `DateTime.fromJSDate` and renders the string as empty — which is how the first attempt shipped an empty tag. Testing `page.date` instead would have stamped every lore entry with a publication time it never had, since Eleventy falls back to file mtime.
  - **`lib/image-size.js`** is new: a memoised JPEG/PNG header reader for the width/height tags. No new dependency — this repo ships zero runtime `dependencies`, and `sharp` would pull per-platform native binaries for two integers sitting in the first few hundred bytes of every file. Only JPEG and PNG are handled (the only formats `src/images/` contains); anything unreadable or unparseable returns null and both tags are omitted, because a guessed dimension makes a platform reserve a box the image doesn't fill.
  - Verified end to end by temporarily giving a chapter an image: Open Graph switched to it with correct alt and real dimensions, it rendered on the page, and validation accepted the new fields. Test artifacts reverted.
- **`/llms.txt`** ([`src/llms.njk`](src/llms.njk)) — an orientation index for language models and agents reading the site, per the llmstxt.org convention. Plain Markdown, generated from the same **filtered** collections `sitemap.xml` uses, so a narrowed clone advertises only what it serves and a private thread stays absent.
  - **It carries the canon model, which is the only thing on this site a crawler cannot infer and the main reason the file exists.** Four canon statuses flatten to one under naive reading, and the consequence is not cosmetic: an agent that quotes a codex entry as settled fact has misread the archive. `llms.txt` states that chapters/lore/glossary are canon, that each codex entry is canon *only for its named author* and may be partial or contradicted, that contested material lives in the Codex by design, and that an overlay is neither. It also warns that a POV block is one character's understanding rather than a narrator's assertion, that terminology is versioned with a migration map, and that story content is CC BY-NC-ND while the engine is MIT.
  - **It is a map, not an inventory, and that was a correction mid-build.** The first version enumerated every chapter, lore entry, glossary term, character and codex entry with descriptions: 352 lines, 66 KB, ~16k tokens spent before the reader reached anything it wanted — and duplicating `/lore/`, `/characters/`, `/glossary/`, `/codex/` and `/timeline/`, which already enumerate their own sections in HTML, plus `sitemap.xml`, which is the complete URL list. The shipped file is **102 lines, 9.7 KB**: orientation, the canon rules, start-here links, per-section links with live counts, and chapters named inline (the one section whose story order isn't recoverable from URLs by anything that hasn't decoded the `sNNeNNcNN` scheme).
- **`scripts/make-lore-cards.ps1`** — a batch driver over `make-codex-cover.ps1` that generates the title cards for the nine lore entries lacking an image, with the card table inline. Local authoring tool: Windows-only, not part of the build, not run by `npm test`, not linted by CI (the shellcheck job covers the three `.sh` files only).
  - **Why a script rather than nine pasted invocations.** The nine cards are data, not decisions — the decisions are already recorded in `images.md` Open work 5. `make-codex-cover.ps1`'s own header gives the rationale: the original codex covers "were produced ad hoc and never scripted, so a broken or missing cover had no reproducible way to be rebuilt." That reasoning covers *which* cards exist as much as how one is drawn. Same shape as `generate-themes.js` — a mechanical derivation belongs in a script so nobody hand-maintains its outputs.
  - **Existing files are skipped unless `-Force`**, so a re-run cannot clobber a card since replaced by hand. `-List` previews the table without generating; `-Only <substring>` regenerates one row.
  - Two constraints found by reading the generator rather than guessing: subtitles are **not** auto-fitted (only titles are), so the driver fails the whole batch up front on any subtitle over 34 characters rather than emitting a card with text running off the canvas; and `Institution`/`Author` are empty on every row, because codex covers carry them for a named in-universe source and lore is the Archive's own voice with no author to name.
- **Seventeen lore entries gain a worked example, a dramatised cross-reference, or both** (2026-07-30). A sweep prompted by a simple observation: only 9 of 97 lore pages linked to a chapter, and several of the most rule-heavy pages read as regulation with nothing behind it. Nothing here changes a fact — every addition is drawn from an existing chapter's `canon_facts` or from settled lore already on file, and no new canon was created.
  - **The direction of reference is deliberate and one-way: lore points at chapters, never the reverse.** A chapter has to stand on its own for a reader who arrives at it first, so nothing was added to `src/seasons/`. The links are all outbound from the reference material, which is the direction a reader already goes when a rule raises a question.
  - **The physics cluster gets its numbers back.** [`teleportation-limitations.md`](src/lore/teleportation-limitations.md) states its three-condition rule and now records where the rule was written and why it carries no exception clause — Thorne argued for one, having just authorised a jump on a thin margin, and was talked out of it. [`quantum-space-harmonics.md`](src/lore/quantum-space-harmonics.md) gains the Threshold coupling as a worked case: three traces overlaid, a 3:7 band coupling, and a forecasting capability of 18–26 minutes rather than a fix. [`boundary-zones.md`](src/lore/boundary-zones.md) grounds Class II in the one figure it always had available and never quoted — forty seconds, eleven years, carried as a calibration fault.
  - **Two pages gained the counter they were missing rather than a link.** [`predatory-entities.md`](src/lore/predatory-entities.md) listed a "how to counter them" for Cute Predators only; Court-Fae and Gilded Saints now have theirs, and the Gilded Saint entry says plainly that the obvious counter is the wrong one — refusing comfort exhausts the person practising it and inconveniences nothing. [`seed-mind-lifecycle.md`](src/lore/seed-mind-lifecycle.md) gains **the stall**, the state between Phase 6 and a completed Phase 7 that the phase list could not name because it is not a phase.
  - **One addition exists to stop a misreading rather than to illustrate one.** [`noogenic-seeding-system.md`](src/lore/noogenic-seeding-system.md)'s "Not a Salvation" section holds that a completed transference carries no person and nobody wakes up in a successor universe. That is unchanged and still correct — and a stalled seed is a different state in which somebody is demonstrably in there. The two are now distinguished on the page, because a reader who meets both without the distinction concludes one of them is wrong.
  - Also: [`cerebraun-hegemony.md`](src/lore/cerebraun-hegemony.md) (what unambiguous authority costs the official holding it — accountability for a reversal independent of whether the reversal was correct), [`cerebraun.md`](src/lore/cerebraun.md), [`levrils.md`](src/lore/levrils.md) (two occasions, one tier constraint — what differed was what was permitted, never what was possible), [`saint-aoife.md`](src/lore/saint-aoife.md), [`dryadic-trees.md`](src/lore/dryadic-trees.md), [`celtic-union-of-planets.md`](src/lore/celtic-union-of-planets.md), [`orbital-habitats-compact.md`](src/lore/orbital-habitats-compact.md), [`habitat-threshold.md`](src/lore/habitat-threshold.md), [`the-fusion-ceiling.md`](src/lore/the-fusion-ceiling.md), [`planets/tir-na-nog.md`](src/lore/planets/tir-na-nog.md), [`kieme-visible-hand.md`](src/lore/kieme-visible-hand.md).
- **`planets/tir-na-nog.md` records the Boirinn Hollow, and `dryadic-trees.md` records it as an unsurveyed instance.** Both were derivable from [`s05e02c01`](src/seasons/s05/e02/s05e02c01.md)'s `canon_facts` and neither page had them. The entry earns its place by what it establishes about the organism rather than about that holding: the two field markers are consistent wherever an anchor of this type occurs, so **the survey record's coverage is a record of where instruments have been allowed, not of where the trees are** — the same selection effect `boundary-zones.md` already names about the two Solar System sites, arriving from a second direction.

### Changed

- **`story-bible/images.md` gains Open work 5: the nineteen lore entries with no image** (audited 2026-07-30). Open work 1 had enumerated missing *character portraits* since July; nothing had ever enumerated missing *lore* illustrations, so the gap was invisible rather than deferred. None blocks a build — `lore-entry.njk` guards the image behind `{% if image %}`.
  - **The section's one editorial contribution is splitting the nineteen by job.** Nine are institutions or factions and belong on designed title cards from the generator, which letters text with a font engine where an image model cannot be trusted to spell. The remaining ten are places, phenomena, technology and cosmology, and get generated images with prompts written to the standing rules — era stated, lettering banned, sheen named rather than substance. (The first version of this entry also claimed the split was *descriptive* of how four existing lore cards were made. It wasn't — see **Fixed** below.)
  - Two prompts carry an explicit constraint about what they must *not* show. `arilon` must not read as a portrait of one figure, since the entry exists to establish that Arilon is a recurring pattern and not a person. `membrane-shadows` must read as an unexplained physical imprint and not as a threat approaching — nothing is coming through, which is the entry's whole point and also where the tone line bites hardest.
  - Also flagged, deliberately as *not* defects, so a future audit doesn't re-derive them: `predatory-entities.jpg` and `teleportation-limitations.jpg` have thin-but-accurate alt text.

### Deprecated

- **`deploy.conf`'s `CUSTOM_LORE_FILE`** (2026-07-30). Still honoured — the deploy logs a warning rather than failing, because `deploy.conf` is untracked and lives on the cPanel account, so a live domain relying on it cannot be migrated from the repo and a hard failure would silently drop that domain's page. `CUSTOM_CSS_FILE` is **not** deprecated, but the reason given here originally — "it is cosmetic and cannot assert a fact" — was wrong and is corrected under **Fixed** below.
  - Two reasons, and the second is the binding one. It keeps story prose outside the repo — unversioned, unreviewable, unseen by `npm test` and both check scripts, outside the CC BY-NC-ND scope `CONTENT-LICENSE.md` describes, and lost with the hosting account. And it writes into `src/lore/`, which is **canon** — making it the only mechanism in the system that could have two domains disagree about a fact rather than show different subsets of one record. Removing it closes the last route to per-domain canon.
  - There is deliberately **no replacement for injecting lore**. `lib/editions.js` replaces the legitimate use (per-domain framing) and cannot express the illegitimate one. A domain needing a page of its own gets a non-canonical one; if material is worth being true, it is worth being true everywhere and belongs in `src/lore/` on every domain.

### Fixed

- **The glossary was never actually sorted, and its A–Z navigation quietly depended on it being sorted** (`.eleventy.js`, `src/glossary/index.md`). The `glossary` collection was `getAll()` plus two filters with no `.sort()`, so it inherited Eleventy's own ordering — alphabetical for the first 22 terms and unordered for the remaining 31.
  - **The page is built entirely on the assumption of a sorted list.** It emits a letter heading whenever an entry's first letter differs from the previous entry's, which is correct for sorted input and nonsense otherwise: **44 headings for 53 terms, with 13 letters appearing more than once** as the sequence flipped back and forth. The A–Z jump bar *is* deduplicated, so each letter anchored to its **first** occurrence and every later group of that letter was unreachable from it — four of the five N terms could not be reached by clicking N. The bar itself rendered in the collection's order (`A B T C E H K M N U W F S O I L Q V G P`) rather than alphabetically.
  - **Leading articles are ignored when filing** (settled 2026-07-30): *The Interval* files under **I** and *The Common Manifold* under **C**, standard printed-glossary practice and where a reader actually looks. The cost is that a heading letter no longer always matches the first character shown, which is the accepted trade.
  - **Order and heading now come from one function.** `glossaryAlphaKey` drives the collection sort, and the new `glossaryAlphaLetter` filter drives both the jump bar and the headings, so the letter a term files under is by construction the letter it sorted by. Comparison uses `localeCompare` with `sensitivity: "base"`, which keeps the four `Noö…` terms together and in order.
  - **Deliberately presentation-only.** `related:` terms resolve through `glossaryUrl` by *exact* title match, leading articles included, so stripping an article for filing cannot change which page a related link reaches. The collection's other two consumers are order-independent (`| length` in `src/llms.njk`, a title lookup in `glossary-entry.njk`). After: 20 headings, none repeated, jump bar identical to the headings and in A–Z order.
- **Home page hero went blank on any deploy whose slide count wasn't 5 or 6** (`src/css/main.css`, `src/index.md`, all `theme-*.css` regenerated) — reported as a missing image on `undercover-pets.com`'s home page, which is the domain the bug was built to catch first.
  - **The slide count is a property of the *deploy*, not of the edition.** `src/index.md` resolves `lib/editions.js`'s `heroCharacterIds` through `charactersByIds` against the **filtered** characters collection, so a narrowed clone drops any hero character its `CHARACTERS`/`TOPICS`/`THREADS` excludes. `main.css` carried crossfade rules for exactly `--n5` and `--n6`, the two counts the registry lists. At any other count no slide got an `animation-name`, every one of them kept the base `opacity: 0`, and the hero rendered as an **empty bordered box** — losing all the portraits, not the one character that was filtered out.
  - **One missing tag on one character page was enough to trigger it.** `undercover-pets.com` narrows by the `undercover-pets.com` tag and is the only edition with a recast five-character hero cast, so its hero sits one tag away from a count of four — and this release's own changelog records a pets-relevant page found *without* that tag. Reproduced directly: `TOPICS=eden` gives four slides, `TOPICS=detective-agency` three, and before this fix both rendered nothing.
  - **Fix is in the stylesheet, where the promise was being broken.** Crossfade variants now exist for 2–8 slides (one 6s slot each, 1s crossfade — the same geometry as the original two, which are unchanged), `:nth-child` delays extend to 8, and a `:first-child { opacity: 1 }` fallback shows one still portrait for a single slide or any count past the range, so a blank box is no longer reachable. `npm run generate-themes` re-propagated all of it, including to `theme-pets.css`.
  - **And when *no* hero character survives the filter, the page now falls back to the site hero image** rather than rendering no image at all — the previously untested case, and the one that leaves the page every reader lands on first with nothing on it. Uses the page's own computed `ogImage`/`ogImageAlt`, so the card preview and the page show the same picture with the same description.
- **Corrected a false claim about `CUSTOM_CSS_FILE`, and added a deploy-time warning for it.** Three places in this release described that key as "cosmetic" and as unable to "assert a fact" (`CHANGELOG.md`, `sample-deploy.conf`, `scripts/cpanel-deploy.sh`). That does not survive scrutiny. The accurate claim is narrower: **it cannot change the record.**
  - **CSS here can inject text.** `content:` on a `::before`/`::after` puts arbitrary prose on a page — an assertion delivered by stylesheet. Nothing restrains it: the site sets no CSP at all, and this file is appended **last** to the built `main.css`, so it overrides the theme and everything else.
  - It can also **hide** content — `display:none` on `.codex-entry__status` suppresses the "not canon, cite it as an account" notice added earlier in this release; on `.site-footer` it hides the CC BY-NC-ND attribution — **reorder** what a human reads relative to the DOM via flex/grid, and **reach external hosts** via `@import`, `url()` and `@font-face`.
  - **What bounds all four is that they affect rendering only.** The DOM is untouched, so crawlers, chatbots, screen readers, reader mode, the Atom feed and `llms.txt` still get the real content. So the canon rule **holds structurally** — nothing CSS does enters the record — and **leaks perceptually**, for a sighted human on one domain. It cuts both ways: `content:`-injected text is invisible to search engines and to an LLM fetching the page, so an injected "fact" reaches nobody else.
  - **The conclusion is still to keep it server-side**, for a narrower reason than originally given: the legitimate use is real and small, hand-edited `theme-*.css` files get overwritten by `generate-themes`, and the realistic risk is not sabotage but forgetting — hiding one of those boxes months from now while tidying something you no longer recognise.
  - `check_custom_css_powers()` in `scripts/cpanel-deploy.sh` now **warns** on the three constructs that cross the line, naming only the elements actually mentioned so the warning never explains something you didn't do. It never fails the deploy: `content: ""` is ordinary pseudo-element decoration, and a false positive that blocks a deploy is worse than a log line. Exercised against seven cases — innocent tweak, `content: ""` and `content: ''`, text injection, hiding the notice, hiding the footer, an external `@import`, `display:none` on an unrelated selector, and all three at once — with silence where silence was wanted.
- **The homepage hero subtitle was invalid HTML, and the class it names was inert.** `src/index.md` wrapped the subtitle in `<p class="home-hero__subtitle">` and the Markdown pipeline then emitted `<p>` elements *inside* it. Browsers auto-close a `<p>` on encountering a nested one, so `.home-hero__subtitle` (1.1rem, muted, `max-width: 55ch`, centred) was applied to an empty paragraph while the visible text rendered as default body copy. Collapsing the `theme ==` branches into a single `{{ edition.heroSubtitle | safe }}` removed the blank-line structure that caused it, so the markup is now one correctly-nested paragraph.
  - **This is a visible change on every domain's homepage** — the subtitle finally picks up its intended styling — and it is called out because it was a side effect of the editions refactor rather than something asked for. The `latestLore` sentence also joins the subtitle as one paragraph instead of becoming a second one, which is what the template always intended.
- **`scripts/check-internal-links.js` now scans `src/` and `lib/`** (`.md`/`.njk`/`.html`/`.js`) rather than `src/` alone. Moving the homepage taglines into `lib/editions.js` took 4 links out of the script's coverage silently, which is the failure mode the script itself exists to catch.
  - **Scanning by root, not by a list of exceptions.** The first version of this fix was an `EXTRA_FILES` list naming `lib/editions.js`; that shipped and is now replaced. A hand-maintained list of exceptions goes stale — the next authored file with links outside `src/` would not have been added to it and nobody would have noticed. Same reasoning that consolidated four drifting image notes into one `images.md`.
  - **Verified safe rather than assumed:** `src/js/pov.js` and `src/js/search.js` contain no `/star-rangers/` strings at all, and `lib/content-filter.js`'s three occurrences are in comments and a regex literal — none href/src/markdown-link shaped, so `linkRe` cannot match them. Page derivation still comes from `src/` only; a `path.relative(SRC, …)` on a `lib/` file would have invented pages like `../lib/…`.
  - **One consequence, arguably a feature:** an illustrative URL written in attribute form inside a code comment is now checked like a real link. It caught a made-up example in `editions.js`'s own comments the day this landed. Fake paths should be described in prose, not quoted as attributes.
  - The summary line now counts files actually **read** (360) rather than every file walked under `src/` (579) — the old figure included images and audio, so it was always larger than the set of files that could hold a link. Verified end to end: a break in `lib/` and a break in `src/` are each reported against the right file, and both exit non-zero.
- **`lib/editions.js` validates itself at build time** (`validateEditions`, called from `.eleventy.js`), throwing on a duplicate edition id or a `themeAudio.file` missing from `src/audio/`. The audio filenames used to be literal `src="…/audio/x.wav"` attributes that the link checker verified; they are now bare strings assembled by a template, which the checker correctly skips as templated — so a typo would have shipped a player pointing at nothing. Duplicate ids are checked for the same reason `validate-content.js` guards `comment_id` collisions: the map would quietly keep the last one.
- **Hidden pages are no longer indexable.** A page this build hides still returns HTTP 200 with a real placeholder body — that *is* the design, so no cross-link ever 404s — which meant every hidden page was advertised to crawlers as ordinary content. `base.njk` now emits `<meta name="robots">` when a `robots` value is set, and two places set it to `noindex, follow`.
  - **`follow`, not `nofollow`, deliberately:** the one useful thing on a placeholder is its link out to the reference domain, and that should still be traversed.
  - **The effect on the public site is larger than the narrowed-clone case that prompted it.** `church-space` is a private thread hidden on *every* build including the default full-site one, so the main site was already publishing 9 indexable "Not included in this edition" pages. Those are now `noindex`. On a narrowed clone the count is 376.
  - **Driven off `isContentIncluded` rather than `data.layout`**, per the hazard documented at the top of `.eleventy.js`: `layout` is itself one of the overridden computed keys, so reading it from inside another computed field can see the already-rewritten value. That exact mismatch caused a real bug once before.
  - Included pages get **no** `robots` tag at all rather than an explicit `index, follow`, and any page may still opt in via its own front matter. Verified both directions: 376 placeholders and 376 `noindex` tags on a `THREADS=founding-era` build with zero gaps; an included lore page and an included scene-POV page each carry none.
- **Scene-POV pages were the blind spot in both the new `noindex` and the existing sitemap filter** — 92 hidden POV placeholders on a narrowed build, and one of them listed in `sitemap.xml` as real content. One root cause: **scene-POV pages opt out of the layout-swap convention.** They keep their own layout and gate their body on `item.included` (set per character in `src/_data/scenePovPages.js`), so anything keyed to the swapped layout misses them — and because they are generated from `src/scene-pov.njk`, `classifyContentPath()` sees that as their `inputPath`, matches none of its content dirs, and passes them through as included.
  - Fixed in the two places that own the decision: `src/scene-pov.njk` sets its own `robots` from `item.included`, and `src/sitemap.njk` gained a second test alongside the layout check. This is precisely the failure mode `CLAUDE.md` warns about for a new page type, arriving from the opposite direction — an *existing* page type that a new field forgot.
  - **Not changed, and flagged rather than silently decided:** `sitemap.xml` lists only **1 of 92** scene-POV pages even on a full build, because Eleventy represents a paginated template once in `collections.all`. Whether ~92 derivative POV views of existing chapters belong in a sitemap at all is an editorial call about duplicate content, plausibly deliberate, and not one to make as a side effect of a `noindex` fix.
- **`story-bible/images.md` Open work 5 shipped with a false claim about how the existing lore cards were made** (introduced in #232, corrected here). It stated that `cerebraun-hegemony`, `celtic-union-of-planets`, `orbital-habitats-compact` and `levrils` were produced with `scripts/make-codex-cover.ps1`, and listed `federation-of-sentient-beings` and `united-space-consortium` alongside them as more of the same. Both halves were wrong.
  - **The error came from reasoning about `image_alt` strings instead of opening the files** — the exact failure mode that file already warns about in its own Auditing section ("To check whether an image matches its description, **read the file**"). Reading them settles it: the four cards are **1600×900 landscape**, individually tinted, and carry a device inside a ringed seal, while `make-codex-cover.ps1` emits **square 1600×1600** in one fixed blue palette with no emblem motif at all — only `rules`, `dissolution`, `none`. It cannot have produced them. `federation-of-sentient-beings` (1200×1509) and `united-space-consortium` (1200×800) are photographs, not cards.
  - **The correction surfaces a real gap rather than just fixing a sentence: the emblem-card recipe is unrecorded and currently unreproducible.** All four landed in Release 1.7.0 (#187) with no entry in `image-prompts.md` — precisely the rule that file exists to enforce, and precisely the failure the Prismere cluster prompted it over. A fifth card in the same family is referenced from `uniforms-and-insignia.md`. Now recorded as open work with three named ways out: recover the original tool and log it; extend `make-codex-cover.ps1` with aspect, palette and seal support (noting every positioning constant in it is tuned for 1600×1600, so this is more than a parameter); or accept the codex look for lore and treat the four as legacy.
  - **Consequence for the new script, stated in both its header and `CLAUDE.md`:** its nine cards will read as codex-style, not like those four, and that inconsistency is deliberately left unresolved — it is a call to make before generating nine cards, not after.

## [1.10.0] - 2026-07-29

The naming release. Every substantive change in it is about a name rather than a fact: a Cascade tier renamed, a Ranger rank restyled, a second rank finally recorded under its full style, one charter clause rewritten so its rank floor cannot be misread, and a journal entry about the single tell the whole set had in common.

**That tell is the release's actual content**, so it belongs at the top. Each entry touched here carried a paragraph whose only job was explaining what its own name did *not* mean. `Dynarch` named a tier after the forces it is least able to alter — the rule-sets are the Worldwright's — and the entry spent a section on the gap. `Starwarden First Officer` named a rank after a relationship it does not have, and its own bullet admitted so in its opening clause. In both cases the explanation was *good*, which is exactly why it survived for months. A well-argued defence of a bad name is a decent piece of writing and a permanent tax on every reader who arrives after it.

**The two renames needed opposite machinery, which is this release's one genuinely load-bearing distinction.** `Dynarch → Mediarch` is the Archive correcting its own vocabulary: no in-universe register ever used the word, so once retired it names nothing, migrates on sight, and takes no dating rule. `Starwarden First Officer → Archwarden` is an institution renaming its own rank in **2833 UCSD**, so both styles are correct for their own periods and pre-2833 records are read as written rather than corrected. The consequence worth having in the record: **not one word of canon prose was altered anywhere in this release.** The two migration rows now sit next to each other in the canonical glossary making opposite claims about the past, and each points at the other so neither can be applied with the wrong rule.

**`MINOR`, and this is the first release where that classification needed an argument.** Earlier entries could justify it with "nothing was removed, renamed or moved" — this one renames two things and moves a page's URL, which reads like that criterion failing. It isn't. `MAJOR` in this project has always concerned the **engine's compatibility surface** — `deploy.conf` keys, themes, build outputs, defaults — and none of those changed. The single moved URL keeps a redirect stub at its old address, and the retired `dynarch` tag is deliberately retained on the renamed entry so any clone narrowing by that topic keeps matching it. Nothing 404s and no configuration breaks, which is the substance the older phrasing was reaching for.

**Tagging.** `v1.10.0` is cut at this release's own merge commit, which does not exist until the release merges — so the tag is always the last step and always separate from this file. The GitHub-automation path used for most changes here cannot publish `refs/tags/*`; where no local clone is in the loop, that is where the backlog recorded under `[1.7.0]` and `[1.8.0]` came from.

### Changed

- **The rank above Deputy and below Senior is recorded as `Field Officer`** (settled 2026-07-29) — the ladder entry previously gave only the short form **Officer** and described the rank without naming it in full. Same rung, same duties, no reordering: full field-qualified rank, the first tier that can act and be posted independently.
  - **Three live forms, and the reason they coexist is worth the paragraph.** Formal style **Field Officer**; short form **Officer**; colloquially just **Star Ranger** or **Star Ranger officer**. Unlike the three Archwarden styles retired above, none of these is deprecated — the entry keeps all three and explains which register each belongs to.
  - **The colloquial form is genuinely ambiguous, and the entry now says so rather than smoothing it.** "A Star Ranger" names *this rung specifically* and *everyone from this rung upward* — a Line Captain is a Star Ranger and so is the newest Field Officer aboard her vessel. Filed not as a defect but as what happens when a rank is the **threshold of belonging** rather than a step inside it: the rung at which a Ranger stops being supervised and starts being one of the people the word is for. The previous wording called *Star Ranger* a collective term "rather than a rung of its own," which is half the truth — it is both.
  - `Deputy` gains its full style **Deputy Officer** and `Senior` its alternate **Senior Star Ranger**, from the same pass. Recorded in [`star-rangers-command-hierarchy.md`](src/lore/star-rangers-command-hierarchy.md), with `field-officer` added to the page's tags.
- **The Field Triumvirate clause gains a fallback composition** (settled 2026-07-29). The clause previously specified exactly one body — the ranking Line Captain, the nearest vessel's Archwarden, and the next most senior Starwarden reachable — and said nothing about what happens when that cannot be assembled, which left the charter's emergency provision silently unavailable in the circumstances most likely to need it. It now has **two forms in strict order of preference**: the named composition where it can be reached, and failing that **the three most senior officers available, none below the rank of Officer**.
  - **The floor is `Field Officer`, and it is the only rank test the clause makes.** Field Officer is the ladder's full field-qualified rank and the first tier that can be posted and act independently; below it — Deputy Officer, Cadet, Novice — a Ranger works under another officer's supervision by definition, and a body of supervised officers authorising emergency action would be a contradiction, since the supervision is the thing being bypassed. The floor is therefore structural rather than a seniority ornament: it is the line between *can act on their own judgement* and *cannot*.
  - **The clause uses the full style deliberately, and this is load-bearing.** "The three most senior officers available" uses the ordinary word; *none below Officer* would collide with it, and *none below Star Ranger* would collide with the collective term for everyone from that rung upward — which would read as no floor at all. **Field Officer** is the only form of the name that cannot be misread in either direction. Same lesson the Archwarden restyling records, applied to a clause before it cost anything rather than after.
  - **Fallback, not alternative** — an officer cannot prefer it for being quicker to assemble or likelier to produce an agreeable third voice; where the named composition is reachable in the time available it is the only lawful one. **`Available` carries the third condition's own clock:** reachable in time to take part in the decision actually being made, so an officer who could join in an hour is not available to a decision that has minutes. And **seniority decides with no discretion** — the three most senior, in order — because a rule permitting selection permits a convening officer to assemble a body that will agree with them.
  - **What the fallback gives up is stated in the entry rather than concealed.** The named composition is independent by construction: a Line Captain, another vessel's Archwarden and a Starwarden on relay are three different commands, none owing its posting to the others. The fallback has no such property and on one hull can be a captain and two of their own crew. That weakening is accepted because the alternative is a body that cannot convene while people are dying — and the safeguards were never compositional: they are the three conditions, each standing on its own, plus the reporting and review that follow every convening.
  - **Below three eligible officers there is no Field Triumvirate**, and the clause manufactures nothing out of two. What one officer may do at the scene stays with the existing [decision doctrine](src/lore/star-rangers-decision-and-communication.md) — authority sitting with whoever is closest and holds the relevant rank — answered for personally, and never recorded as a Triumvirate.
  - **Nothing retroactive.** The 2831 Dock Seven convening reached the named composition in full and is now noted as the standing example of it; the fallback has never yet appeared on a record anyone has had to review. Recorded in [`star-rangers-command-hierarchy.md`](src/lore/star-rangers-command-hierarchy.md).
- **The force-governance tier is renamed `Dynarch` → `Mediarch`** (settled 2026-07-29) — a pure rename, with **no change of meaning, rank, property or constraint**. Every statement the corpus filed about a Dynarch stands unaltered about a Mediarch, which is why this is filed as Changed rather than as a settlement: nothing was decided about the tier here except what to call it.
  - **The old name pointed at the wrong thing, and the corpus already said so.** *Dynarch* (from *dynamis*, force) named the tier after its subject matter — and the tier's first listed constraint, unchanged since it was written, is that it governs the *operation* of a force and not its design: the rule-sets are the Worldwright's, and no Mediarch can rewrite a zone's physics. The name emphasised the one thing the office is least able to alter.
  - **The new name records what the 2026-07-26 settlement established.** *Mediarch* (from *medius*, the middle) names the mediating office — the Telearch line reaching inside a universe by proxy, standing between a continuity branch native to nowhere and the inside of the thing that branch has to reach. "A Mediarch is a proxy Telearch" now reads as an unpacking of the name rather than as a fact arriving from elsewhere.
  - **`Exarch` is untouched and still co-canonical.** The older devotional and provincial form was already naming the same mediation — a delegated governor holding a province from a distant throne — so the rename brings the formal name into line with the devotional one rather than displacing it. **No in-universe register keeps `Dynarch`**, which is why there is no quoted-as-written exception of the kind the Solar System Concord row carries: devotional sources say *Exarch*, and [The Reading Before a First Vigil](src/codex/the-reading-before-a-first-vigil.md) needed no change beyond its archive note.
  - **Recorded as terminology law**, per the standing rule that a cosmological rename means the canonical-terms list *and* a migration row *and* a corpus sweep: the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md) gains a `Dynarch → Mediarch` migration row and a `The name` block, and its "higher celestials" row now notes that the term has been migrated **twice** — `higher celestials` → `Dynarch` → `Mediarch` — so a legacy reading lands on the current name rather than on the intermediate one.
  - **Swept across the corpus in one pass**, hierarchy spines included: [`glossary/mediarch.md`](src/glossary/mediarch.md) (renamed from `dynarch.md`), `telearch.md`, `celestials.md`, `concordant-principal.md`, `high-celestials.md`, `glossary/cosmic-cascade.md`, `lore/cosmic-cascade.md`, `who-governs-a-universe.md`, `physics-comparison.md`, `concordant-membranes.md`, the codex archive note, and `story-bible/canon-consistency-audit-2026-07.md`. Where a page explains the tier's naming history rather than merely using the name — `lore/cosmic-cascade.md` and `who-governs-a-universe.md` — it now records all three names and why each was replaced, so the rename is legible from the record instead of only from this file.
  - **The old URL does not 404.** `/glossary/dynarch/` becomes a redirect stub ([`glossary/dynarch.njk`](src/glossary/dynarch.njk)) pointing at `/glossary/mediarch/`, on the same pattern as `lore/solar-system-concordant.njk`: canonical link, `noindex, follow`, meta refresh, and a sentence saying what moved and why. **`dynarch` is also kept as a tag** on the renamed entry alongside `mediarch`, so any cPanel clone whose untracked `deploy.conf` narrows by that topic keeps matching the page.
  - Verified with `npm test`, `check-internal-links.js` (1832 links, all resolve) and `check-related-terms.js` (204 terms, all resolve) — the last of which is the check that matters here, since `related:` resolves by page **title** and a stale term would have silently degraded to `/glossary/` rather than failing.
- **The rank above Starwarden and below Line Captain is restyled `Starwarden First Officer` → `Archwarden`** (settled 2026-07-29; **2833 UCSD** in-universe). A vessel's second-in-command, one rung above Starwarden and one below Line Captain — **seniority, duties and ladder position all unchanged**. Only the words on the tab moved.
  - **The old style was the most misread title on the ladder, and its own entry said so** in its opening clause. *First Officer* named the post — first officer **of a vessel** — and never meant first officer *to* a Starwarden, but that is how it read to anyone meeting it cold, and the two ranks are consecutive rungs rather than a commander and a deputy. The entry answered this with a paragraph of explanation, which is a tax on every reader rather than a fix. **Archwarden** says senior warden and needs no paragraph. Three written forms are also retired with it — `Starwarden First Officer`, `Starwarden First Class` and `Executive Starwarden First Class` — since one rank carrying three styles was half the problem.
  - **Where the old style actually cost something: the Field Triumvirate clause.** It names a warden-rank and a Starwarden in one breath, so the previous wording read as *the Starwarden First Officer … and the next most senior Starwarden* — implying a chain of command between the two that does not exist, in the one clause where three officers have to know who is who under pressure. Recorded as the reason the Provost track's standing objection was actioned. The 2833 restyling amended that clause and every other regulation naming the rank; **the composition itself did not change.**
  - **No canon prose was altered, by design.** This is an *institution renaming its own rank on a date*, not the Archive correcting its own vocabulary — so both styles are correct, each for its period, and the restyling is dated **after every published chapter**. [`s05e02c03`](src/seasons/s05/e02/s05e02c03.md) is right as published: Shepherd's 2831 Field Triumvirate and the officer who leaves on the last shuttle both keep the older style, quoted as written. Same treatment the 2723 charter preamble gets under the Solar System Concord row.
  - **The two renames in this release need opposite machinery, and the migration map now says so explicitly.** `Dynarch → Mediarch` has **no** dating rule — no in-universe register ever used the word, so once retired it names nothing and migrates on sight. `Starwarden First Officer → Archwarden` **has** one, because the old style was correct until 2833. The rows sit next to each other in the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md) making opposite claims about the past, which is correct; each row points at the other so neither is applied with the wrong rule.
  - Recorded in [`star-rangers-command-hierarchy.md`](src/lore/star-rangers-command-hierarchy.md) (ladder bullet, restyling note, Field Triumvirate clause and wording note) and the canonical glossary (new `Archwarden` term entry with its dating rule, plus a migration row). Character pages carry the `Canonical Term (formerly Legacy Term)` first-mention form per the guide's own deprecation policy: [`tissadelle-shepherd.md`](src/characters/tissadelle-shepherd.md)'s Season 4 progression and [`karla-wender.md`](src/characters/karla-wender.md)'s open progression both name the rank currently while flagging the style she held it under. `archwarden` is added to the hierarchy page's tags alongside the retained `starwarden-first-officer`.

### Added

- **Journal: [A Name You Have to Argue With](src/journal/a-name-you-have-to-argue-with.md)** — the craft entry for both renames above, and the one observation that connects two changes from opposite ends of the setting: **each entry carried a paragraph whose whole job was explaining what its own name did not mean.** The rank's bullet flagged itself in its opening clause; the cosmology entry's gap was subtler because the explanation was doing real work. The rule it ends on is that a well-argued defence of a bad name is a decent piece of writing and a small permanent tax on every reader who arrives after it.
  - Covers what the changelog entries cannot: that the cosmology rename was **the name lagging a settlement already made** (the proxy-Telearch finding relocated the tier three days before the word caught up), that the devotional register had the better name the whole time, and that the two renames turned out to need **opposite migration machinery** — the point nearly blundered past, since applying the Archive-correction rule to a dated institutional restyling would have "corrected" a published chapter that was never wrong.
  - Also records the engine half briefly, which is journal-appropriate rather than lore: why a moved URL is a broken link on four production domains, and why `check-related-terms.js` is the check that matters after a rename — `related:` resolves by page title and a miss falls back to the glossary index instead of failing, so a stale term sends readers somewhere plausible and wrong rather than breaking the build.
- **Title card for [The Reading Before a First Vigil](src/codex/the-reading-before-a-first-vigil.md)** — the one codex entry shipped without one, closing the loop opened when it merged. Same designed template as the rest of `src/images/codex/`: `DOCTRINAL RECORD` category line, `rules` motif, a tilted `DEVOTIONAL` stamp, and *Fellowship of Light · Contemplative Chapters* under the title.
  - **Rendered from a faithful port of `make-codex-cover.ps1`, not by an image generator** — same 1600×1600 canvas, same gradient stack and alphas, same coordinates, same tracking rules, same auto-fit title (landed at 106px). The `.ps1` is GDI+ and Windows-only, so it cannot run in a Linux session; the port reproduces its geometry exactly and the text is still font-rendered, which is the property that matters — the template exists precisely because generators cannot spell.
  - **One deliberate deviation, worth knowing before regenerating:** Georgia and Consolas are Windows fonts and are not available here. Georgia was substituted with **Gelasio**, which is metric-compatible, so the auto-fit title geometry lands where Georgia's would; Consolas was substituted with **DejaVu Sans Mono**, for which no metric clone exists, so the small tracked lines differ very slightly in width. Re-running the `.ps1` on Windows will produce a near-identical card with the true faces.

### Fixed

- **`package-lock.json`'s version had drifted to `1.8.0`.** The 1.9.0 release bumped `package.json` and the README's "Current version" line but not the lockfile, whose two `version` fields sit outside the README's release checklist and so were never part of the step that "gets missed" — they were never part of any step. Both now read `1.10.0`, bumped with `npm version --no-git-tag-version` rather than edited by hand so the two files cannot disagree, and **the checklist's first step now names the lockfile** so this does not silently recur. Found while cutting this release, which is the only moment anyone ever looks.

## [1.9.0] - 2026-07-29

The Dock Seven release. The event every Season 6–7 chapter reaches backwards for had no chapter and no POV of its own — `narrative-gaps-checklist.md` had it as the one genuinely unwritten item in the whole Tissadelle arc — and it has both now, in **Season 5** rather than at the S5/S6 seam, appended with no renumbering.

Its spine is that **the event is written once and then refracted through every institution that had to account for it**, none of which can see all of it: a claims adjuster who can write *dragons* in the box marked Proximate Cause because settling a bill requires no cosmology, an Institute paper whose section 4 prints the four observations that undo its own section 3, a lawful commercial concern that was never charged with anything, a comparative cross-reference withdrawn before submission, and a eulogy that declines to concede the death it is held to mark. The Corps' reputation ends up resting on a generous, secular, entirely wrong account of why its people survived — while the competence itself is real and on the page four hours earlier.

The other half of the release is **expand, don't replace**, made explicit: `canon_facts` were added to four published S6–7 chapters and **not one word of prose was altered**, so the six chapters stay canon exactly as written and now carry the *why* that was never written under them. Three deliberate non-changes are recorded as decisions rather than left to look like oversights, so a later pass doesn't "fix" them.

`MINOR` per the changelog's own rule: the release is additive surface area — one chapter, four codex entries, two lore entries, and front-matter insertions. Nothing was removed, renamed or moved, so nothing 404s and no `deploy.conf` key, theme or build output changed.

**Tagging gap closed.** `v1.7.0` and `v1.8.0` were cut from a local clone at their release commits (`3577221` and `9f69f24`) and pushed, clearing a backlog both of those release notes had recorded as pending; `v1.9.0` was cut at this release's own merge commit, which is why it could not go up with them. The "not yet tagged" notes under `[1.7.0]` and `[1.8.0]` are superseded, and are **left in place deliberately** as the record of why those two were late rather than edited away. The GitHub-automation path used for most changes here still cannot publish `refs/tags/*` — tagging remains a local step, and the backlog is what happens when no local clone is in the loop.

### Added

- **Chapter: [S05E02C03, "What Came Off the Ship"](src/seasons/s05/e02/s05e02c03.md)** — **the Last Stand, written.** `narrative-gaps-checklist.md` had it as the one genuinely unwritten item in the whole Tissadelle arc: every S6–7 chapter reaches it backwards as "Dock Seven", and the event had no chapter and no POV of its own. It has one now, in **Season 5** rather than "end of S5 / opening of S6" — 2831 Mid-Autumn, nineteen days before `s06e01c02`, appended to the existing episode with **no renumbering** and a freshly generated `comment_id`.
  - **What it commits the story to.** The sixteen-year chronometer discrepancy marked a real narrowed Interval, and a licensed commercial concern — the Hyperfold Yield Combine — located it and worked it industrially, holding a fold aperture open four months past its transit to take Class III boundary fauna for fold-transit stabiliser. **Industrial unauthorised folding is what tore the boundary**, not anything gestating in the dock. Shepherd convenes a **Field Triumvirate** by the clause in `star-rangers-command-hierarchy.md` and in the open, evacuates eleven crew in forty-one minutes by shuttle and chartered Farline courier, and stays with the *Sufficient Cause*.
  - **The strike is a Celestial enforcement action under Telearch delegation**, which `src/glossary/celestials.md` already covered verbatim (unauthorised Higher-Dimensional Folding breaching a Concordant boundary). Levrils are present only in canon-permitted roles — detection, containment, perimeter isolation, the `threnos-omega.md` precedent — and **at no point reach in**, which preserves the tier constraint stated as binding `canon_facts` in three published chapters and carrying the S7 climax. Every witness records **dragons**; nothing corrects the impression, and no Ranger present is told anything at all.
  - **The Rangers are not spared, they are not included.** Enforcement scopes to the unlawful process, so everything outside it is untouched without any finding about anyone's conduct. **The fauna are still filed Class III afterward** — "predatory, entropic, or ecologically disruptive", no Cascade standing, no protection — and the classification is not revised. Shepherd dies defending things the Archive calls a hazard, and nobody in the text says so.
  - **Five POVs — Shepherd, Syra, Wender, Rook-7, Tobble — and none of them can name what they saw.** Tobble's is the last, and is the first canon appearance of a Tobble viewpoint: not load-bearing at the moment of compression, and therefore the member left with structure to spend. Pronouns they/them, still unset.
  - Ends on the grey-then-gold morning that opens `s06e01c01`, so the two chapters meet at the seam without either changing.
- **Codex: ["Statement of Proximate Cause — Dock Seven"](src/codex/dock-seven-loss-adjustment.md)** — a Farline Courier Guild loss adjustment, and the only contemporaneous account of the action by somebody with no stake in the Corps' version. A commercial claims officer has no standing to adjudicate Cascade questions and no interest in acquiring any, which is exactly why the entry can write *dragons* in the box marked Proximate Cause and settle a bill around it. Carries **no `canon_facts`**, per the codex rule. Its live disagreement — two vessel masters hold they were let go; the third holds they were never taken up, and that being spared and being no part of the thing are different — is the decision above, argued by people who cannot settle it.

- **Lore: [The Institute](src/lore/the-institute.md)** — the secular, sceptical counterweight to devotional readings of boundary phenomena, and the source of the Dock Seven account most of settled space actually believes. **Not atheist, and deliberately not a strawman:** its published position is that no claim about meaning, intention or moral order has ever survived an instrument, and it *declines* the Cascade rather than denying it. Its house rule — **it publishes what it failed to find**, at the same length as the finding — is why the Survey Corps cites papers whose philosophy it rejects.
  - **Its reading of Dock Seven is correct about the mechanism.** Instability from overused, miscalibrated fold equipment held against an already-anomalous boundary is exactly what tore that boundary; the reported dragons are read as structured discharge along a strain gradient, seen through a viewport with no instrumentation. What the Institute does is take a correct account of a *cause* for a complete account of an *event* — which it states up front that it will always do.
  - **On dragons it says the Rangers are not lying, they are quoting** — dragon-talk in the Corps read as inherited devotional vocabulary carrying no observational content, since the Fellowship had the imagery first, the memorial repertoire was inherited rather than commissioned, and even *warden* was picked up from older guardianship traditions. What it grants is large: read as metaphor the tradition is very nearly exact, and the Institute teaches it in its own fold-transit safety courses, because a crew taught to imagine something enormous and patient behind a held aperture treats it with exactly the caution the physics warrants, faster than a crew taught the tensors. Filed as **superstition in form and wisdom in content**. Strong on the inheritance, which is documented; weak in the next step, treating an inherited word as evidence its referent is unreal.
  - **It attributes the Rangers' survival to superior protocols and practical boundary knowledge.** Careful, generous, entirely secular, and wrong: they were outside a perimeter drawn around a process rather than a party. **This is the account that established the Corps' reputation**, so that reputation rests on a version of their competence the instrument record does not support — while the competence itself is real and on the page four hours earlier.
- **[`star-rangers-safety-corps.md`](src/lore/star-rangers-safety-corps.md): "The Voluntary Standard"** — the Star Rangers are the **de facto regulator of boundary-proximate and fold-adjacent operations across settled space, and the regulation is entirely voluntary.** The entry had described only internal scope; everything it listed binds Rangers and, outside the Corps, binds nobody. **Local governments and corporations remain fully autonomous** — a world governs itself, a licensed concern answers to whoever issued the licence, and the Corps cannot fine, suspend, board, compel a filing or revoke anything.
  - **The mechanism was already in the file and had never been drawn out:** Underwriters Laboratories held no legal power over any manufacturer in 1894 either. It had independence and a method, insurers wanted evidence rather than assurances, and the market did the rest. Neutrality and self-funding are what make it work — a standards body with a commercial interest is a competitor with a rulebook.
  - **The Corps declines the word *regulator*, doctrinally rather than modestly**: accepting the title means accepting an authority the charter forbids, and it forbids it because the outer-station delegations of 2723 would not vote for a boundary body with compulsory power, having concluded it would be the MSC under better branding.
  - **What it costs was paid in 2831.** A voluntary standard is one the single operator who most needs it can decline, and the Combine declined it. Not an oversight in the design — the design, with the argument for it fully aired at founding. The Safety Corps has not proposed amending the charter, and its president's published reason runs to one sentence: *a corps that acquires the power to prevent this acquires, in the same motion, the power to permit it.*
- **Lore: [The Hyperfold Yield Combine](src/lore/hyperfold-yield-combine.md)** — the concern that worked the boundary, written so the uncomfortable thing about it is that **it was not a criminal enterprise**. Licence, counsel's opinion, audited product, a market that wanted more than it could supply, and every part of it lawful up to the moment it was destroyed.
  - **The loop is the point:** stabiliser makes apertures cheaper to hold, cheaper apertures make more boundary reachable, more boundary means more feedstock. Its own filings project the curve out four decades without remarking on it. Every part of the cycle closed except the aperture, which was the only thing in it supposed to.
  - **Three things made it untouchable, none of them a loophole** — the fauna have no Cascade standing so any argument on their behalf fails at the first line; the permit was silent on collapse and counsel's reading of that silence was *correct*; and **the only body competent to judge it had no power over it**, since it held no Safety Corps certification, was under no obligation to, and could not have been made to. What remained was a formal finding, which required a named officer to put her judgement on a second-guessable record, against a concern in good standing, on behalf of animals with none. For four months every authority had a reason not to look, and every reason was a good one.
  - **Two things left open, and neither is the expected one.** No insurer has corresponded at any address since — the ownership chain terminates in instruments that are lawful, opaque, and under no obligation, and since the destruction was not a crime anyone was charged with, no authority has had cause to compel an answer. And **the product shipped**: four months of industrial rendering, all of it sold, with where it went unestablished because a loss event generates no discovery. The licence framework was never amended, because the officer preparing the finding did not come back.
- **Codex: [A Material Account of the Dock Seven Event](src/codex/dock-seven-material-account.md)** — the Institute paper itself. Its **section 4 prints, unhedged, the four observations that undo its own section 3**, including that the losses sorted by *registry*, which no strain model can make a physical property. The strongest argument against the Institute is published by the Institute, which is the point.
- **Codex: [Shepherd Among the Comparanda](src/codex/shepherd-among-the-comparanda.md)** — the Arilon cross-reference, prepared and **withdrawn before submission**. Part One establishes the match on all five elements of the pattern and says outright it is the strongest comparandum this desk has ever assembled; Part Two is three people declining to send it, for three different reasons, recorded as a disagreement rather than smoothed into one voice.
  - **Cael:** element four is not satisfied, it is *assumed*. The pattern needs the namer absent when the record catches up — and a signal is still iterating at the failure point. So indexing the cross-reference would not record a match, it would **complete** one, handing the Council the finding Wender has refused for two years to sign, by a route the Council never had to take.
  - **Wyn:** the filing consoles, and that is the whole objection. A shape that fits perfectly, arrives as comfort, and cannot be checked is precisely what `s07e01c02` warns about — it would do in a comparative index what the citation does on a wall.
  - **Sen:** the comparative archive takes submissions from *named* correspondents, so the one correction this desk cannot make quietly is this one. Declines, and refuses to let a better reason stand in for the true one: not yet willing to pay what signing costs. **This is the story bible's "chose not to annotate" failure mode written as its own document** — and it is retained rather than destroyed, deliberately, because an absence in an archive is indistinguishable from a thought nobody had.
  - Marginalia gets the last word, and notices what the others didn't: the name is already at the top of the page.
- **Codex: [Address at the Official Funeral of Line Captain T. Shepherd](src/codex/wender-funeral-address.md)** — 2832 Mid-Autumn, eleven months after Dock Seven, just before `s07e01c01`. **Wender gives the eulogy having never signed the confirmed-loss citation, and says so from the podium.** The document grieves without once conceding the death it is held to mark, and refuses both consolations in the same breath — she will not say Shepherd is dead and will not say she is out there, having a finding for neither. Then holds both unresolved, and notes who taught her how. The ceremonial record omits the second half; three newsnets printed it whole, and it is the second half that gets quoted.

### Changed

- **Tobble uses he/him** (settled 2026-07-29), swept the same day across [`tissadelle-shepherd.md`](src/characters/tissadelle-shepherd.md) — which now states it outright, since being mis-recorded is his entire institutional situation — [`plural-minds.md`](src/glossary/plural-minds.md), `s05e02c03`, and the three story-bible notes that recorded the pronouns as unset. The they/them used before that date is **superseded, not an alternative**.
- **[`cosmic-cascade.md`](src/lore/cosmic-cascade.md): a "Who Accepts This Model" section** — the entry had stated the framework for its whole life without ever saying how widely it is held, which let it read as consensus. It is not: sceptical, contemplative and devotional traditions each decline or reorder it, and most people hold no position at all. Two consequences recorded — a cosmology with no virtue, reward or punishment **has nothing to console anybody with**, which limits its spread independently of whether it is right; and the existing Partial Observation rule cuts both ways, so disagreement here is not error to be corrected. A statement about *reception*, not about truth, so the canon/record split holds and lore stays internally consistent.
- **[`ballad-of-the-stars.md`](src/codex/ballad-of-the-stars.md): the second reading, filed 2832** — **and no retcon was required**, because the entry already had Tissadelle on vocals *pre-enlistment*. The surviving members of Slipwave reissue the recording with a reading attached: *she went to the stars* first meant she left the band for the Rangers, and now means both, **and they decline to choose**. They are equally explicit that the song foresaw nothing. Sen's second note draws the distinction that keeps this clear of the closure-prevention the later `baby-universe-ballad` is flagged for: a record with two entries in it is precise, not vague.
- **[`fellowship-of-light.md`](src/lore/fellowship-of-light.md): Dock Seven settled the neutral-broker argument** — rivals had put the case against Ranger neutrality and boundary competence on the record first, and the record went on to answer it. The sharpest detail is that **the Farline Guild's own claims file undercuts the Guild's own argument**: a vessel master came when a Ranger asked and negotiated the rate afterward, and the adjuster wrote it down against interest.
- **Three more `canon_facts` on [`s07e01c03`](src/seasons/s07/e01/s07e01c03.md)**, where the contested-record theme already lives: the Institute's material account is settled space's majority version; the Corps' reputation rests on an unsupported account of why its people survived; and the Cascade is not majority opinion. Prose untouched, as with the rest.
- **`canon_facts` added to four published S6–7 chapters; not one word of prose altered.** This is the expand-not-replace decision made explicit: the six chapters stay canon exactly as written and now carry the *why* that was never written under them. `git diff` on `src/seasons/s06` and `src/seasons/s07` shows front-matter insertions only, and no deletions.
  - [`s06e01c02`](src/seasons/s06/e01/s06e01c02.md) — the drift marked a real narrowed Interval that a commercial concern worked for four months before Survey Corps classification caught up; and **the closure also ended an unauthorised operation nobody had managed to stop**, which the confirmed-loss report buries along with everything else. That second fact makes the Oversight Council's hurry to close the file considerably more motivated, and more damning, without altering a line Wender says.
  - [`s06e01c01`](src/seasons/s06/e01/s06e01c01.md) — **the largest reframing in the set, done entirely in front matter.** What persisted did so on a substrate, the lost vessel's own archival stores; the Boirinn Uplands interior was **preserved by Tobble rather than generated by the stall, and only partly preserved.** The clearing stops being a floating dream and becomes a located thing.
  - [`s07e01c01`](src/seasons/s07/e01/s07e01c01.md) — the surviving witness record says dragons, nothing corrects it, and no Ranger authority ever established what standing the entities held: the same question the Union's scholars left open over Aoife's thorn well. Deliberately does **not** assert the Celestial basis here, so this chapter's in-universe contestedness survives intact.
  - [`s07e01c03`](src/seasons/s07/e01/s07e01c03.md) — a levril's inability to reach into a stalled seed is a **tier constraint, not a local circumstance**; what differs between Dock Seven and the warm edge is what was permitted, never what was possible.
- **[`celestials.md`](src/glossary/celestials.md): enforcement scope attaches to the process, not the persons** — bystanders are spared *structurally*, never assessed and found worthy, because a Celestial no more adjudicates worth than a Levril does. Makes explicit at the tier that actually acts what `levrils.md` already said as "enforcement tiers are structural, not moral". Closes with the observation that Material-layer witnesses routinely read the distinction backwards and nothing has ever been obliged to correct them.
- **[`metafold.md`](src/glossary/metafold.md): *hyperfold* recorded as commercial branding** — the same error sold rather than mis-reported, in an entry already categorised as a Misconception. The distinction kept is the useful one: the journalist writing *metafold* is guessing in good faith, while a concern that puts *hyperfold* on a hull has chosen a word that cannot be held to a meaning, and chose it on advice.
- **Recorded as decisions rather than left to look like oversights** — three deliberate non-changes, so a later pass doesn't "fix" them:
  - **No change to `overfold.md`, or to `metafold.md`'s Overfold definition.** Characters say *fell into the overfold*; the Archive says a boundary rupture. The **Register rule** in `canonical-glossary-and-migration-guide.md` already licenses exactly this — chapters and characters are voices, lore and glossary are the Archive — so there is **no new canonical term, no migration-map row, and no corpus sweep**.
  - **No change to `meta-dimensional-beings.md`.** The fauna stay Class III. The taxonomy not moving is the point.
  - **No change to `levrils.md`.** Its Operational Constraints already cover morality and directives, and `threnos-omega.md` already supplies the permitted enforcement roles; restating either in a second place is how this corpus drifts.
- **Story bible** — `tissadelle-arc-s6-7.md` gains a dated 29 July canon-direction section recording the direction verbatim, the seven rulings with their canon basis, and the **supersession of its own opening premise**: the "something has been quietly gestating there for eleven years, and it's being born" framing is not what happened. It also flags a precision trap for future sessions — the levril constraint is on *initiating this enforcement* and on *completing another mind's process*, **not** a blanket "levrils never act unbidden", which would contradict three published chapters. `narrative-gaps-checklist.md` ticks the Last Stand and answers the question that entry itself posed; `story-bible-summary.md`'s season spine is corrected to put the event in S5.
- **[`baby-universe-ballad.md`](src/codex/baby-universe-ballad.md)** — its "Last Stand at Dock Seven" link retargeted from `s06e01c02` (the aftermath) to `s05e02c03` (the event).
- **Season 5 index copy** — one added sentence each on `src/seasons/s05/index.md` and `src/seasons/s05/e02/index.md`. The chapter lists themselves are template loops and updated on their own.

### Fixed

- **The `church-space` registry comment under-listed its own thread** ([`lib/storyline-threads.js`](lib/storyline-threads.js)) — it enumerated the overlay's contents as "lore, codex entries, characters, and an author's FAQ" and omitted the tagged journal entry (`softening-the-real-cosmos-side`), which is excluded from every public build like the rest of the thread. Corrected, and **rewritten so it cannot go stale the same way again**: the list is now explicitly a shape rather than an inventory, with the authoritative query recorded in its place — every page in the thread carries the `church-space` tag except the two under `src/threads/church-space/`, which acquire membership from their `threadId` via `privateThreadForPage`. The one durable claim, that no chapters are among them, is kept.

## [1.8.0] - 2026-07-28

A cosmology-and-canon release, and the largest single sweep of Cascade terminology the project has done. Its spine is one recurring defect rather than a theme: **each of the 2026-07-26 Cascade settlements had been recorded where it was decided and never carried to the term on the other side of it.** Four instances turned up, and chasing them exposed two questions the corpus had been implying without answering — what crosses the Interval, and why physical law is reliable at all.

Also here: the Solar System Concordant becomes the **Solar System Concord**, retiring the last collision on that word; a new Season 1 chapter; and the first codex entry written to read a settled mechanism devotionally without contradicting it.

**Not yet tagged**, and neither is its predecessor — see the note under `[1.7.0]`. **`v1.7.0` and `v1.8.0` both need cutting from a local clone**, at each release's merge commit; the automation path used here cannot publish `refs/tags/*`.

### Added

- **Character: Sorcha** (`src/characters/sorcha.md`) — an itinerant, institutionally unaffiliated healer who works boundary-proximate settlements, carrying ordinary medicine in one half of her satchel and chalk, thread and a notebook in the other. **Constraint-literate in the same self-taught tradition as [Elvira](src/characters/elvira.md), with one difference: Elvira reads Etheric structure expressed through a *place*, Sorcha reads it as expressed through a *person*.** Three canon decisions worth recording: **(1)** she explicitly does **not** heal "the spirit" — Layer 1 is an origin condition without agency or intervention semantics, so nothing there can be treated; her work sits at the Conceptual and Etheric layers, and the page says so while allowing that the people she treats call it whatever they like (the register rule, applied). **(2)** Her reason for going toward pain is mechanical rather than sentimental: unresolved grief is a *feeding condition* for [Threnes and Gilded Saints](src/lore/meta-dimensional-beings.md), whose whole method is closure-prevention — so a closure practitioner is in direct functional opposition to a class of meta-dimensional predator. She does not know this, has no Survey Corps record, and needs neither to work. **(3)** No origin, training, homeworld or season placement is asserted; those are left open rather than invented. Portrait added at `src/images/characters/sorcha.jpg`.
- **Sorcha's weakness: central fatigue** — and the term is canon rather than a coinage. [Teleportation Constraints](src/lore/teleportation-limitations.md) already documents it as a consequence of repeated jump exposure, [Dr. Iona Vale](src/characters/dr-iona-vale.md) gates jumps on it (*"if the crew member shows unresolved central fatigue markers, the jump does not happen"*), and two Season 1 `canon_facts` establish that repeated exposure raises the markers and that Threshold Station adopted **mandatory screening** for them. Sorcha runs the same measurable deficit from a different cause — sustained constraint-literate perception of distressed people, with no off switch, because caring is not incidental to her method but the instrument itself. The sharp end is institutional rather than sentimental: **the protocol Iona Vale fought a room to establish covers Rangers, and Sorcha is not one** — the measure, thresholds and mandatory intake all exist, and the one practitioner whose whole method is a sustained draw on that reserve sits outside every one of them. Recorded as an open thread rather than a plot: her practice starves predators that feed on prevented closure, *and* spends down a reserve rest doesn't replace, so her most depleted hours are the ones she earns by working. Whether anything has ever waited for that is explicitly not established.

- **Chapter: [S01E03C04, "The One He Did Not Name"](src/seasons/s01/e03/s01e03c04.md)** — the morning after the grove. **Galahad Thorne met Elvira at Dún Rí in 2806 UCSD, sixteen years before any other record places her there, and she has not changed since.** He says nothing. He tests her instead, feeding her own private vocabulary back to her without attribution and watching whether she receives it as *familiar* or as *impressive*.
  - **It reaches back into an already-published line rather than adding one.** At their first causeway meeting (E02C01) Galahad says *"the layer underneath — the pattern substrate,"* and Elvira's look is written as *"Where did you learn that language"*; the next line is *"He chose not to answer the implied question."* That phrase is now hers, given to him in 2806 — so the moment she revised her model of him upward, she was being impressed by her own words. Not one word of E02C01 changed.
  - **Built to resolve nothing.** His memory is the only source: no image, instrument reading or third-party record corroborates it, he enters none of it in the survey file and states no reason, and whether Elvira remembers is explicitly not established. It is the first hard data point on the standing "What is Elvira?" thread and it deliberately stays a sample size of one.
  - **Answers E03C03's closing note without answering it.** Elvira ends that chapter writing *"That should have been harder. Find out why it wasn't."* Galahad has a reason, and it is unfalsifiable, unfileable and his alone.
  - **Inverts the season's own mechanic.** Season 1 turns on naming a thing correctly (E03C02, "The Right Question"); this closes it on a name deliberately withheld. Aldera sees the whole test, reconstructs the method, and files it unspoken under the same volunteered-information protocol that made her useful two chapters earlier — noting only, and without recommendation, that the last time everyone in the sector waited to be asked it cost three years.

- **Glossary entry: [Council of Harmonarchs](src/glossary/council-of-harmonarchs.md)** — the composition the 2026-07-26 settlement made load-bearing had no reader-facing page. Worldwright, Telearch, Dynarch, Celestials, High Celestials, Concordant Principal and Universal Authority all had one; the Council, which is *what a Universal Authority is made of*, existed only inside the canonical glossary and other entries. The tell was `universal-authority.md`, which linked **Consensus Field** to the whole canonical-glossary page with no anchor, because there was nowhere else to send a reader.
  - Covers the four things that only lived in the reference doc: the Council is **not a Cascade tier** (the same minds in their second seat, which is why it appears nowhere in the spine); the **Council is not the Authority** (Authority = Council + presiding seat, and neither half is an Authority alone); decisions come through a **Consensus Field** rather than a vote count; and **two seats** — a Principal's writ ends at its zone, the Council's is universe-wide, and a Harmonarch outvoted in council is bound at home regardless. Keeps the canonical glossary's own comparison to the Star Rangers' **Council of High Captains** (`star-rangers-command-hierarchy.md`), noting the resemblance is structural rather than borrowed.
  - Cross-linked from `universal-authority.md` and `concordant-principal.md`; the canonical glossary's Council entry now points at it. **Lead Harmonarch** and **Founder** are covered inside it rather than given pages of their own — both are seats rather than tiers.

- **Redirect at the old Concordant URL** — `/lore/solar-system-concordant/` now points at `/lore/solar-system-concord/` by two mechanisms, because no single one covers every host this repo deploys to. A **`Redirect 301`** in [`src/static/.htaccess`](src/static/.htaccess) serves the cPanel production domains; mod_alias resolves it before the filesystem lookup, so it wins wherever Apache is in front. A **meta-refresh stub** at [`src/lore/solar-system-concordant.njk`](src/lore/solar-system-concordant.njk) covers GitHub Pages and anything else that ignores `.htaccess`, and carries `rel="canonical"` plus `noindex, follow`.
  - The stub is a `.njk` rather than a `.md` on purpose, and that decides three things at once: `validate-content.js` scans only Markdown, so it isn't schema-checked as a lore entry; the `lore` collection filters on `layout === "lore-entry.njk"`, so it never appears in a listing; and it carries `eleventyExcludeFromCollections: true`, which keeps it out of `sitemap.njk`'s `collections.all` loop — a redirect has no business in a sitemap. It also has no `data-pagefind-body`, so Pagefind skips it (401 of 405 built pages indexed).
  - The stub's links are written with the ordinary hardcoded `/star-rangers/` prefix rather than being special-cased, so both prefix mechanisms handle it unchanged: GitHub Pages serves it as written, and `cpanel-deploy.sh`'s `sed` step rewrites it to `/lore/solar-system-concord/` along with every other page.

### Changed

- **Codex: [The Reading Before a First Vigil](src/codex/the-reading-before-a-first-vigil.md)** — the devotional half of the law's grounding, filed where a devotional reading belongs. A Fellowship of Light address read aloud to a novice on the night of their first watch at a thin place, answering the question every novice has and none asks: whether it will hold. Its answer is that it holds because **the Exarchs keep faith** — the same invariance `dynarch.md` grounds in design, read as fidelity.
  - **Written so it cannot contradict the lore, because it declines to try.** The reading names the surveyors' account outright — that an Exarch is *made* so, that nothing is being chosen and therefore nothing is being kept — instructs the novice not to argue with it, and answers only: *a thing made faithful is faithful.* Same fact, different reading, no competing claim. That is the lore/codex boundary working rather than being managed.
  - **In the Fellowship's own register**, which the corpus already established: **Exarch** for Dynarch (recorded as "the older form still preferred in devotional and provincial records") and **Overfold** for boundary-zone Etheric phenomena. It engages with none of the mechanism — no bands, no Interval, no Telearch line — which is authentic to a contemplative order rather than an omission.
  - **Author is `Traditional — no chapter claims first authorship`**, following the precedent of [Hymn of the Thin Places](src/codex/hymn-of-the-thin-places.md); the Fellowship's cross-chapter oral tradition is itself the stance, and no new named figure or chapter house was invented to carry it. Deliberately **not** attributed to the Communion of the Called, which is a `church-space`-tagged page — an entry authored from there would inherit the overlay's privacy and be hidden on every public domain.
  - No `canon_facts`, per the codex rule. **Title card still to do** — `make-codex-cover.ps1` is Windows-only, so the entry ships imageless for now, which several existing codex entries already do.

- **Why physical law is predictable: Dynarchs are stable by design** (settled 2026-07-28) — answering a question the corpus had already half-asked. `physics-comparison.md` has said for some time that "causal structure is **maintained** rather than merely emergent," which implies a maintainer and never named one. It names the Dynarchs now, and grounds the reliability in **design rather than in conduct**: the Worldwright designs the rule-sets *and* the office that keeps them, invariance included. A Dynarch is built to be invariant in the same sense a constant is.
  - **The proxy structure leaves no room for it to be otherwise.** Continuity is what a Dynarch substantially *is*, so there is no separate faculty of reliability that could fail on its own while the rest of the office carried on. **Drift is not forbidden to a Dynarch; it is undefined for one.** That is what keeps the claim from making physics contingent on anybody's will.
  - **The measurable consequence is no measurable consequence, and that is the design working.** Physics inside a universe is indistinguishable from physics in a universe with no Dynarchs at all: constants measure as constant, experiments repeat, nothing in a laboratory result points upward and nothing needs to. The maintained case and the merely-emergent case are experimentally identical, which is why the distinction is cosmological rather than experimental.
  - **Boundary anomalies are not counterexamples**, and the corpus already had the sentence for it: boundary zones produce *lawful disagreement, not law failure*. Threshold's forty-second discrepancy has stood unexplained for eleven years without once implying a force did anything other than what it does.
  - Written into [`dynarch.md`](src/glossary/dynarch.md) and [`physics-comparison.md`](src/lore/physics-comparison.md). **A devotional reading of the same invariance — as character and integrity rather than design — is deliberately not filed here**, per the lore/codex boundary: it is a named source's account, and it awaits its source.

- **What crosses the Interval, generalized — and why it is Telearch business** (settled 2026-07-28). Gravity's reach across a narrowed Interval was inferable from three separate pages and stated on none. It is now one rule rather than three exceptions: **a thing crosses the Interval exactly to the extent that a band of the Common Manifold carries it.** The manifold has three bands and each carries one thing — **12–15** geometric strain (**gravity**: Membrane Shadows, transient gravity tunnels), **16–19** causal timing (timebase disagreement), **20+** Etheric archetypal templates (Resonance, Intermembrane Bleed). Everything else is membrane-confined: matter, ships, beings, and the other force-domains — **electromagnetics and Nature have no band and therefore no far-side reach**. This is why "nothing crosses the gap" and "the far side's mass is measurable here" are both true of the same event.
  - **Gravity is not a special case; it is the visible one.** Gravity is geometry and the 12–15 band is the geometric one, so gravity is the single force whose nature matches what the between is made of. Neither "emergent per membrane" (which the Membrane Shadow forbids) nor "purely higher-dimensional" (which ignores its ordinary local behaviour — Lagrange points, high-gravity worlds, ascent javelins).
  - **The governance has the same shape as the physics, and not by coincidence.** Every force-domain divides into a **local** aspect, governed by the zone's Concordant Principal, and a **continuity** aspect that runs up the Telearch line — because continuity across membranes is a trans-membrane fact and the Telearch branch, native to no universe, is the only tier that is itself trans-membrane. The 20+ band already encodes "the nonlocal boundary conditions that Worldwright and Telearch operations must satisfy," so the continuity branch was already documented as operating where the between is. **A Dynarch's dual obligation is that division restated from the physics side**, and it is why continuity prevails on conflict: the local aspect is the one that stops at the edge.
  - Written into [`concordant-membranes.md`](src/lore/concordant-membranes.md) (new section, *What Crosses the Interval, and Why*), with the rule cross-stated in [`post-eleven-dimensional-manifold.md`](src/lore/post-eleven-dimensional-manifold.md), [`dynarch.md`](src/glossary/dynarch.md), [`telearch.md`](src/glossary/telearch.md) and the canonical glossary's Dynarchs entry.

- **Noögenic descent is an axis, not a category** (settled 2026-07-28) — closing a gap the taxonomy had carried silently. **Primary / secondary / derived classifies a universe by how it was *made*; noögenic descent describes who it came *from*.** A seeded universe is neither primary (no Worldwright) nor derived (it does not arise from the interaction of primaries), and it is not a fourth entry on that list, because it is not answering that list's question. Previously it was defined only negatively, by the one line saying it receives no Worldwright.
  - **Both ends of the relation are free.** A parent universe may itself be **primary, secondary, or derived** — seeding requires a mind capable of it, not a pedigree — and a matured noögenic universe's own standing is not set by its parent's.
  - **Maturity is the entire difference from *derived*.** A derived universe is permanently subordinate: its physics stays a function of its source primaries' constraints and it never stops running on inherited authority. A protouniverse is subordinate only *while unfinished*, and parent Telearch jurisdiction ends the moment it stabilizes a Universal Authority of its own. **What it becomes is the adult child of its parent** — grown, independent, answerable in nothing, with its own Council, its own founder presiding, its own continuity to keep. The kinship is permanent and carries no authority; the authority was temporary and is finished. *Proto* is a stage, not a class: it never acquires a Worldwright and never needed one, because what it was owed was a start, not an author.
  - Recorded in [`universe-overlap-and-folding.md`](src/lore/universe-overlap-and-folding.md), [`noogenic-protouniverse.md`](src/glossary/noogenic-protouniverse.md) and a new canonical-terms entry. Two pieces of superseded phrasing went with it: the protouniverse entry's "generate its own governing intelligences" (the mind-for-body error, same as above) and its "Concordant-equivalent authority", which the migration map had already retired in favour of *independent Universal Authority*.

- **A Universal Authority is a governance *body*, not a governance *intelligence*** — wording only; the 2026-07-26 composition is unchanged. That settlement made the Authority "a **body**, not a separate mind installed above the zones," and never reached the definition sentence four lines above it, which still opened *"the native governance **intelligence** of a universe as a whole."* `universal-authority.md` then closed by saying the term "names the universe-level governing **mind**" — the same page denying it is a mind and asserting it is one.
  - **This was not a judgement call:** the canonical glossary already carries a migration row retiring *"'Universal Authority' as a singular mind above the Principals"* in favour of *"the Council of Harmonarchs plus its presiding seat."* The term's own definition was on the wrong side of its own migration rule.
  - Corrected in [`universal-authority.md`](src/glossary/universal-authority.md) (short, lead, legacy note), [`who-governs-a-universe.md`](src/lore/who-governs-a-universe.md), the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md)'s Role Clarification and Definition, and the migration row that repeated "governance intelligences". `concordant.md`'s legacy note now says "governance office" rather than "governance minds."
  - **Keeps the asymmetry visible, because it is the point:** a Concordant Principal *is* a mind; the Authority those minds constitute is not a larger mind but a council. Both revised pages now say so in as many words.

- **The Telearch↔Dynarch relationship is recorded from the Telearch side too** — the 2026-07-26 settlement ("a Dynarch is a proxy Telearch") landed on the Dynarch page and in `who-governs-a-universe.md`, and never crossed over. `telearch.md` still carried the *pre*-settlement framing — co-created, stands under both, continuity prevails — reproduced as three bare assertions on the page best placed to explain them, and never said a Dynarch **is** a proxy Telearch at all.
  - Sharpest form of the gap: `telearch.md` already documented how a Telearch reaches inside a universe *by Avatar* — the occasional, personal route — while omitting the Dynarch tier, the **standing, structural** one. New section, *How a Telearch Reaches Inside a Universe*, gives both and says why there are two: a Telearch is native to no universe, and a force must be administered from within the thing it runs through.
  - The canonical glossary's **Telearchs** entry was two lines (a one-sentence definition and a protouniverse note) against a full `Nature (settled 2026-07-26)` block on the Dynarch side. It now records the continuity branch and Worldwright peerage, native-to-no-universe, and both routes inside.

- **"A membrane is a universe" tightened to "a membrane is universe-scale"** — phrasing only; the 2026-07-26 settlement is unchanged. That sentence stated an *identity* where it meant a *scale*, and sat awkwardly beside the definition two lines above it, which has a universe **occupying** its membrane. A thing cannot both be X and occupy X, and the corpus leans on the occupancy reading elsewhere — `membrane-shadows.md` has a mass staying "within **its own** membrane," which only parses if a membrane is an envelope. Corrected in [`concordant-membranes.md`](src/lore/concordant-membranes.md), the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md)'s scale rule, and the migration-map row that repeated it.
  - **Also records what the two words are actually for**, which is the question the wording invited: *universe* and *membrane* name the same object and differ in which way the sentence faces. **Universe** is the thing from inside — its contents and its governing authority. **Membrane** is that same object as the Common Manifold sees it: bounded, with neighbours and an Interval between. Inside-facing statements take the first, anything measured across takes the second, and that is why a Concordant Zone sits inside a *universe* while a bleed event happens between *membranes*.
  - No migration-map row, because nothing is renamed and no legacy usage becomes wrong. The `[1.7.0]` entry below keeps its original "A membrane is a universe, not a zone" heading — that is the record of what was settled that day, and released entries are not rewritten.

- **Concordant Zone gains two synonyms with declared registers** (settled 2026-07-28) — **coherent cosmic region**, the Survey Corps' plain-language gloss for first mention and cross-species records, exact and clumsy in prose; and **megaexpanse**, the popular word, recorded together with why it is imprecise: it names the *size* and drops the *internal consistency*, which is the whole content of the term. A megaexpanse is merely enormous. Neither replaces the canonical term, and the Archive's own voice keeps using it. Follows the existing Harmonarch/Exarch pattern, where a synonym earns its place by carrying a register rather than by being interchangeable. Recorded in [`glossary/concordant.md`](src/glossary/concordant.md) and the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md), with a migration row that applies **only** to Archive voice — both words stand in dialogue, popular sources, and first-mention glosses.

- **The Solar System Concordant is now the Solar System Concord** (settled 2026-07-28) — the last collision on the word *Concordant*, and the one the July sweep left standing. That sweep moved the governance sense to **Universal Authority** and left the compact holding a borrowed cosmological word, managed by a three-senses disambiguation in [`glossary/concordant.md`](src/glossary/concordant.md) and two sections of the entry itself. Managing an ambiguity in prose forever is what the canonical glossary exists to stop, so the name went instead. **`Concordant` now has exactly one live sense: the cosmological zone.**
  - **Done as an in-universe restyling, not a retcon.** The compact was founded in 2543 UCSD *as* the Solar System Concordant — the overreach was the point, a body of law with no enforcement mechanism naming itself after the largest structure in the sky — and was restyled **the Solar System Concord in 2790 UCSD**, keeping the root and giving the word back. Nothing about the institution changed: no clause, no commission, no signatory. The founding story survives intact, which is what the alternative (pretending it was always called Concord) would have cost. New section: *The Restyling of 2790*.
  - **The old name is now a date stamp rather than an error.** Pre-2790 documents carry it correctly and are quoted as written — the Star Rangers' 2723 charter preamble in [`formation-of-star-rangers.md`](src/lore/formation-of-star-rangers.md) still reads *"the limits of Solar System Concordant coherence"*, and is the one deliberate instance left in the corpus. Post-2790 usage is the tell of a document retyped from an older source without attention.
  - **Swept the corpus, not just the entry.** 24 files: the era (*the Concordant period* → **the Concord period**), the adjectival forms (*Concordant-wide*, *Concordant standards*, *Concordant baseline*, *Concordant-certified*, *Concordant personhood standards*), the civilizational sense (*"in the Concordant"*, *"the Concordant's engineers"*), and the in-universe institution names (**Concord Governance Archive**, the document header on two codex entries). Chapter prose needed no changes — every season-file use of the word is cosmological (*Concordant phase oscillation*), which is the result the rename was aiming at.
  - **Page moved** from `/lore/solar-system-concordant/` to [`/lore/solar-system-concord/`](src/lore/solar-system-concord.md). Since giscus maps non-chapter pages by pathname, any existing discussion thread on the old URL will not follow the page.
  - Canonical-terms entry and two migration-map rows added to the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md); the superseded ruling in `story-bible/canon-consistency-audit-2026-07.md` corrected in place rather than left to go stale.

### Fixed

- **Elvira's Dún Rí prequel was dated four years too early in two places** — `src/characters/elvira.md` said Season 1 Episode 0 covered Dún Rí *"roughly four years before her arrival at the outpost,"* and `s01e00c01.md`'s description said *"Years before Elvira ever reaches the Marsh Causeway."* E00 is Year -4, Late Autumn; she reaches the causeway Year -3, Early Spring — about **six months**. Someone read "Year -4" as "four years before the outpost" rather than four years before Year 0. Both now say what the timestamps say. Found while checking whether a twenty-year-old meeting fit her established chronology; it does, but not for the reason the character page implied.

- **Misdirected cross-link in [The Crisis Centuries](src/lore/crisis-centuries-terran-wildlife.md)** — the "now credited" link on *the standing authority* keeping Earth's unmanaged biomes healthy pointed at [The Celtic Union of Planets](src/lore/celtic-union-of-planets.md), which is not an Earth environmental authority and has explicitly declined SSDC jurisdiction. The authority in that clause is the **Solar System Defence Command**, so the link now goes to [Military Space Command](src/lore/military-space-command.md), the page documenting the MSC's narrowing into the SSDC and the established anchor for it elsewhere in the corpus. The link resolved and so passed both `npm test` and the internal-link checker — a stale *target* under valid link text, the same failure mode `check-related-terms.js` exists to catch on `related:` lists. The likely origin is the sentence's echo: the Celtic Union entry states the same "healthier than at any point since the early industrial era" claim, and the link appears to have attached to the echo rather than to the institution. The Celtic Union remains linked from the page's See-also list, where it belongs.

## [1.7.0] - 2026-07-26

A cosmology release. Four structural questions about the Cascade were settled in one sitting — what a Universal Authority is made of, what a Dynarch actually is, what scale a membrane is, and which of two mechanisms "boundary zone" names — and in three of the four the answer resolved a contradiction the corpus was already carrying rather than adding new ground.

**Not yet tagged.** `v1.7.0` needs cutting from a local clone at the merge commit for this release; the automation path used here cannot publish `refs/tags/*`.

### Changed

- **A membrane is a universe, not a zone** (settled 2026-07-26) — resolving a contradiction the corpus was already carrying. `concordant-membranes.md` said "each **Concordant** occupies its own bounded cosmic membrane", while `tir-tairngire.md` describes "a **primary universe** whose membrane sits close enough to Tír na nÓg's own Concordant" — comparing a universe's membrane to a zone's in one sentence. The scale is now fixed: **a membrane is a primary universe**, a Concordant Zone is a region *inside* one, and **a single Worldwright authors many primary membranes** (consistent with "Worldwrights create primary universe *sets*", already canonical).
  - **The structure nests, and that decides which effects can happen where.** The **Interval** exists only *between* membranes. Two zones inside one universe share an ordinary internal edge with no Interval across it — so an internal zone edge produces instrument drift, timebase mismatch and lawful disagreement, but **never** Intermembrane Bleed, a Membrane Shadow, or a transient gravity tunnel, however sharply the two rule-sets differ. All Boundary-State Class grading belongs to the inter-membrane case.
  - **"Boundary zone" now covers two mechanisms** and is qualified where the difference is load-bearing: *intra-universe* (an internal edge) versus *inter-membrane* (a narrowing Interval between whole universes). Both `boundary-zones.md` and `boundary-zone.md` say so directly, since the unqualified term previously implied one phenomenon.
  - **Threshold Station is on an inter-universe boundary.** Its Dock Seven record includes a bleed signature, which under the nested model is only available between membranes — so Threshold stands at an Interval, not at an internal edge between two zones of its own universe. Stated explicitly in `concordant-membranes.md` rather than left to inference, since Season 1 and several codex entries lean on that location.
  - Swept across `concordant-membranes.md`, `membrane-shadows.md`, `intermembrane-bleed.md`, `membrane-shadow.md`, `interval.md`, `boundary-zone.md`, `boundary-zones.md` and the canonical glossary, with two migration-map rows — one for the scale correction, one for the ambiguous "boundary zone".

- **A Dynarch is a proxy Telearch** (settled 2026-07-26) — not merely *aligned* with the Telearch line, as the entries previously had it, but the Telearch line acting **inside** a universe, scoped to a single force-domain. Telearchs are native to no universe and supervise from outside; a force has to be administered from within the thing it runs through, and the Dynarch tier is how that reach is made. Recorded in [`dynarch.md`](src/glossary/dynarch.md), [`cosmic-cascade.md`](src/lore/cosmic-cascade.md), [`who-governs-a-universe.md`](src/lore/who-governs-a-universe.md) and the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md).
  - **Turns three asserted properties into consequences.** *Continuity prevails on conflict* — previously a bare arbitration rule — now has a reason: continuity is what a Dynarch substantially **is**, and a proxy cannot rule against the office it proxies for. *Co-created* stops meaning "two parents of equal kind" and becomes **substance from the Telearch line, jurisdiction from the Principal line**. And the *dual obligation* stops reading as divided loyalty: a proxy answering to the local office for local matters and to its own principal for everything else is an ordinary arrangement.
  - **Explains the legacy confusion rather than just correcting it.** Older records filed Dynarchs with the Telearchs — the ambiguity that retired "higher celestials" and "High Celestials" both. Those records had noticed something true and lacked the word for which part was true.
  - **Explicitly not an avatar.** The Cascade's other instrument for acting from inside crosses a temporal register, appears at Celestial level or below, and is a separate limited person immanent in Material time. A Dynarch stays in conceptual time above the Celestials and is a standing office, not a person sent down — two answers to one problem, at different depths.

- **A Universal Authority's composition, settled** — it is **not** a separate mind installed above a universe's zones. An Authority is a **body**: the **Council of Harmonarchs** (every Concordant Principal in the universe, sitting together) plus a presiding seat held by the universe's **founder** — its originating **seed mind**, where the universe was raised by noögenic seeding — or, where there is no founder, by a **lead Harmonarch** drawn from the Council itself. Same office either way; only the occupant's origin differs. Recorded in [`universal-authority.md`](src/glossary/universal-authority.md), [`concordant-principal.md`](src/glossary/concordant-principal.md), [`who-governs-a-universe.md`](src/lore/who-governs-a-universe.md), and the [canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md), with new canonical-term entries for **Council of Harmonarchs**, **Lead Harmonarch**, and **Founder**.
  - **Resolves the "singular or plural mind" ambiguity** the record has carried since the Concordant/Universal Authority split. Both phrasings describe one institution: a Consensus Field is *how the Council decides*, a founder is *who presides while it does*, and a plural formation descended from a seed mind (a Synarch Mind among them) takes the presiding seat as one member rather than as a second council.
  - **Reframes "Principals answer to the Authority"**, which now needs care — a Principal answers to the council it *sits in*, not to a superior being. The Council's writ is universe-wide where a Principal's ends at its zone boundary, and a Harmonarch outvoted in council is bound by the decision inside its own zone regardless. Every Harmonarch holds two seats. Migration-map row added, since the old phrasing reads as a fourth tier.
  - **Explains the protouniverse rule from the inside** rather than by assertion. A Noögenic Protouniverse is *proto* because it has no stabilized Concordants, hence no Harmonarchs, hence no Council: **a founder without a Council is not an Authority.** This is why a seeded universe doesn't inherit governance the day its seed mind lands, and it aligns with `noogenic-seeding-system.md`'s existing rule that jurisdiction passes only once the seed mind "matures into a Universal Authority in its own right."
- **Threnos-Ω's "Council of Archons" identified** ([`threnos-omega.md`](src/lore/universes/threnos-omega.md)) — an unregistered term that turns out to be that universe's own word for its Council of Harmonarchs, its Static Synod being that Council in a near-terminal-CEI state. Deliberately **not** a deprecation: separately-authored universes aren't required to share vocabulary, so *Archon* stays in Threnos-Ω's records and is simply mapped. The migration row says to read it as *Harmonarch* and not to carry it outward — elsewhere in the corpus the word names nothing in the Cascade.

### Fixed

- **Telearch glossary URL misspelling** — the entry's title has always read "Telearch", but its filename and `id` were `telerarch` (transposed letters), so the page shipped at `/glossary/telerarch/`. Nothing was broken — all 12 inbound links used the misspelling consistently, and `related:` terms resolve by *title* rather than slug, so they were unaffected — but the typo was visible to any reader looking at the address bar. Renamed to `src/glossary/telearch.md` with `id: telearch`, and swept every reference in `src/` and `story-bible/`. **This changes the page's URL**, which for a non-chapter page also changes its giscus discussion mapping (comments map by pathname) — **confirmed safe here: the page had no existing comments**, so no thread was orphaned. Contrast the Journal URLs, which were deliberately *not* re-parented when `/story-engine/` was introduced, because there the threads were known to exist. Same rule, opposite call, decided on whether a discussion was actually there.

## [1.6.0] - 2026-07-26

First release cut deliberately, rather than reconstructed after the fact. Versions 1.0.0–1.5.0 were written retroactively in one sitting on 2026-07-09; `package.json` went straight from 1.0.0 to 1.5.0 in a single commit, so 1.1.0–1.4.0 never existed as states of the repository. See the note under 1.5.0.

**Tagged** — `v1.6.0` at this release, plus `v1.0.0` and `v1.5.0` cut retroactively at `a0ca09a` and `45f72f3`, the only two historical versions with a real commit behind them. Tags are pushed from a local clone; the automation path used for these releases cannot publish `refs/tags/*`.

### Added

- **A fourth canon status: Chartered** (settled 2026-07-26) — for approved derivative work by another hand, above a fan work and below canon, a gap the three existing statuses left open. One rule governs it: **canon binds it; it does not bind canon.** It must be consistent with canon as it stood at approval; when later canon contradicts it, the chartered work is *superseded* rather than retconned or withdrawn; the main story may never depend on one; and it carries **no `canon_facts`**, for the same reason the Codex doesn't — it establishes nothing. What separates the four statuses is who wrote the thing and where they stand: Codex is in-universe authorship, Overlay is a lens across the same events, Chartered is new events by another hand, out-of-universe and endorsed. **None exist yet** — the policy is written in advance so the first one doesn't have to invent it. Recorded in `story-bible-summary.md`'s canon-status table, `CLAUDE.md`, and `CONTENT-LICENSE.md`.
- **Chartered works are registered, not hosted** — the repo will list an approved work and point at it; the work stays with its own author. This is a rights boundary rather than a filing preference: `CONTENT-LICENSE.md` scopes CC BY-NC-ND over `src/` as Dermot's content against an MIT engine, and a third party's work has a different rights holder, so filing it under `src/` would put content inside a licence scope he cannot grant. The register becomes a published page when a first chartered work exists, and is deliberately not created empty.
- **A referencing rule for fan images and music: "referenced, not endorsed"** — fan-made images and music may be linked from the site where appropriate. The formula exists to resolve a real collision: the Fan Works Policy requires a fan work not imply Dermot's endorsement, and an official-site link is on its face a mild endorsement. The neutral wording settles it without amending the licence — a link is a pointer, not approval, and referencing something never confers chartered status. The alternative considered and not taken, "featured with permission", is genuine endorsement and would require a carve-out in the Fan Works Policy; it remains available as a deliberate licence change rather than a wording tweak. Documented alongside the three-axis split (licence / canon force / visibility) that keeps these decisions from being conflated.
- **Lore entry: "New London Space Habitat"** (`src/lore/new-london-space-habitat.md`) — three 1.5 km Stanford torus rings (Westminster, City, Crown) at **Earth–Moon L5**, built in the 2380s UCSD as a refuge for British cultural heritage after the Crisis Centuries' coastal fragmentation events, joined to a non-rotating hub ("The Spire") and threaded by the Thames Canal. Roughly 180,000 residents under a constitutional monarchy. Three canon interactions resolved rather than glossed: **(1)** it is explicitly *not* a member of the [Orbital Habitats Compact](src/lore/orbital-habitats-compact.md), which preserves that entry's hard "five self-governing habitats" figure and is what makes its ties to Eden genuinely diplomatic rather than intra-jurisdictional — the largest inhabited body in Earth orbit no Compact officer has jurisdiction over. **(2)** [Lagrange Points and Fold-Space Transit](src/lore/lagrange-fold-points.md) makes L4/L5 of any planet–moon system a **Tier 1 designated fold-staging zone**, so the designation (post-2712 UCSD) arrived around a city that had held station there for three centuries — a sovereign polity outside the Compact sitting on Survey Corps/SSDC charter infrastructure with no shared chain to arbitrate through. **(3)** Because [the Crisis Centuries](src/lore/crisis-centuries-terran-wildlife.md) records Earth's recovery under SSDC stewardship, the habitat's founding rationale is written as *expired*: it preserves a heritage at a remove rather than against extinction, an unresolved argument in its own politics. Closes with the deliberate contrast against the [Celtic Union](src/lore/celtic-union-of-planets.md)'s Currach Fleet — two opposite answers to the same fear, both still running — and both archives still printing CE beside UCSD for the same reason. Rotation figures check out: 1.5 km diameter gives 1.09 rpm and 86 m/s rim speed for 1 g, under the vestibular-comfort threshold.
- **`scripts/check-related-terms.js`** — a local authoring check (like `check-internal-links.js`; not wired into `npm test` or CI, exits non-zero so it can be) that verifies every front-matter `related:` term matches a real page title. This covers a gap all three existing checks structurally miss: `glossaryUrl` in `.eleventy.js` resolves a related term against page *titles* and falls back to `/glossary/` on a miss rather than erroring, so a stale term still renders a perfectly valid link to the wrong place — schema validation passes (related terms are free text), the Eleventy build passes, and the link checker passes (the emitted URL is real). Retitling a single lore or glossary page silently degrades every `related:` list naming it, which is exactly what the Common Manifold rename could have done. Matching is exact — case, punctuation and leading articles included — so misses print near-miss suggestions. Also warns, without failing, on duplicated page titles where only the first is reachable; it currently reports two long-standing glossary/lore title pairs (`FTL Mechanics`, `Allocation Units of Cognition (AUC)`), left as they are since the glossary entry winning is the sensible resolution.

### Fixed

- **`npm test` restored on `main`** — `scripts/validate-content.js` declared `privateThreadForPage` twice after the private-thread-boundary merge, so schema validation crashed before checking a single file. Collapsed to one import.
- **Canon errors inherited from Copilot-era edits** — a targeted pass over the six files whose last commit was the Copilot bot found and fixed: United Space Industry Standards described as holding its independence "through the Coherence Wars" 163 years *before* those wars began (now the 2130s–40s CE climate-crisis decades, immediately preceding the USC's founding); a claim that the UCSD calendar is anchored to the USC's founding, flatly contradicted by [Universal Cosmic Stardate](src/lore/universal-cosmic-stardate.md)'s own `UCSD = CE + 200` rule; the Three-Era Framework presented as USC-era scholarship analysing the 29th century — its own future — and shaping a 2340 charter with the result (reframed as historiography maturing across eras, with the SSSA anticipating the conclusion in practice); an undefined "Phase A/B" periodization; and a fourth-wall leak ("not canon-valid") in the in-universe [FTL Mechanics](src/glossary/ftl-mechanics.md) entry. Mixed bare CE/UCSD years across the pre-USC timeline cluster are now labelled per the CE+200 rule.
- **MSC lifespan contradiction** — [Military Space Command](src/lore/military-space-command.md) and [Jehu Among the Comparanda](src/codex/jehu-among-the-comparanda.md) both put the MSC's span at "approximately two centuries", against a dated record fixing it at ~114 years (Imperium ends 2609; charter 2723) and against [the Imperium](src/lore/the-imperium.md)'s own "a century after". Both now read "a little over a century" — which strengthens the Jehu comparison, that span being almost exactly the four generations the scriptural captain's house was granted.
- **`src/images/codex/cosmic-limitation-on-evil.jpg` rebuilt** — was a stray blurry stock photo breaking the designed-cover template every other codex entry uses. Regenerated as a proper title card (a ring dissolving round its own circumference; `CONTESTED` stamp taken from the paper's own filed status) using the new cover generator, so the title renders in real type rather than generated lettering.

- **Four mismatched character portraits removed** — a portrait audit found Naomi Kestrel, Rosalind Vey, and Tamsin Reyes (all written as women) rendered with male stock portraits, two of them pixel-identical duplicates of Bertram Ashcombe's and Kai Larsen's own images, and Sethka Ru (an alien Serephine Dunekin) shown as an ordinary human. Dropped all four `image`/`image_alt` front-matter fields and deleted the wrong image files rather than leave inaccurate art in place; `character.njk` already renders cleanly with no portrait (five other characters have none).
- **`src/images/lore/trigrian.jpg` re-graded toward an alien palette** — the site's own copy of "Three Suns over the Mara" (distinct from the source photo it derives from) was an unmodified Earth sunset standing in for an alien planet's sky. Applied a global hue/saturation shift plus distinctly-toned halos on the three suns matching the lore page's own trinary description (a tight orange pair, a paler wide-orbit companion).

### Changed

- **"Etheric" split into three canonical terms** — the word had been carrying three jobs at once: the Layer 3 causal mode, the seven compactified spatial dimensions of the home universe's 3+7+1 floor, and (historically) the separation between adjacent cosmic membranes. **Etheric keeps the first only**, and the other two are now named. The seven dimensions are **compact dimensions** ([`src/glossary/compact-dimensions.md`](src/glossary/compact-dimensions.md)) — ordinary space, too tightly wound to traverse, and the geometry Etheric templates have extent *through* rather than anything the layer is made of. The membrane separation is **the Interval** ([`src/glossary/interval.md`](src/glossary/interval.md)) — a distance term rather than a place, previously unnamed and described only as "manifold distance"; a boundary zone is a narrowed Interval, and Membrane Resonance, Intermembrane Bleed, Membrane Shadows and transient gravity tunnels are all consequences of that narrowing. Same shape as the earlier Concordant zone-versus-mind split: where one term named both a place and a non-place, the place got the new name. Canonical-terms entries and five legacy→canonical migration rows added to [the canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md); swept across `post-eleven-dimensional-manifold.md`, `ensemble-multiverse.md`, `concordant-membranes.md`, `membrane-shadows.md`, and the `intermembrane-bleed`/`membrane-shadow`/`etheric` glossary entries. **Idiomatic "into the Etheric layer" is deliberately not migrated** — roots reaching into a mode of constraint is correct in its own register, and the ~190 uses of the layer sense (chapters, `canon_facts`, characters, story bible) are untouched. The canonical glossary and [the Etheric entry](src/glossary/etheric.md) now both carry an explicit **register rule** so a later sweep doesn't re-derive it: *Etheric* works as working jargon even where it isn't literally accurate, and which register is speaking decides whether looseness is a fault. Chapters, characters and Codex sources are **voices** — imprecise field vocabulary there is characterization, not error, and is not a migration target. Lore and glossary pages are the **Archive** — held to the definition, where asserting the Etheric *is* a place is a mistake. Same principle the setting already applies to in-world "magic," and the reason [the Overfold](src/glossary/overfold.md) isn't ranked below Etheric.
- **A latent contradiction the shared word was hiding, now resolved** — `ensemble-multiverse.md` had Etheric templates propagating through the seven local dimensions while `concordant-membranes.md` routed template coupling through the trans-concordant constraint field at Dimensions 20+. Both are right, at different scopes: a Concordant's compact dimensions carry its templates *at home*, inside its own membrane, and the constraint field carries them *across the Interval* to a neighbour. Stated explicitly in all three affected pages, since an analyst who conflates the two looks for a bleed signature in the wrong band. Two paragraphs of self-disclaiming prose ("a geometric substrate claim, not a revision of the Five Layers…") that existed only to hold the overloaded word apart are gone, the distinction now being carried by the terms themselves.
- **The post-11 manifold named: the Common Manifold** ([`src/glossary/common-manifold.md`](src/glossary/common-manifold.md)) — "post-11" is positional, and positioned relative to *our* dimensional floor, which `ensemble-multiverse.md` explicitly establishes is "a local fact rather than a cosmic constant." Analysts in a universe with a different floor would count from their own number and mean the same structure, so the old name silently assumed the arithmetic it exists to deny. **Both names stay canonical**: "Common Manifold" is the formal name and records what it is (the one structure universes with different floors demonstrably share), "post-11 manifold" remains correct Survey Corps shorthand and in-world voice, now flagged as knowingly parochial — the same naming-from-where-you-stand habit that already gave the setting three names for one boundary phenomenon (see [Overfold](src/glossary/overfold.md)). The manifold's band numbering (12–15, 16–19, 20+) is likewise labelled local bookkeeping. **The page URL is unchanged** at `/lore/post-eleven-dimensional-manifold/`, so no inbound link or comment thread moves.
- **Church Space reframed as an overlay, not a parallel storyline** — the registry and its landing page described "a parallel storyline with its own cast," which is not what is on disk: there are no church-space chapters, `seasons` is deliberately empty, and the material is lore, codex entries, characters, and an author's FAQ. It is an optional commentary layer reading the *same* reality through an explicitly Christian and evangelical lens, concentrated on the spiritual layer, opted into per domain. Both descriptions now say so. This adds a third canon status to the internal glossary — **overlay**, alongside canon and valid-for-its-author: laid *across* the record rather than filed inside it, binding nothing and never load-bearing for the main story. The awkwardness of housing it in a registry of storyline threads is recorded rather than papered over: the mismatch is vocabulary, not behaviour, since tag-based membership plus opt-in visibility is exactly what an overlay needs, and the note says what a cleaner split would look like if the overlay ever grows chapters of its own.
- **New `/story-engine/` section, parent of the Journal** — the out-of-character part of the site now has a home of its own: the Journal's entries listed in full, plus a spoiler-free orientation to how the story is put together (seasons as timeline slots rather than release order, storyline threads, scenes and points of view, and the three different promises Lore, Glossary, and Codex each make). Replaces "Journal" in the main navigation, which now highlights for both URLs. **Journal URLs are unchanged** — every entry stays at `/journal/<slug>/`, deliberately, because giscus maps non-chapter discussions by pathname and re-parenting the URLs would orphan every existing journal comment thread. The Journal index gains a breadcrumb to its new parent.
- **An internal glossary of story-engine terms**, in `story-bible/story-bible-summary.md`'s Terminology sections: thread/strand/arc, visibility and filtering, the front-matter fields whose confusions are silent (`id` vs `comment_id`, `date` vs `timestamp`, `canon_facts`, `povs`), canon status (canon vs. valid-for-its-author vs. the lore/codex boundary vs. house voice), and craft (scene, POV block, scene-POV page, convergence point, deliberate mystery, reserved season, gap). Also records the standing policy that `story-bible/` is **permanently unpublished** — it is the only place spoilers live, and `/story-engine/` is derived from it rather than a mirror of it.
- **"Thread" split into thread and strand** — the story bible had been using one word for two structurally different things: a **storyline thread** groups whole seasons and is real machinery (`lib/storyline-threads.js`, `/threads/`, `threadForSeason`, `deploy.conf`'s `THREADS`, private-thread hiding), while "Thread A / Thread B" meant two parallel storylines *inside* one season, with no id, page, or registry anywhere. The within-season sense is now a **strand** — a thread is braided from strands, so the metaphor nests instead of colliding — and **arc** is reserved for a character's trajectory, which is not a reading path at all. Terminology table, the rules that follow (a season has exactly one thread and any number of strands; deploy filtering acts only on threads), and a legacy→canonical migration map are in `story-bible/story-bible-summary.md`; swept across all three story-bible documents. No reader-facing page or template used "Thread A/B", so no URL, thread id, or `deploy.conf` key changed. Also records the scope split the sweep depended on: the canonical glossary governs *in-universe* terminology, while craft vocabulary (thread, strand, arc, scene, POV block) belongs in `story-bible/` rather than a page readers see as lore.
- **The Codex's canon status stated explicitly, and `author` made a required field** — a codex entry is not canon; it is one named in-universe source's account, held to being valid *from that source's point of view* (their era, access, institution, bias) rather than to being true. The rule is now in `CLAUDE.md` alongside the lore/codex boundary it underwrites, and `lib/content-schema.js` enforces the attribution it depends on: an entry with nobody behind it has no viewpoint to be valid from and silently reverts to Archive-voice fact. All 29 existing entries already carry an `author`, so nothing needed backfilling. `scripts/new-content.js` now prompts for any schema-required field beyond `title`, so a scaffolded codex entry can't be created in a state that fails validation.
- **All seasons ratified as canon (2026-07-25)** — nothing in `src/seasons/` is provisional any longer, the Season 6–7 arc (`s06e01c01–c03`, `s07e01c01–c03`) included. Every chapter's `canon_facts` now binds on anything written afterward, and `story-bible/tissadelle-arc-s6-7.md` becomes background reasoning rather than a specification those chapters are measured against. Two consequences recorded in `story-bible/narrative-gaps-checklist.md`: four of its five Season 6–7 boxes were already satisfied by chapters on disk and are now ticked (the note claiming otherwise was stale), and **The Last Stand itself remains genuinely unwritten** — every existing chapter reaches Dock Seven backwards, through a citation draft, a residual signal, and an unreliable interior, with no chapter or POV of its own.
- **Threshold Station's chronometer discrepancy re-aged in `s06e01c02`** — the drift is eleven years old in 2826 (Season 1) and therefore sixteen in 2831, but the chapter carried the Season 1 figure into Season 6 twice, in prose and in a canon fact. Caught by ratification: the number only became load-bearing once the arc stopped being provisional.
- **Narrative drafting permission changed (2026-07-25)** — the rule that every narrative first draft is the author's, and that unprompted narrative prose is unwelcome, is retired in favour of standing permission to draft new scenes. `CLAUDE.md`'s prose-authorship convention now carries the constraints that come with it: work from `story-bible/narrative-gaps-checklist.md`, leave the deliberately-unwritten seasons and the standing mysteries (the *Patience First* terminus, the 2732 recovery) alone, add to the Season 6–7 outline rather than resolving it, match the `scene`/`pov` container structure, and state plainly what any drafting session wrote.
- **The voice and style of all existing prose approved by the author (2026-07-25)** — the corpus as it stands is now the reference for the house voice, and the two authorship-provenance notes (`story-bible/prose-needs-your-voice.md`, `prose-needs-review.md`) are records of who wrote what rather than tone-review queues. Scope is deliberately narrow: register only, not canon — the Season 6–7 chapters (`s06e01c01–c03`, `s07e01c01–c03`) need no rewrite for voice, but whether their events are settled story remains open, to be read against `story-bible/tissadelle-arc-s6-7.md`. The go-forward convention in `CLAUDE.md` is unchanged — narrative first drafts are the author's, and unprompted narrative prose is still out of scope.
- **Heroes of their own for `/story-engine/` and the Journal** — both pages (and the About page) had been opening with the same `hero/about-writer.jpg`. [Story Engine](src/story-engine/index.md) now leads with `hero/story-engine.jpg`, a brass machine whose riveted nameplate reads *Story Engine*, and the [Journal](src/journal/index.md) index with `hero/journal-notebook.jpg`, an open journal headed *Author's Journal* above a handwritten entry; About keeps the writer-at-a-desk photograph it always had. Both were picked from eight generated candidates on the same rule the codex-cover generator exists to enforce — every legible word must be short and correctly spelled — which ruled out six of them, and both were cropped to 16:9 around the lettering so `.page-hero-image`'s 3.4:1 centre band doesn't cut it off. Selection criteria and crop recipe in `story-bible/images.md`.
- **Cascade terminology law: the three senses of "Concordant" separated** ([canonical glossary](src/lore/glossary/canonical-glossary-and-migration-guide.md), with migration mappings). The word had been carrying both a bounded *region* and a universe-level governing *mind*; the governance sense is now **Universal Authority**, the zone-level governing mind a **Concordant Principal** (commonly **Harmonarch**), and the polity [the Solar System Concordant](src/lore/solar-system-concordant.md). Legacy phrasings migrated across affected lore and glossary pages. Also retires "High Celestials" as an ambiguous term rather than a Telearch synonym — it was used for both Telearchs and the new [Dynarchs](src/glossary/dynarch.md), and [its page](src/glossary/high-celestials.md) is now the disambiguation rule.
- **The Cascade documented as *mostly* hierarchical, with time in three registers** ([Cosmic Cascade](src/lore/cosmic-cascade.md)) — Worldwrights and Telearchs are parallel branches under the Archewrights rather than a chain; Dynarchs are co-created under both the Telearchs and the Concordant Principals; descent is creative before imperative. Material-layer time holds the Celestials and below, **conceptual time** the middle strata, and outright eternity only above the Worldwrights — superseding the earlier flat "timeless above the Celestials" framing and legitimising the operational record's active verbs for those strata.
- **Twelve character/lore/hero images replaced**, each `image_alt` rewritten to describe actual content — including the four worst content mismatches (an Imperium cyber-revenant shown as a smiling US Army soldier, the protagonist Aldera as a blue-furred fantasy cat contradicting her own gallery photos, a historical MSC general as a contemporary policewoman, and the Cerebraun species as the grey-alien bust its sibling entry explicitly avoids). Prompts and per-file notes in `story-bible/images.md`.

- **About page's fan-fiction-clone links now point to on-site pages instead of GitHub repo files** — the Fan Works Policy link now goes to the homepage's [Licence section](src/index.md) (`/star-rangers/#license`), which already carries the full CC BY-NC-ND text, and the forking pointer now goes to a new `src/forking/index.md` page instead of linking directly to `CONTENT-LICENSE.md`/`FORKING.md` in the repository.

### Added

- **S01E03C03, "The Root and the Rest of It"** (`src/seasons/s01/e03/s01e03c03.md`) — the dryadic grove's Etheric root-anchor intervention, planned at the end of S01E02C02 and explicitly deferred by S01E03C02's closing log ("that work is not tonight's"), now written. Two scenes, four POVs (Elvira, Galahad, Rook-7, Aldera): Elvira severs the Court-Fae's coupling at the root-plate overlap the seven trees make together, with Galahad reading the classification aloud — the vocabulary he once withheld from her now supplied as part of the working. The grove's own Etheric rooting survives the severance untouched; leaf phase-lag settles from 0.22 s to 0.19 s and is logged rather than explained; the entity's destination is deliberately not established. The chapter's substantive canon move is a **retraction**: Threshold Station's forty-second chronometer discrepancy is unchanged four hours after a clean severance 1.3 km away, so Rook-7 withdraws the two-source boundary model he filed in S01E02C02 and the drift reverts to an active unknown. Placed after the season's naming endpoint deliberately — S01E03C02 stays the thematic close, and this is its physical follow-through.
- **Cascade governance expanded: who actually governs a universe** — new lore entry [Who Governs a Universe](src/lore/who-governs-a-universe.md) (universe vs. Concordant Zone; the three kinds of authority; the matrix exceptions; the timeless/conceptual/immanent split) with supporting glossary entries [Universal Authority](src/glossary/universal-authority.md), [Concordant Principal](src/glossary/concordant-principal.md), [Dynarch](src/glossary/dynarch.md) (the force-governance tier — Gravity, Electromagnetics, Nature — commonly **Exarchs**), and [Avatar](src/glossary/avatar.md) (the one lawful crossing between temporal registers: a higher stratum incarnated as a limited person, like its source without being the same person as it). Plus [Asteria the Sage](src/characters/asteria-the-sage.md), the retired Star Captain whose sigh — *"Where do I even begin?"* — now opens the Archive's own primer.
- **[The Solar System Concordant](src/lore/solar-system-concord.md)** — the polity referenced across a dozen pages but never defined: not a government but a standing compact of shared civil law founded 2543 UCSD with the Tycho Accords, enforced by every regime since and owned by none, and the reason "the Concordant period" names an era.
- **[The Star Rangers Frontier Corps](src/lore/star-rangers-frontier-corps.md)** — caretaker law enforcement for frontier worlds with no local government of any kind, reconciled with the charter's do-not-police clause via a self-terminating mandate that ends at a colony's first sworn sheriff. Includes the second-landing problem (failed or abandoned governments, where re-entry needs an invitation a dead government cannot issue) and the Corps' non-combatant posture — shield, evacuate, document, never fight.
- **[The Fusion Ceiling](src/lore/the-fusion-ceiling.md) and [Ascent Javelins](src/lore/ascent-javelins.md)** — fusion is the top of the lawful energy ladder (no antimatter, no exotic taps, with the refusal argued from certification practice and the Kernel's waste invariant), sufficient everywhere and immediate nowhere; and the missile-launched winged craft that carry passengers surface-to-orbit, chemical-cycle to ~1g and fusion-thermal above it. Together they yield the frontier consequence that a heavy world is easier to land on than to leave.
- **[Embodied Cognition](src/lore/embodied-cognition.md)** — why a small embodied mind with a grounded world model resolves what a vastly larger ship or habitat AI cannot (boundary-zone telemetry contradictions, schematic drift, presence as a first-invariant obligation), and why Concordant practice pairs the two classes rather than choosing.
- **The charter's terraforming prohibition** — the Star Rangers may not conduct, certify, or support the terraforming of any world bearing existing life of any kind, with no sapience threshold or significance test ([charter](src/timeline/2723-star-rangers-charter.md), [Frontier Transformation Protocols](src/lore/frontier-transformation-protocols.md)). Living worlds are settled by compatibility, not conversion.
- **Two codex entries** — [The Twelve Sultans of AI](src/codex/the-twelve-sultans-of-ai.md) (the Imperium era's AI oligarchs, their systems' informal cooperation without convergence, and why no singularity is on record) and [The Friend Who Forgot Her Cat](src/codex/the-friend-who-forgot-her-cat.md) (a Verification Desk case file on six years of relay correspondence undone by one small impossible absence, filed without verdict).
- **Local authoring tools** (`scripts/`, none wired into the build) — `make-codex-cover.ps1` regenerates a codex cover card in the designed template with real type; `import-image.ps1` converts a source image to a repo-ready JPG at site conventions; `check-internal-links.js` verifies every internal `/star-rangers/` link resolves, catching the link rot `npm test` cannot see. Documented in `CLAUDE.md`.

- **A second scene added to S05E02C01, "What the Hill Keeps"** (`src/seasons/s05/e02/s05e02c01.md`): the chapter's existing hillside content is now wrapped as Scene 1, followed by a new Scene 2 — Shepherd's escorted departure from the Union shuttle gate the next morning, POVs Galen and Tissadelle. Alongside the Highland Guard's correct, chilly farewell, she tells the Rangers' own liaison "It is good to home. I mean back home. With the Star Rangers" — a small, unrehearsed slip that names, ahead of the [Season 6–7 arc](story-bible/tissadelle-arc-s6-7.md), which home actually holds her. New minor character **`src/characters/galen.md`**, the Star Rangers' standing liaison officer at the gate. This is the chapter's first use of the `::::: scene N` wrapper (`lib/markdown-containers.js`) — every prior chapter has relied on the implicit single-scene fallback — so it's also the first chapter to produce a `scene-2/` set of per-POV pages. Deliberately left "A Fraction of a Second" at its existing `s05e02c02` slot rather than renumbering to make room, since S07E01C03 already cross-links it by that path.
- **`src/characters/reeves-eden.md`, "Reeves (Eden Space Habitat)"** — a new character page for Eden Space Habitat's own Detective Assistant Reeves, previously referenced in prose by `eden-warden.md` and `rasa-oyelaran.md` but with no page of its own; both pages' "Reeves" links (which pointed at [Threshold Station's Reeves](src/characters/reeves.md) despite the text explicitly calling Eden's Reeves "a separate deployment") now point here instead. `reeves.md`'s existing cross-reference note is now a working link too.
- **A line acknowledging "Orbital Five-O" is also a nod to the classic *Hawaii Five-O*** — added to `src/threads/orbital-five-o/index.md`'s intro, alongside the existing in-universe explanation (the task force covers all five Compact habitats).

- **`src/forking/index.md`, "Forking This Site"** — a reader-facing page adapting `FORKING.md`'s guide (the two licences, Path A vs. Path B, what's engine vs. content, step-by-step fork instructions) so the About page can link to an on-site page rather than a repository file.

- **Shared deploy machinery extracted to `scripts/deploy-lib.sh`** — the site-agnostic parts of `scripts/cpanel-deploy.sh` (log-file setup, the notification-email list, the notify-on-any-outcome mailer, per-run `deploy-logs/` persistence and pruning) now live in a new `scripts/deploy-lib.sh`, kept **byte-identical** with the same file in the `dermot-cochran-photography` repository (the same convention `scripts/ensure-node.sh` already follows), so fixes to that machinery are ported by copying one file instead of hand-merging two diverged scripts. `cpanel-deploy.sh` keeps everything site-specific (deploy.conf keys, multi-domain/ALT_DOMAINS handling, themes, giscus, the `/star-rangers/` prefix rewrite, pagefind) and behaves identically — same log/email/exit-code semantics, verified against a stubbed end-to-end deploy simulation of the success, failure, and multi-domain paths. CI's shellcheck step now covers the new file.

- **Season 0 is now presented as "Founding Era" in the UI** — a display-only relabel, leaving every URL (`/seasons/s00/…`), chapter `id` (`s00e01c01`), and the `season`/`episode`/`chapter` schema untouched, so no links or comment threads move. New `seasonLabel` Eleventy filter (`.eleventy.js`) maps season `0` → "Founding Era" and every other season → "Season N", used by the dynamic breadcrumbs (`src/_includes/chapter.njk`, `src/_includes/scene-pov.njk`) and the season-block titles on `src/seasons/index.md` and both thread pages (`src/threads/founding-era/index.md`, `src/threads/tissadelle-arc/index.md`). The hardcoded `src/seasons/s00/index.md` and its three episode pages (title, breadcrumb, `<h1>`, prose, descriptions) were relabelled to match. "Season 0" no longer appears anywhere reader-facing; the prequel reads under its existing `founding-era` thread name instead. A companion `seasonsInThread` filter lets the thread-grouped listings (`src/threads/founding-era/index.md`, `src/threads/tissadelle-arc/index.md`, and the per-thread sections of `src/seasons/index.md`) **suppress the per-season block title when a thread renders only one season** — so the Founding Era, whose single season's label is identical to its thread heading, no longer prints that heading twice; multi-season threads (Tissadelle's Season 1/3/5) keep their per-season titles.

### Added

- **`src/characters/turquoise-dove.md`, "The Turquoise Dove"** — a new Active character: a Higher Levril, distinguished among named dragons by the turquoise iridescence of her dimensional signature. Consistent with the Higher Levril subclass's Cascade-permitted role ([Levrils lore](src/lore/levrils.md)), she's associated with cross-Concordant coordination and post-instability advisory contact rather than direct intervention, and with mentoring — in constraint literacy — Champions selected by other Levrils rather than selecting them herself. No Material-layer archive holds a first-person account of an actual meeting; her presence is inferred from field-anomaly logs. Introduced ahead of an upcoming chapter appearance.
- **`src/characters/osric-fenholt.md`, "Osric Fenholt"** — a new Historical character: an Imperium-era Assistant Deputy Viceroy in the Belt Settlements Directorate's Bureau of Outer Compliance, whose nineteen years of unaltered settlement condition reports survived the Imperium precisely because the institution's own doctrine of technical rigor read his accuracy as competence rather than sedition. Filed under the Imperium/pre-MSC period, cross-linked to [The Imperium](src/lore/the-imperium.md) and [Military Space Command](src/lore/military-space-command.md).
- **Seasons 6 and 7 of Tissadelle Shepherd's Arc — the Last Stand and after** (`lib/storyline-threads.js`, `src/threads/tissadelle-arc/index.md`, six new chapters, two season and two episode index pages, one Codex working paper). The `tissadelle-arc` thread now claims seasons `[1, 3, 5, 6, 7]` (was `[1, 3, 5]`); its registry description and landing-page intro update from "Seasons 1, 3, 5, and beyond" to "Seasons 1, 3, 5, 6, and 7". No template change was needed — the thread and season listings pick up the new seasons automatically. New index pages `src/seasons/s06/index.md`, `src/seasons/s06/e01/index.md`, `src/seasons/s07/index.md`, `src/seasons/s07/e01/index.md` follow the `s02` no-hero-image pattern (no `season-06`/`season-07` hero asset exists yet, so they omit the image rather than reference a placeholder). The arc dramatizes the [Season 6–7 treatment](story-bible/tissadelle-arc-s6-7.md) with **no new cosmological system** — every mechanism cites existing canon ([Seed Mind Lifecycle](src/lore/seed-mind-lifecycle.md), [Noögenic Protouniverse](src/glossary/noogenic-protouniverse.md), [Levrils](src/lore/levrils.md), [Gilded Saints](src/glossary/gilded-saints.md)/[predatory-entities](src/lore/predatory-entities.md) counter-doctrine, [Saint Aoife](src/lore/saint-aoife.md)'s thorn-well account). Line Captain Shepherd is a stalled noöseed — a vulnerable, still-becoming protouniverse — kept from collapse by three help vectors the story keeps strictly inside canon limits: a **levril** holding her boundary open (the same Cascade-entity class the Union reads behind Aoife's vision, able to mentor constraint literacy but forbidden to complete her transference or reach in); **Saint Aoife's** inherited discipline of refusing to collapse *shown* into *healed*, carried as the Shepherd holding's own "keep the rule without inventing the reason"; and her IWD-reconstructed **dreams of friends** (Órla and Karla Wender), written to refuse to be more comforting than the originals. Cognitive-horror-into-tender-rescue, no magic and no sexualized content, per the treatment's tone note. The resolution follows the treatment's recommended option 3 (contested on the record and off it — Council citation, Codex, and what Wender privately knows never fully reconcile) with option 2's mechanics (real two-way contact) as the private truth.
  - **`src/seasons/s06/e01/s06e01c01.md`, "First Light, Off the Record"** — Shepherd (POV) and Órla-as-remembered: the interior homecoming opens, warm and total, then produces two of the same clearing, and her mother chooses, on the doorstep, to let a daughter who *is and is not* her daughter be both true at once rather than smooth the discrepancy away.
  - **`src/seasons/s06/e01/s06e01c02.md`, "What the Instruments Kept"** — Wender and Rook-7 (POVs): Wender leaves the Oversight Council's hero's citation unsigned; Rook-7's non-standard arrays log a warm, iterating signal at the Dock Seven failure point that is inconsistent with total loss and phrases the way the [Sentinel pulse](src/seasons/s03/e01/s03e01c01.md) once did. Forwarded to Sen's reconciliation desk.
  - **`src/seasons/s06/e01/s06e01c03.md`, "The Well Ran Warm Again"** — Shepherd and a self-aware reconstructed Wender (POVs): Shepherd runs her own anomaly diagnostic on herself, finds the recursion never resolves, names her condition before anyone outside can, and a levril arrives at the edge of her inner world — folding in without tearing, at every distance at once — and *holds* without entering. Season 6's confirmation-without-rescue turn.
  - **`src/seasons/s07/e01/s07e01c01.md`, "The Accurate Account"** — Wender and Syra (POVs): the external team assembles the true account, first ruling out whether the closure is being held open by a [Gilded Saint](src/glossary/gilded-saints.md), by physics, or by the Council's own martyrdom narrative (all real hazards; the counter-doctrine's precision aimed at each), and correlates the Dock Seven warmth with the eight-century-old warm-well signature in Sen's archive.
  - **`src/seasons/s07/e01/s07e01c02.md`, "Shown, Not Healed"** — Shepherd and Órla-as-remembered (POVs): the real danger is the *mercy* — the pull to let the loop resolve into a finished, unbearably kind homecoming and stop. Shepherd holds it open using Aoife's exact discipline (shown ≠ healed) and her family's, and the levril teaches her to see the wall she is inside without breaking it for her.
  - **`src/seasons/s07/e01/s07e01c03.md`, "Enough Is Enough"** — Shepherd and Wender (POVs): the accurate account becomes a rope, a detail only Shepherd could know (the unfiled Hollow from [S05E02C02](src/seasons/s05/e02/s05e02c02.md)) crosses the warm edge in both directions, and the two threads finally touch. Nobody pulls her out; nobody closes the file; she names her own state true and keeps it open on her own terms — not gone, not saved, and not alone.
  - **`src/codex/the-warm-edge-correlation.md`, "The Warm-Edge Correlation"** — a Codex working paper (a Ridgemoot chaplain of Saint Aoife's Celtic Union devotion, Bréanainn Ó Sé, with the Survey Archive reconciliation desk) reading the Dock Seven warm signature against Aoife's thorn-well account. Filed in the **Codex, not Lore**, per the lore/codex boundary rule: the *instrument signature* correlation is settled and cited, but the identification (same levril? mercy or mere observation?) is contested and left standing — with an explicit caution that the devotion has every reason to want the warm edge to mean mercy, which is exactly why it must not be the one to say it does, and that "divine" fixes no referent here (it points at Kieme, or the whole Cascade, or nothing the invariant record names). Kept to public Aoife-devotion framing rather than the private `church-space` Communion, per the one-way public→private link boundary. Cross-referenced from the existing [Dock Seven Night Watch Addendum](src/codex/dock-seven-night-watch-addendum-day-52.md).

- **`src/codex/baby-universe-ballad.md`, "Baby Universe (Circulating Ballad)" — a Cultural Record with recovered audio**: an unattributed ballad that circulates *about* Line Captain Shepherd after the Last Stand, transcribed lyrics plus the recovered recording embedded via a `<video controls>` player (`src/video/baby-universe.mp4`, served at `/star-rangers/video/`, the first real use of the existing `src/video` passthrough). The song opens naming her in the third person and slips into a first-person plea in the chorus ("St. Aoife, find the thread / Before I fade and die"), voicing the S6–7 arc's own beats — the seam into higher space, the bruised "baby universe," the levrils "with lantern hands and grace," the "dreams of friends" — from the outside. Filed in the **Codex** and, within it, under the Survey Archive's *reconciliation* binders rather than the music stock: [Sen's](src/characters/sen.md) archivist note keeps the [closure-prevention caution](src/lore/predatory-entities.md) beside it, since a beautiful, never-quite-closed sacrifice sung by people with good reasons to keep singing it is the exact shape the counter-doctrine watches. Cross-linked to [Ballad of the Stars](src/codex/ballad-of-the-stars.md) (whose audio/transcription convention it follows), [The Warm-Edge Correlation](src/codex/the-warm-edge-correlation.md), and the [S7 finale](src/seasons/s07/e01/s07e01c03.md).

- **One-way public→private thread link boundary, enforced at build time** (`scripts/validate-content.js`, plus a new `privateThreadForPage` reused from `lib/content-filter.js`): a public page may no longer hardcode a link *into* a private-thread page (currently `church-space`'s), since on the full site and every clone that hasn't opted the thread in, that target renders the "not included in this edition" placeholder rather than the real page — a dead end for the reader. `npm test` now fails, file-by-file, on any such link. The reverse direction stays allowed: a private page can link out to public pages freely (a private page is only ever seen where its thread is opted in, and the public pages it references exist there too). Removed the two pre-existing violations this surfaced — `src/lore/physics-comparison.md`'s editorial note pointing at the private `softening-the-real-cosmos-side` journal entry, and `src/lore/universes/si-gaoithe.md`'s comparison linking the private `cnoc-na-mbeach` lore page — by trimming each to stand without the private reference.
- **Explicit friendly-AI-crawler section in `robots.txt`** (`src/robots.njk`): named `User-agent` groups welcoming the major AI assistant, answer-engine, and training/search crawlers — Anthropic (`ClaudeBot`, `Claude-Web`, `Claude-User`, `Claude-SearchBot`, `anthropic-ai`), OpenAI (`GPTBot`, `ChatGPT-User`, `OAI-SearchBot`), `Google-Extended`, `Applebot-Extended`, Perplexity (`PerplexityBot`, `Perplexity-User`), and `CCBot` (Common Crawl) — each with `Allow: /`. Declarative rather than functional: the existing `User-agent: * / Allow: /` wildcard already permits every crawler, so this grants no new access — it makes the welcome explicit and gives one obvious place to adjust any single crawler later without touching the wildcard rule.
- **Character `known_codex` field**: characters can now carry an optional `known_codex` front-matter array of codex-entry filename slugs (e.g. `[star-rangers-anthem, ballad-of-the-stars]`), rendered as a "Known Codex Documents" list on `character.njk` (`.character-known-codex` in `main.css`, regenerated across every theme) via a new `codexByIds` Eleventy filter (`.eleventy.js`, matching codex entries by `fileSlug` since codex has no `id` field). `scripts/validate-content.js` cross-checks every listed slug against actual `src/codex/` files. `src/characters/tissadelle-shepherd.md` is the first entry to use it, formalizing the two Slipwave-era recordings her page already linked in prose.
- **`major-character` POV validation**: `scripts/validate-content.js` now requires every chapter to have at least 2 `povs` entries, and — when at least one POV id resolves to an actual character file — that at least one of them carry the existing `major-character` tag (a convention 6 characters already used informally; a chapter built entirely from undocumented, chapter-only POV figures isn't held to this second check, since there's no page to classify). One pre-existing chapter, `src/seasons/s01/e00/s01e00c02.md` ("What the Stones Confirmed," a deliberate single-POV scene), is grandfathered past the POV-count minimum rather than rewritten. Promoted three characters to `major-character` to satisfy the rule for their own storylines: [Kai Larsen](src/characters/kai-larsen.md) (Orbital Five-O's lead across all of Season 2 so far), [Tissadelle Shepherd](src/characters/tissadelle-shepherd.md) (the multi-season protagonist of Tissadelle's Arc, previously untagged), and [Maren Solveig Krast](src/characters/maren-solveig-krast.md) (the Military Space Command's founder-in-memory, sole resolvable POV of `s00e02c02`).
- **The Orbital Five-O storyline thread** (`lib/storyline-threads.js`, `src/threads/orbital-five-o/index.md`): a new, non-private thread registry entry for the Governor's Investigative Task Force — Commander Kai Larsen, Governor Petra Voss, and Chief Commissioner Dorian Calloway, all previously tagged `orbital-five-o` but with no registered thread or landing page to collect them. Claims Season 2 (left open by `tissadelle-arc` for exactly this — a storyline that doesn't run through Tissadelle Shepherd), now that the thread's first chapter exists (see below).
- **`src/seasons/s02/e01/s02e01c01.md`, "The Desk That Called First"**: Season 2's first chapter, opening the Orbital Five-O thread. Two POVs — Commander Kai Larsen and Governor Petra Voss — cover an Eden compliance-framework referral (a ration-manifest discrepancy Detective Inspector Wendell Albercombe couldn't trace past Eden's own jurisdiction) turning out to touch a Compact-wide routing code, the exact shape of case Five-O was chartered to close. Albercombe and his household intelligence [Jeeves](src/characters/jeeves.md) appear as minor/supporting characters in Larsen's POV, not as leads. Adds `src/seasons/s02/index.md` and `src/seasons/s02/e01/index.md` season/episode index pages, matching the existing `s01`/`s03` pattern (no hero image — none exists yet for this season, so the pages omit it rather than reference a placeholder asset).
- **`src/seasons/s02/e01/s02e01c02.md`, "The Office That Reads Everything"**: Season 2, Episode 1's second chapter. Larsen and Chief Commissioner Dorian Calloway POVs — Larsen brings the reassigned routing code to Calloway's office and gets back a name, Threnody Logistics, plus the reconciliation discrepancy Calloway's office had already noticed six weeks earlier and filed as a curiosity rather than escalated. Ends on Calloway quietly following a second thread of his own (an unnamed habitat Commissioner's countersigned freight authorization) before handing Larsen anything more than a fact that "hadn't finished arguing for itself yet" — a deliberate cliffhanger, not yet a named suspect.
- **`src/seasons/s02/e01/s02e01c03.md`, "A Very Ordinary Call"**: Season 2, Episode 1's third chapter. Calloway and Larsen POVs — Calloway places his routine liaison call to Meridian's Commissioner, Anselm Duarte, and gets back answers that are individually true and collectively too well-rehearsed for an unannounced question. Introduces **Meridian** as the Compact's second named member habitat (alongside Eden) and Duarte as a new named, chapter-level character — kept to dialogue and description here rather than a dedicated character/lore page, the same way `s02e01c02` introduced Threnody Logistics without one. Ends with Larsen deciding to visit Meridian in person, the first time Five-O has exercised its five-habitat charter beyond Eden.
- **`src/characters/wendell-albercombe.md`, "Detective Inspector Wendell Albercombe"**: a new minor/supporting character — an Eden Space Habitat detective inspector under Superintendent Oyelaran, with no standing in Five-O's own casework but occasionally the first call when an "ordinary" referral turns out bigger than it read. Cross-linked from [Superintendent Rasa Oyelaran](src/characters/rasa-oyelaran.md) (the existing bureau-roster mention is now a link) and tagged `orbital-five-o`.
- **`src/characters/jeeves.md`, "Jeeves"**: a new character, a domestic-companion AI assigned to Detective Inspector Wendell Albercombe under Eden Space Habitat's welfare program — distinct from the bureau-assigned [Reeves](src/characters/reeves.md) at Eden (and from Threshold Station's own Reeves/Ashcombe pairing), working Albercombe's caseload sideways from outside institutional process rather than from inside it. Cross-linked from [Superintendent Rasa Oyelaran](src/characters/rasa-oyelaran.md) and, now that his own page exists, [Wendell Albercombe](src/characters/wendell-albercombe.md); tagged `orbital-five-o` as a minor/supporting figure in that thread.
- **`src/codex/jehu-among-the-comparanda.md`, "Jehu Among the Comparanda"** and **`src/codex/the-captain-who-drove-furiously.md`, "The Captain Who Drove Furiously"**: two paired Codex entries reading an Old-Earth scriptural pair against Solar System founding memory. The first is a Fellowship comparative-archive working paper (Sister Ione Marrec, contemplative chapter) that files the prophet **Elijah** as an [Arilon](src/lore/arilon.md)-pattern witness — names the boundary truth the royal house won't, is opposed, is gone before the naming is vindicated — and pairs the captain **Jehu** (who ends the rotten house but "departs not from" its older idol, and whose dynasty gets a bounded span then is narrowed into its successors) with General **[Maren Solveig Krast](src/characters/maren-solveig-krast.md)** and the [Military Space Command](src/lore/military-space-command.md): the founder-in-memory (not in fact) whose word ended one failure by reproducing its deeper one, whose institution got ~two centuries then was narrowed into the SSDC, and whose own sentence became the outer-station critics' vindication after the namers were gone. The second is the plain-voice telling — a contemplative answering a cadet at the Eden commissioning who asks why the Rangers sing an Old-Earth prophet's song ([Hymn of the Thin Places](src/codex/hymn-of-the-thin-places.md)), threading the twinned story to Krast without ever naming her. Both are filed in the Codex, not Lore, per the contested/interpretive rule (the mapping is one correspondent's, non-diegetic to the political record); the Cascade's no-moral-cosmology and "misattribution as divine will" cautions are carried through both, and the mythic overstatement that gives the MSC a founder it never had is itself flagged as an Arilon-adjacent effect. Reciprocal links added from `src/lore/arilon.md` and `src/characters/maren-solveig-krast.md` (settled MSC lore left unedited).
- **Multilingual approach: English as canon, browser translation, and human translations as fan works** — no official per-locale build, by design (an unverifiable translation on the canonical site would assert canon its author can't confirm, worst of all in the Codex/narrative where precise diction is the point). `src/_includes/base.njk`'s footer gains a labeled note (`.site-lang-note` in `main.css`, regenerated across every theme) telling readers their browser can translate any page but that such translations are automatic/**unofficial** and the English text is always authoritative on canon. `CONTENT-LICENSE.md`'s Fan Works Policy and the About page's Fan Works section (now anchored `#fan-works`) explicitly welcome human translations as non-commercial, clearly-labeled fan works. New Journal entry **`src/journal/a-canon-i-cannot-verify.md`, "A Canon I Can't Verify in Another Language"** records the reasoning.
- **Named giscus comment-repo profiles (`GISCUS_PROFILE`) + a local override file for forks** (`src/_data/giscus.js`, `scripts/cpanel-deploy.sh`, `sample-deploy.conf`, `sample-giscus.local.json`, `.gitignore`, `README.md`, `FORKING.md`). The project's own comment repos are now a `GISCUS_PROFILES` registry in `giscus.js` (`default` → `sciencefiction-site-comments` for the full-site/reference domains incl. starquest.*; `church-space` → `churchspace-site-comments` for church-space.site/.online and their Fellowship of Light addon domains). A deploy selects one with a single `GISCUS_PROFILE` key instead of pasting six repo/category IDs — so `sample-deploy.conf`'s church-space example blocks collapse from ~7 giscus lines each to one, and a Fellowship of Light sample block was added alongside. An unknown profile name fails the build (typo guard) instead of silently using the default board. The six explicit `GISCUS_REPO`/`GISCUS_REPO_ID`/`GISCUS_CATEGORY_*_ID` keys remain as a per-field escape hatch (each overrides just its field of the selected profile). Separately, a **third-party fork** can now point comments at its own repo without editing engine code or `deploy.conf`: an untracked `giscus.local.json` (copy `sample-giscus.local.json`) is read at build time and becomes the fork's default board on every path — `npm start`, GitHub Pages, and any cPanel deploy that doesn't select a profile — with `GISCUS_PROFILE`/`GISCUS_*` env vars still taking precedence. Default (unset) behavior is unchanged: the same `sciencefiction-site-comments` board with the same IDs as before.
- **`src/journal/two-copies-forty-seconds-apart.md`, "Two Copies, Forty Seconds Apart"**: a new Journal entry giving an honest account of the tools the site has been written with over time — GitHub Copilot and Microsoft 365 Copilot for early scaffolding (folded invisibly into human-authored commits, since accepted suggestions land under the author's name), Sudowrite for some of the Codex's documentary prose, and Claude Code once a Sudowrite draft and the committed file drifted out of sync and the repository became the single source of truth. Frames the sync-drift problem as a small real-world echo of the story's own premise (records that disagree and stay that way). The About page's new "How this site is written" section (below) links to it.
- **"How this site is written" section on the About page** (`src/about/index.md`): a short, honestly-scoped note on the drafting/editing toolchain (GitHub Copilot + M365 Copilot early on, Sudowrite for parts of the Codex, Claude Code against the repository now that it's the single source of truth), sitting alongside the existing "How this site is built" (tech stack) and "How this site is deployed" sections, and linking to the fuller Journal entry above.
- **`src/lore/universes/tir-tairngire.md`, "Tír Tairngire: The Harmonic Membrane"** and **`src/lore/universes/si-gaoithe.md`, "Sí Gaoithe: The Discordant Membrane"**: two new Universes entries documenting a mechanical, hard-SF explanation for Celtic Union "fair folk" folklore — a lawful, mechanism-level reading rather than a magical one. Tír Tairngire's membrane cycles on a rhythm regular enough to correlate with [Tír na nÓg's own Conjunction](src/lore/planets/tir-na-nog.md); its Class II crossers (the "Good People") are documented with no feeding signature of any kind. Sí Gaoithe's membrane never settles into a stable Boundary-State Class, tied speculatively to its own high [Creative Entropy Index](src/lore/creative-entropy-framework.md); its crossers resist clean Class II/III sorting and produce only localized mischief, never a confirmed drain. Both are explicitly unrelated discoveries, not two faces of one thing, despite the shared mythic register. `src/lore/concordant-membranes.md` gains a new "Beyond Class III: Transient Gravity Tunnels" section naming the shared, rarer matter-crossing mechanism (distinct from a Membrane Shadow's gravitational-imprint-only case) that both entries document instances of. Cross-linked from `tir-na-nog.md`, `meta-dimensional-beings.md` (a "not every Class II being is predatory" clarification), `predatory-entities.md`, and `creative-entropy-framework.md`.
- **`src/lore/lagrange-fold-points.md`**: new "Quiet-Zone Tunnels: Undetected Natural Fold Conduits" section documenting a distinct, intra-Concordant natural fold phenomenon — a stable (non-collapsing) tunnel that can form wherever deep interstellar space is gravitationally flat enough, by distance rather than Lagrange balance, for an admissible fold corridor to thread through it. Unlike the cross-membrane transient gravity tunnels above, this stays within one universe; unlike engineered Higher-Dimensional Folding, it requires no scaffold and carries no Lagrange-like signature to survey by, so a vessel typically confirms one only after already drifting through it. Left deliberately unresolved whether this accounts for any of the setting's existing founding-era fold mysteries. Cross-linked from `src/lore/ftl-mechanics.md`.
- **`src/journal/sorting-doctrine-out-of-lore.md`, "Sorting Doctrine Out of Lore, and a Paragraph That Knew Too Much"**: a new Journal entry documenting the editorial reasoning behind moving `cosmic-limitation-on-evil.md` to the Codex and trimming `predatory-entities.md`'s "How to counter them" paragraphs (both below), without restating the specific mechanism the trim removed.
- **`src/codex/cosmic-limitation-on-evil.md`** (moved from `src/lore/cosmic-limitation-on-evil.md`): "The Cosmic Limitation on Evil" refiled as a Doctrinal Division working paper — a named theorist, institution, and archive location — rather than settled lore, since its Archive-theorist-vs-devotional-tradition argument and its unresolved tension with the Cascade's own no-moral-cosmology invariant is exactly the kind of contested, POV-laden content the Codex is for, not Lore. Content and canon facts unchanged; added a closing archivist's note cautioning against reading it as predictive of any specific case. `src/images/lore/cosmic-limitation-on-evil.jpg` moved to `src/images/codex/` alongside it. Cross-links updated in `cosmic-cascade.md`, `kieme-visible-hand.md`, `communion-of-the-called.md`, and `glossary/kieme.md` (the last also trimmed to state the claim's contested status more tersely, consistent with lore/glossary now leaning harder toward settled, hard-SF-consistent fact and leaving argued positions to the Codex).
- **Trimmed `src/lore/predatory-entities.md`**: removed the "How to counter them" paragraphs for Court-Fae and Gilded Saints, which stated outright the specific tactical mechanism resolving an in-progress/planned story convergence. General lore is allowed to state settled cosmological fact ahead of the prose, per the section's own intro, but not the answer to a scene not yet told. The underlying craft/canon detail now lives in `story-bible/story-bible-summary.md`'s "Threats/entities" section (not rendered on the site) and `prompts/star-rangers-universe-engine.md`, both of which also gain a new standing editorial rule on the lore/codex boundary and spoiler discipline (see `story-bible-summary.md`'s new "Editorial note" section for the full reasoning).
- **Backlink to the page's true home on the "Not included in this edition" placeholder** (`src/_includes/excluded.njk`, `lib/storyline-threads.js`, `lib/content-filter.js`, `.eleventy.js`): a narrowed deploy (e.g. `church-space.site`, `undercover-pets.com`) already renders this placeholder in place of any page its own `CHARACTERS`/`TOPICS`/`THREADS` filtering excludes, but gave the reader no way to actually reach that content — it just said the page wasn't part of this deployment. Now links to the same path on the domain that *actually has* the page, built from `page.url` rather than hardcoded, so it resolves correctly for every excluded page regardless of content type. Ordinary excluded pages link to the full-site reference domain (`sciencefiction.site`, now the `DEFAULT_REFERENCE_DOMAIN` constant in `lib/storyline-threads.js`), which is a superset of everything ordinary narrowing hides. A **private thread**'s pages are the exception: the reference domain excludes those too (a private thread is opt-in on *every* build, including the default full-site one), so pointing there produced a **self-referential loop** — on `sciencefiction.site` itself, a church-space page's placeholder linked straight back to the same church-space URL, which just re-rendered the same placeholder. A private thread now carries an optional `homeDomain` (`church-space` → `church-space.site`), and its excluded pages link there — the one clone that actually opts the thread in — instead. Resolved per page by a new `referenceDomain` Eleventy computed field (`.eleventy.js`) backed by `privateThreadForPage` (`lib/content-filter.js`). A plain link, not a redirect — the reader chooses whether to follow it.
- **`src/glossary/cyborg.md`, "Cyborg"**: a new glossary entry defining physical cybernetic modification as a category distinct from [Smart Pet](src/glossary/smart-pet.md)'s purely cognitive AI subsystem — a distinction `smart-pet.md` had already asserted ("A Smart Pet is not, by definition, a cyborg") without the term ever being defined anywhere. Covers its history under [the Imperium](src/lore/the-imperium.md)'s military augmentation programs and its regulation since, first by the Military Space Command and tightened by every government after. Cross-linked from `smart-pet.md`'s `related` list, `the-imperium.md`, and `military-space-command.md`, both of which gain a paragraph on the same history from their own side.
- **`src/lore/star-rangers-science-corps.md`, "The Star Rangers Science Corps"**: a new Institutions entry for the specialist guild that interprets, models, and publishes on what the [Survey Corps](src/codex/survey-corps-protocol.md) collects in the field — Survey collects, Science explains what was collected — and for the **Chief Science Officer** duty title that leads it post by post. `star-rangers-command-hierarchy.md`'s specialist-corps list and Chief-rank duty-title rundown both named Survey, Safety, Provost, Medical, and Engineering but never Science, despite the site already using terms like "boundary science" loosely elsewhere; this closes that gap and gives the role its own institutional history — descended from the MSC's own Boundary Research Division, reconstituted at founding on the same independence principle that reconstituted the SSSA as the Safety Corps. Cross-linked from `star-rangers-command-hierarchy.md` (ladder entry + See Also) and `star-rangers-safety-corps.md`'s See Also.

- **`src/characters/lorien-the-wanderer.md`, "Lorien the Wanderer"**: a new character, an unaffiliated freelance captain who charts unclaimed fold corridors and sells the survey data to whoever pays first — Survey Corps included, when the price and the politics both work out. Flies the *Restless Verge*, a stripped-down long-range courier hull. Not yet featured in any chapter; her own "See also" links out to the existing `src/lore/lagrange-fold-points.md` and `src/lore/frontier-transformation-protocols.md` entries (no reciprocal link added on either, since neither is specifically about her).
- **Character gallery field**: characters can now carry an optional `gallery` front-matter array (`image`/`image_alt` pairs, rendered under a new "Gallery" section on `character.njk` via `.character-gallery`/`.character-gallery__grid` in `main.css`, propagated to every theme). `src/characters/aldera.md` is the first entry to use it — four candid field photos in `src/images/characters/aldera/`, tagged `pets` alongside her existing tags.
- **`src/glossary/metafold.md`, "Metafold"**: a new Misconception-category glossary entry for the popular misnomer that conflates the [Overfold](src/glossary/overfold.md) (a standing boundary-zone condition) with [Higher-Dimensional Folding](src/glossary/higher-dimensional-folding.md) (the FTL transit mechanism) — the shared "fold" root invites the mistake even though the two share no actual mechanism. Cross-linked from both entries' `related` lists.
- **`src/lore/eden-ring-rail.md`, "The Ring-Rail"**: a new Technology entry for Eden's circumferential commuter line — the everyday transit system running the inner rim of the spin ring, distinct from the short radial spoke-cars connecting the ring to the low-gravity hub. Scheduled and dispatched by the Eden Warden (Ward's largest routine administrative task, and by its own account an informal early-warning signal), funded and defended by Mayor Petrakis's office against Commissioner Wayland's periodic Compact-wide standardisation proposals — the same Mayor/Commissioner dual line `orbital-habitats-compact.md` already documents, applied to an ordinary utility instead of a governance question. One stretch, the hub-ward spoke terminating near the Eden Passage monitoring stations, requires Survey Corps/SSDC clearance the rest of the line doesn't — the one point where Eden's civil transit visibly touches the habitat's separate fold-route oversight chain. Cross-linked from `eden-space-habitat.md`, `orbital-habitats-compact.md`, `eden-warden.md`, `imogen-petrakis.md`, and `zara-wayland.md`.
- **`src/lore/planets/thalassa.md`, "Thalassa"**: a new virgin ocean-world Locations entry, colonised entirely on Saltmere/Venice-style platform hulls and mooring towers rather than any landmass — its two founding cities, **New Venice** and **New Atlantis**, deliberately disagree about how to be a floating settlement. New Venice, built by heritage engineers with direct professional lineage to Earth's own Venice, reproduces the historic city's campaniles, facades, and calli-width geometry from nothing rather than preserving it from something; New Atlantis, a later and less heritage-minded second wave, builds low and structurally exposed, wearing the myth's warning as an ironic civic motto ("built to bend, not to boast") instead of hiding from it. Framed as the first colony to take Saltmere's platform-hull method off-world by choice rather than necessity. Cross-linked from `src/lore/venice.md` (which gains a new closing paragraph on Venetian engineers exporting the retrofit method off-world), `src/lore/planets/saltmere.md`, and `src/lore/environmental-technology-design.md`.
- **`src/lore/venice.md`, "Venice"**: a new Locations entry for Earth's Venice, in 2826 UCSD a fully engineered floating city rather than a flood-prone one — its historic foundation replaced sestiere by sestiere with the same platform-hull-and-mooring-tower construction documented at `src/lore/planets/saltmere.md`, contrasted against Saltmere's build-from-nothing approach since Venice's retrofit had to happen underneath a city already occupied and unwilling to relocate. Ties the Crisis Centuries' coastal fragmentation events (`src/lore/crisis-centuries-terran-wildlife.md`) to a concrete Earth outcome, and reads its "old face, new foundation" preservation choice in the same old-and-new register `src/lore/environmental-technology-design.md` documents elsewhere, explicitly paralleled with Mars's own popular-vote refusal to finish terraforming (`src/lore/planets/mars.md`). Cross-linked from all four.
- **Journal** (`/journal/`), a sixth content type alongside characters/lore/codex/glossary/chapters — out-of-character author's notes, kept next to About as the site's only two non-diegetic sections. New `journal` entry in `lib/content-schema.js` (`title`/`date` required, `tags`/`description` optional), `src/_includes/journal-entry.njk` layout, `src/journal/index.md` listing page (newest-first, via a new `journalEntries` collection and `previousJournalEntryIn`/`nextJournalEntryIn` filters in `.eleventy.js` for prev/next nav), a "Journal" nav link in `base.njk` next to About, and `npm run new -- journal` scaffolding (auto-sets `date` to today, same convention as a chapter). Comments map by plain `pathname` (safe — journal entries are flat and never get repositioned the way chapters do), via a new fourth page-mapped giscus category, **Journal** (`src/_data/giscus.js`'s `GISCUS_CATEGORY_JOURNAL_ID`, threaded through `scripts/cpanel-deploy.sh`'s per-clone override mechanism and documented in `README.md`/`sample-deploy.conf` alongside the existing three). Seeded with a first entry, ["A Photo, a Homophone, and How Membrane Shadows Got Their Name"](src/journal/a-photo-a-homophone-and-membrane-shadows.md), covering the naming decision in the entry right above this one.
- **`src/lore/membrane-shadows.md`, "Membrane Shadows: Gravitational Imprints from Adjacent Membranes"**: names the Physical-layer counterpart to the Etheric-layer Intermembrane Bleed already documented in `src/lore/concordant-membranes.md` — at Class III boundary-zone proximity, a coherent mass on the far side of a narrowed membrane gap (usually a living being) leaks enough gravitational field structure across it to bend and dim ambient light, producing a full silhouette with nothing physically present on this side to cast it. Explicitly disambiguated from the Class III non-sapient shadow beings in `src/lore/meta-dimensional-beings.md`, which have actually entered material space — a Membrane Shadow hasn't, and falls outside the meta-dimensional-being classification entirely rather than at the bottom of it. Also explicitly not an alternate-timeline double when it happens to resemble someone familiar: primary universes are independently authored, not branches of a shared history, so there is nothing for a far-side being to have diverged from. Paired with a new `src/glossary/membrane-shadow.md` entry. Cross-linked from `concordant-membranes.md`, `meta-dimensional-beings.md`, and `intermembrane-bleed.md`, each updated to note the Physical channel this entry covers.
- **Renamed "brane" to "cosmic membrane" (short form "membrane") across the boundary-zone cosmology**: the word "brane" — Survey Corps' existing shorthand for a Concordant's bounded region in the post-11 manifold, itself already short for "membrane" in real brane-world physics — reads too easily as "brain" when spoken over a comm channel or read aloud, which the newly-added Membrane Shadows entry made a live problem rather than a hypothetical one. `src/lore/concordant-branes.md` → `src/lore/concordant-membranes.md` ("Concordant Branes and Interbrane Bleed" → "Concordant Membranes and Intermembrane Bleed"), `src/glossary/interbrane-bleed.md` → `src/glossary/intermembrane-bleed.md`, `src/glossary/brane-shadow.md` → `src/glossary/membrane-shadow.md`, plus every "Brane Resonance"/"brane-proximity"/"cross-brane" mention across `ensemble-multiverse.md`, `meta-dimensional-beings.md`, `fellowship-of-light.md`, and `overfold.md`. Purely a rename — no mechanism, class, or hierarchy changed.
- **S00E02C01, "Unauthorised, Therefore"** (`src/seasons/s00/e02/s00e02c01.md`): a year after *Patience First* disappeared, Adaeze Okafor — sister of the ship's boundary analyst — travels to Mars in person after eleven months of form-letter correspondence gets her nowhere. Dramatizes the family pressure `src/lore/formation-of-star-rangers.md` credits as part of what eventually forces the 2719 Hearing, previously only asserted, never shown. Told from Adaeze and from Lieutenant Rutger Voss, staffing the MSC's Casualty Correspondence desk — the same Voss who returns, years later and several ranks up, in "What They Were Forbidden to Become" (S00E03C01), planting the seed of the disillusionment that later lets him accept the charter's accountability limits without calling it a concession. Cross-linked from `src/lore/formation-of-star-rangers.md` and `src/lore/eden-space-habitat.md`.
- **Renumbered "The Volunteered Word" from S00E02C01 to S00E02C02** (`src/seasons/s00/e02/s00e02c02.md`) to keep Episode 2 in chronological order (2715 UCSD before 2719 UCSD) after adding "Unauthorised, Therefore" above as the new E02C01. All cross-links updated: `src/timeline/2719-outer-stations-consolidation-hearing.md`, `src/lore/formation-of-star-rangers.md`, `src/lore/military-space-command.md`. `story-bible/narrative-gaps-checklist.md` updated for both chapters.
- **`church-space` theme**: a new domain re-skin (`src/css/theme-church-space.css`, generated via `scripts/generate-themes.js`) for the private `church-space` thread's own domains — a candlelit stone background with wine-red accents and stained-glass jewel tones for the five `.pov-block--<character>` colors, alongside the existing `fellowship`/`pets`/`starquest` re-skins. `sample-deploy.conf`'s church-space.site/.online example block now sets `THEME=church-space` for both. Documented in `README.md`'s theme table.
- **`DEPLOY_PRIMARY`** `deploy.conf`/`scripts/cpanel-deploy.sh` key (default `true`): set `false` to skip deploying to `/home/<CPANEL_USER>/public_html/` entirely, for a cPanel account whose `public_html` is reserved for something else (or deliberately left parked) and should only serve one or more `ALT_DOMAINS`. `DEPLOY_PRIMARY=false` with `ALT_DOMAINS` also unset/empty fails the run loudly rather than silently deploying nothing — same "fail loud, not silent" philosophy the script already applies to a misconfigured `ALT_<id>_*` entry. Documented in `README.md` and `sample-deploy.conf`.
- **S00E03C02, "No Longer Supplementary"** (`src/seasons/s00/e03/s00e03c02.md`): a closing beat for Season 0's two returning POVs, same signing day as "What They Were Forbidden to Become." Dr. Pitch reads her once-buried "supplementary material" cited by name as a founding asset in the ratified charter — the payoff of "Supplementary Material" (S00E01C01)'s title. Captain Kilbride, recruited into the new Survey Corps on the strength of the *Constant Margin* transit, carries what the charter's carefully-worded *Patience First* recovery mandate actually means to the person who trained alongside Okafor before she left. Resolves nothing about the terminus or the eventual 2732 recovery — deliberately — but gives the season's founding generation a human close rather than ending on the charter's text alone. Cross-linked from `src/lore/formation-of-star-rangers.md`'s "What the Star Rangers Inherited" section.
- **S00E03C01, "What They Were Forbidden to Become"** (`src/seasons/s00/e03/s00e03c01.md`): the Star Rangers Charter's ratification, 2723 UCSD, dramatized from the inside — the season's founding moment the Charter timeline entry has always documented only from the outside. Four years on from the Hearing, Delegate Petra Solano is now doing the patient, unglamorous negotiation her viral quote only bought time for; Commodore Rutger Voss of the newly formed SSDC argues the charter's jurisdiction limits sincerely (citing a real prior incident, Kessler Anchorage) and comes around to Solano's position anyway, without ever framing it to himself as a concession. Resolves the last open item on Season 0's checklist: the season is now fully dramatized from the *Constant Margin*'s 2712 departure through the 2723 signing. New Season 0, Episode 3 directory and index page. Cross-linked from `src/timeline/2723-star-rangers-charter.md` and both `src/lore/formation-of-star-rangers.md` and `src/lore/military-space-command.md`, which previously only described the negotiation and the SSDC's objection from the outside. `story-bible/narrative-gaps-checklist.md` updated — Season 0's checklist is now fully checked.
- **S00E02C01, "The Volunteered Word"** (`src/seasons/s00/e02/s00e02c01.md`): the Outer Stations Consolidation Hearing of 2719 UCSD, eleven weeks into a session scheduled for three. Dramatizes General Krast's honestly-meant defense of technocratic command — already quoted secondhand in `src/lore/military-space-command.md` and `src/lore/formation-of-star-rangers.md` — and the moment three uncoordinated civilian recordings turn one volunteered sentence into the quote that outruns six years of unresolved MSC findings. Told from Krast (who means every word, and goes on believing she was right to say it) and Delegate Petra Solano of Halyard Station, the first of the three to release her recording. New Season 0, Episode 2 directory and index page, and `src/timeline/2719-outer-stations-consolidation-hearing.md` mirroring the existing 2712/2714 entries. Cross-linked from both lore pages that previously only quoted the line secondhand. `story-bible/narrative-gaps-checklist.md` updated.
- **S00E01C02, "Beyond MSC Liability"** (`src/seasons/s00/e01/s00e01c02.md`): two years after "Supplementary Material," a six-person crew takes *Patience First* past the transit point *Constant Margin* only touched and returned from, aiming to investigate the fold terminus itself — ground Dr. Pitch's model has never been tested against. Dramatizes only the departure, told from Pitch (watching her buried model get treated as settled fact rather than a checked one) and the ship's own boundary analyst, Dr. Wren Okafor, who states the same caveat Pitch does and goes anyway. Stops at the moment of transit, well short of the ship's disappearance, its eighteen-year absence, or its 2732 recovery — all of which stay untouched per the site's existing deliberate mystery around what *Patience First*'s crew found. New `src/timeline/2714-patience-first-departs.md`, mirroring `2712-eden-fold-route.md`'s existing structure and cross-linked from `src/lore/eden-space-habitat.md`. `story-bible/narrative-gaps-checklist.md` updated.
- **`src/lore/arilon.md`**: a generic, cross-cultural "prophet archetype" — the Fellowship of Light's own comparative-archive shorthand for a recurring narrative pattern (a teacher or witness who names a boundary truth no local authority will name first) found independently across unconnected cultures, mechanically grounded in the existing `cosmic-cascade.md`'s Champions/Heroes tier and its documented "persistent misattribution as divine will by low-context observers" rather than tied to any one real-world religion. Cross-linked from `cosmic-cascade.md`'s Champions/Heroes section and `fellowship-of-light.md`. Paired with **`src/codex/hymn-of-the-thin-places.md`**, an ecumenical hymn of disputed origin sung at Ranger memorials and commissionings — deliberately uncredited to any single tradition, consistent with the charter's Neutrality clause. Written so future religious/mythic content across any culture can reference "an Arilon-pattern figure" instead of modeling a new one on a specific real-world faith.
- **Retconned `src/lore/saint-aoife.md`, `src/codex/life-of-saint-aoife.md`, `src/characters/saint-aoife.md`, and `src/characters/brother-daire.md`** to remove direct references to a specific real-world religion and a real historical/legendary figure: "the Church," "God," "Roman process," "confessor," and "IN THIS YEAR OF OUR LORD" are now generic devotional-authority language, and the vision's named figure is now an invented local patroness (**Saint Muirenn**) rather than the real Saint Brigid. The vision itself, its ambiguity, and both existing readings (hagiography vs. Levril encounter) are unchanged.

- **S00E01C01, "Supplementary Material"** (`src/seasons/s00/e01/s00e01c01.md`): Season 0's first published chapter, dramatizing the 2712 UCSD departure documented from the outside in `src/timeline/2712-eden-fold-route.md` — Dr. Ysolde Maren Pitch's fold-mechanics navigation model, buried in the Military Space Command's review process as a footnoted "supplementary" submission, handed to the *Constant Margin*'s three-person crew before any committee finishes deciding whether it's authorised. Ends at the moment of transit, deliberately stopping short of the *Patience First*'s 2714 disappearance and 2732 recovery, which stay untouched per the site's own established mystery. New `src/seasons/s00/e01/` episode directory and index page (Season 0 had none before). Cross-linked from `src/timeline/2712-eden-fold-route.md`, `src/lore/eden-space-habitat.md`, and `src/lore/formation-of-star-rangers.md`; `story-bible/narrative-gaps-checklist.md` updated. Also fixes a pre-existing continuity error caught while researching the chapter's institutional backdrop: `src/lore/military-space-command.md` dated the Krast "We tried democracy..." quote to "2689 UCSD," contradicting `formation-of-star-rangers.md`'s unambiguous "2719 UCSD" for the same Outer Stations Consolidation Hearing (and Season 0's own stated 2712-2723 range); corrected to 2719.

- **S05E02C02, "A Fraction of a Second"** (`src/seasons/s05/e02/s05e02c02.md`): ten days after "What the Hill Keeps," back on duty, a fleet-wide anomaly bulletin — Sen's unsigned cross-referencing work — matches her family's hillside against the Marsh Causeway grove's dryadic-anchor marker, filed only as an unattributed coordinate. Shepherd flags it for formal survey on documented grounds without disclosing the personal connection; High Captain Wender notices the tell and lets it go, consistent with her own established command philosophy. Advances both threads `story-bible/tissadelle-arc-s6-7.md` credits Season 5 with planting — the Threshold-drift/dryadic-anchor connection and what she chose not to file — without resolving either, since that payoff is reserved for the Last Stand (see `story-bible/narrative-gaps-checklist.md`, updated here). Cross-links added to `src/characters/tissadelle-shepherd.md`, `karla-wender.md`, `sen.md`, and `src/lore/dryadic-trees.md`'s "Known Locations" section.

- **S03E01C02, "Independent Verification"** (`src/seasons/s03/e01/s03e01c02.md`): the Sentinel reclassification survives Survey Corps review six days after the catch — reproduced independently under supervision, not taken on Shepherd's word — while Sohrel absorbs the Prime Ganglion's office noting, not approving, the authorisation that made it possible. Dramatizes "what it costs to be believed about it" from `src/seasons/s03/index.md`'s own framing, per `story-bible/narrative-gaps-checklist.md` (updated here). Introduces Sohrel's fourth declined internal request to consult Meridian's reef-cities about the Sentinel's structure — updates `src/characters/sohrel.md` and `src/lore/planets/sentinel.md`'s existing "three" to "four" and cross-links both to the new chapter, since the connection remains a deliberately open thread rather than something this chapter resolves.

- **S01E03C02, "The Right Question"** (`src/seasons/s01/e03/s01e03c02.md`): Season 1's long-missing convergence chapter. Galahad finally discloses the full survey archive to Elvira — now including the station-drift/grove-residue harmonic coupling confirmed in "Harmonic Debrief" (S01E02C03) — and Elvira, for the first time in three years, directly asks Aldera what she's found rather than simply co-existing with her. Aldera answers, per her own established protocol of only sharing requested findings, and the Court-Fae feeding loop breaks: the season's stated endpoint per `story-bible/story-bible-summary.md`'s "Season 1 endpoint (per site synopsis)," previously undramatized (see `story-bible/narrative-gaps-checklist.md`). Deliberately leaves the dryadic grove's separate Etheric root-anchor intervention (planned at the end of S01E02C02) unresolved — naming the feeding pattern and severing its physical anchor are two different problems, and this chapter closes only the first. Does not resolve what Elvira herself is; that stays an open thread per her own character page.

- **`story-bible/narrative-gaps-checklist.md`**: a season-by-season checklist of what's actually published in `src/seasons/` versus what the story bible and site copy have already committed to — Season 0 and Seasons 6-7 fully described/outlined but entirely unwritten, Season 1's own established convergence endpoint still missing, Seasons 3 and 5 each a single chapter standing in for a whole rank-era, plus the standing undramatized waypoints (Wender's rank progression, the Tissadelle/Wender relationship, founding-era open questions) from the story bible summary. Not rendered on the site - a personal tracking doc, re-derive rather than trust once stale.
- **Atom feed** (`src/feed.njk`, at `/feed/feed.xml`, autodiscoverable via a `<link rel="alternate">` in `base.njk`) listing the 20 most recently published chapters newest-first, via a new `recentChapters` collection sorted by a chapter's real-world `date` rather than story order. `date` (`YYYY-MM-DD`, distinct from a chapter's in-universe `timestamp` string) is now a required chapter field in `lib/content-schema.js`; `scripts/new-content.js` sets it to today automatically when scaffolding a new chapter, and the 13 pre-existing chapters were backfilled from each file's own first git commit date so the feed's initial publish order is accurate rather than uniformly "now." Considered `@11ty/eleventy-plugin-rss` first but skipped it: it registers its own `absoluteUrl` Nunjucks filter that would have silently overridden this project's existing one (different, deliberately-correct behavior for the `/star-rangers/` subpath — see `sitemap.njk`), and the package is ESM-only, which doesn't `require()` into this CommonJS config. Hand-rolled the one filter actually needed (`dateToRfc3339`, via the already-installed `luxon`) instead.
- **Open Graph / Twitter Card meta tags** on every page (`base.njk`'s `<head>`): title, description, url, and an image, so links shared on Discord/Reddit/etc. get a proper preview instead of a bare URL. The image is computed per page type in `.eleventy.js`'s new `eleventyComputed.ogImage` from the same `image` front-matter field each layout's own `<img>` tag already uses (character/lore/codex/glossary), falling back to the homepage's hero image for chapters and listing pages. Caught and fixed one bug while verifying: the fallback was originally written as a Nunjucks-side `ogImage | default(...)`, but Nunjucks' `default` filter only substitutes for `undefined`, not `null` — since the computed value was `null`, it silently passed through to the `absoluteUrl` filter, which has its own `!path` fallback to the bare site root. Moved the fallback into the computed function itself so `ogImage` is never null in the first place.
- **Full-text search**, powered by [Pagefind](https://pagefind.app/): `npm run build` now runs `eleventy && pagefind --site _site`, indexing the built output. A hand-rolled header search box (`src/js/search.js`, styled in `main.css`'s new `/* --- Search --- */` section) uses Pagefind's JS API directly rather than its prebuilt `pagefind-ui` widget, so it matches the site's own dark theme instead of overriding another package's bundled CSS. `<main data-pagefind-body>` scopes indexing to actual page content; breadcrumbs, "back to X" footers, chapter pagination, the chapter-scenes list, and the comments widget all carry `data-pagefind-ignore` so search results aren't full of repeated nav boilerplate. The search widget's `data-pagefind-bundle` path carries the same `/star-rangers/` prefix as every other internal link, so `SITE_PATH_PREFIX` rewrites it automatically too. Verified end-to-end with a headless-browser test against a real built+indexed site: query → results with correct titles/excerpts/highlighting → click-through to the right page, at desktop and mobile viewport widths, with zero console/network errors.
- **`npm run new`** (`scripts/new-content.js`): scaffolds a new character, lore entry, codex entry, glossary entry, or chapter, prompting for each type's fields instead of requiring a contributor to memorize a layout's exact front-matter shape by copying an existing file. Chapter numbers auto-derive the `id`, directory, and filename, removing a whole class of copy-paste drift. Timeline entries aren't covered (see the script's own header comment for why).
- **Content validation** (`scripts/validate-content.js`, run by `npm test` before the Eleventy dry run): checks every content file's front matter against a shared schema (`lib/content-schema.js`, the same one `npm run new` scaffolds from) and fails with a clear per-file message — a missing required field, a non-numeric `season`/`episode`/`chapter`/`sort_order`, or a chapter whose `id` or filename disagrees with its own front matter — instead of a blank page or a cryptic Nunjucks error surfacing later. Verified against all 223 existing content files (passes clean) and against several deliberately-broken test files (each catches correctly), including through `npm test`'s actual short-circuit behavior on failure.
- **giscus-powered discussion comments** on every character, lore, glossary, codex, episode, and chapter/scene page, backed by GitHub Discussions on a separate, dedicated `Star-Rangers/sciencefiction-site-comments` repo (kept apart from this repo's own dev-facing Discussions). Config lives in `src/_data/giscus.js`; the embed is in `src/_includes/base.njk`, gated on new `comments`/`commentsCategory` front matter set by the six relevant layouts. Three page-mapped categories (**Characters**, **Lore & Worldbuilding**, **Episode Discussion**) get one auto-created discussion per page path; five more (**Announcements**, **General**, **Q&A**, **Theories & Predictions**, **Fan Creations**) are for open community discussion unrelated to any specific page. See the README's new "Discussion forum (giscus)" section for the full category list and the one-time GitHub/giscus.app setup steps needed before the repo/category IDs can be filled in.
- **`COMMENTS_ENABLED`** `deploy.conf`/`ALT_<id>_COMMENTS_ENABLED` key (default `true`) to build a clone with the giscus widget above turned off entirely — read as a plain `.eleventy.js` global-data toggle (`process.env.COMMENTS_ENABLED`), the same pattern `THEME` already uses. The GitHub Pages workflow sets it `false`, since Pages' `/star-rangers/`-prefixed URLs would otherwise create a second, disconnected set of pathname-mapped discussions alongside the root-served cPanel domains'. In `scripts/cpanel-deploy.sh`, `build_and_deploy()`'s existing 11 fixed positional settings weren't given a 12th: this and any future simple on/off setting is instead passed as a trailing `NAME=value` string, parsed by a small `case` inside the function (unrecognized names fail the deploy loudly) — keeps deploy.conf's growing option list from making that function's call sites harder to read or reorder correctly.
- **`scripts/fetch-giscus-ids.js`** (`npm run fetch-giscus-ids`): fetches the giscus repo ID and every configured category's ID from GitHub's GraphQL API in one call, using the repo/category names already declared in `src/_data/giscus.js`, and can patch them straight into that file with `--write` — an alternative to re-running the giscus.app wizard once per category.
- **"Fan fiction" section on the About page**, pointing readers who want to write their own Star Rangers stories to AO3 or Wattpad rather than this site or its comments repo, with a suggested tag so their work stays findable. The comments repo's **Fan Creations** category (see the giscus category table above) is for linking/discussing those works, not hosting them — no in-repo fic-hosting or forum was built for this, by design, per the tradeoffs discussed when the category list was drafted.

### Removed

- **Root-level `lore/` directory** (`lore/codex/exploration-budget.md`, `lore/codex/protocols/redline-protocol.md`, `lore/codex/dialogues/codex-debate-entropy.md`) — early drafts of the Exploration Budget/Redline Protocol doctrine, sitting outside Eleventy's `src/` input dir and never built or published. Deleted as superseded: all three are fully covered, in more developed form, by the published `src/codex/exploration-budget-doctrine.md`, `src/codex/redline-protocol.md`, and `src/codex/hall-of-quiet-instruments-dialogue.md`.

### Changed

- **Tagged `src/codex/bramble-and-the-backwards-key.md`** (the Bramble Storybooks excerpt, the Concordant's best-known Smart Pet children's franchise) with `undercover-pets.com`, the tag `TOPICS`/`CHARACTERS`/`THREADS` narrowing matches to pull a page into that domain's deploy — the same convention already used by `bubochka.md`, `agent-barsik.md`, `smart-pet.md`, and over a dozen other pets-relevant pages. It was the one Cultural Record codex entry about a Smart Pet missing the tag.
- **Disambiguated the three legendary figures in `src/codex/flight-of-aldera-chronicle-of-dun-ri.md`** from their same-named modern counterparts: the legend's Princess Elvira, her cat Aldera, and Brother Daire were referred to by bare first name/title throughout, identical to the canonical `elvira.md`/`aldera.md` (Season 1 boundary practitioner and detective-agency cat) and `brother-daire.md` (the historical 1216 AD Cill Aoife scribe) character pages, even though the codex entry's own closing note already treats all three as distinct, unconfirmed namesakes rather than the same individuals. Added "of Dún Rí"/"of Dún Rí Abbey" at each figure's first named mention in the story text, and extended the closing note to explain this as a citation habit for clarity, not a claim about descent one way or the other — the underlying in-universe mystery stays exactly as open as it was.
- **Reassigned Rook-7 from the detective agency to a police department**: `rook-7.md`, `rook-7-first-week-log.md`, `aldera.md`, and `smart-pet.md` all described Rook-7 (an ordinary robot, not a Smart Pet or cyber-enhanced animal) as part of the Undercover Pets Detective Agency's own field-unit network — conflating "shares the same asymmetry-detecting cognitive architecture" with "belongs to the same institution." Rook-7 now belongs to the Threshold Station Police Department instead — technical support, investigation, and forensics, with no formal power of arrest — a separate institution from the detective agency that happens to prefer the same kind of non-rationalising cognition in robots that the agency prefers in Smart Pets and cyber-enhanced animals. `rook-7-first-week-log.md`'s institution/tags updated to match; `aldera.md`'s note that she's "aware of Rook-7 through the detective agency network" corrected to inter-agency channels rather than a shared chain of command. Ordinary robots accordingly stay out of `undercover-pets.com`'s narrowing — they were never tagged for it to begin with, but the prose no longer implies they should be.
- **Expanded `src/lore/the-imperium.md`'s Origins section**: the Inner System Defence Fleet's unified command at the Tycho Accords is now explicitly grounded in a small number of wealthy and powerful families who had already gained control of the Inner System's major technology and space industries before the Coherence Wars ended — the same "legacy economic power" `united-space-consortium.md`'s Character section already credits with skewing the USC's own assembly, continuing past the USC's collapse rather than disappearing with it. Also adds a "Militarily augmented" bullet to the Imperium's Character section, on its use of military cyborg technology, cross-linked to the new `cyborg.md` glossary entry. `military-space-command.md` gains a matching paragraph on the MSC winding down those programs and starting the certification regime later governments kept tightening.

### Fixed

- **Home-page hero slideshow dipped to the background between portraits instead of cross-fading** (`main.css`, propagated to every `theme-*.css`). Each slide's `home-hero-crossfade-{5,6}` keyframe faded it back to `opacity: 0` exactly at its slot boundary, and the next slide only began fading in from that same instant, so every transition passed through the empty `--color-surface` background — a fade-out-then-in flash, not a crossfade. The fade-out window now overlaps the incoming slide's fade-in window (each slide fades out over `[slot, slot+fade]` while the next fades in over the same interval, including across the loop seam), producing a true crossfade. Timing, slide counts (5 for `pets`, 6 elsewhere), and the `prefers-reduced-motion` fallback are unchanged; re-ran `npm run generate-themes` to propagate it.
- **Rebuilt CSS/JS could be served stale from browser/CDN cache** (`src/_includes/base.njk`, `src/_data/build.js`). `css/main.css`, `js/pov.js`, and `js/search.js` were linked with no version token, so a returning visitor could keep an old `main.css` after a redeploy — and because `scripts/cpanel-deploy.sh` rewrites `_site/css/main.css` (theme swap + optional `CUSTOM_CSS_FILE`) *after* Eleventy runs, a build-time content hash of the source wouldn't reflect what's actually served. Added `build.assetVersion` (a per-build token, computed once at build) and appended it as `?v=…` to all three same-origin asset URLs, so every rebuild/redeploy busts the cache uniformly regardless of which theme a clone deploys.
- **`sample-deploy.conf`'s `THREADS` comment listed only two of the three registered storyline threads**, omitting `church-space` — even though the same file's own church-space example block sets `THREADS=church-space`, which then read as coming from nowhere. Updated the comment to list all three (matching `README.md`) and to note that `church-space` is a `private: true` thread (hidden on every build unless explicitly named, the inverse of how the other two narrow an otherwise-full site).
- **Character portrait/text overlap on mobile** (`.character-header__inner` in `main.css`): the character header's portrait image and name/badge text column were laid out side-by-side with `flex-shrink: 0` on the image and no responsive override, so below the existing 640px breakpoint the text column collapsed to zero width and the portrait overflowed past the viewport's right edge instead of shrinking to fit — visible on every character page (e.g. `/characters/bubochka/`) on a phone-width screen, propagated to every theme including `theme-pets.css` (undercover-pets.com). Added `flex-direction: column` for `.character-header__inner`, full-width `.character-header__text`, and `max-width: 100%` for `.character-portrait` inside the same `@media (max-width: 640px)` block, and re-ran `npm run generate-themes` to propagate the fix to every theme file.
- **Chapter prev/next pagination**, dead on every single chapter since `chapter.njk` referenced `previousChapter`/`nextChapter` template variables that nothing ever actually set. Two new Nunjucks filters, `previousChapterIn`/`nextChapterIn` (`.eleventy.js`), find a chapter's immediate neighbor in the already-sorted `chapters` collection — one continuous publication order across episode and season boundaries, the same order a season/episode index's own chapter list already reads in. Naturally skips anything `CHARACTERS`/`TOPICS`/`THREADS` filtering has excluded, since the collection itself omits those chapters, so a narrowed deploy never links to hidden content. Verified against the first chapter (no previous), the last chapter (no next), the new S01E03C02 convergence chapter's neighbors on both sides, and a `CHARACTERS`-narrowed build.
- **Chapter/scene comment threads losing their original context on renumber.** Chapters and per-POV scene pages mapped their giscus discussion by `pathname` (like every other page type), but a chapter's URL isn't permanent — episodes get renumbered in place to keep story chronology in order (see this file's own "renumbered" entries), reassigning a URL like `/seasons/s00/e02/s00e02c01/` to entirely different content. Any comments already attached to that pathname would silently reattach to whatever content next occupied the slot. Chapters now require a permanent `comment_id` front-matter field (`lib/content-schema.js`), independent of the position-derived `id`/URL, and `base.njk` maps them via `data-mapping="specific"`/`data-term` on it instead; scene pages derive their own term from the parent chapter's `comment_id` (`src/scene-pov.njk`). `comment_id` is set once — `npm run new -- chapter` generates it from the title — and must be carried forward by hand if a chapter is later renumbered, rather than regenerated, so its thread stays with its content; `scripts/validate-content.js` now fails the build if two chapters ever collide on the same `comment_id`. Backfilled onto all 22 existing chapters, including S00E02C01/C02 — "The Volunteered Word" keeps the identity it had before this year's renumbering, and "Unauthorised, Therefore" (the new chapter that took over its old slot) gets its own. Other page types (characters/lore/glossary/codex) are unaffected: their URLs are never reassigned to different content, so pathname mapping stays safe for them. Also flips `data-strict` from `0` to `1`: giscus's discussion search is a title match, and without strict mode it's a loose one — a chapter's own `comment_id` (e.g. `unauthorised-therefore`) could loosely match one of its scenes' (`unauthorised-therefore-scene-1-adaeze`, which contains it as a prefix), pulling in the wrong discussion. Strict mode also makes a stale `comment_id` (e.g. a deleted chapter's) fail gracefully — no exact-title discussion to find means an empty thread, not a guessed near match.

### Added

- **`FORKING.md`**: a step-by-step guide to forking this repo either as an original interactive fiction site (replace the content, keep the engine) or as a Star Rangers fan fiction clone (keep some/all content too, per the new Fan Works Policy). Covers rebranding, the `/star-rangers/` path prefix, standing up a separate comments repo, and deployment for both GitHub Pages and cPanel.
- **`SITE_PATH_PREFIX`** env var (`.eleventy.js`): rewrites every hardcoded `/star-rangers/` absolute link across layouts *and* content (~200 files) to a different base path at build time, via an Eleventy transform. Exists for forkers serving from a different subpath or domain root, without hand-editing every file it appears in; unset by default, which leaves this project's own output byte-for-byte unchanged. The GitHub Pages workflow now passes it through from an optional `vars.SITE_PATH_PREFIX` repository variable. cPanel deploys don't need it — `scripts/cpanel-deploy.sh` already strips the same prefix with its own post-build sed step, independently of this.
- **Fan Works Policy** in `CONTENT-LICENSE.md`: an explicit, standing exception to the story content's No-Derivatives term for non-commercial fan fiction, fan art, and fan fiction clones of this site (forking the engine and some or all of the existing content) — conditioned on staying non-commercial, clearly labeled as unofficial, and attributed back to the original. The About page's "Fan fiction" section and the site's own on-page licence section both now point to it.

### Changed

- **Split the repository's licence in two**, so the site engine can be forked without implying any rights over the story itself. `LICENSE` is now plain **MIT**, covering `.eleventy.js`, `lib/`, `src/_includes/`, `src/css/`, `src/js/`, and `scripts/`; the story content (`src/seasons/`, `src/threads/`, `src/characters/`, `src/timeline/`, `src/lore/`, `src/glossary/`, `src/codex/`, `story-bible/`, `prompts/`) keeps the original **CC BY-NC-ND 4.0** terms, now in a new `CONTENT-LICENSE.md`. Previously the repo-root `LICENSE` file applied CC BY-NC-ND's NoDerivatives term to the whole repository, code included, which technically forbade forking the engine at all — a mismatch with `src/index.md`'s own licence section, which already scoped its wording to "text and world-building content" only. `package.json`'s stale `"license": "ISC"` (never matched either license) is now `"MIT"` to match, and `src/index.md`'s licence section gains a short pointer to the code's separate MIT licence.
- **Storyline Threads** as a top-level site category (`/threads/`), grouping seasons under the independent narratives established in `story-bible/story-bible-summary.md`'s "Narrative Structure" section: **Founding Era** (Season 0) and **Tissadelle Shepherd's Arc** (Seasons 1, 3, 5). The canonical thread→season registry lives in the new `lib/storyline-threads.js` (re-exported as global template data via `src/_data/storylineThreads.js`, and as the new `threadForSeason` Nunjucks filter), so it's the single source of truth for both the site's grouping and `deploy.conf`'s new filter below. `/seasons/` now groups its season blocks under a thread heading instead of listing seasons flat, each season's own index page carries a "Part of: `<thread>`" link, and each thread gets its own page at `/threads/<id>/` listing the seasons it covers. A season not yet assigned to a thread falls back to an "Unsorted" grouping rather than silently disappearing.
- **`THREADS` `deploy.conf` key**, alongside the existing `CHARACTERS`/`TOPICS`: a comma-separated list of storyline thread ids that narrows a deployed clone to the seasons those threads cover (every chapter whose `season` falls under a listed thread, plus any lore/timeline/glossary/codex entry explicitly tagged with the thread id). Combines with `CHARACTERS`/`TOPICS` rather than replacing them — matching any one of the three includes a page. Wired through `lib/content-filter.js` (`getContentFilter`, `isChapterIncluded`'s new `isSeasonInIncludedThread` check), `scripts/cpanel-deploy.sh` (including its own `ALT_<id>_THREADS` per-alt-domain variant), and documented in `sample-deploy.conf`, `.cpanel.yml`, and the README.
- **"Narrative Structure — Multiple Independent Storylines"** section in `story-bible/story-bible-summary.md`: formalizes the series' two-level storyline pattern already implicit in the published content — a macro-level season spine (Tissadelle Shepherd's career across S1/S3/S5/S6-7, with Seasons 2 and 4 deliberately left open as slots for storylines that don't involve her) and a micro-level pattern of independent, non-overlapping POV threads within a season that converge at a planned point (Season 1's Threshold Station/Marsh Causeway threads; the Season 6-7 treatment's institutional/interior threads). Documents the five criteria that make two threads "independent" rather than just alternating scenes, and flags Seasons 3 and 5 as the current exception to the pattern rather than a second template.
- **The Knarr Line** (`src/lore/knarr-line.md`), a five-century-old Belt shipping and ice-mining cooperative with Norwegian, Swedish, Danish, and Icelandic/Faroese founding roots, present at the 2337 Navigation and Trade Accords that grew into the United Space Consortium — and, having never left Sol System and never dissolved, the closest thing its own scattered habitats have to a government today, run through a delegate assembly it still calls **the Thing**. Two of its settlements get their own location entries: [Knarrheim](src/lore/knarrheim.md), its oldest habitat (a converted ice-hauler mothership and the Thing's usual meeting ground), and [Saltvik](src/lore/saltvik.md), its largest ore-refinery cluster. Cross-linked into `united-space-consortium.md`'s Origins section and contrasted, in `knarr-line.md` itself, against the Celtic Union of Planets' very different relationship to Solar System sovereignty.
- Corrected the existing `aethelrock-tir-na-nog.md` codex entry's framing: the real Aethelrock song it links is about the *mythical* Irish Otherworld, not literally the Celtic Union's Tír na nÓg planet (which merely borrows the same name) — retitled to "The Fires of Tír na nÓg" and reworded throughout to make that distinction explicit, while explaining why Union listeners (Tír na nÓg's own colonists especially) treat the difference as academic. Also added the reciprocal link from `tir-na-nog.md`'s own "See also" line, which never pointed back to this entry.
- **Clan Avalon** (`src/lore/planets/aethelrock.md`'s new "Clan Avalon: A Fellowship Without Ground" section) — a cross-clan bardic fellowship on Aethelrock, distinct from (and holding no Ridgemoot seat alongside) the world's five landholding clans, organised around keeping alive the Arthurian Avalon myth-cycle that already gave neighbouring [Ynys Wydrin](/star-rangers/lore/planets/ynys-wydrin/)'s home system, Avallach, its name — cross-linked reciprocally into that page's "See also" line. Its anthem is filed as a new codex entry, [Clan Avalon's Anthem](src/codex/clan-avalons-anthem.md), an externally hosted YouTube recording ("Echoes of Avalon" by the band Aethelrock) linked rather than archived, following the same pattern the existing `aethelrock-tir-na-nog.md` entry set.
- `ALT_DOMAINS` support in `deploy.conf`, letting one cPanel Git Version Control clone deploy additional domains from the same checkout — each to its own sibling folder under `$HOME`, at the same level as `public_html/` (typically an addon domain's document root) — instead of requiring a separate clone per domain. Each `ALT_DOMAINS` entry gets its own `ALT_<id>_DIR`/`ALT_<id>_DOMAIN` (required) plus optional `THEME`/`CHARACTERS`/`TOPICS`/`SITE_NAME`/`SITE_TITLE`/`ADMIN_EMAIL`/`CUSTOM_LORE_FILE`/`CUSTOM_CSS_FILE`, defaulting the same way their unprefixed primary-domain counterparts already do. `scripts/cpanel-deploy.sh`'s new `build_and_deploy()` runs one independent build+deploy per domain (clearing `_site/` between them so one domain's build never leaks into another's), aggregating every domain's result into one overall pass/fail rather than stopping at the first failure, and adding every configured `ADMIN_EMAIL` to the one run's notification list. See `sample-deploy.conf` and the README's new "`ALT_DOMAINS`" section for the full key reference.
- `SITE_NAME` and `SITE_TITLE` `deploy.conf` keys, letting a cPanel clone customize the brand name shown in the header/footer and the browser `<title>` tag independently of the shared repo default (`Star Rangers` for both). Wired through `scripts/cpanel-deploy.sh` into `src/_data/site.js`, consumed by `src/_includes/base.njk`.
- **Mnemari** (`src/lore/mnemari.md`), a sapient Federation species native to Ilenne with no natural upper bound on lifespan and no natural decay of episodic memory — balanced by an actuarial profile driven entirely by accident and violence rather than age, a deliberately low birthrate, and **the Setting-Down**, the voluntary retirement custom that gives a species with no natural mortality curve a working substitute for institutional turnover. Mnemari mediators and treaty-witnesses are noted as disproportionately valued in Federation assembly sessions revisiting old, contested compacts, for the same reason the Star Rangers themselves keep running into elsewhere in the setting: what happens to the truth of an event once everyone who could contradict a convenient retelling is gone. Cross-linked into `federation-of-sentient-beings.md`'s member-world roster and reciprocally into `krenyi.md`, `chthonari.md`, and `prismeri.md`.
- **Virtual Reality** (`src/glossary/virtual-reality.md`): mass-market VR entertainment declined well before 2826 UCSD because it reads as low-novelty recombination to the same Creative Entropy Index culture that audits pop-culture artifacts like "Paper Galaxies" — while fold-scaffold visualization remains a valued, unremarkable exception, since it renders a real, otherwise-imperceptible higher-dimensional structure rather than manufacturing one. Cross-linked into `creative-entropy-index-cei.md`'s example and related-terms list.
- **"AI: Present, Never Intrusive"** section added to `src/lore/environmental-technology-design.md`: names 2826 UCSD's ambient AI texture explicitly — Kernel-compliant systems running everywhere, announcing almost none of it — as the lived, day-to-day expression of the AI Safety Kernel's "obedience narrow, protection wide" doctrine.
- **"Bramble and the Backwards Key"** (`src/codex/bramble-and-the-backwards-key.md`): a codex excerpt from the Bramble Storybooks, the Concordant's best-loved Smart Pet children's-book franchise, plus a "Public Reputation" section added to `src/glossary/smart-pet.md` covering the cute/cool/clever cultural image the series cemented — dramatic irony fully intact, since the author has no idea any of it resembles real agency work.
- **Chthonari** (`src/lore/chthonari.md`) and their home, **the Undersong Belt** (`src/lore/undersong-belt.md`): a low-gravity, asteroid-burrowing insectoid Federation species whose vibration-based senses unify their engineering aptitude, abstract mathematics, and music into one structural trait, and who are disproportionately represented in the Star Rangers' Engineering Corps fold-scaffold specialization. Adds "Engineering" to the named specialist corps in `star-rangers-command-hierarchy.md` and cross-links the Undersong Belt into `federation-of-sentient-beings.md`'s member-world roster alongside Aspenar and Prismere.
- `CHARACTERS`-filtered deploys now automatically include any lore, timeline, or glossary entry that an included character's own bio links to directly, on top of the existing tag/category matching (`lib/content-filter.js`'s new `getRelatedContentUrls`). A character's bio is already the record of what background matters for understanding them, so that background no longer needs separate hand-tagging to survive a narrowed deploy.

- **Saltmere** (`src/lore/planets/saltmere.md`), a newly first-contacted planet in the Annwyn System with no landmass at all — a single global ocean settled entirely on engineered floating city-platforms tethered to the seafloor by mooring towers, built to give under storm load rather than hold rigid. Its **aquabots** — Kernel-compliant autonomous underwater maintenance vehicles — are noted as a convergent, not descended, echo of Earth's classified Subsea Cable Warden Programme, with a reciprocal cross-link added to that page.
- **Prismere** (`src/lore/planets/prismere.md`) and its native people, the **Prismeri** (`src/lore/prismeri.md`): a new Federation of Sentient Beings charter world with a bioluminescent silicate-carbon biosphere and a naked-eye fold-corridor phenomenon, the Lattice, tying into existing Lagrange Fold Points and Quantum Space Harmonics canon.
- **Bubochka**, a trainee rabbit in the Undercover Pets Detective Agency, mentored by Agent Barsik at Eden Space Habitat (`src/characters/bubochka.md`).
- **The Orbital Habitats Compact** (`src/lore/orbital-habitats-compact.md`): the civil governance chain for Eden and the other four self-governing orbital habitats around Earth, running parallel to — and never overlapping — the Star Rangers' charter-limited authority. Introduces Superintendent Rasa Oyelaran, Space Commissioner Zara Wayland, Mayor Imogen Petrakis, and Chief Commissioner Dorian Calloway, and gives Governor Petra Voss (previously only referenced from Commander Kai Larsen's page) her own character profile.
- **The Eden Warden** (`src/characters/eden-warden.md`): a per-habitat AI collective, plural in the same sense the [Plural Minds](/star-rangers/glossary/plural-minds/) glossary entry already describes for organic minds — two Kernel-compliant personas, Ward and Custos, sharing one system, reporting jointly to the Mayor and Commissioner, and holding standards authority over mobile AI humanoids (Reeves, Lucene-9000) and welfare responsibility for cyber-enhanced animals, alongside — not through — the Detective Superintendent's operational chain.
- **Smart Pet** (`src/glossary/smart-pet.md`): the formal classification for an animal carrying a certified AI cognitive subsystem — distinct from, and broader than, "cyber-enhanced" — with its own independent, heavily regulated animal-welfare certification track separate from Kernel compliance. Retroactively clarifies that Barsik and Bubochka hold Smart Pet status without being cyber-enhanced, unlike Aldera, who holds both.
- **"Obedience Is Narrow. Protection Is Not."** section added to `src/lore/ai-safety-kernel.md`: names the Kernel's core asymmetry between invariant 3 (obedience limited to lawful, legitimate authority) and invariant 1 (protection owed universally, without the protected party needing standing to claim it) — the doctrinal basis for the Habitat AI Collectives' welfare authority over parties, human or not, with no seat at the table that assigns their own protection.
- **Frontier Transformation Protocols** (`src/lore/frontier-transformation-protocols.md`): the safety doctrine governing when a frontier world may be terraformed at all — guarding against undetected native sapience, boundary-proximate instability (the Eden precedent), and irreversibility — plus the AI Safety Kernel's own structural backstop against a Kernel-compliant system being ordered past it. Documents each major polity's own version (SSDC centralised review, Federation per-world autonomy, Celtic Union's never-yet-met unanimous-consent bar, Cerebraun Hegemony central authorisation), explaining why Kernowek Reach's reclamation, Mars's incomplete terraforming, and Aspenar's domes-over-conversion choice all read the way they already did.
- **"First Breath: A Founding Thanksgiving"** and **"Survey Note: On the Origins of Tír na nÓg's Compatibility"** (`src/codex/`): two codex documents on Tír na nÓg's mysterious origins — the founding cooperative's own devotional thanksgiving text crediting Manannán mac Lir, and a Survey Corps xenobiology review of six competing theories (convergent evolution, precursor engineering, an undetected Concordant mechanism, Levrilic influence/divine providence, a wild fringe theory of an ancient lost inter-planetary fold gate predating Lagrange-point staging, and a more physically grounded theory involving transient natural fold apertures during rare conjunctions of Tír na nÓg's two moons, Donn and Cliona). Levrilic influence/divine providence is the most popular opinion among the public, but explicitly *not* the least credible of the six — that distinction belongs to the fold-gate theory, since it contradicts confirmed fold-shear physics, while the lunar-conjunction theory is consistent with it (per Eden's own confirmed natural fold route) and is the survey note's recommended candidate for future funding. Cross-linked into `tir-na-nog.md`'s anomaly section, `lagrange-fold-points.md`, and `eden-space-habitat.md`.

### Changed

- **Tír na nÓg** (`src/lore/planets/tir-na-nog.md`) is reframed as a standing, unexplained scientific anomaly rather than a comfortable "convergent evolution" story, to establish as firm setting canon that no world or species is ever naturally compatible with Earthlike gravity/atmosphere without engineering — Tír na nÓg remains the sole confirmed exception, now explicitly flagged as one nobody has closed the file on. Cross-linked update in `celtic-union-of-planets.md`. `kingdom-of-the-four-islands.md` gets a small clarifying addition (centuries-old, since-forgotten atmospheric engineering by the lost colony's founders) so it doesn't read as a second, unflagged exception to the same rule.
- `ADMIN_EMAIL` deploy.conf key now defaults to `admin@<DOMAIN>` instead of staying unset, so every cPanel clone gets deploy-log notifications out of the box without needing its own `deploy.conf` entry for it.

## [1.5.0] - 2026-07-09

> **Note on 1.0.0–1.5.0.** These six entries were written in one sitting, in the commit that introduced this changelog (`45f72f3`, "Add theme generator, 4 standard themes, and a real changelog/semver"). They reconstruct development phases rather than record releases that were cut at the time: `package.json` held 1.0.0 from the first content commit (`a0ca09a`, 2026-05-27) until that commit set it directly to 1.5.0, so **1.1.0, 1.2.0, 1.3.0 and 1.4.0 never existed as states of this repository**. Only 1.0.0 and 1.5.0 correspond to a real commit (`a0ca09a` and `45f72f3`); the other four are not taggable and should not be given tags pointing at stand-in commits. The history below is still accurate about *what changed*; it is not a record of *when it shipped*.

### Added

- `scripts/generate-themes.js` (`npm run generate-themes`): generates every `src/css/theme-<name>.css` from `main.css` plus a small per-theme palette registry, so a theme can only ever differ from `main.css` in its `:root` custom properties, its five `.pov-block--<character>` colors, and `.character-badge--status-active` — everything else is guaranteed structurally identical to `main.css`.
- Four new standard, non-domain-specific themes: `light` (day mode), `high-contrast` (low-vision accessibility), `sepia` (e-reader tone), `solarized` (Solarized Dark).
- Theme catalog documented in `sample-deploy.conf` and `README.md`.

### Fixed

- The `pets` theme (undercover-pets.com) had drifted significantly out of sync with `main.css`: it was missing link-color styling across nearly every content body (`.chapter-body a`, `.codex-entry__body a`, `.character-body a`, `.glossary-entry__body a`, `.lore-entry__body a`, and `.character-portrait`), meaning most internal links on that production domain rendered in the browser's default blue instead of the theme's own accent color. The `fellowship` and `starquest` themes had smaller drift (missing the 1.2.0 link-color fallback and two newer component rules). All three are now regenerated in sync with `main.css` by `scripts/generate-themes.js`; their palettes are unchanged.

## [1.4.0] - 2026-07-09

### Added

- `CUSTOM_LORE_FILE` deploy.conf key: a clone-local, untracked markdown file with valid lore-entry front matter, copied into `src/lore/custom/` before the build (builds at `/lore/custom/<name>/`) and removed again afterward, so one cPanel clone can carry a domain-exclusive lore page without touching the shared repo.
- `CUSTOM_CSS_FILE` deploy.conf key: a clone-local, untracked CSS file appended after the theme stylesheet, so one clone can override a handful of rules without a whole new `theme-<name>.css`.
- Both fail the deploy loudly if set to a path that doesn't exist.

## [1.3.0] - 2026-07-09

### Added

- `DOMAIN` deploy.conf key, exported as `SITE_DOMAIN` for the Eleventy build.
- `src/_data/site.js`, `src/robots.njk`, `src/sitemap.njk`: `robots.txt` and a real `sitemap.xml` are now rendered at build time from the clone's own domain, instead of `robots.txt` being a static file hardcoded to one production domain.

### Fixed

- `robots.txt`'s `Sitemap:` line pointed at `https://sciencefiction.site/sitemap.xml` on every deploy target, including the other four production domains this repo serves — and no `sitemap.xml` was ever actually generated by any build, so the directive 404'd everywhere, including on sciencefiction.site itself.

## [1.2.0] - 2026-07-09

### Changed

- cPanel deploy (`scripts/cpanel-deploy.sh`) now fails the deployment — and triggers the existing `ADMIN_EMAIL` failure notification — if `.htaccess` or `.well-known/security.txt` are missing from the deployed destination, instead of only checking for `index.html`. cPanel is the only deploy target that reads `.htaccess` at all (GitHub Pages ignores it), so a silent regression there would previously have shipped with none of its security headers and still reported success.

### Notes

- Audited both deploy targets for security posture: cPanel's `.htaccess` already set CSP, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `Permissions-Policy`; `.well-known/security.txt` was present and current. GitHub Pages has no supported mechanism for custom response headers at all, so it ships with none of these — a platform limitation, not a config gap.

## [1.1.0] - 2026-07-09

### Added

- Lore entry: "The Grand Ensemble Multiverse" (`src/lore/ensemble-multiverse.md`) — names the infinite collection of Worldwright-authored universes, documents our home universe's 3 visible-physical + 7 invisible-Etheric + 1 time = 11-dimensional structure, and explains why other universes in the Cascade can use a different dimensional split with different fundamental particles and forces. Cross-linked from `post-eleven-dimensional-manifold.md`.
- Default-theme homepage hero paragraph rewritten to link into the new lore entry instead of just the Concordant glossary term.

### Fixed

- Several inline links (the homepage hero paragraph, the Atlas page intro) sat outside any styled component class and had no color rule, so they rendered in the browser's default blue instead of the site's accent color. Added a sitewide fallback `a` rule in `main.css`.

## [1.0.0] - 2026-07-09

Baseline release, consolidating prior undated project history (this is the first version this changelog covers retroactively).

### Added

- Lore entries defining teleportation constraints and the story pressures those limits create.
- Expanded fold-space material around Lagrange fold-points, linked across related FTL articles.
- Canon for the Subsea Cable Warden Programme.
- Lore for the Star Rangers Safety Corps and its historical lineage.
- Timeline events from before the United Stellar Concord.

### Changed

- Revised timeline event titles and updated charter references for the Safety Corps and the SSSA.
- Renamed references to "UL Standards International" as "United Space Industry Standards" across lore and timeline content.

### Removed

- Direct mentions of UL Solutions by name from lore and timeline entries.
