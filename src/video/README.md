# `src/video/` — video assets

One file, referenced from a codex entry; `story-bible/video-prompts.md` records that it has no prompt kept, and that a second, an orphan nothing referenced, was removed on 18 September 2026 at Dermot's instruction. The house motion signature and the camera-is-a-witness rule live in that note. Check what references a file (`grep -rn <name> src lib`) before adding, replacing or removing one.
